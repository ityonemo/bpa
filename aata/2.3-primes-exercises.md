# The Integers — Primes (Exercises)

Exercises for Judson §2.2/§2.3 (Greatest Common Divisors & Primes), companion to
`aata/2.3-primes.md` (source: `vendor/aata/src/integers.xml`; © 1997–2026 Judson,
GFDL 1.3). Per `.claude/rules/aata-guide.md`, the **exercises** section uses
`std` **liberally** — except a result the main text proves, which is forwarded by
a prose pointer only. Run `bpa check aata/2.3-primes-exercises.md`.

We reuse the §2.3 vocabulary: ℤ with its ring, order, divisibility, the Division
Algorithm, and the prime theory of `std/primes.bpa`. The nonnegativity predicate
is aliased under the bare name `nonneg` so the linear-arithmetic accelerant
recognizes it.

```bpa
import int_divides <<< "std/integer-divides.bpa"
import order <<< "std/integer-order.bpa"
import wellordering <<< "std/integer-wellordering.bpa"
import primes <<< "std/primes.bpa"

sort Int = int_divides.Int
const ZERO = int_divides.ZERO
const ONE = int_divides.ONE
func succ = int_divides.succ
func add = int_divides.add
func mul = int_divides.mul
pred less_than = int_divides.less_than
pred nonneg = int_divides.is_nonneg
pred does_divide = int_divides.divides
define TWO = int_divides.TWO
define FOUR = int_divides.FOUR

theorem divisionAlgorithmExists = int_divides.divisionAlgorithmExists
theorem lessThanSuccRight = order.lessThanSuccRight
theorem zeroLessThanOne = order.zeroLessThanOne
theorem succInjective = wellordering.succInjective
theorem addSuccRight = order.addSuccRight
theorem addZeroRight = order.addZeroRight
theorem nonnegCases = order.nonnegCases
theorem nonnegAdd = order.nonnegAdd
theorem mulOneLeft = primes.mulOneLeft
theorem mulOneRight = int_divides.mulOneRight
theorem mulAddDistribLeft = int_divides.mulAddDistribLeft
theorem mulAddDistribRight = int_divides.mulAddDistribRight
theorem mulZeroRight = int_divides.mulZeroRight
theorem mulIsAssociative = int_divides.mulIsAssociative
theorem mulIsCommutative = int_divides.mulIsCommutative
theorem addIsAssociative = int_divides.addIsAssociative
theorem addIsCommutative = int_divides.addIsCommutative
theorem addLeftSwap = int_divides.addLeftSwap
theorem mulLeftSwap = primes.mulLeftSwap
theorem mulSuccRight = int_divides.mulSuccRight
axiom nonnegSuccNotZero = order.nonnegSuccNotZero
axiom lessThanElim = order.lessThanElim
axiom addSuccLeft = order.addSuccLeft
axiom addZeroLeft = order.addZeroLeft
axiom mulSuccLeft = order.mulSuccLeft
axiom mulZeroLeft = int_divides.mulZeroLeft
axiom oneIsSuccZero = int_divides.oneIsSuccZero
```

## Exercises

**Exercise 17 (Fibonacci Numbers).** With `f₁ = f₂ = 1` and `f_{n+2} = f_{n+1} + fₙ`,
prove (a) `fₙ < 2ⁿ`; (b) `f_{n+1} f_{n−1} = fₙ² + (−1)ⁿ` for `n ≥ 2`;
(c) the closed form `fₙ = [(1 + √5)ⁿ − (1 − √5)ⁿ] / 2ⁿ√5`; (d) the golden-ratio
limit `φ = limₙ f_{n+1}/fₙ = (√5 + 1)/2`; (e) `gcd(fₙ, f_{n+1}) = 1`.

**Deferred:** requires a recursively-defined integer sequence (the Fibonacci
recurrence) plus, for parts (c) and (d), the real numbers, `√5`, and limits.
Deferred until a recursively-defined integer sequence and reals are available.

**Exercise 18.** If `gcd(a, b) = 1` and `ar + bs = 1`, then
`gcd(a, s) = gcd(r, b) = gcd(r, s) = 1`.

**Deferred:** needs the GCD-as-least-positive-combination characterization applied
three ways; deferred to keep to the results with std support.

**Exercise 19.** If `x, y ∈ ℕ` are relatively prime and `xy` is a perfect square,
then `x` and `y` are both perfect squares.

**Deferred:** needs a perfect-square predicate and an FTA-based multiplicativity
argument over prime factorizations; deferred.

**Exercise 20.** Using the division algorithm, every perfect square is of the
form `4k` or `4k + 1` for some integer `k`.

The Division Algorithm at divisor `2` writes any `n` as `2q + r` with `0 ≤ r < 2`,
so `r = 0` or `r = 1` (the small-remainder split below). If `n = 2q` then
`n² = (2q)² = 4·q²` (form `4k`, `k = q²`); if `n = 2q + 1` then
`n² = 4q² + 4q + 1 = 4·(q² + q) + 1` (form `4k + 1`, `k = q² + q`). The square
expansions are the ring drudgery Judson elides, so we take them by `polynomial`,
which folds the numeral coefficients (`2·2 → 4`, `2q + 2q → 4q`) directly.

First, the remainder of a division by `2` is `0` or `1`: from `r < 2` the
order-eliminator gives a nonnegative gap `d` with `r + succ(d) = 2`; splitting `d`
by `nonnegCases`, `d = 0` forces `succ(r) = 2`, i.e. `r = 1`, and `d = succ(m)`
forces `r + m = 0`, i.e. `r = 0`.

```bpa
theorem remainderBelowTwoIsZeroOrOne: forall r: Int; nonneg(r) -> less_than(r, TWO) -> (r = ZERO or r = ONE)
proof
  @generalize-r |
    fix r: Int {
      @given-r-nonneg |
        assume nonneg(r) {
          @given-r-below-two |
            assume less_than(r, TWO) {
              @r-below-two |
                less_than(r, TWO)
                [by hypothesis given-r-below-two]
              @gap-exists |
                exists d: Int; nonneg(d) and add(r, succ(d)) = TWO
                [by specialize lessThanElim(r, TWO) r-below-two]
              @with-gap-d |
                unpack d: Int from gap-exists {
                  @gap-nonneg-and-sum |
                    nonneg(d) and add(r, succ(d)) = TWO
                    [by hypothesis with-gap-d]
                  @gap-nonneg |
                    nonneg(d)
                    [by and_elim_left gap-nonneg-and-sum]
                  @remainder-plus-gap-is-two |
                    add(r, succ(d)) = TWO
                    [by and_elim_right gap-nonneg-and-sum]
                  @gap-is-zero-or-successor |
                    d = ZERO or (exists m: Int; nonneg(m) and d = succ(m))
                    [by specialize nonnegCases(d) gap-nonneg]
                  @remainder-is-zero-or-one |
                    r = ZERO or r = ONE
                    case gap-is-zero-or-successor {
                      @when-gap-zero |
                        assume d = ZERO {
                          @gap-is-zero |
                            d = ZERO
                            [by hypothesis when-gap-zero]
                          @remainder-plus-succ-zero-is-two |
                            add(r, succ(ZERO)) = TWO
                            [by rewrite gap-is-zero remainder-plus-gap-is-two]
                          @add-succ-zero-unfolds |
                            add(r, succ(ZERO)) = succ(add(r, ZERO))
                            [by specialize addSuccRight(r, ZERO)]
                          @add-zero-right |
                            add(r, ZERO) = r
                            [by specialize addZeroRight(r)]
                          @add-succ-zero-is-succ-r |
                            add(r, succ(ZERO)) = succ(r)
                            [by rewrite add-zero-right add-succ-zero-unfolds]
                          @succ-r-is-two |
                            succ(r) = TWO
                            [by chain add-succ-zero-is-succ-r remainder-plus-succ-zero-is-two]
                          @two-is-succ-one |
                            TWO = succ(ONE)
                            [by simplify]
                          @succ-r-is-succ-one |
                            succ(r) = succ(ONE)
                            [by chain succ-r-is-two two-is-succ-one]
                          @remainder-is-one |
                            r = ONE
                            [by specialize succInjective(r, ONE) succ-r-is-succ-one]
                          @conclusion-remainder-is-one |
                            r = ZERO or r = ONE
                            [by or_intro_right remainder-is-one]
                        }
                      @when-gap-successor |
                        assume exists m: Int; nonneg(m) and d = succ(m) {
                          @gap-successor-exists |
                            exists m: Int; nonneg(m) and d = succ(m)
                            [by hypothesis when-gap-successor]
                          @with-inner-m |
                            unpack m: Int from gap-successor-exists {
                              @inner-nonneg-and-gap-succ |
                                nonneg(m) and d = succ(m)
                                [by hypothesis with-inner-m]
                              @gap-is-succ-m |
                                d = succ(m)
                                [by and_elim_right inner-nonneg-and-gap-succ]
                              @inner-nonneg |
                                nonneg(m)
                                [by and_elim_left inner-nonneg-and-gap-succ]
                              @remainder-plus-succ-succ-is-two |
                                add(r, succ(succ(m))) = TWO
                                [by rewrite gap-is-succ-m remainder-plus-gap-is-two]
                              @add-succ-outer |
                                add(r, succ(succ(m))) = succ(add(r, succ(m)))
                                [by specialize addSuccRight(r, succ(m))]
                              @add-succ-inner |
                                add(r, succ(m)) = succ(add(r, m))
                                [by specialize addSuccRight(r, m)]
                              @add-succ-succ-unfolds |
                                add(r, succ(succ(m))) = succ(succ(add(r, m)))
                                [by chain add-succ-outer add-succ-inner]
                              @succ-succ-sum-is-two |
                                succ(succ(add(r, m))) = TWO
                                [by chain add-succ-succ-unfolds remainder-plus-succ-succ-is-two]
                              @one-is-succ-zero |
                                ONE = succ(ZERO)
                                [by axiom oneIsSuccZero]
                              @two-is-succ-one-again |
                                TWO = succ(ONE)
                                [by simplify]
                              @two-is-succ-succ-zero |
                                TWO = succ(succ(ZERO))
                                [by rewrite one-is-succ-zero two-is-succ-one-again]
                              @succ-succ-sum-is-succ-succ-zero |
                                succ(succ(add(r, m))) = succ(succ(ZERO))
                                [by chain succ-succ-sum-is-two two-is-succ-succ-zero]
                              @succ-sum-is-succ-zero |
                                succ(add(r, m)) = succ(ZERO)
                                [by specialize succInjective(succ(add(r, m)), succ(ZERO)) succ-succ-sum-is-succ-succ-zero]
                              @sum-remainder-inner-is-zero |
                                add(r, m) = ZERO
                                [by specialize succInjective(add(r, m), ZERO) succ-sum-is-succ-zero]
                              @remainder-nonneg-here |
                                nonneg(r)
                                [by hypothesis given-r-nonneg]
                              // r + m = 0 with r, m nonneg forces r = 0: were r a
                              // successor, r + m would be a nonneg successor, hence
                              // != 0.
                              @remainder-zero-or-successor |
                                r = ZERO or (exists j: Int; nonneg(j) and r = succ(j))
                                [by specialize nonnegCases(r) remainder-nonneg-here]
                              @remainder-is-zero-inner |
                                r = ZERO
                                case remainder-zero-or-successor {
                                  @when-remainder-already-zero |
                                    assume r = ZERO {
                                      @conclusion-remainder-already-zero |
                                        r = ZERO
                                        [by hypothesis when-remainder-already-zero]
                                    }
                                  @when-remainder-successor |
                                    assume exists j: Int; nonneg(j) and r = succ(j) {
                                      @remainder-successor-exists |
                                        exists j: Int; nonneg(j) and r = succ(j)
                                        [by hypothesis when-remainder-successor]
                                      @with-remainder-predecessor |
                                        unpack j: Int from remainder-successor-exists {
                                          @predecessor-nonneg-and-remainder-succ |
                                            nonneg(j) and r = succ(j)
                                            [by hypothesis with-remainder-predecessor]
                                          @predecessor-nonneg |
                                            nonneg(j)
                                            [by and_elim_left predecessor-nonneg-and-remainder-succ]
                                          @remainder-is-succ-j |
                                            r = succ(j)
                                            [by and_elim_right predecessor-nonneg-and-remainder-succ]
                                          @succ-j-plus-m-is-zero |
                                            add(succ(j), m) = ZERO
                                            [by rewrite remainder-is-succ-j sum-remainder-inner-is-zero]
                                          @succ-j-plus-m-unfolds |
                                            add(succ(j), m) = succ(add(j, m))
                                            [by specialize addSuccLeft(j, m)]
                                          @succ-of-sum-is-zero |
                                            succ(add(j, m)) = ZERO
                                            [by chain succ-j-plus-m-unfolds succ-j-plus-m-is-zero]
                                          @inner-sum-nonneg |
                                            nonneg(add(j, m))
                                            [by specialize nonnegAdd(j, m) predecessor-nonneg inner-nonneg]
                                          @succ-of-sum-not-zero |
                                            succ(add(j, m)) != ZERO
                                            [by specialize nonnegSuccNotZero(add(j, m)) inner-sum-nonneg]
                                          @conclusion-remainder-successor-absurd |
                                            r = ZERO
                                            [by absurd succ-of-sum-is-zero succ-of-sum-not-zero]
                                        }
                                      @conclusion-remainder-successor |
                                        r = ZERO
                                        [by exists_elim with-remainder-predecessor]
                                    }
                                }
                              @conclusion-remainder-is-zero |
                                r = ZERO or r = ONE
                                [by or_intro_left remainder-is-zero-inner]
                            }
                          @exported-remainder-is-zero |
                            r = ZERO or r = ONE
                            [by exists_elim with-inner-m]
                        }
                    }
                }
              @remainder-cases-exported |
                r = ZERO or r = ONE
                [by exists_elim with-gap-d]
            }
          @below-two-gives-cases |
            less_than(r, TWO) -> (r = ZERO or r = ONE)
            [by implies_intro given-r-below-two]
        }
      @nonneg-gives-cases |
        nonneg(r) -> less_than(r, TWO) -> (r = ZERO or r = ONE)
        [by implies_intro given-r-nonneg]
    }
  @conclusion |
    forall r: Int; nonneg(r) -> less_than(r, TWO) -> (r = ZERO or r = ONE)
    [by forall_intro generalize-r]
qed
```

The two square expansions are the ring drudgery Judson elides. `polynomial` now
folds these directly — it expands ring products AND folds numeral coefficients by
expansion (`2·2 → 4`, `2q + 2q → 4q`), so `(2q)² = 4q²` and `(2q+1)² = 4q² + 4q + 1`
are each a single step. Only the odd square, whose form `4·(q² + q) + 1` we reuse
below, needs stating as a named identity.

```bpa
// The odd square: (2q + 1)² = 4q² + 4q + 1 = 4·(q² + q) + 1. `polynomial` now
// expands the product AND folds the numeral coefficients (2·2 → 4, 2q + 2q → 4q),
// landing directly on the factored form 4·(q² + q) + 1.
theorem oddSquareIdentity: forall q: Int;
  mul(add(mul(TWO, q), ONE), add(mul(TWO, q), ONE)) = add(mul(FOUR, add(mul(q, q), q)), ONE)
proof
  @conclusion |
    forall q: Int; mul(add(mul(TWO, q), ONE), add(mul(TWO, q), ONE)) = add(mul(FOUR, add(mul(q, q), q)), ONE)
    [by polynomial_quantified(int_divides)]
qed
```

Now the exercise proper. `n²` is `4k` when `n` is even and `4k + 1` when `n` is odd.

```bpa
theorem perfectSquareIsFourKOrFourKPlusOne: forall n: Int;
  exists k: Int; (mul(n, n) = mul(FOUR, k)) or (mul(n, n) = add(mul(FOUR, k), ONE))
proof
  @generalize-n |
    fix n: Int {
      // 0 < 2, so the Division Algorithm applies at divisor 2.
      @zero-below-one |
        less_than(ZERO, ONE)
        [by theorem zeroLessThanOne]
      @zero-below-succ-one |
        less_than(ZERO, succ(ONE))
        [by specialize lessThanSuccRight(ZERO, ONE) zero-below-one]
      @two-is-succ-one |
        TWO = succ(ONE)
        [by simplify]
      @zero-below-two |
        less_than(ZERO, TWO)
        [by rewrite two-is-succ-one zero-below-succ-one]
      @quotient-remainder-exist |
        exists q: Int; exists r: Int; n = add(mul(TWO, q), r) and nonneg(r) and less_than(r, TWO)
        [by specialize divisionAlgorithmExists(n, TWO) zero-below-two]
      @with-quotient-q |
        unpack q: Int from quotient-remainder-exist {
          @remainder-exists |
            exists r: Int; n = add(mul(TWO, q), r) and nonneg(r) and less_than(r, TWO)
            [by hypothesis with-quotient-q]
          @with-remainder-r |
            unpack r: Int from remainder-exists {
              @decomposition-and-bounds |
                n = add(mul(TWO, q), r) and nonneg(r) and less_than(r, TWO)
                [by hypothesis with-remainder-r]
              @decomposition-and-nonneg |
                n = add(mul(TWO, q), r) and nonneg(r)
                [by and_elim_left decomposition-and-bounds]
              @decomposition |
                n = add(mul(TWO, q), r)
                [by and_elim_left decomposition-and-nonneg]
              @remainder-nonneg |
                nonneg(r)
                [by and_elim_right decomposition-and-nonneg]
              @remainder-below-two |
                less_than(r, TWO)
                [by and_elim_right decomposition-and-bounds]
              @remainder-is-zero-or-one |
                r = ZERO or r = ONE
                [by specialize remainderBelowTwoIsZeroOrOne(r) remainder-nonneg remainder-below-two]
              @square-is-four-k-form |
                exists k: Int; (mul(n, n) = mul(FOUR, k)) or (mul(n, n) = add(mul(FOUR, k), ONE))
                case remainder-is-zero-or-one {
                  // even case: n = 2q, so n² = 4·q², witness k = q².
                  @when-remainder-zero |
                    assume r = ZERO {
                      @remainder-is-zero |
                        r = ZERO
                        [by hypothesis when-remainder-zero]
                      @n-is-two-q |
                        n = mul(TWO, q)
                        [by arithmetic remainder-is-zero decomposition]
                      // n² = (2q)² = 4·q².
                      @square-reflexive |
                        mul(n, n) = mul(n, n)
                        [by reflexivity]
                      @square-left-substituted |
                        mul(n, n) = mul(mul(TWO, q), n)
                        [by rewrite n-is-two-q square-reflexive]
                      @square-both-substituted |
                        mul(n, n) = mul(mul(TWO, q), mul(TWO, q))
                        [by rewrite n-is-two-q square-left-substituted]
                      @even-square-expands |
                        mul(mul(TWO, q), mul(TWO, q)) = mul(FOUR, mul(q, q))
                        [by polynomial]
                      @even-square-is-four-q-squared |
                        mul(n, n) = mul(FOUR, mul(q, q))
                        [by chain square-both-substituted even-square-expands]
                      @even-square-is-four-k |
                        (mul(n, n) = mul(FOUR, mul(q, q))) or (mul(n, n) = add(mul(FOUR, mul(q, q)), ONE))
                        [by or_intro_left even-square-is-four-q-squared]
                      @conclusion-even-witness |
                        exists k: Int; (mul(n, n) = mul(FOUR, k)) or (mul(n, n) = add(mul(FOUR, k), ONE))
                        [by exists_intro(mul(q, q)) even-square-is-four-k]
                    }
                  // odd case: n = 2q + 1, so n² = 4(q² + q) + 1, witness k = q² + q.
                  @when-remainder-one |
                    assume r = ONE {
                      @remainder-is-one |
                        r = ONE
                        [by hypothesis when-remainder-one]
                      @n-is-two-q-plus-one |
                        n = add(mul(TWO, q), ONE)
                        [by rewrite remainder-is-one decomposition]
                      @square-reflexive |
                        mul(n, n) = mul(n, n)
                        [by reflexivity]
                      @square-left-substituted |
                        mul(n, n) = mul(add(mul(TWO, q), ONE), n)
                        [by rewrite n-is-two-q-plus-one square-reflexive]
                      @square-both-substituted |
                        mul(n, n) = mul(add(mul(TWO, q), ONE), add(mul(TWO, q), ONE))
                        [by rewrite n-is-two-q-plus-one square-left-substituted]
                      // (2q + 1)² = 4·(q² + q) + 1.
                      @odd-square-expands |
                        mul(add(mul(TWO, q), ONE), add(mul(TWO, q), ONE)) = add(mul(FOUR, add(mul(q, q), q)), ONE)
                        [by specialize oddSquareIdentity(q)]
                      @odd-square-is-four-k-plus-one |
                        mul(n, n) = add(mul(FOUR, add(mul(q, q), q)), ONE)
                        [by chain square-both-substituted odd-square-expands]
                      @odd-square-is-four-k-form |
                        (mul(n, n) = mul(FOUR, add(mul(q, q), q))) or (mul(n, n) = add(mul(FOUR, add(mul(q, q), q)), ONE))
                        [by or_intro_right odd-square-is-four-k-plus-one]
                      @conclusion-odd-witness |
                        exists k: Int; (mul(n, n) = mul(FOUR, k)) or (mul(n, n) = add(mul(FOUR, k), ONE))
                        [by exists_intro(add(mul(q, q), q)) odd-square-is-four-k-form]
                    }
                }
            }
          @square-form-from-remainder |
            exists k: Int; (mul(n, n) = mul(FOUR, k)) or (mul(n, n) = add(mul(FOUR, k), ONE))
            [by exists_elim with-remainder-r]
        }
      @square-form-from-quotient |
        exists k: Int; (mul(n, n) = mul(FOUR, k)) or (mul(n, n) = add(mul(FOUR, k), ONE))
        [by exists_elim with-quotient-q]
    }
  @conclusion |
    forall n: Int; exists k: Int; (mul(n, n) = mul(FOUR, k)) or (mul(n, n) = add(mul(FOUR, k), ONE))
    [by forall_intro generalize-n]
qed
```

**Exercise 21.** If `a, b, r, s` are pairwise relatively prime and `a² + b² = r²`,
`a² − b² = s²`, then `a, r, s` are odd and `b` is even.

**Deferred:** heavy parity + coprimality case analysis across four pairwise-coprime
integers; deferred.

**Exercise 27.** If `gcd(a, b) = 1` and `a | bc`, then `a | c`.

(Forwarded — this is Euclid's Lemma / the coprime corollary proved in the main
text §2.3; see `coprimeBezout` and the Euclid's-Lemma theorem `euclidsLemma` in
`std/primes.bpa`.)

**Exercise 28.** If `p ≥ 2` and `2^p − 1` is prime, then `p` is prime.

**Deferred:** the contrapositive needs the factorization
`2^{ab} − 1 = (2^a − 1)(1 + 2^a + ⋯ + 2^{a(b−1)})`, i.e. `(2^a − 1) | (2^p − 1)`
whenever `a | p`; this geometric-series divisibility identity over ℤ is not yet in
`std`. Deferred.

**Exercise 29.** There are infinitely many primes of the form `6n + 5`.

**Deferred:** the Dirichlet-type argument needs the mod-6 residue classification
of primes and a Euclid-style construction specialized to `6n + 5`; deferred.

**Exercise 30.** There are infinitely many primes of the form `4n − 1`.

**Deferred:** same as Exercise 29 — needs the mod-4 residue classification and a
`4n − 1`-specialized Euclid construction; deferred.

**Exercise 31.** Using that `2` is prime, there do not exist integers `p, q` with
`p² = 2q²`; hence `√2` is not rational.

The integer core — no coprime pair `(p, q)` has `p² = 2q²` — is proved in
`examples/sqrt2.bpa` over ℕ (`sqrtTwoIrrational`), by the parity descent: `2 | p²`
gives `2 | p` (the crux `evenSquareEven`), then cancelling a `2` gives `2 | q`, so
`2 | gcd(p, q) = 1`, impossible.

**Deferred:** the ℕ proof in `examples/sqrt2.bpa` leans on `std/peano-parity.bpa`'s
`evenSquareEven` (`2 | p² → 2 | p`) and the ℕ `gcd`/`mulCancelLeft`; the ℤ port
needs the corresponding integer parity crux (recoverable as `euclidsLemma` at the
prime `2`, once `is_prime(2)` and a descent/coprimality normalization are set up),
which is a substantial development. Deferred with a pointer to
`examples/sqrt2.bpa` for the completed integer core. The `√2 ∉ ℚ` conclusion
itself is separately deferred: it needs the rational numbers, which `bpa` does not
yet have.
