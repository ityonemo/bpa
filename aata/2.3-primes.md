# The Integers — Primes

A literate `bpa` transliteration of **Chapter 2 ("The Integers"), §2.2 (Greatest
Common Divisors and Primes)** of Thomas W. Judson's *Abstract Algebra* (source:
`vendor/aata/src/integers.xml`; © 1997–2026 Judson, GFDL 1.3). Run
`bpa check aata/2.3-primes.md`.

This section proves the **GCD / Bézout theorem** and its **coprime corollary**,
following Judson's argument. They build on the Division Algorithm proved in
`std/integer-division.bpa` (§2.2). We bring in that library, the ℤ order,
divisibility, and the Well-Ordering schema, naming the arithmetic vocabulary
this section will use. Because there is no `gcd` function, we *characterize* the
greatest common divisor by its defining properties (positive, a common divisor,
divisible by every common divisor).

```bpa
import division <<< "std/integer-division.bpa"
import order <<< "std/integer-order.bpa"
import divides <<< "std/integer-divides.bpa"
import ring <<< "std/integer.bpa"
import wellordering <<< "std/integer-wellordering.bpa"

sort Int = division.Int
const ZERO = division.ZERO
const ONE = division.ONE
func succ = division.succ
func prev = order.prev
func add = division.add
func mul = division.mul
func sub = division.sub
func neg = division.neg
pred less_than = division.less_than
pred at_most = division.at_most
pred is_nonneg = division.is_nonneg
pred does_divide = division.does_divide

theorem divisionAlgorithmExists = division.divisionAlgorithmExists
theorem divisionAlgorithmUnique = division.divisionAlgorithmUnique
axiom oneIsSuccZero = division.oneIsSuccZero

// -- order and nonneg vocabulary the argument names explicitly --------------
theorem trichotomy = order.trichotomy
theorem lessThanIrreflexive = order.lessThanIrreflexive
theorem lessThanTransitive = order.lessThanTransitive
theorem lessThanToLessOrEqual = order.lessThanToLessOrEqual
theorem lessOrEqualSplit = order.lessOrEqualSplit
theorem lessThanSuccRight = order.lessThanSuccRight
theorem negLessThanZero = order.negLessThanZero
theorem nonnegCases = order.nonnegCases
axiom lessThanElim = order.lessThanElim
axiom lessThanIntro = order.lessThanIntro
axiom nonnegZero = order.nonnegZero
axiom nonnegSucc = order.nonnegSucc
axiom addZeroLeft = order.addZeroLeft
axiom addSuccLeft = order.addSuccLeft
axiom prevSucc = division.prevSucc

// -- divisibility intro/elim and the closure lemmas -------------------------
// `does_divide` is now a transparent `define` (= `divides.divides(d, n)` = the
// existential `exists k; n = mul(d, k)`), so the old `dividesIntro`/`dividesElim`
// AXIOMS are gone. Recover them here as local THEOREMS proved from the definition:
// intro is `exists_intro`, elim is the identity (the pred IS the existential).
theorem dividesMul = divides.dividesMul
theorem dividesAdd = divides.dividesAdd

theorem dividesIntro: forall n, d, k: Int; n = mul(d, k) -> does_divide(d, n)
proof
  @generalize-n |
    fix n: Int {
      @generalize-d |
        fix d: Int {
          @generalize-k |
            fix k: Int {
              @given-n-is-dk |
                assume n = mul(d, k) {
                  @n-is-dk |
                    n = mul(d, k)
                    [by hypothesis given-n-is-dk]
                  @some-k-works |
                    does_divide(d, n)
                    [by exists_intro(k) n-is-dk]
                }
              @intro-at-n-d-k |
                n = mul(d, k) -> does_divide(d, n)
                [by implies_intro given-n-is-dk]
            }
          @discharge-k |
            forall k: Int; n = mul(d, k) -> does_divide(d, n)
            [by forall_intro generalize-k]
        }
      @discharge-d |
        forall d, k: Int; n = mul(d, k) -> does_divide(d, n)
        [by forall_intro generalize-d]
    }
  @conclusion |
    forall n, d, k: Int; n = mul(d, k) -> does_divide(d, n)
    [by forall_intro generalize-n]
qed

theorem dividesElim: forall d, m: Int; does_divide(d, m) -> exists k: Int; m = mul(d, k)
proof
  @generalize-d |
    fix d: Int {
      @generalize-m |
        fix m: Int {
          @given-d-divides-m |
            assume does_divide(d, m) {
              @the-existential |
                exists k: Int; m = mul(d, k)
                [by hypothesis given-d-divides-m]
            }
          @elim-at-d-m |
            does_divide(d, m) -> exists k: Int; m = mul(d, k)
            [by implies_intro given-d-divides-m]
        }
      @discharge-m |
        forall m: Int; does_divide(d, m) -> exists k: Int; m = mul(d, k)
        [by forall_intro generalize-m]
    }
  @conclusion |
    forall d, m: Int; does_divide(d, m) -> exists k: Int; m = mul(d, k)
    [by forall_intro generalize-d]
qed

// -- ring rearrangements the book elides ------------------------------------
axiom subDef = order.subDef
theorem mulOneRight = ring.mulOneRight
theorem mulZeroRight = ring.mulZeroRight
theorem mulNegRight = ring.mulNegRight
theorem mulAddDistribLeft = ring.mulAddDistribLeft
theorem mulAddDistribRight = ring.mulAddDistribRight
theorem mulIsAssociative = ring.mulIsAssociative
theorem addIsCommutative = ring.addIsCommutative
theorem addIsAssociative = ring.addIsAssociative
theorem addZeroRight = ring.addZeroRight
theorem addSuccRight = ring.addSuccRight
theorem addNegRight = ring.addNegRight
theorem addNegLeft = ring.addNegLeft
theorem negNeg = ring.negNeg
theorem negAdd = ring.negAdd
theorem addDiffReaches = order.addDiffReaches
theorem addCancelLeft = order.addCancelLeft

// -- the Well-Ordering schema ------------------------------------------------
theorem nonnegWellOrdering = wellordering.nonnegWellOrdering
```

A positive integer is in particular nonnegative — the fact that lets the
Well-Ordering schema's guard (`is_nonneg`) be discharged from `less_than(ZERO, x)`.
Unfolding `less_than(ZERO, x)` gives a nonnegative gap `d` with `ZERO + succ(d) = x`;
since `add(ZERO, succ(d)) = succ(d)` and the successor of a nonnegative is
nonnegative, `x` itself is nonnegative.

```bpa
theorem positiveIsNonneg: forall x: Int; less_than(ZERO, x) -> is_nonneg(x)
proof
  @generalize-x |
    fix x: Int {
      @given-x-positive |
        assume less_than(ZERO, x) {
          @x-positive |
            less_than(ZERO, x)
            [by hypothesis given-x-positive]
          @less-than-elim |
            forall a, b: Int; less_than(a, b) -> exists d: Int; is_nonneg(d) and add(a, succ(d)) = b
            [by axiom lessThanElim]
          @gap-exists |
            exists d: Int; is_nonneg(d) and add(ZERO, succ(d)) = x
            [by specialize lessThanElim(ZERO, x) x-positive]
          @with-gap-d |
            unpack d: Int from gap-exists {
              @gap-nonneg-and-sums |
                is_nonneg(d) and add(ZERO, succ(d)) = x
                [by hypothesis with-gap-d]
              @gap-nonneg |
                is_nonneg(d)
                [by and_elim_left gap-nonneg-and-sums]
              @gap-sums-to-x |
                add(ZERO, succ(d)) = x
                [by and_elim_right gap-nonneg-and-sums]
              @add-zero-left |
                forall n: Int; add(ZERO, n) = n
                [by axiom addZeroLeft]
              @zero-plus-succ-is-succ |
                add(ZERO, succ(d)) = succ(d)
                [by forall_elim(succ(d)) add-zero-left]
              @succ-gap-is-x |
                succ(d) = x
                [by rewrite zero-plus-succ-is-succ gap-sums-to-x]
              @succ-gap-nonneg |
                is_nonneg(succ(d))
                [by specialize nonnegSucc(d) gap-nonneg]
              @conclusion-x-nonneg |
                is_nonneg(x)
                [by rewrite succ-gap-is-x succ-gap-nonneg]
            }
          @x-nonneg |
            is_nonneg(x)
            [by exists_elim with-gap-d]
        }
      @positive-implies-nonneg |
        less_than(ZERO, x) -> is_nonneg(x)
        [by implies_intro given-x-positive]
    }
  @conclusion |
    forall x: Int; less_than(ZERO, x) -> is_nonneg(x)
    [by forall_intro generalize-x]
qed
```

The Bézout argument will, at its crux, learn of a remainder `r'` that is both
nonnegative and *not* positive, and must conclude `r' = ZERO`. A nonnegative
integer splits (`nonnegCases`) into `ZERO` or a successor `succ(m)` of a
nonnegative `m`; a successor is positive (`add(ZERO, succ(m)) = succ(m)`, so
`lessThanIntro` gives `less_than(ZERO, succ(m))`), so if the integer is *not*
positive the successor case is impossible and it must be `ZERO`.

```bpa
theorem nonnegNotPositiveIsZero: forall x: Int; is_nonneg(x) -> (not less_than(ZERO, x)) -> x = ZERO
proof
  @generalize-x |
    fix x: Int {
      @given-x-nonneg |
        assume is_nonneg(x) {
          @given-x-not-positive |
            assume not less_than(ZERO, x) {
              @x-nonneg |
                is_nonneg(x)
                [by hypothesis given-x-nonneg]
              @x-not-positive |
                not less_than(ZERO, x)
                [by hypothesis given-x-not-positive]
              @nonneg-cases |
                forall n: Int; is_nonneg(n) -> (n = ZERO or (exists m: Int; is_nonneg(m) and n = succ(m)))
                [by theorem nonnegCases]
              @x-zero-or-successor |
                x = ZERO or (exists m: Int; is_nonneg(m) and x = succ(m))
                [by specialize nonnegCases(x) x-nonneg]
              @conclusion-x-is-zero |
                x = ZERO
                case x-zero-or-successor {
                  @when-x-is-zero |
                    assume x = ZERO {
                      @conclusion-x-is-zero |
                        x = ZERO
                        [by hypothesis when-x-is-zero]
                    }
                  @when-x-is-successor |
                    assume exists m: Int; is_nonneg(m) and x = succ(m) {
                      @successor-exists |
                        exists m: Int; is_nonneg(m) and x = succ(m)
                        [by hypothesis when-x-is-successor]
                      @with-predecessor-m |
                        unpack m: Int from successor-exists {
                          @gap-nonneg-and-x-is-successor |
                            is_nonneg(m) and x = succ(m)
                            [by hypothesis with-predecessor-m]
                          @gap-nonneg |
                            is_nonneg(m)
                            [by and_elim_left gap-nonneg-and-x-is-successor]
                          @x-is-successor |
                            x = succ(m)
                            [by and_elim_right gap-nonneg-and-x-is-successor]
                          @add-zero-left |
                            forall n: Int; add(ZERO, n) = n
                            [by axiom addZeroLeft]
                          @zero-plus-successor-is-successor |
                            add(ZERO, succ(m)) = succ(m)
                            [by forall_elim(succ(m)) add-zero-left]
                          @successor-is-x |
                            succ(m) = x
                            [by symmetry x-is-successor]
                          @zero-plus-successor-is-x |
                            add(ZERO, succ(m)) = x
                            [by rewrite successor-is-x zero-plus-successor-is-successor]
                          @less-than-intro |
                            forall a, d, b: Int; is_nonneg(d) -> add(a, succ(d)) = b -> less_than(a, b)
                            [by axiom lessThanIntro]
                          @x-is-positive |
                            less_than(ZERO, x)
                            [by specialize lessThanIntro(ZERO, m, x) gap-nonneg zero-plus-successor-is-x]
                          @conclusion-x-is-zero |
                            x = ZERO
                            [by absurd x-is-positive x-not-positive]
                        }
                      @conclusion-x-is-zero |
                        x = ZERO
                        [by exists_elim with-predecessor-m]
                    }
                }
            }
          @not-positive-implies-zero |
            (not less_than(ZERO, x)) -> x = ZERO
            [by implies_intro given-x-not-positive]
        }
      @nonneg-implies-not-positive-implies-zero |
        is_nonneg(x) -> (not less_than(ZERO, x)) -> x = ZERO
        [by implies_intro given-x-nonneg]
    }
  @conclusion |
    forall x: Int; is_nonneg(x) -> (not less_than(ZERO, x)) -> x = ZERO
    [by forall_intro generalize-x]
qed
```

Judson's nonemptiness argument splits on the sign of `a`: when `a` is positive
the witness `a·1 + b·0 = a` is a positive member; when `a` is negative the
witness `a·(−1) + b·0 = −a` is, and this needs that the negation of a negative
integer is positive. Unfolding `less_than(a, ZERO)` gives a nonnegative gap `d` with
`add(a, succ(d)) = ZERO`; adding `−a` on the left and cancelling `a` shows
`succ(d) = −a`, and `lessThanIntro` reads `less_than(ZERO, −a)` off that.

```bpa
theorem negativeNegIsPositive: forall a: Int; less_than(a, ZERO) -> less_than(ZERO, neg(a))
proof
  @generalize-a |
    fix a: Int {
      @given-a-negative |
        assume less_than(a, ZERO) {
          @a-negative |
            less_than(a, ZERO)
            [by hypothesis given-a-negative]
          @less-than-elim |
            forall x, y: Int; less_than(x, y) -> exists d: Int; is_nonneg(d) and add(x, succ(d)) = y
            [by axiom lessThanElim]
          @gap-exists |
            exists d: Int; is_nonneg(d) and add(a, succ(d)) = ZERO
            [by specialize lessThanElim(a, ZERO) a-negative]
          @with-gap-d |
            unpack d: Int from gap-exists {
              @gap-nonneg-and-sums |
                is_nonneg(d) and add(a, succ(d)) = ZERO
                [by hypothesis with-gap-d]
              @gap-nonneg |
                is_nonneg(d)
                [by and_elim_left gap-nonneg-and-sums]
              @a-plus-successor-is-zero |
                add(a, succ(d)) = ZERO
                [by and_elim_right gap-nonneg-and-sums]
              // succ(d) = -a: add -a on the left, regroup, and cancel a.
              @add-is-associative |
                forall x, y, z: Int; add(add(x, y), z) = add(x, add(y, z))
                [by theorem addIsAssociative]
              @regroup |
                add(add(neg(a), a), succ(d)) = add(neg(a), add(a, succ(d)))
                [by forall_elim(neg(a), a, succ(d)) add-is-associative]
              @add-neg-left |
                forall n: Int; add(neg(n), n) = ZERO
                [by theorem addNegLeft]
              @neg-a-plus-a-is-zero |
                add(neg(a), a) = ZERO
                [by specialize addNegLeft(a)]
              @add-zero-right-lemma |
                forall n: Int; add(n, ZERO) = n
                [by theorem addZeroRight]
              @neg-a-plus-zero-is-neg-a |
                add(neg(a), ZERO) = neg(a)
                [by forall_elim(neg(a)) add-zero-right-lemma]
              // read less_than(ZERO, -a) off add(ZERO, succ(d)) = -a.
              @zero-plus-successor-is-neg-a |
                add(ZERO, succ(d)) = neg(a)
                [by chain neg-a-plus-a-is-zero regroup a-plus-successor-is-zero neg-a-plus-zero-is-neg-a]
              @less-than-intro |
                forall x, e, y: Int; is_nonneg(e) -> add(x, succ(e)) = y -> less_than(x, y)
                [by axiom lessThanIntro]
              @neg-a-positive-if-summed |
                is_nonneg(d) -> add(ZERO, succ(d)) = neg(a) -> less_than(ZERO, neg(a))
                [by forall_elim(ZERO, d, neg(a)) less-than-intro]
              @conclusion-neg-a-positive |
                less_than(ZERO, neg(a))
                [by tautology neg-a-positive-if-summed gap-nonneg zero-plus-successor-is-neg-a]
            }
          @neg-a-positive |
            less_than(ZERO, neg(a))
            [by exists_elim with-gap-d]
        }
      @negative-implies-neg-positive |
        less_than(a, ZERO) -> less_than(ZERO, neg(a))
        [by implies_intro given-a-negative]
    }
  @conclusion |
    forall a: Int; less_than(a, ZERO) -> less_than(ZERO, neg(a))
    [by forall_intro generalize-a]
qed
```

The heart of Judson's argument that `d = ar + bs` divides `a` runs the Division
Algorithm to write `a = dq + r'` and shows the remainder `r'` is *itself* a
member of `S`: substituting `d = ar + bs`, Judson computes `r' = a − dq =
a − (ar + bs)q = a(1 − rq) + b(−sq)` in one line. We isolate that elided ring
identity here. From `a = dq + r'` we recover `r' = a − dq` by cancelling `dq`
(via `addDiffReaches` and `addCancelLeft`); substituting `d` and rearranging by
distributing and collecting the `a` and `b` coefficients (`simplify` over the
ring lemmas) gives the member form `r' = a(1 − rq) + b(−sq)`.

```bpa
theorem remainderIsMember(a: Int, b: Int, d: Int, r: Int, s: Int, q: Int, remainder: Int):
  a = add(mul(d, q), remainder) -> d = add(mul(a, r), mul(b, s))
  -> remainder = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
proof
  @given-a-decomposes |
    assume a = add(mul(d, q), remainder) {
      @given-d-value |
        assume d = add(mul(a, r), mul(b, s)) {
          @a-decomposes |
            a = add(mul(d, q), remainder)
            [by hypothesis given-a-decomposes]
          @d-value |
            d = add(mul(a, r), mul(b, s))
            [by hypothesis given-d-value]
          // remainder = a - dq, by cancelling dq off a = dq + remainder.
          @difference-reaches |
            forall x, y: Int; add(x, sub(y, x)) = y
            [by theorem addDiffReaches]
          @dq-plus-difference-is-a |
            add(mul(d, q), sub(a, mul(d, q))) = a
            [by forall_elim(mul(d, q), a) difference-reaches]
          @dq-plus-difference-is-decomposition |
            add(mul(d, q), sub(a, mul(d, q))) = add(mul(d, q), remainder)
            [by rewrite a-decomposes dq-plus-difference-is-a]
          @cancel-left |
            forall c, x, y: Int; add(c, x) = add(c, y) -> x = y
            [by theorem addCancelLeft]
          @cancel-the-quotient-term |
            add(mul(d, q), sub(a, mul(d, q))) = add(mul(d, q), remainder) -> sub(a, mul(d, q)) = remainder
            [by forall_elim(mul(d, q), sub(a, mul(d, q)), remainder) cancel-left]
          @difference-is-remainder |
            sub(a, mul(d, q)) = remainder
            [by modus_ponens cancel-the-quotient-term dq-plus-difference-is-decomposition]
          // substitute d = ar + bs into a - dq, then the elided algebra
          // a - (ar + bs)q = a(1 - rq) + b(-sq).
          @difference-collects-coefficients |
            sub(a, mul(add(mul(a, r), mul(b, s)), q)) = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
            [by simplify subDef mulAddDistribLeft mulAddDistribRight mulNegRight mulOneRight mulIsAssociative addIsAssociative negAdd]
          @conclusion-remainder-is-member |
            remainder = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
            [by chain difference-is-remainder d-value difference-collects-coefficients]
        }
      @d-value-implies-member |
        d = add(mul(a, r), mul(b, s)) -> remainder = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
        [by implies_intro given-d-value]
    }
  @conclusion |
    a = add(mul(d, q), remainder) -> d = add(mul(a, r), mul(b, s)) -> remainder = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
    [by implies_intro given-a-decomposes]
qed
```

The coprime corollary will produce a positive common divisor `d` that must equal
`ONE` or `−ONE`; the second alternative is ruled out because `−1` is *less_than*
zero, so it cannot also be positive. We record that `−1 < 0` here: `−1` is
nonnegatively-gapped less_than `0` by `succ(ZERO) = ONE`, since
`add(neg(ONE), succ(ZERO)) = add(neg(ONE), ONE) = ZERO`, and `lessThanIntro`
reads `less_than(neg(ONE), ZERO)` off that.

```bpa
theorem negOneLessThanZero: less_than(neg(ONE), ZERO)
proof
  @one-is-succ-zero |
    ONE = succ(ZERO)
    [by axiom oneIsSuccZero]
  @add-neg-left |
    forall n: Int; add(neg(n), n) = ZERO
    [by theorem addNegLeft]
  @neg-one-plus-one-is-zero |
    add(neg(ONE), ONE) = ZERO
    [by specialize addNegLeft(ONE)]
  @neg-one-plus-succ-zero-is-zero |
    add(neg(ONE), succ(ZERO)) = ZERO
    [by rewrite one-is-succ-zero neg-one-plus-one-is-zero]
  @zero-is-nonneg |
    is_nonneg(ZERO)
    [by axiom nonnegZero]
  @less-than-intro |
    forall a, d, b: Int; is_nonneg(d) -> add(a, succ(d)) = b -> less_than(a, b)
    [by axiom lessThanIntro]
  @neg-one-negative-if-summed |
    is_nonneg(ZERO) -> add(neg(ONE), succ(ZERO)) = ZERO -> less_than(neg(ONE), ZERO)
    [by forall_elim(neg(ONE), ZERO, ZERO) less-than-intro]
  @conclusion |
    less_than(neg(ONE), ZERO)
    [by tautology neg-one-negative-if-summed zero-is-nonneg neg-one-plus-succ-zero-is-zero]
qed
```

Before invoking Well-Ordering, Judson must exhibit *some* positive member of
`S = {am + bn : am + bn > 0}` — the set is nonempty because `|a| ∈ S`. Since
`a ≠ 0`, trichotomy splits `a` into positive or negative (the `a = 0` case is
excluded). If `a > 0`, the combination `a·1 + b·0 = a` is a positive member; if
`a < 0`, then `−a > 0` (`negativeNegIsPositive`) and `a·(−1) + b·0 = −a` is a
positive member. Either way the witness is positive, hence nonnegative
(`positiveIsNonneg`), discharging the schema's guard. We package this nonemptiness
as a schema in `a`, `b`, so its conclusion matches the exact shape the
Well-Ordering instance less_than consumes.

```bpa
theorem combinationSetNonempty(a: Int, b: Int):
  a != ZERO
  -> exists n: Int; is_nonneg(n) and (less_than(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
proof
  @given-a-nonzero |
    assume a != ZERO {
      @a-nonzero |
        a != ZERO
        [by hypothesis given-a-nonzero]
      @order-is-trichotomous |
        forall x, y: Int; less_than(x, y) or x = y or less_than(y, x)
        [by theorem trichotomy]
      @a-sign-split |
        less_than(a, ZERO) or a = ZERO or less_than(ZERO, a)
        [by specialize trichotomy(a, ZERO)]
      @conclusion-set-nonempty |
        exists n: Int; is_nonneg(n) and (less_than(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
        case a-sign-split {
          @when-a-negative |
            assume less_than(a, ZERO) {
              @a-negative |
                less_than(a, ZERO)
                [by hypothesis when-a-negative]
              @neg-a-positive |
                less_than(ZERO, neg(a))
                [by specialize negativeNegIsPositive(a) a-negative]
              @positive-is-nonneg |
                forall x: Int; less_than(ZERO, x) -> is_nonneg(x)
                [by theorem positiveIsNonneg]
              @positive-is-nonneg-at-neg-a |
                less_than(ZERO, neg(a)) -> is_nonneg(neg(a))
                [by forall_elim(neg(a)) positive-is-nonneg]
              @neg-a-nonneg |
                is_nonneg(neg(a))
                [by modus_ponens positive-is-nonneg-at-neg-a neg-a-positive]
              @neg-a-is-combination |
                neg(a) = add(mul(a, neg(ONE)), mul(b, ZERO))
                [by simplify mulNegRight mulOneRight mulZeroRight addZeroRight]
              @neg-a-is-some-combination-in-nn |
                exists nn: Int; neg(a) = add(mul(a, neg(ONE)), mul(b, nn))
                [by exists_intro(ZERO) neg-a-is-combination]
              @neg-a-is-a-combination |
                exists mm: Int; exists nn: Int; neg(a) = add(mul(a, mm), mul(b, nn))
                [by exists_intro(neg(ONE)) neg-a-is-some-combination-in-nn]
              @neg-a-positive-and-combination |
                less_than(ZERO, neg(a)) and (exists mm: Int; exists nn: Int; neg(a) = add(mul(a, mm), mul(b, nn)))
                [by and_intro neg-a-positive neg-a-is-a-combination]
              @neg-a-is-nonneg-member |
                is_nonneg(neg(a)) and (less_than(ZERO, neg(a)) and (exists mm: Int; exists nn: Int; neg(a) = add(mul(a, mm), mul(b, nn))))
                [by and_intro neg-a-nonneg neg-a-positive-and-combination]
              @conclusion-set-nonempty |
                exists n: Int; is_nonneg(n) and (less_than(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
                [by exists_intro(neg(a)) neg-a-is-nonneg-member]
            }
          @when-a-zero |
            assume a = ZERO {
              @a-is-zero |
                a = ZERO
                [by hypothesis when-a-zero]
              @conclusion-set-nonempty |
                exists n: Int; is_nonneg(n) and (less_than(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
                [by absurd a-is-zero a-nonzero]
            }
          @when-a-positive |
            assume less_than(ZERO, a) {
              @a-positive |
                less_than(ZERO, a)
                [by hypothesis when-a-positive]
              @a-nonneg |
                is_nonneg(a)
                [by specialize positiveIsNonneg(a) a-positive]
              @a-is-combination |
                a = add(mul(a, ONE), mul(b, ZERO))
                [by simplify mulOneRight mulZeroRight addZeroRight]
              @a-is-some-combination-in-nn |
                exists nn: Int; a = add(mul(a, ONE), mul(b, nn))
                [by exists_intro(ZERO) a-is-combination]
              @a-is-a-combination |
                exists mm: Int; exists nn: Int; a = add(mul(a, mm), mul(b, nn))
                [by exists_intro(ONE) a-is-some-combination-in-nn]
              @a-positive-and-combination |
                less_than(ZERO, a) and (exists mm: Int; exists nn: Int; a = add(mul(a, mm), mul(b, nn)))
                [by and_intro a-positive a-is-a-combination]
              @a-is-nonneg-member |
                is_nonneg(a) and (less_than(ZERO, a) and (exists mm: Int; exists nn: Int; a = add(mul(a, mm), mul(b, nn))))
                [by and_intro a-nonneg a-positive-and-combination]
              @conclusion-set-nonempty |
                exists n: Int; is_nonneg(n) and (less_than(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
                [by exists_intro(a) a-is-nonneg-member]
            }
        }
    }
  @conclusion |
    a != ZERO
      -> exists n: Int; is_nonneg(n) and (less_than(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
    [by implies_intro given-a-nonzero]
qed
```

## The GCD / Bézout Theorem

> **Theorem 2.10.** Let $a$ and $b$ be nonzero integers. Then there exist integers
> $r$ and $s$ such that $\gcd(a,b) = ar + bs$. Furthermore, the greatest common
> divisor of $a$ and $b$ is unique.

There is no `gcd` *function* in this development, so we prove the theorem in the
form that *characterizes* the gcd by its defining properties. `bezout` asserts a
single positive integer `d` that (i) divides `a` and `b`, (ii) is divisible by
every common divisor `e`, and (iii) is an integer combination `d = ar + bs`.
Uniqueness is then automatic: any two integers with property (ii) divide each
other, and — being positive — are equal; and (iii) is Bézout's identity itself.

Judson's proof: let `S = {am + bn : am + bn > 0}`. `S` is nonempty
(`combinationSetNonempty`), and every member is positive hence nonnegative, so
Well-Ordering hands us a *least* member `d = ar + bs`. We claim `d = gcd(a,b)`.
Running the Division Algorithm on `a` by `d` gives `a = dq + r'` with
`0 ≤ r' < d`. Were `r' > 0`, the identity `r' = a(1 − rq) + b(−sq)`
(`remainderIsMember`) would make `r'` a *member of `S` smaller than `d`*,
contradicting minimality; so `r' = 0` and `d ∣ a`. Symmetrically `d ∣ b`. Finally
any common divisor `e` of `a`, `b` divides `ar + bs = d`, so `d` is the greatest.

```bpa
theorem bezout: forall a, b: Int; a != ZERO -> b != ZERO ->
  exists d: Int;
  less_than(ZERO, d)
  and does_divide(d, a) and does_divide(d, b)
  and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d))
  and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @given-a-nonzero |
            assume a != ZERO {
              @given-b-nonzero |
                assume b != ZERO {
                  @a-nonzero |
                    a != ZERO
                    [by hypothesis given-a-nonzero]
                  // Nonemptiness of S = {am + bn > 0}, from combinationSetNonempty.
                  @set-nonempty-if-a-nonzero |
                    a != ZERO
                      -> exists n: Int; is_nonneg(n) and (less_than(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
                    [by instantiate combinationSetNonempty(a, b)]
                  @set-is-nonempty |
                    exists n: Int; is_nonneg(n) and (less_than(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
                    [by modus_ponens set-nonempty-if-a-nonzero a-nonzero]
                  // Well-Ordering: S has a least member d.
                  @well-ordering-on-S |
                    (exists n: Int; is_nonneg(n) and (less_than(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn)))))
                      -> exists least: Int; is_nonneg(least) and (less_than(ZERO, least) and (exists mm: Int; exists nn: Int; least = add(mul(a, mm), mul(b, nn)))) and (forall m: Int; is_nonneg(m) -> (less_than(ZERO, m) and (exists mm: Int; exists nn: Int; m = add(mul(a, mm), mul(b, nn)))) -> at_most(least, m))
                    [by instantiate nonnegWellOrdering((fun x: Int => less_than(ZERO, x) and (exists mm: Int; exists nn: Int; x = add(mul(a, mm), mul(b, nn)))))]
                  @least-member-exists |
                    exists least: Int; is_nonneg(least) and (less_than(ZERO, least) and (exists mm: Int; exists nn: Int; least = add(mul(a, mm), mul(b, nn)))) and (forall m: Int; is_nonneg(m) -> (less_than(ZERO, m) and (exists mm: Int; exists nn: Int; m = add(mul(a, mm), mul(b, nn)))) -> at_most(least, m))
                    [by modus_ponens well-ordering-on-S set-is-nonempty]
                  @with-least-member-d |
                    unpack d: Int from least-member-exists {
                      @d-is-least-member |
                        is_nonneg(d) and (less_than(ZERO, d) and (exists mm: Int; exists nn: Int; d = add(mul(a, mm), mul(b, nn)))) and (forall m: Int; is_nonneg(m) -> (less_than(ZERO, m) and (exists mm: Int; exists nn: Int; m = add(mul(a, mm), mul(b, nn)))) -> at_most(d, m))
                        [by hypothesis with-least-member-d]
                      @d-is-positive |
                        less_than(ZERO, d)
                        [by tautology d-is-least-member]
                      @d-is-a-combination |
                        exists mm: Int; exists nn: Int; d = add(mul(a, mm), mul(b, nn))
                        [by tautology d-is-least-member]
                      @d-is-least |
                        forall m: Int; is_nonneg(m) -> (less_than(ZERO, m) and (exists mm: Int; exists nn: Int; m = add(mul(a, mm), mul(b, nn)))) -> at_most(d, m)
                        [by tautology d-is-least-member]
                      // name the coefficients: d = ar + bs.
                      @with-coefficient-r |
                        unpack r: Int from d-is-a-combination {
                          @d-is-combination-in-r |
                            exists nn: Int; d = add(mul(a, r), mul(b, nn))
                            [by hypothesis with-coefficient-r]
                          @with-coefficient-s |
                            unpack s: Int from d-is-combination-in-r {
                              @d-equals-combination |
                                d = add(mul(a, r), mul(b, s))
                                [by hypothesis with-coefficient-s]
                              // d | a: run the Division Algorithm a = dq + r'.
                              @a-divided-by-d |
                                exists q: Int; exists rem: Int; a = add(mul(d, q), rem) and is_nonneg(rem) and less_than(rem, d)
                                [by specialize divisionAlgorithmExists(a, d) d-is-positive]
                              @with-quotient-q |
                                unpack q: Int from a-divided-by-d {
                                  @a-remainder-exists |
                                    exists rem: Int; a = add(mul(d, q), rem) and is_nonneg(rem) and less_than(rem, d)
                                    [by hypothesis with-quotient-q]
                                  @with-remainder |
                                    unpack rem: Int from a-remainder-exists {
                                      @a-decomposes-and-remainder-bounded |
                                        a = add(mul(d, q), rem) and is_nonneg(rem) and less_than(rem, d)
                                        [by hypothesis with-remainder]
                                      @a-decomposes |
                                        a = add(mul(d, q), rem)
                                        [by tautology a-decomposes-and-remainder-bounded]
                                      @remainder-nonneg |
                                        is_nonneg(rem)
                                        [by tautology a-decomposes-and-remainder-bounded]
                                      @remainder-less_than-d |
                                        less_than(rem, d)
                                        [by tautology a-decomposes-and-remainder-bounded]
                                      // a positive remainder would be a member of S smaller than d.
                                      @less-than-is-irreflexive |
                                        forall n: Int; not less_than(n, n)
                                        [by theorem lessThanIrreflexive]
                                      @d-not-less_than-itself |
                                        not less_than(d, d)
                                        [by specialize lessThanIrreflexive(d)]
                                      @suppose-remainder-positive |
                                        assume less_than(ZERO, rem) {
                                          @remainder-positive |
                                            less_than(ZERO, rem)
                                            [by hypothesis suppose-remainder-positive]
                                          @remainder-is-member-identity |
                                            a = add(mul(d, q), rem) -> d = add(mul(a, r), mul(b, s)) -> rem = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
                                            [by instantiate remainderIsMember(a, b, d, r, s, q, rem)]
                                          @remainder-is-member-form |
                                            rem = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
                                            [by tautology remainder-is-member-identity a-decomposes d-equals-combination]
                                          @remainder-is-some-combination-in-nn |
                                            exists nn: Int; rem = add(mul(a, sub(ONE, mul(r, q))), mul(b, nn))
                                            [by exists_intro(neg(mul(s, q))) remainder-is-member-form]
                                          @remainder-is-a-combination |
                                            exists mm: Int; exists nn: Int; rem = add(mul(a, mm), mul(b, nn))
                                            [by exists_intro(sub(ONE, mul(r, q))) remainder-is-some-combination-in-nn]
                                          @remainder-is-positive-combination |
                                            less_than(ZERO, rem) and (exists mm: Int; exists nn: Int; rem = add(mul(a, mm), mul(b, nn)))
                                            [by and_intro remainder-positive remainder-is-a-combination]
                                          @remainder-least-comparison |
                                            is_nonneg(rem) -> (less_than(ZERO, rem) and (exists mm: Int; exists nn: Int; rem = add(mul(a, mm), mul(b, nn)))) -> at_most(d, rem)
                                            [by forall_elim(rem) d-is-least]
                                          @d-at-most-remainder |
                                            at_most(d, rem)
                                            [by tautology remainder-least-comparison remainder-nonneg remainder-is-positive-combination]
                                          @less-or-equal-split |
                                            forall x, y: Int; at_most(x, y) -> (less_than(x, y) or x = y)
                                            [by theorem lessOrEqualSplit]
                                          @less-or-equal-split-at-d-remainder |
                                            at_most(d, rem) -> (less_than(d, rem) or d = rem)
                                            [by specialize lessOrEqualSplit(d, rem)]
                                          @d-less_than-or-equal-remainder |
                                            less_than(d, rem) or d = rem
                                            [by modus_ponens less-or-equal-split-at-d-remainder d-at-most-remainder]
                                          @remainder-would-be-a-smaller-member |
                                            less_than(d, d)
                                            case d-less_than-or-equal-remainder {
                                              @when-d-less_than-remainder |
                                                assume less_than(d, rem) {
                                                  @d-less_than-remainder |
                                                    less_than(d, rem)
                                                    [by hypothesis when-d-less_than-remainder]
                                                  @less-than-transitive |
                                                    forall x, y, z: Int; less_than(x, y) -> less_than(y, z) -> less_than(x, z)
                                                    [by theorem lessThanTransitive]
                                                  @transitive-at-d-remainder-d |
                                                    less_than(d, rem) -> less_than(rem, d) -> less_than(d, d)
                                                    [by specialize lessThanTransitive(d, rem, d)]
                                                  @conclusion-d-less_than-itself |
                                                    less_than(d, d)
                                                    [by tautology transitive-at-d-remainder-d d-less_than-remainder remainder-less_than-d]
                                                }
                                              @when-d-equals-remainder |
                                                assume d = rem {
                                                  @d-equals-remainder |
                                                    d = rem
                                                    [by hypothesis when-d-equals-remainder]
                                                  @remainder-equals-d |
                                                    rem = d
                                                    [by symmetry d-equals-remainder]
                                                  @conclusion-d-less_than-itself |
                                                    less_than(d, d)
                                                    [by rewrite remainder-equals-d remainder-less_than-d]
                                                }
                                            }
                                        }
                                      @remainder-not-positive |
                                        not less_than(ZERO, rem)
                                        [by not_intro suppose-remainder-positive remainder-would-be-a-smaller-member d-not-less_than-itself]
                                      // nonneg and not positive forces the remainder to be zero.
                                      @nonneg-not-positive-forces-zero |
                                        forall x: Int; is_nonneg(x) -> (not less_than(ZERO, x)) -> x = ZERO
                                        [by theorem nonnegNotPositiveIsZero]
                                      @nonneg-not-positive-is-zero |
                                        is_nonneg(rem) -> (not less_than(ZERO, rem)) -> rem = ZERO
                                        [by specialize nonnegNotPositiveIsZero(rem)]
                                      @remainder-is-zero |
                                        rem = ZERO
                                        [by tautology nonneg-not-positive-is-zero remainder-nonneg remainder-not-positive]
                                      @add-zero-right |
                                        forall n: Int; add(n, ZERO) = n
                                        [by theorem addZeroRight]
                                      @dq-plus-zero-is-dq |
                                        add(mul(d, q), ZERO) = mul(d, q)
                                        [by forall_elim(mul(d, q)) add-zero-right]
                                      @a-is-dq |
                                        a = mul(d, q)
                                        [by chain a-decomposes remainder-is-zero dq-plus-zero-is-dq]
                                      @divides-intro |
                                        forall n, dd, k: Int; n = mul(dd, k) -> does_divide(dd, n)
                                        [by theorem dividesIntro]
                                      @conclusion-d-divides-a |
                                        does_divide(d, a)
                                        [by specialize dividesIntro(a, d, q) a-is-dq]
                                    }
                                  @conclusion-d-divides-a |
                                    does_divide(d, a)
                                    [by exists_elim with-remainder]
                                }
                              @d-divides-a |
                                does_divide(d, a)
                                [by exists_elim with-quotient-q]
                              // d | b: symmetric, dividing b by d.
                              @add-is-commutative |
                                forall x, y: Int; add(x, y) = add(y, x)
                                [by theorem addIsCommutative]
                              @combination-commutes |
                                add(mul(a, r), mul(b, s)) = add(mul(b, s), mul(a, r))
                                [by forall_elim(mul(a, r), mul(b, s)) add-is-commutative]
                              @d-equals-combination-swapped |
                                d = add(mul(b, s), mul(a, r))
                                [by rewrite combination-commutes d-equals-combination]
                              @b-divided-by-d |
                                exists q: Int; exists rem: Int; b = add(mul(d, q), rem) and is_nonneg(rem) and less_than(rem, d)
                                [by specialize divisionAlgorithmExists(b, d) d-is-positive]
                              @with-quotient-q2 |
                                unpack q2: Int from b-divided-by-d {
                                  @b-remainder-exists |
                                    exists rem: Int; b = add(mul(d, q2), rem) and is_nonneg(rem) and less_than(rem, d)
                                    [by hypothesis with-quotient-q2]
                                  @with-remainder-of-b |
                                    unpack rem2: Int from b-remainder-exists {
                                      @b-decomposes-and-remainder-bounded |
                                        b = add(mul(d, q2), rem2) and is_nonneg(rem2) and less_than(rem2, d)
                                        [by hypothesis with-remainder-of-b]
                                      @b-decomposes |
                                        b = add(mul(d, q2), rem2)
                                        [by tautology b-decomposes-and-remainder-bounded]
                                      @remainder-nonneg |
                                        is_nonneg(rem2)
                                        [by tautology b-decomposes-and-remainder-bounded]
                                      @remainder-less_than-d |
                                        less_than(rem2, d)
                                        [by tautology b-decomposes-and-remainder-bounded]
                                      @less-than-is-irreflexive |
                                        forall n: Int; not less_than(n, n)
                                        [by theorem lessThanIrreflexive]
                                      @d-not-less_than-itself |
                                        not less_than(d, d)
                                        [by specialize lessThanIrreflexive(d)]
                                      @suppose-remainder-positive |
                                        assume less_than(ZERO, rem2) {
                                          @remainder-positive |
                                            less_than(ZERO, rem2)
                                            [by hypothesis suppose-remainder-positive]
                                          @remainder-is-member-identity |
                                            b = add(mul(d, q2), rem2) -> d = add(mul(b, s), mul(a, r)) -> rem2 = add(mul(b, sub(ONE, mul(s, q2))), mul(a, neg(mul(r, q2))))
                                            [by instantiate remainderIsMember(b, a, d, s, r, q2, rem2)]
                                          @remainder-is-member-form-swapped |
                                            rem2 = add(mul(b, sub(ONE, mul(s, q2))), mul(a, neg(mul(r, q2))))
                                            [by tautology remainder-is-member-identity b-decomposes d-equals-combination-swapped]
                                          @member-form-commutes |
                                            add(mul(b, sub(ONE, mul(s, q2))), mul(a, neg(mul(r, q2)))) = add(mul(a, neg(mul(r, q2))), mul(b, sub(ONE, mul(s, q2))))
                                            [by forall_elim(mul(b, sub(ONE, mul(s, q2))), mul(a, neg(mul(r, q2)))) add-is-commutative]
                                          @remainder-is-member-form |
                                            rem2 = add(mul(a, neg(mul(r, q2))), mul(b, sub(ONE, mul(s, q2))))
                                            [by rewrite member-form-commutes remainder-is-member-form-swapped]
                                          @remainder-is-some-combination-in-nn |
                                            exists nn: Int; rem2 = add(mul(a, neg(mul(r, q2))), mul(b, nn))
                                            [by exists_intro(sub(ONE, mul(s, q2))) remainder-is-member-form]
                                          @remainder-is-a-combination |
                                            exists mm: Int; exists nn: Int; rem2 = add(mul(a, mm), mul(b, nn))
                                            [by exists_intro(neg(mul(r, q2))) remainder-is-some-combination-in-nn]
                                          @remainder-is-positive-combination |
                                            less_than(ZERO, rem2) and (exists mm: Int; exists nn: Int; rem2 = add(mul(a, mm), mul(b, nn)))
                                            [by and_intro remainder-positive remainder-is-a-combination]
                                          @remainder-least-comparison |
                                            is_nonneg(rem2) -> (less_than(ZERO, rem2) and (exists mm: Int; exists nn: Int; rem2 = add(mul(a, mm), mul(b, nn)))) -> at_most(d, rem2)
                                            [by forall_elim(rem2) d-is-least]
                                          @d-at-most-remainder |
                                            at_most(d, rem2)
                                            [by tautology remainder-least-comparison remainder-nonneg remainder-is-positive-combination]
                                          @less-or-equal-split |
                                            forall x, y: Int; at_most(x, y) -> (less_than(x, y) or x = y)
                                            [by theorem lessOrEqualSplit]
                                          @less-or-equal-split-at-d-remainder |
                                            at_most(d, rem2) -> (less_than(d, rem2) or d = rem2)
                                            [by specialize lessOrEqualSplit(d, rem2)]
                                          @d-less_than-or-equal-remainder |
                                            less_than(d, rem2) or d = rem2
                                            [by modus_ponens less-or-equal-split-at-d-remainder d-at-most-remainder]
                                          @remainder-would-be-a-smaller-member |
                                            less_than(d, d)
                                            case d-less_than-or-equal-remainder {
                                              @when-d-less_than-remainder |
                                                assume less_than(d, rem2) {
                                                  @d-less_than-remainder |
                                                    less_than(d, rem2)
                                                    [by hypothesis when-d-less_than-remainder]
                                                  @less-than-transitive |
                                                    forall x, y, z: Int; less_than(x, y) -> less_than(y, z) -> less_than(x, z)
                                                    [by theorem lessThanTransitive]
                                                  @transitive-at-d-remainder-d |
                                                    less_than(d, rem2) -> less_than(rem2, d) -> less_than(d, d)
                                                    [by specialize lessThanTransitive(d, rem2, d)]
                                                  @conclusion-d-less_than-itself |
                                                    less_than(d, d)
                                                    [by tautology transitive-at-d-remainder-d d-less_than-remainder remainder-less_than-d]
                                                }
                                              @when-d-equals-remainder |
                                                assume d = rem2 {
                                                  @d-equals-remainder |
                                                    d = rem2
                                                    [by hypothesis when-d-equals-remainder]
                                                  @remainder-equals-d |
                                                    rem2 = d
                                                    [by symmetry d-equals-remainder]
                                                  @conclusion-d-less_than-itself |
                                                    less_than(d, d)
                                                    [by rewrite remainder-equals-d remainder-less_than-d]
                                                }
                                            }
                                        }
                                      @remainder-not-positive |
                                        not less_than(ZERO, rem2)
                                        [by not_intro suppose-remainder-positive remainder-would-be-a-smaller-member d-not-less_than-itself]
                                      @nonneg-not-positive-forces-zero |
                                        forall x: Int; is_nonneg(x) -> (not less_than(ZERO, x)) -> x = ZERO
                                        [by theorem nonnegNotPositiveIsZero]
                                      @nonneg-not-positive-is-zero |
                                        is_nonneg(rem2) -> (not less_than(ZERO, rem2)) -> rem2 = ZERO
                                        [by specialize nonnegNotPositiveIsZero(rem2)]
                                      @remainder-is-zero |
                                        rem2 = ZERO
                                        [by tautology nonneg-not-positive-is-zero remainder-nonneg remainder-not-positive]
                                      @add-zero-right |
                                        forall n: Int; add(n, ZERO) = n
                                        [by theorem addZeroRight]
                                      @dq-plus-zero-is-dq |
                                        add(mul(d, q2), ZERO) = mul(d, q2)
                                        [by forall_elim(mul(d, q2)) add-zero-right]
                                      @b-is-dq |
                                        b = mul(d, q2)
                                        [by chain b-decomposes remainder-is-zero dq-plus-zero-is-dq]
                                      @divides-intro |
                                        forall n, dd, k: Int; n = mul(dd, k) -> does_divide(dd, n)
                                        [by theorem dividesIntro]
                                      @conclusion-d-divides-b |
                                        does_divide(d, b)
                                        [by specialize dividesIntro(b, d, q2) b-is-dq]
                                    }
                                  @conclusion-d-divides-b |
                                    does_divide(d, b)
                                    [by exists_elim with-remainder-of-b]
                                }
                              @d-divides-b |
                                does_divide(d, b)
                                [by exists_elim with-quotient-q2]
                              // any common divisor e divides d = ar + bs.
                              @generalize-e |
                                fix e: Int {
                                  @given-e-divides-a |
                                    assume does_divide(e, a) {
                                      @given-e-divides-b |
                                        assume does_divide(e, b) {
                                          @e-divides-a |
                                            does_divide(e, a)
                                            [by hypothesis given-e-divides-a]
                                          @e-divides-b |
                                            does_divide(e, b)
                                            [by hypothesis given-e-divides-b]
                                          @divides-mul |
                                            forall dd, x, c: Int; does_divide(dd, x) -> does_divide(dd, mul(x, c))
                                            [by theorem dividesMul]
                                          @e-divides-ar |
                                            does_divide(e, mul(a, r))
                                            [by specialize dividesMul(e, a, r) e-divides-a]
                                          @e-divides-bs |
                                            does_divide(e, mul(b, s))
                                            [by specialize dividesMul(e, b, s) e-divides-b]
                                          @divides-add |
                                            forall dd, x, y: Int; does_divide(dd, x) -> does_divide(dd, y) -> does_divide(dd, add(x, y))
                                            [by theorem dividesAdd]
                                          @e-divides-sum-if-parts |
                                            does_divide(e, mul(a, r)) -> does_divide(e, mul(b, s)) -> does_divide(e, add(mul(a, r), mul(b, s)))
                                            [by forall_elim(e, mul(a, r), mul(b, s)) divides-add]
                                          @e-divides-combination |
                                            does_divide(e, add(mul(a, r), mul(b, s)))
                                            [by tautology e-divides-sum-if-parts e-divides-ar e-divides-bs]
                                          @combination-is-d |
                                            add(mul(a, r), mul(b, s)) = d
                                            [by symmetry d-equals-combination]
                                          @conclusion-e-divides-d |
                                            does_divide(e, d)
                                            [by rewrite combination-is-d e-divides-combination]
                                        }
                                      @e-divides-b-implies-e-divides-d |
                                        does_divide(e, b) -> does_divide(e, d)
                                        [by implies_intro given-e-divides-b]
                                    }
                                  @e-divides-a-implies-rest |
                                    does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)
                                    [by implies_intro given-e-divides-a]
                                }
                              @common-divisor-divides-d |
                                forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)
                                [by forall_intro generalize-e]
                              // assemble the five defining properties of the gcd.
                              @d-is-some-combination-in-s |
                                exists s2: Int; d = add(mul(a, r), mul(b, s2))
                                [by exists_intro(s) d-equals-combination]
                              @d-is-a-combination-again |
                                exists r2: Int; exists s2: Int; d = add(mul(a, r2), mul(b, s2))
                                [by exists_intro(r) d-is-some-combination-in-s]
                              @d-positive-and-divides-a |
                                less_than(ZERO, d) and does_divide(d, a)
                                [by and_intro d-is-positive d-divides-a]
                              @d-divides-a-and-b |
                                less_than(ZERO, d) and does_divide(d, a) and does_divide(d, b)
                                [by and_intro d-positive-and-divides-a d-divides-b]
                              @d-divides-and-greatest |
                                less_than(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d))
                                [by and_intro d-divides-a-and-b common-divisor-divides-d]
                              @d-has-all-gcd-properties |
                                less_than(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r2: Int; exists s2: Int; d = add(mul(a, r2), mul(b, s2)))
                                [by and_intro d-divides-and-greatest d-is-a-combination-again]
                              @conclusion-bezout |
                                exists d2: Int; less_than(ZERO, d2) and does_divide(d2, a) and does_divide(d2, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d2)) and (exists r2: Int; exists s2: Int; d2 = add(mul(a, r2), mul(b, s2)))
                                [by exists_intro(d) d-has-all-gcd-properties]
                            }
                          @conclusion-bezout |
                            exists d2: Int; less_than(ZERO, d2) and does_divide(d2, a) and does_divide(d2, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d2)) and (exists r2: Int; exists s2: Int; d2 = add(mul(a, r2), mul(b, s2)))
                            [by exists_elim with-coefficient-s]
                        }
                      @conclusion-bezout |
                        exists d2: Int; less_than(ZERO, d2) and does_divide(d2, a) and does_divide(d2, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d2)) and (exists r2: Int; exists s2: Int; d2 = add(mul(a, r2), mul(b, s2)))
                        [by exists_elim with-coefficient-r]
                    }
                  @conclusion-bezout |
                    exists d2: Int; less_than(ZERO, d2) and does_divide(d2, a) and does_divide(d2, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d2)) and (exists r2: Int; exists s2: Int; d2 = add(mul(a, r2), mul(b, s2)))
                    [by exists_elim with-least-member-d]
                }
              @b-nonzero-gives-bezout |
                (b != ZERO)
                  -> exists d: Int; less_than(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
                [by implies_intro given-b-nonzero]
            }
          @a-nonzero-gives-bezout |
            (a != ZERO)
              -> (b != ZERO) -> exists d: Int; less_than(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
            [by implies_intro given-a-nonzero]
        }
      @discharge-b |
        forall b: Int; (a != ZERO) -> (b != ZERO) -> exists d: Int; less_than(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b: Int; (a != ZERO) -> (b != ZERO) -> exists d: Int; less_than(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
    [by forall_intro generalize-a]
qed
```

## The Coprime Corollary

> **Corollary 2.11.** Let $a$ and $b$ be two integers that are relatively prime.
> Then there exist integers $r$ and $s$ such that $ar + bs = 1$.

If `a` and `b` are coprime — every common divisor is `±1` — Bézout's positive
common divisor `d = gcd(a,b)` must itself be `1` or `−1`. It is positive
(`less_than(ZERO, d)`), and `−1` is *not* positive (`negOneLessThanZero` puts `−1 < 0`,
which together with `0 < −1` and transitivity would give `0 < 0`), so `d = 1` and
Bézout's identity `d = ar + bs` becomes `ar + bs = 1`.

```bpa
theorem coprimeBezout: forall a, b: Int; a != ZERO -> b != ZERO ->
  (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE)))
  -> exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @given-a-nonzero |
            assume a != ZERO {
              @given-b-nonzero |
                assume b != ZERO {
                  @given-coprime |
                    assume forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE)) {
                      @a-nonzero |
                        a != ZERO
                        [by hypothesis given-a-nonzero]
                      @b-nonzero |
                        b != ZERO
                        [by hypothesis given-b-nonzero]
                      @coprime |
                        forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE))
                        [by hypothesis given-coprime]
                      // Bézout supplies a positive gcd d = ar + bs dividing a and b.
                      @gcd-exists |
                        exists d: Int; less_than(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
                        [by specialize bezout(a, b) a-nonzero b-nonzero]
                      @with-gcd-d |
                        unpack d: Int from gcd-exists {
                          @d-has-all-gcd-properties |
                            less_than(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
                            [by hypothesis with-gcd-d]
                          @d-is-positive |
                            less_than(ZERO, d)
                            [by tautology d-has-all-gcd-properties]
                          @d-divides-a |
                            does_divide(d, a)
                            [by tautology d-has-all-gcd-properties]
                          @d-divides-b |
                            does_divide(d, b)
                            [by tautology d-has-all-gcd-properties]
                          @d-is-a-combination |
                            exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s))
                            [by tautology d-has-all-gcd-properties]
                          // coprimality forces the gcd to be 1 or -1.
                          @coprime-at-d |
                            does_divide(d, a) -> does_divide(d, b) -> (d = ONE or d = neg(ONE))
                            [by forall_elim(d) coprime]
                          @d-is-one-or-neg-one |
                            d = ONE or d = neg(ONE)
                            [by tautology coprime-at-d d-divides-a d-divides-b]
                          @conclusion-bezout-identity |
                            exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
                            case d-is-one-or-neg-one {
                              @when-d-is-one |
                                assume d = ONE {
                                  @d-is-one |
                                    d = ONE
                                    [by hypothesis when-d-is-one]
                                  @with-coefficient-r |
                                    unpack r: Int from d-is-a-combination {
                                      @d-is-combination-in-r |
                                        exists s: Int; d = add(mul(a, r), mul(b, s))
                                        [by hypothesis with-coefficient-r]
                                      @with-coefficient-s |
                                        unpack s: Int from d-is-combination-in-r {
                                          @d-equals-combination |
                                            d = add(mul(a, r), mul(b, s))
                                            [by hypothesis with-coefficient-s]
                                          @combination-equals-one |
                                            add(mul(a, r), mul(b, s)) = ONE
                                            [by chain d-equals-combination d-is-one]
                                          @combination-in-s-is-one |
                                            exists s2: Int; add(mul(a, r), mul(b, s2)) = ONE
                                            [by exists_intro(s) combination-equals-one]
                                          @conclusion-bezout-identity |
                                            exists r2: Int; exists s2: Int; add(mul(a, r2), mul(b, s2)) = ONE
                                            [by exists_intro(r) combination-in-s-is-one]
                                        }
                                      @conclusion-bezout-identity |
                                        exists r2: Int; exists s2: Int; add(mul(a, r2), mul(b, s2)) = ONE
                                        [by exists_elim with-coefficient-s]
                                    }
                                  @conclusion-bezout-identity |
                                    exists r2: Int; exists s2: Int; add(mul(a, r2), mul(b, s2)) = ONE
                                    [by exists_elim with-coefficient-r]
                                }
                              @when-d-is-neg-one |
                                assume d = neg(ONE) {
                                  @d-is-neg-one |
                                    d = neg(ONE)
                                    [by hypothesis when-d-is-neg-one]
                                  // d positive but d = -1 < 0 is impossible.
                                  @neg-one-is-positive |
                                    less_than(ZERO, neg(ONE))
                                    [by rewrite d-is-neg-one d-is-positive]
                                  @neg-one-is-negative |
                                    less_than(neg(ONE), ZERO)
                                    [by theorem negOneLessThanZero]
                                  @less-than-transitive |
                                    forall x, y, z: Int; less_than(x, y) -> less_than(y, z) -> less_than(x, z)
                                    [by theorem lessThanTransitive]
                                  @transitive-through-neg-one |
                                    less_than(ZERO, neg(ONE)) -> less_than(neg(ONE), ZERO) -> less_than(ZERO, ZERO)
                                    [by forall_elim(ZERO, neg(ONE), ZERO) less-than-transitive]
                                  @zero-less_than-itself |
                                    less_than(ZERO, ZERO)
                                    [by tautology transitive-through-neg-one neg-one-is-positive neg-one-is-negative]
                                  @less-than-is-irreflexive |
                                    forall n: Int; not less_than(n, n)
                                    [by theorem lessThanIrreflexive]
                                  @zero-not-less_than-itself |
                                    not less_than(ZERO, ZERO)
                                    [by specialize lessThanIrreflexive(ZERO)]
                                  @conclusion-bezout-identity |
                                    exists r2: Int; exists s2: Int; add(mul(a, r2), mul(b, s2)) = ONE
                                    [by absurd zero-less_than-itself zero-not-less_than-itself]
                                }
                            }
                        }
                      @conclusion-bezout-identity |
                        exists r2: Int; exists s2: Int; add(mul(a, r2), mul(b, s2)) = ONE
                        [by exists_elim with-gcd-d]
                    }
                  @coprime-implies-bezout-identity |
                    (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE)))
                      -> exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
                    [by implies_intro given-coprime]
                }
              @b-nonzero-implies-coprime-bezout |
                (b != ZERO)
                  -> (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE)))
                  -> exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
                [by implies_intro given-b-nonzero]
            }
          @a-nonzero-implies-coprime-bezout |
            (a != ZERO)
              -> (b != ZERO)
              -> (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE)))
              -> exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
            [by implies_intro given-a-nonzero]
        }
      @discharge-b |
        forall b: Int; (a != ZERO) -> (b != ZERO) -> (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE))) -> exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b: Int; (a != ZERO) -> (b != ZERO) -> (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE))) -> exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
    [by forall_intro generalize-a]
qed
```

## Prime Numbers

A **prime** is an integer `p > 1` whose only *positive* divisors are `1` and `p`
itself (Judson, Definition 2.10). Since `bpa` predicates are opaque, we spell the
condition out inline as the conjunction

```
less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
```

wherever "`p` is prime" is needed. This section proves the two theorems that make
primes the multiplicative atoms of `ℤ`: **Euclid's Lemma** — a prime dividing a
product divides one of the factors — and, as the classical corollary, a note on
the **infinitude of primes**. Euclid's Lemma rests on `coprimeBezout` (proved
above) and on the ℤ number-theory library; we bring in the last few names the
argument spells out, plus the finite-product sequence library for the
infinitude discussion.

```bpa
import sequence <<< "std/integer-sequence.bpa"

axiom mulZeroLeft = ring.mulZeroLeft
axiom mulSuccLeft = ring.mulSuccLeft
axiom negZero = division.negZero
theorem mulIsCommutative = order.mulIsCommutative
theorem mulNegLeft = order.mulNegLeft
theorem nonnegNotLessThanZero = wellordering.nonnegNotLessThanZero
theorem dividesRefl = divides.dividesRefl

sort Seq = sequence.Seq
func at = sequence.at
func productUpTo = sequence.productUpTo
pred inSeqBefore = sequence.inSeqBefore
axiom productUpToZero = sequence.productUpToZero
axiom productUpToSucc = sequence.productUpToSucc
theorem everyEntryDividesProduct = sequence.everyEntryDividesProduct
theorem memberDividesProduct = sequence.memberDividesProduct
theorem primeFactorExists = sequence.primeFactorExists
theorem dividesSubtracts = sequence.dividesSubtracts
theorem oneAtMostPositive = sequence.oneAtMostPositive
theorem divisorAtMost = sequence.divisorAtMost
theorem lastEntryDividesProduct = sequence.lastEntryDividesProduct
theorem atMostThenLessThan = division.atMostThenLessThan
theorem mulCancelLeft = division.mulCancelLeft
theorem mulCancelRight = division.mulCancelRight
theorem lessThanSelf = wellordering.lessThanSelf

// -- sequence-construction axioms + the order/arith facts the Fundamental
//    Theorem of Arithmetic (less_than) needs but the sections above did not ------
axiom seqSingletonExists = sequence.seqSingletonExists
axiom seqConcatExists = sequence.seqConcatExists
axiom seqRemoveExists = sequence.seqRemoveExists
axiom seqPrependExists = sequence.seqPrependExists
axiom seqShiftPastExists = sequence.seqShiftPastExists
theorem nonnegStrongInduction = division.nonnegStrongInduction
axiom nonnegInduction = order.nonnegInduction
theorem productWithPositiveIsPositive = division.productWithPositiveIsPositive
theorem additionPreservesOrder = order.additionPreservesOrder
theorem lessThanAddCancelLeft = order.lessThanAddCancelLeft
theorem lessThanSuccSplit = wellordering.lessThanSuccSplit
theorem signTrichotomy = order.signTrichotomy
theorem nonnegMul = order.nonnegMul
theorem nonnegAdd = order.nonnegAdd
theorem lessOrEqualRefl = order.lessOrEqualRefl
theorem lessOrEqualAntisymmetric = order.lessOrEqualAntisymmetric
// `at_most` (= less_or_equal) is now `less_than or =`, so its old gap-existential
// intro/elim AXIOMS are gone. `lessOrEqualIsGapExistential` proves at_most(a,b) iff
// (exists d; is_nonneg(d) and add(a,d)=b) — recover intro/elim as local theorems by
// projecting each direction of that iff.
theorem lessOrEqualIsGapExistential = order.lessOrEqualIsGapExistential
```

```bpa
theorem lessOrEqualElim: forall a, b: Int; at_most(a, b) -> exists d: Int; is_nonneg(d) and add(a, d) = b
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @gap-iff |
            forall a2, b2: Int; at_most(a2, b2) iff (exists d: Int; is_nonneg(d) and add(a2, d) = b2)
            [by theorem lessOrEqualIsGapExistential]
          @gap-iff-at-a-b |
            at_most(a, b) iff (exists d: Int; is_nonneg(d) and add(a, d) = b)
            [by specialize lessOrEqualIsGapExistential(a, b)]
          @elim-at-a-b |
            at_most(a, b) -> exists d: Int; is_nonneg(d) and add(a, d) = b
            [by iff_elim_forward gap-iff-at-a-b]
        }
      @discharge-b |
        forall b: Int; at_most(a, b) -> exists d: Int; is_nonneg(d) and add(a, d) = b
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b: Int; at_most(a, b) -> exists d: Int; is_nonneg(d) and add(a, d) = b
    [by forall_intro generalize-a]
qed

theorem lessOrEqualIntro: forall a, g, b: Int; is_nonneg(g) -> add(a, g) = b -> at_most(a, b)
proof
  @generalize-a |
    fix a: Int {
      @generalize-g |
        fix g: Int {
          @generalize-b |
            fix b: Int {
              @given-nonneg-g |
                assume is_nonneg(g) {
                  @given-gap |
                    assume add(a, g) = b {
                      @nonneg-g |
                        is_nonneg(g)
                        [by hypothesis given-nonneg-g]
                      @the-gap |
                        add(a, g) = b
                        [by hypothesis given-gap]
                      @nonneg-and-gap |
                        is_nonneg(g) and add(a, g) = b
                        [by and_intro nonneg-g the-gap]
                      @gap-exists |
                        exists d: Int; is_nonneg(d) and add(a, d) = b
                        [by exists_intro(g) nonneg-and-gap]
                      @gap-iff |
                        forall a2, b2: Int; at_most(a2, b2) iff (exists d: Int; is_nonneg(d) and add(a2, d) = b2)
                        [by theorem lessOrEqualIsGapExistential]
                      @gap-iff-at-a-b |
                        at_most(a, b) iff (exists d: Int; is_nonneg(d) and add(a, d) = b)
                        [by specialize lessOrEqualIsGapExistential(a, b)]
                      @gap-gives-at-most |
                        (exists d: Int; is_nonneg(d) and add(a, d) = b) -> at_most(a, b)
                        [by iff_elim_backward gap-iff-at-a-b]
                      @a-at-most-b |
                        at_most(a, b)
                        [by modus_ponens gap-gives-at-most gap-exists]
                    }
                  @gap-gives-le |
                    add(a, g) = b -> at_most(a, b)
                    [by implies_intro given-gap]
                }
              @nonneg-gives |
                is_nonneg(g) -> add(a, g) = b -> at_most(a, b)
                [by implies_intro given-nonneg-g]
            }
          @discharge-b |
            forall b: Int; is_nonneg(g) -> add(a, g) = b -> at_most(a, b)
            [by forall_intro generalize-b]
        }
      @discharge-g |
        forall g, b: Int; is_nonneg(g) -> add(a, g) = b -> at_most(a, b)
        [by forall_intro generalize-g]
    }
  @conclusion |
    forall a, g, b: Int; is_nonneg(g) -> add(a, g) = b -> at_most(a, b)
    [by forall_intro generalize-a]
qed
```

The elided ring algebra Euclid's Lemma needs — collapsing `b·(ar+ps)` to a
sum of two `p`-related products — is a nonlinear identity handled by the
`polynomial` tactic. That tactic expects the ring's associativity/commutativity
lemmas *and their left-swap forms* in scope; ℤ has the swaps only implicitly, so
we prove the three names it looks up (`mulOneLeft`, `mulLeftSwap`, `addLeftSwap`)
first. `mul(1, n) = n` mirrors `mulOneRight` on the left, unfolding `1 = succ 0`
through `mulSuccLeft`.

```bpa
theorem mulOneLeft: forall n: Int; mul(ONE, n) = n
proof
  @generalize-n |
    fix n: Int {
      @one-is-succ-zero |
        ONE = succ(ZERO)
        [by axiom oneIsSuccZero]
      @mul-succ-zero-n |
        mul(succ(ZERO), n) = add(mul(ZERO, n), n)
        [by specialize mulSuccLeft(ZERO, n)]
      @mul-zero-left |
        forall b: Int; mul(ZERO, b) = ZERO
        [by axiom mulZeroLeft]
      @mul-zero-n-is-zero |
        mul(ZERO, n) = ZERO
        [by specialize mulZeroLeft(n)]
      @add-zero-left |
        forall b: Int; add(ZERO, b) = b
        [by axiom addZeroLeft]
      @zero-plus-n-is-n |
        add(ZERO, n) = n
        [by specialize addZeroLeft(n)]
      // unfold ONE, collapse mul(ZERO, n) and add(ZERO, n).
      @conclusion-mul-one-left |
        mul(ONE, n) = n
        [by chain one-is-succ-zero mul-succ-zero-n mul-zero-n-is-zero zero-plus-n-is-n]
    }
  @conclusion |
    forall n: Int; mul(ONE, n) = n
    [by forall_intro generalize-n]
qed
```

The left-swap forms `a·(b·c) = b·(a·c)` and `a+(b+c) = b+(a+c)` reassociate,
commute the exposed pair, then reassociate back.

```bpa
theorem mulLeftSwap: forall a, b, c: Int; mul(a, mul(b, c)) = mul(b, mul(a, c))
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @generalize-c |
            fix c: Int {
              @abc-regroups-flat |
                mul(mul(a, b), c) = mul(a, mul(b, c))
                [by specialize mulIsAssociative(a, b, c)]
              @a-b-swaps |
                mul(a, b) = mul(b, a)
                [by specialize mulIsCommutative(a, b)]
              @ba-c-regroups |
                mul(mul(b, a), c) = mul(b, mul(a, c))
                [by specialize mulIsAssociative(b, a, c)]
              @conclusion-mul-left-swap |
                mul(a, mul(b, c)) = mul(b, mul(a, c))
                [by chain abc-regroups-flat a-b-swaps ba-c-regroups]
            }
          @discharge-c |
            forall c: Int; mul(a, mul(b, c)) = mul(b, mul(a, c))
            [by forall_intro generalize-c]
        }
      @discharge-b |
        forall b, c: Int; mul(a, mul(b, c)) = mul(b, mul(a, c))
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b, c: Int; mul(a, mul(b, c)) = mul(b, mul(a, c))
    [by forall_intro generalize-a]
qed
```

The additive left-swap is the identical argument on `add`.

```bpa
theorem addLeftSwap: forall a, b, c: Int; add(a, add(b, c)) = add(b, add(a, c))
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @generalize-c |
            fix c: Int {
              @abc-regroups-flat |
                add(add(a, b), c) = add(a, add(b, c))
                [by specialize addIsAssociative(a, b, c)]
              @a-b-swaps |
                add(a, b) = add(b, a)
                [by specialize addIsCommutative(a, b)]
              @ba-c-regroups |
                add(add(b, a), c) = add(b, add(a, c))
                [by specialize addIsAssociative(b, a, c)]
              @conclusion-add-left-swap |
                add(a, add(b, c)) = add(b, add(a, c))
                [by chain abc-regroups-flat a-b-swaps ba-c-regroups]
            }
          @discharge-c |
            forall c: Int; add(a, add(b, c)) = add(b, add(a, c))
            [by forall_intro generalize-c]
        }
      @discharge-b |
        forall b, c: Int; add(a, add(b, c)) = add(b, add(a, c))
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b, c: Int; add(a, add(b, c)) = add(b, add(a, c))
    [by forall_intro generalize-a]
qed
```

A small positivity fact used repeatedly: `0 < 1`, the gap being `0`, since
`add(0, succ 0) = succ 0 = 1`.

```bpa
theorem zeroLessThanOne: less_than(ZERO, ONE)
proof
  @one-is-succ-zero |
    ONE = succ(ZERO)
    [by axiom oneIsSuccZero]
  @add-zero-left |
    forall b: Int; add(ZERO, b) = b
    [by axiom addZeroLeft]
  @zero-plus-one-is-one |
    add(ZERO, ONE) = ONE
    [by specialize addZeroLeft(ONE)]
  @zero-plus-succ-zero-is-one |
    add(ZERO, succ(ZERO)) = ONE
    [by rewrite one-is-succ-zero zero-plus-one-is-one]
  @zero-is-nonneg |
    is_nonneg(ZERO)
    [by axiom nonnegZero]
  @less-than-intro |
    forall a, d, b: Int; is_nonneg(d) -> add(a, succ(d)) = b -> less_than(a, b)
    [by axiom lessThanIntro]
  @conclusion |
    less_than(ZERO, ONE)
    [by specialize lessThanIntro(ZERO, ZERO, ONE) zero-is-nonneg zero-plus-succ-zero-is-one]
qed
```

`neg(e)` divides whatever `e` divides: unfolding `n = e·k` gives
`n = neg(e)·neg(k)` (`mulNegLeft`, `mulNegRight`, `negNeg` cancel the two sign
flips), so `neg(e) | n`. This lets the coprimality argument reduce a negative
common divisor to its positive counterpart.

```bpa
theorem negDivides: forall e, n: Int; does_divide(e, n) -> does_divide(neg(e), n)
proof
  @generalize-e |
    fix e: Int {
      @generalize-n |
        fix n: Int {
          @given-e-divides-n |
            assume does_divide(e, n) {
              @e-divides-n |
                does_divide(e, n)
                [by hypothesis given-e-divides-n]
              @divides-elim |
                forall d, m: Int; does_divide(d, m) -> exists k: Int; m = mul(d, k)
                [by theorem dividesElim]
              @quotient-exists |
                exists k: Int; n = mul(e, k)
                [by specialize dividesElim(e, n) e-divides-n]
              @with-quotient-k |
                unpack k: Int from quotient-exists {
                  @n-is-e-times-k |
                    n = mul(e, k)
                    [by hypothesis with-quotient-k]
                  @e-times-k-is-neg-e-times-neg-k |
                    mul(e, k) = mul(neg(e), neg(k))
                    [by simplify mulNegLeft mulNegRight negNeg]
                  @n-is-neg-e-times-neg-k |
                    n = mul(neg(e), neg(k))
                    [by rewrite e-times-k-is-neg-e-times-neg-k n-is-e-times-k]
                  @divides-intro |
                    forall m, d, j: Int; m = mul(d, j) -> does_divide(d, m)
                    [by theorem dividesIntro]
                  @neg-e-divides-n-if-product |
                    n = mul(neg(e), neg(k)) -> does_divide(neg(e), n)
                    [by forall_elim(n, neg(e), neg(k)) divides-intro]
                  @neg-e-divides-n |
                    does_divide(neg(e), n)
                    [by modus_ponens neg-e-divides-n-if-product n-is-neg-e-times-neg-k]
                }
              @neg-e-divides-n |
                does_divide(neg(e), n)
                [by exists_elim with-quotient-k]
            }
          @e-divides-implies-neg-e-divides |
            does_divide(e, n) -> does_divide(neg(e), n)
            [by implies_intro given-e-divides-n]
        }
      @discharge-n |
        forall n: Int; does_divide(e, n) -> does_divide(neg(e), n)
        [by forall_intro generalize-n]
    }
  @conclusion |
    forall e, n: Int; does_divide(e, n) -> does_divide(neg(e), n)
    [by forall_intro generalize-e]
qed
```

A negative integer negates to a positive one — exactly `negativeNegIsPositive`,
proved above for the Bézout nonemptiness argument (`negLessThanZero` gives
`neg(e) ≥ 0`, and `neg(e) ≠ 0` since that would force `e = 0`, so `nonnegCases`
exhibits it as a successor). We reuse it here under a name local to the coprimality
argument.

```bpa
theorem positiveOfNegative = negativeNegIsPositive
```

Euclid's Lemma proceeds by cases on whether `p | a`, and `ℤ`'s intuitionistic
kernel offers no free excluded middle — so we first establish that divisibility
by a positive integer *is decidable*. Run the Division Algorithm: `a = pq + r`
with `0 ≤ r < p`. By trichotomy the remainder `r` is `< 0` (impossible, `r` is
nonnegative), `= 0` (then `a = p·q`, so `p | a`), or `> 0`. In the last case,
were `p | a` we would have a second decomposition `a = p·m + 0`; the Division
Algorithm's *uniqueness* forces `r = 0`, contradicting `r > 0` — so `p ∤ a`.

```bpa
theorem dividesDecidable: forall p, a: Int; less_than(ZERO, p) -> (does_divide(p, a) or (not does_divide(p, a)))
proof
  @generalize-p |
    fix p: Int {
      @generalize-a |
        fix a: Int {
          @given-p-positive |
            assume less_than(ZERO, p) {
              @p-positive |
                less_than(ZERO, p)
                [by hypothesis given-p-positive]
              @quotient-remainder-exist |
                exists q: Int; exists r: Int; a = add(mul(p, q), r) and is_nonneg(r) and less_than(r, p)
                [by specialize divisionAlgorithmExists(a, p) p-positive]
              @with-quotient-q |
                unpack q: Int from quotient-remainder-exist {
                  @remainder-exists |
                    exists r: Int; a = add(mul(p, q), r) and is_nonneg(r) and less_than(r, p)
                    [by hypothesis with-quotient-q]
                  @with-remainder-r |
                    unpack r: Int from remainder-exists {
                      @decomposition-and-bounds |
                        a = add(mul(p, q), r) and is_nonneg(r) and less_than(r, p)
                        [by hypothesis with-remainder-r]
                      @decomposition-and-remainder-nonneg |
                        a = add(mul(p, q), r) and is_nonneg(r)
                        [by and_elim_left decomposition-and-bounds]
                      @decomposition |
                        a = add(mul(p, q), r)
                        [by and_elim_left decomposition-and-remainder-nonneg]
                      @remainder-nonneg |
                        is_nonneg(r)
                        [by and_elim_right decomposition-and-remainder-nonneg]
                      @remainder-less_than-p |
                        less_than(r, p)
                        [by and_elim_right decomposition-and-bounds]
                      @r-against-zero |
                        less_than(r, ZERO) or r = ZERO or less_than(ZERO, r)
                        [by specialize trichotomy(r, ZERO)]
                      @divides-decidable |
                        does_divide(p, a) or (not does_divide(p, a))
                        case r-against-zero {
                          @when-remainder-less_than-zero |
                            assume less_than(r, ZERO) {
                              @remainder-less_than-zero |
                                less_than(r, ZERO)
                                [by hypothesis when-remainder-less_than-zero]
                              @remainder-not-less_than-zero |
                                not less_than(r, ZERO)
                                [by specialize nonnegNotLessThanZero(r) remainder-nonneg]
                              @divides-decidable |
                                does_divide(p, a) or (not does_divide(p, a))
                                [by absurd remainder-less_than-zero remainder-not-less_than-zero]
                            }
                          @when-remainder-zero |
                            assume r = ZERO {
                              @remainder-zero |
                                r = ZERO
                                [by hypothesis when-remainder-zero]
                              @add-zero-right |
                                forall n: Int; add(n, ZERO) = n
                                [by theorem addZeroRight]
                              @product-plus-zero |
                                add(mul(p, q), ZERO) = mul(p, q)
                                [by forall_elim(mul(p, q)) add-zero-right]
                              @a-is-p-times-q |
                                a = mul(p, q)
                                [by chain decomposition remainder-zero product-plus-zero]
                              @divides-intro |
                                forall n, d, k: Int; n = mul(d, k) -> does_divide(d, n)
                                [by theorem dividesIntro]
                              @p-divides-a-if-product |
                                a = mul(p, q) -> does_divide(p, a)
                                [by specialize dividesIntro(a, p, q)]
                              @p-divides-a |
                                does_divide(p, a)
                                [by modus_ponens p-divides-a-if-product a-is-p-times-q]
                              @divides-decidable |
                                does_divide(p, a) or (not does_divide(p, a))
                                [by or_intro_left p-divides-a]
                            }
                          @when-remainder-positive |
                            assume less_than(ZERO, r) {
                              @remainder-positive |
                                less_than(ZERO, r)
                                [by hypothesis when-remainder-positive]
                              @given-p-divides-a |
                                assume does_divide(p, a) {
                                  @p-divides-a |
                                    does_divide(p, a)
                                    [by hypothesis given-p-divides-a]
                                  @divides-elim |
                                    forall d, n: Int; does_divide(d, n) -> exists k: Int; n = mul(d, k)
                                    [by theorem dividesElim]
                                  @exact-quotient-exists |
                                    exists k: Int; a = mul(p, k)
                                    [by specialize dividesElim(p, a) p-divides-a]
                                  @with-exact-quotient-m |
                                    unpack m: Int from exact-quotient-exists {
                                      @a-is-p-times-m |
                                        a = mul(p, m)
                                        [by hypothesis with-exact-quotient-m]
                                      @add-zero-right |
                                        forall n: Int; add(n, ZERO) = n
                                        [by theorem addZeroRight]
                                      @product-m-plus-zero |
                                        add(mul(p, m), ZERO) = mul(p, m)
                                        [by forall_elim(mul(p, m)) add-zero-right]
                                      @a-is-p-times-m-plus-zero |
                                        a = add(mul(p, m), ZERO)
                                        [by chain a-is-p-times-m product-m-plus-zero]
                                      @zero-is-nonneg |
                                        is_nonneg(ZERO)
                                        [by axiom nonnegZero]
                                      @uniqueness |
                                        forall x, y, q1, r1, q2, r2: Int; less_than(ZERO, y) -> x = add(mul(y, q1), r1) -> is_nonneg(r1) -> less_than(r1, y) -> x = add(mul(y, q2), r2) -> is_nonneg(r2) -> less_than(r2, y) -> (q1 = q2 and r1 = r2)
                                        [by theorem divisionAlgorithmUnique]
                                      @uniqueness-for-a |
                                        less_than(ZERO, p) -> a = add(mul(p, q), r) -> is_nonneg(r) -> less_than(r, p) -> a = add(mul(p, m), ZERO) -> is_nonneg(ZERO) -> less_than(ZERO, p) -> (q = m and r = ZERO)
                                        [by forall_elim(a, p, q, r, m, ZERO) uniqueness]
                                      @quotient-and-remainder-agree |
                                        q = m and r = ZERO
                                        [by tautology uniqueness-for-a p-positive decomposition remainder-nonneg remainder-less_than-p a-is-p-times-m-plus-zero zero-is-nonneg]
                                      @remainder-is-zero |
                                        r = ZERO
                                        [by and_elim_right quotient-and-remainder-agree]
                                      @zero-less_than-zero |
                                        less_than(ZERO, ZERO)
                                        [by rewrite remainder-is-zero remainder-positive]
                                      @less-than-irreflexive |
                                        forall n: Int; not less_than(n, n)
                                        [by theorem lessThanIrreflexive]
                                      @zero-not-less_than-zero |
                                        not less_than(ZERO, ZERO)
                                        [by specialize lessThanIrreflexive(ZERO)]
                                      @contradiction |
                                        not does_divide(p, a)
                                        [by absurd zero-less_than-zero zero-not-less_than-zero]
                                    }
                                  @p-does-not-divide-a-inner |
                                    not does_divide(p, a)
                                    [by exists_elim with-exact-quotient-m]
                                }
                              @p-does-not-divide-a |
                                not does_divide(p, a)
                                [by not_intro given-p-divides-a p-divides-a p-does-not-divide-a-inner]
                              @divides-decidable |
                                does_divide(p, a) or (not does_divide(p, a))
                                [by or_intro_right p-does-not-divide-a]
                            }
                        }
                    }
                  @divides-decidable |
                    does_divide(p, a) or (not does_divide(p, a))
                    [by exists_elim with-remainder-r]
                }
              @divides-decidable |
                does_divide(p, a) or (not does_divide(p, a))
                [by exists_elim with-quotient-q]
            }
          @positive-p-gives-decidable |
            less_than(ZERO, p) -> (does_divide(p, a) or (not does_divide(p, a)))
            [by implies_intro given-p-positive]
        }
      @discharge-a |
        forall a: Int; less_than(ZERO, p) -> (does_divide(p, a) or (not does_divide(p, a)))
        [by forall_intro generalize-a]
    }
  @conclusion |
    forall p, a: Int; less_than(ZERO, p) -> (does_divide(p, a) or (not does_divide(p, a)))
    [by forall_intro generalize-p]
qed
```

The bridge from primality to Bézout's coprimality hypothesis. Judson takes
`gcd(a, p) = 1` for granted when `p ∤ a`; over `ℤ` the common divisors come with
signs, so we prove it: any common divisor `e` of `a` and `p` is `±1`. Trichotomy
splits `e`. If `e > 0`, primality gives `e = 1` or `e = p`, and `e = p` would
make `p | a` (via `e | a`), against the hypothesis — so `e = 1`. If `e < 0`,
`neg(e) > 0` divides both (`negDivides`), so the same argument gives `neg(e) = 1`,
i.e. `e = -1`. And `e = 0` is impossible, since `0 | p` forces `p = 0 < p`.

```bpa
theorem coprimalityFromPrime: forall p, a: Int;
  less_than(ZERO, p)
  -> (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
  -> (not does_divide(p, a))
  -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
proof
  @generalize-p |
    fix p: Int {
      @generalize-a |
        fix a: Int {
          @given-p-positive |
            assume less_than(ZERO, p) {
              @given-p-prime |
                assume forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p) {
                  @given-p-not-divide-a |
                    assume not does_divide(p, a) {
                      @p-positive |
                        less_than(ZERO, p)
                        [by hypothesis given-p-positive]
                      @only-positive-divisors-are-one-and-p |
                        forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)
                        [by hypothesis given-p-prime]
                      @p-does-not-divide-a |
                        not does_divide(p, a)
                        [by hypothesis given-p-not-divide-a]
                      @common-divisors-are-units |
                        fix e: Int {
                          @given-e-divides-a |
                            assume does_divide(e, a) {
                              @given-e-divides-p |
                                assume does_divide(e, p) {
                                  @e-divides-a |
                                    does_divide(e, a)
                                    [by hypothesis given-e-divides-a]
                                  @e-divides-p |
                                    does_divide(e, p)
                                    [by hypothesis given-e-divides-p]
                                  @e-against-zero |
                                    less_than(e, ZERO) or e = ZERO or less_than(ZERO, e)
                                    [by specialize trichotomy(e, ZERO)]
                                  @e-is-unit |
                                    e = ONE or e = neg(ONE)
                                    case e-against-zero {
                                      @when-e-negative |
                                        assume less_than(e, ZERO) {
                                          @e-negative |
                                            less_than(e, ZERO)
                                            [by hypothesis when-e-negative]
                                          @neg-e-is-positive |
                                            less_than(ZERO, neg(e))
                                            [by specialize positiveOfNegative(e) e-negative]
                                          @neg-e-divides-p |
                                            does_divide(neg(e), p)
                                            [by specialize negDivides(e, p) e-divides-p]
                                          @neg-e-divides-a |
                                            does_divide(neg(e), a)
                                            [by specialize negDivides(e, a) e-divides-a]
                                          @primality-at-neg-e |
                                            less_than(ZERO, neg(e)) -> does_divide(neg(e), p) -> (neg(e) = ONE or neg(e) = p)
                                            [by forall_elim(neg(e)) only-positive-divisors-are-one-and-p]
                                          @neg-e-is-one-or-p |
                                            neg(e) = ONE or neg(e) = p
                                            [by tautology primality-at-neg-e neg-e-is-positive neg-e-divides-p]
                                          @e-is-unit |
                                            e = ONE or e = neg(ONE)
                                            case neg-e-is-one-or-p {
                                              @when-neg-e-is-one |
                                                assume neg(e) = ONE {
                                                  // neg(e)=1 => e = neg(neg(e)) = neg(1).
                                                  @neg-e-is-one |
                                                    neg(e) = ONE
                                                    [by hypothesis when-neg-e-is-one]
                                                  @e-is-neg-neg-e |
                                                    neg(neg(e)) = e
                                                    [by specialize negNeg(e)]
                                                  @e-is-neg-one |
                                                    e = neg(ONE)
                                                    [by chain e-is-neg-neg-e neg-e-is-one]
                                                  @e-is-unit |
                                                    e = ONE or e = neg(ONE)
                                                    [by or_intro_right e-is-neg-one]
                                                }
                                              @when-neg-e-is-p |
                                                assume neg(e) = p {
                                                  // neg(e)=p and neg(e)|a => p|a, contradiction.
                                                  @neg-e-is-p |
                                                    neg(e) = p
                                                    [by hypothesis when-neg-e-is-p]
                                                  @p-divides-a |
                                                    does_divide(p, a)
                                                    [by rewrite neg-e-is-p neg-e-divides-a]
                                                  @e-is-unit |
                                                    e = ONE or e = neg(ONE)
                                                    [by absurd p-divides-a p-does-not-divide-a]
                                                }
                                            }
                                        }
                                      @when-e-zero |
                                        assume e = ZERO {
                                          // e=0 and e|p => p = 0*k = 0, but p>0 is irreflexive-absurd.
                                          @e-is-zero |
                                            e = ZERO
                                            [by hypothesis when-e-zero]
                                          @zero-divides-p |
                                            does_divide(ZERO, p)
                                            [by rewrite e-is-zero e-divides-p]
                                          @divides-elim |
                                            forall d, n: Int; does_divide(d, n) -> exists k: Int; n = mul(d, k)
                                            [by theorem dividesElim]
                                          @zero-quotient-exists |
                                            exists k: Int; p = mul(ZERO, k)
                                            [by specialize dividesElim(ZERO, p) zero-divides-p]
                                          @with-zero-quotient-k |
                                            unpack k: Int from zero-quotient-exists {
                                              @p-is-zero-times-k |
                                                p = mul(ZERO, k)
                                                [by hypothesis with-zero-quotient-k]
                                              @mul-zero-left |
                                                forall b: Int; mul(ZERO, b) = ZERO
                                                [by axiom mulZeroLeft]
                                              @zero-times-k-is-zero |
                                                mul(ZERO, k) = ZERO
                                                [by specialize mulZeroLeft(k)]
                                              @p-is-zero |
                                                p = ZERO
                                                [by rewrite zero-times-k-is-zero p-is-zero-times-k]
                                              @zero-is-positive |
                                                less_than(ZERO, ZERO)
                                                [by rewrite p-is-zero p-positive]
                                              @less-than-irreflexive |
                                                forall n: Int; not less_than(n, n)
                                                [by theorem lessThanIrreflexive]
                                              @zero-not-less_than-zero |
                                                not less_than(ZERO, ZERO)
                                                [by specialize lessThanIrreflexive(ZERO)]
                                              @e-is-unit |
                                                e = ONE or e = neg(ONE)
                                                [by absurd zero-is-positive zero-not-less_than-zero]
                                            }
                                          @e-is-unit |
                                            e = ONE or e = neg(ONE)
                                            [by exists_elim with-zero-quotient-k]
                                        }
                                      @when-e-positive |
                                        assume less_than(ZERO, e) {
                                          @e-positive |
                                            less_than(ZERO, e)
                                            [by hypothesis when-e-positive]
                                          @primality-at-e |
                                            less_than(ZERO, e) -> does_divide(e, p) -> (e = ONE or e = p)
                                            [by forall_elim(e) only-positive-divisors-are-one-and-p]
                                          @e-is-one-or-p |
                                            e = ONE or e = p
                                            [by tautology primality-at-e e-positive e-divides-p]
                                          @e-is-unit |
                                            e = ONE or e = neg(ONE)
                                            case e-is-one-or-p {
                                              @when-e-is-one |
                                                assume e = ONE {
                                                  @e-is-one |
                                                    e = ONE
                                                    [by hypothesis when-e-is-one]
                                                  @e-is-unit |
                                                    e = ONE or e = neg(ONE)
                                                    [by or_intro_left e-is-one]
                                                }
                                              @when-e-is-p |
                                                assume e = p {
                                                  @e-is-p |
                                                    e = p
                                                    [by hypothesis when-e-is-p]
                                                  @p-divides-a |
                                                    does_divide(p, a)
                                                    [by rewrite e-is-p e-divides-a]
                                                  @e-is-unit |
                                                    e = ONE or e = neg(ONE)
                                                    [by absurd p-divides-a p-does-not-divide-a]
                                                }
                                            }
                                        }
                                    }
                                }
                              @e-divides-p-gives-unit |
                                does_divide(e, p) -> (e = ONE or e = neg(ONE))
                                [by implies_intro given-e-divides-p]
                            }
                          @e-divides-a-gives-unit |
                            does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE))
                            [by implies_intro given-e-divides-a]
                        }
                      @common-divisors-are-units-for-all |
                        forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE))
                        [by forall_intro common-divisors-are-units]
                    }
                  @not-divide-gives-coprime |
                    (not does_divide(p, a))
                      -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
                    [by implies_intro given-p-not-divide-a]
                }
              @prime-gives-coprime |
                (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
                  -> (not does_divide(p, a))
                  -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
                [by implies_intro given-p-prime]
            }
          @positive-p-gives-coprime |
            less_than(ZERO, p)
              -> (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
              -> (not does_divide(p, a))
              -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
            [by implies_intro given-p-positive]
        }
      @discharge-a |
        forall a: Int; less_than(ZERO, p)
          -> (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
          -> (not does_divide(p, a))
          -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
        [by forall_intro generalize-a]
    }
  @conclusion |
    forall p, a: Int; less_than(ZERO, p)
      -> (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
      -> (not does_divide(p, a))
      -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
    [by forall_intro generalize-p]
qed
```

**Euclid's Lemma** (Judson, Lemma 2.11). Suppose the prime `p` divides `a·b`.
If `p | a` we are done; otherwise `p ∤ a`, so `a` and `p` are coprime and
`coprimeBezout` yields `r, s` with `a·r + p·s = 1` (the hypotheses `a ≠ 0` and
`p ≠ 0` come from `p ∤ a` — `p | 0` always holds — and from `p > 0`). Multiplying
by `b`, `b = b·1 = b·(a·r + p·s) = (a·b)·r + p·(b·s)`, the last identity the only
elided ring algebra (discharged by `polynomial`). Now `p | (a·b)·r` because
`p | a·b`, and `p | p·(b·s)` because `p | p`; by `dividesAdd` their sum, which is
`b`, is divisible by `p`. So `p | b`.

```bpa
theorem euclidsLemma: forall a, b, p: Int;
  (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
  -> does_divide(p, mul(a, b))
  -> (does_divide(p, a) or does_divide(p, b))
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @generalize-p |
            fix p: Int {
              @given-p-prime |
                assume less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)) {
                  @given-p-divides-product |
                    assume does_divide(p, mul(a, b)) {
                      @prime-conjunction |
                        less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
                        [by hypothesis given-p-prime]
                      @one-less_than-p |
                        less_than(ONE, p)
                        [by and_elim_left prime-conjunction]
                      @only-positive-divisors-are-one-and-p |
                        forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)
                        [by and_elim_right prime-conjunction]
                      @p-divides-product |
                        does_divide(p, mul(a, b))
                        [by hypothesis given-p-divides-product]
                      // p > 0, from 0 < 1 < p.
                      @zero-less_than-one |
                        less_than(ZERO, ONE)
                        [by theorem zeroLessThanOne]
                      @less-than-transitive |
                        forall x, y, z: Int; less_than(x, y) -> less_than(y, z) -> less_than(x, z)
                        [by theorem lessThanTransitive]
                      @zero-less_than-p-if-chained |
                        less_than(ZERO, ONE) -> less_than(ONE, p) -> less_than(ZERO, p)
                        [by specialize lessThanTransitive(ZERO, ONE, p)]
                      @p-positive |
                        less_than(ZERO, p)
                        [by tautology zero-less_than-p-if-chained zero-less_than-one one-less_than-p]
                      // decide whether p | a.
                      @divides-decidable |
                        forall x, y: Int; less_than(ZERO, x) -> (does_divide(x, y) or (not does_divide(x, y)))
                        [by theorem dividesDecidable]
                      @p-divides-a-or-not |
                        does_divide(p, a) or (not does_divide(p, a))
                        [by specialize dividesDecidable(p, a) p-positive]
                      @p-divides-a-or-b |
                        does_divide(p, a) or does_divide(p, b)
                        case p-divides-a-or-not {
                          @when-p-divides-a |
                            assume does_divide(p, a) {
                              @p-divides-a |
                                does_divide(p, a)
                                [by hypothesis when-p-divides-a]
                              @p-divides-a-or-b |
                                does_divide(p, a) or does_divide(p, b)
                                [by or_intro_left p-divides-a]
                            }
                          @when-p-does-not-divide-a |
                            assume not does_divide(p, a) {
                              @p-does-not-divide-a |
                                not does_divide(p, a)
                                [by hypothesis when-p-does-not-divide-a]
                              // a != 0: else p | 0 = a, against the branch.
                              @given-a-zero |
                                assume a = ZERO {
                                  @a-is-zero |
                                    a = ZERO
                                    [by hypothesis given-a-zero]
                                  @zero-is-p-times-zero |
                                    ZERO = mul(p, ZERO)
                                    [by simplify mulZeroRight]
                                  @a-is-p-times-zero |
                                    a = mul(p, ZERO)
                                    [by rewrite zero-is-p-times-zero a-is-zero]
                                  @divides-intro |
                                    forall n, d, k: Int; n = mul(d, k) -> does_divide(d, n)
                                    [by theorem dividesIntro]
                                  @p-divides-a-if-product |
                                    a = mul(p, ZERO) -> does_divide(p, a)
                                    [by specialize dividesIntro(a, p, ZERO)]
                                  @p-divides-a |
                                    does_divide(p, a)
                                    [by modus_ponens p-divides-a-if-product a-is-p-times-zero]
                                  @a-not-zero-inner |
                                    a != ZERO
                                    [by absurd p-divides-a p-does-not-divide-a]
                                }
                              @a-not-zero |
                                a != ZERO
                                [by not_intro given-a-zero a-is-zero a-not-zero-inner]
                              // p != 0, from p > 0.
                              @given-p-zero |
                                assume p = ZERO {
                                  @p-is-zero |
                                    p = ZERO
                                    [by hypothesis given-p-zero]
                                  @zero-less_than-zero |
                                    less_than(ZERO, ZERO)
                                    [by rewrite p-is-zero p-positive]
                                  @less-than-irreflexive |
                                    forall n: Int; not less_than(n, n)
                                    [by theorem lessThanIrreflexive]
                                  @zero-not-less_than-zero |
                                    not less_than(ZERO, ZERO)
                                    [by specialize lessThanIrreflexive(ZERO)]
                                  @p-not-zero-inner |
                                    p != ZERO
                                    [by absurd zero-less_than-zero zero-not-less_than-zero]
                                }
                              @p-not-zero |
                                p != ZERO
                                [by not_intro given-p-zero p-is-zero p-not-zero-inner]
                              // a and p are coprime.
                              @a-and-p-coprime |
                                forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE))
                                [by specialize coprimalityFromPrime(p, a) p-positive only-positive-divisors-are-one-and-p p-does-not-divide-a]
                              // Bezout: a*r + p*s = 1.
                              @bezout-coefficients-exist |
                                exists r: Int; exists s: Int; add(mul(a, r), mul(p, s)) = ONE
                                [by specialize coprimeBezout(a, p) a-not-zero p-not-zero a-and-p-coprime]
                              @with-bezout-r |
                                unpack r: Int from bezout-coefficients-exist {
                                  @bezout-s-exists |
                                    exists s: Int; add(mul(a, r), mul(p, s)) = ONE
                                    [by hypothesis with-bezout-r]
                                  @with-bezout-s |
                                    unpack s: Int from bezout-s-exists {
                                      @bezout-holds |
                                        add(mul(a, r), mul(p, s)) = ONE
                                        [by hypothesis with-bezout-s]
                                      // b = b*1 = b*(a*r + p*s) = (a*b)*r + p*(b*s).
                                      @mul-one-right |
                                        forall n: Int; mul(n, ONE) = n
                                        [by theorem mulOneRight]
                                      @b-times-one-is-b |
                                        mul(b, ONE) = b
                                        [by specialize mulOneRight(b)]
                                      @product-distributes |
                                        mul(b, add(mul(a, r), mul(p, s))) = add(mul(mul(a, b), r), mul(p, mul(b, s)))
                                        [by polynomial]
                                      @b-splits-into-product-and-p-multiple |
                                        b = add(mul(mul(a, b), r), mul(p, mul(b, s)))
                                        [by chain b-times-one-is-b bezout-holds product-distributes]
                                      // p | (a*b)*r, since p | a*b.
                                      @divides-mul |
                                        forall d, x, c: Int; does_divide(d, x) -> does_divide(d, mul(x, c))
                                        [by theorem dividesMul]
                                      @p-divides-product-times-r-if |
                                        does_divide(p, mul(a, b)) -> does_divide(p, mul(mul(a, b), r))
                                        [by forall_elim(p, mul(a, b), r) divides-mul]
                                      @p-divides-product-times-r |
                                        does_divide(p, mul(mul(a, b), r))
                                        [by modus_ponens p-divides-product-times-r-if p-divides-product]
                                      // p | p*(b*s), since p | p.
                                      @p-divides-p |
                                        does_divide(p, p)
                                        [by specialize dividesRefl(p)]
                                      @p-divides-p-times-bs-if |
                                        does_divide(p, p) -> does_divide(p, mul(p, mul(b, s)))
                                        [by forall_elim(p, p, mul(b, s)) divides-mul]
                                      @p-divides-p-times-bs |
                                        does_divide(p, mul(p, mul(b, s)))
                                        [by modus_ponens p-divides-p-times-bs-if p-divides-p]
                                      // p | their sum, which is b.
                                      @divides-add |
                                        forall d, x, y: Int; does_divide(d, x) -> does_divide(d, y) -> does_divide(d, add(x, y))
                                        [by theorem dividesAdd]
                                      @p-divides-sum-if |
                                        does_divide(p, mul(mul(a, b), r)) -> does_divide(p, mul(p, mul(b, s))) -> does_divide(p, add(mul(mul(a, b), r), mul(p, mul(b, s))))
                                        [by forall_elim(p, mul(mul(a, b), r), mul(p, mul(b, s))) divides-add]
                                      @p-divides-sum |
                                        does_divide(p, add(mul(mul(a, b), r), mul(p, mul(b, s))))
                                        [by tautology p-divides-sum-if p-divides-product-times-r p-divides-p-times-bs]
                                      @sum-is-b |
                                        add(mul(mul(a, b), r), mul(p, mul(b, s))) = b
                                        [by symmetry b-splits-into-product-and-p-multiple]
                                      @p-divides-b |
                                        does_divide(p, b)
                                        [by rewrite sum-is-b p-divides-sum]
                                      @p-divides-a-or-b |
                                        does_divide(p, a) or does_divide(p, b)
                                        [by or_intro_right p-divides-b]
                                    }
                                  @p-divides-a-or-b |
                                    does_divide(p, a) or does_divide(p, b)
                                    [by exists_elim with-bezout-s]
                                }
                              @p-divides-a-or-b |
                                does_divide(p, a) or does_divide(p, b)
                                [by exists_elim with-bezout-r]
                            }
                        }
                    }
                  @product-implies-divides-factor |
                    does_divide(p, mul(a, b)) -> (does_divide(p, a) or does_divide(p, b))
                    [by implies_intro given-p-divides-product]
                }
              @prime-implies-divides-factor |
                (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
                  -> does_divide(p, mul(a, b)) -> (does_divide(p, a) or does_divide(p, b))
                [by implies_intro given-p-prime]
            }
          @discharge-p |
            forall p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> does_divide(p, mul(a, b)) -> (does_divide(p, a) or does_divide(p, b))
            [by forall_intro generalize-p]
        }
      @discharge-b |
        forall b, p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> does_divide(p, mul(a, b)) -> (does_divide(p, a) or does_divide(p, b))
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b, p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> does_divide(p, mul(a, b)) -> (does_divide(p, a) or does_divide(p, b))
    [by forall_intro generalize-a]
qed
```

Euclid's Lemma lifts by induction on the length `k` to a whole finite product:
a prime dividing `productUpTo(s, k)` must divide one of the first `k` entries.
At `k = 0` the product is `1`, which no prime divides (`divisorAtMost` would
force `p ≤ 1`); at `k+1` the product is `productUpTo(s, k) · at(s, k)`, and
Euclid's Lemma sends `p` into the left factor (induction hypothesis: an entry
less_than `k`) or the right one (the entry at `k`). This is the crux of
factorization uniqueness. (`lessThanSuccSelf`: `k < k+1`, the bound-lifting step.)

```bpa
// k < succ(k): the successor of a value strictly exceeds it. The gap is exactly
// ONE (= succ(ZERO)), which is nonneg, so lessThanIntro applies.
theorem lessThanSuccSelf: forall k: Int; less_than(k, succ(k))
proof
  @generalize-k |
    fix k: Int {
      @zero-is-nonneg |
        is_nonneg(ZERO)
        [by axiom nonnegZero]
      @gap-of-one-reaches-successor |
        add(k, succ(ZERO)) = succ(k)
        [by simplify addSuccRight addZeroRight]
      @less_than-intro-at-k-zero-succ-k |
        forall a, d, b: Int; is_nonneg(d) -> add(a, succ(d)) = b -> less_than(a, b)
        [by axiom lessThanIntro]
      @less_than-intro-specialized |
        is_nonneg(ZERO) -> add(k, succ(ZERO)) = succ(k) -> less_than(k, succ(k))
        [by forall_elim(k, ZERO, succ(k)) less_than-intro-at-k-zero-succ-k]
      @k-less_than-succ-k |
        less_than(k, succ(k))
        [by tautology less_than-intro-specialized zero-is-nonneg gap-of-one-reaches-successor]
    }
  @conclusion |
    forall k: Int; less_than(k, succ(k))
    [by forall_intro generalize-k]
qed

// FTA-uniqueness crux: a prime dividing a length-k product divides one of the
// first k entries. Induction on k via nonnegInduction. Base (k=0): the product
// is ONE, and no p>1 divides ONE (divisorAtMost forces p<=1, contradicting p>1),
// so the goal holds vacuously by absurdity. Step: productUpTo(s,succ(k)) factors
// as productUpTo(s,k)*at(s,k); Euclid's lemma splits the divisibility, and either
// the inductive hypothesis supplies an earlier witness i<k (still < succ(k)) or
// p divides the new last entry at(s,k) (witness i=k).
theorem primeDividesProductImpliesMember: forall k: Int; is_nonneg(k) ->
  forall s: Seq; forall p: Int;
  (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
  -> does_divide(p, productUpTo(s, k))
  -> (exists i: Int; is_nonneg(i) and less_than(i, k) and does_divide(p, at(s, i)))
proof
  // base case: k = 0 — the empty product is ONE, which no prime divides.
  @base-case-proof |
    fix s: Seq {
      @generalize-p-base |
        fix p: Int {
          @given-prime-base |
            assume less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)) {
              @given-p-divides-empty-product |
                assume does_divide(p, productUpTo(s, ZERO)) {
                  @prime-p-base |
                    less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
                    [by hypothesis given-prime-base]
                  @one-is-less_than-p |
                    less_than(ONE, p)
                    [by tautology prime-p-base]
                  @empty-product-is-one-for-all |
                    forall t: Seq; productUpTo(t, ZERO) = ONE
                    [by axiom productUpToZero]
                  @empty-product-is-one |
                    productUpTo(s, ZERO) = ONE
                    [by specialize productUpToZero(s)]
                  @p-divides-empty-product |
                    does_divide(p, productUpTo(s, ZERO))
                    [by hypothesis given-p-divides-empty-product]
                  @p-divides-one |
                    does_divide(p, ONE)
                    [by rewrite empty-product-is-one p-divides-empty-product]
                  @zero-less_than-one |
                    less_than(ZERO, ONE)
                    [by theorem zeroLessThanOne]
                  @less_than-is-transitive |
                    forall a, b, c: Int; less_than(a, b) -> less_than(b, c) -> less_than(a, c)
                    [by theorem lessThanTransitive]
                  @zero-less_than-one-less_than-p |
                    less_than(ZERO, ONE) -> less_than(ONE, p) -> less_than(ZERO, p)
                    [by specialize lessThanTransitive(ZERO, ONE, p)]
                  @zero-less_than-p |
                    less_than(ZERO, p)
                    [by tautology zero-less_than-one-less_than-p zero-less_than-one one-is-less_than-p]
                  @divisor-at-most |
                    forall d, n: Int; less_than(ZERO, d) -> less_than(ZERO, n) -> does_divide(d, n) -> at_most(d, n)
                    [by theorem divisorAtMost]
                  @p-at-most-one-conditions |
                    less_than(ZERO, p) -> less_than(ZERO, ONE) -> does_divide(p, ONE) -> at_most(p, ONE)
                    [by specialize divisorAtMost(p, ONE)]
                  @p-at-most-one |
                    at_most(p, ONE)
                    [by tautology p-at-most-one-conditions zero-less_than-p zero-less_than-one p-divides-one]
                  @at-most-splits |
                    forall a, b: Int; at_most(a, b) -> (less_than(a, b) or a = b)
                    [by theorem lessOrEqualSplit]
                  @p-less_than-one-or-equals-one-conditions |
                    at_most(p, ONE) -> (less_than(p, ONE) or p = ONE)
                    [by specialize lessOrEqualSplit(p, ONE)]
                  @p-less_than-one-or-equals-one |
                    less_than(p, ONE) or p = ONE
                    [by modus_ponens p-less_than-one-or-equals-one-conditions p-at-most-one]
                  @less_than-is-irreflexive |
                    forall n: Int; not less_than(n, n)
                    [by theorem lessThanIrreflexive]
                  @p-not-less_than-p |
                    not less_than(p, p)
                    [by specialize lessThanIrreflexive(p)]
                  // both branches of at_most(p, ONE) contradict less_than(ONE, p).
                  @one-not-less_than-one |
                    not less_than(ONE, ONE)
                    [by specialize lessThanIrreflexive(ONE)]
                  @when-p-less_than-one |
                    assume less_than(p, ONE) {
                      @p-less_than-one |
                        less_than(p, ONE)
                        [by hypothesis when-p-less_than-one]
                      @one-less_than-p-less_than-one |
                        less_than(ONE, p) -> less_than(p, ONE) -> less_than(ONE, ONE)
                        [by specialize lessThanTransitive(ONE, p, ONE)]
                      @one-less_than-one |
                        less_than(ONE, ONE)
                        [by tautology one-less_than-p-less_than-one one-is-less_than-p p-less_than-one]
                      @conclusion-empty-witness |
                        exists i: Int; is_nonneg(i) and less_than(i, ZERO) and does_divide(p, at(s, i))
                        [by absurd one-less_than-one one-not-less_than-one]
                    }
                  @when-p-equals-one |
                    assume p = ONE {
                      @p-equals-one |
                        p = ONE
                        [by hypothesis when-p-equals-one]
                      @one-less_than-one-from-p |
                        less_than(ONE, ONE)
                        [by rewrite p-equals-one one-is-less_than-p]
                      @conclusion-empty-witness |
                        exists i: Int; is_nonneg(i) and less_than(i, ZERO) and does_divide(p, at(s, i))
                        [by absurd one-less_than-one-from-p one-not-less_than-one]
                    }
                  @some-entry-less_than-zero-divides |
                    exists i: Int; is_nonneg(i) and less_than(i, ZERO) and does_divide(p, at(s, i))
                    [by or_elim p-less_than-one-or-equals-one when-p-less_than-one when-p-equals-one]
                }
              @p-divides-empty-gives-witness |
                does_divide(p, productUpTo(s, ZERO))
                  -> (exists i: Int; is_nonneg(i) and less_than(i, ZERO) and does_divide(p, at(s, i)))
                [by implies_intro given-p-divides-empty-product]
            }
          @prime-gives-empty-witness |
            (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
              -> does_divide(p, productUpTo(s, ZERO))
              -> (exists i: Int; is_nonneg(i) and less_than(i, ZERO) and does_divide(p, at(s, i)))
            [by implies_intro given-prime-base]
        }
      @empty-product-claim-for-all-p |
        forall p: Int;
          (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
          -> does_divide(p, productUpTo(s, ZERO))
          -> (exists i: Int; is_nonneg(i) and less_than(i, ZERO) and does_divide(p, at(s, i)))
        [by forall_intro generalize-p-base]
    }
  @base-case |
    forall s: Seq; forall p: Int;
      (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
      -> does_divide(p, productUpTo(s, ZERO))
      -> (exists i: Int; is_nonneg(i) and less_than(i, ZERO) and does_divide(p, at(s, i)))
    [by forall_intro base-case-proof]

  // inductive step: from the claim at k, derive it at succ(k).
  @induction-step |
    fix k: Int {
      @given-k-nonneg |
        assume is_nonneg(k) {
          @given-inductive-hypothesis |
            assume forall s: Seq; forall p: Int;
              (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
              -> does_divide(p, productUpTo(s, k))
              -> (exists i: Int; is_nonneg(i) and less_than(i, k) and does_divide(p, at(s, i))) {
              @inductive-hypothesis |
                forall s: Seq; forall p: Int;
                  (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
                  -> does_divide(p, productUpTo(s, k))
                  -> (exists i: Int; is_nonneg(i) and less_than(i, k) and does_divide(p, at(s, i)))
                [by hypothesis given-inductive-hypothesis]
              @k-nonneg |
                is_nonneg(k)
                [by hypothesis given-k-nonneg]
              @k-less_than-succ-k |
                less_than(k, succ(k))
                [by specialize lessThanSuccSelf(k)]
              @claim-at-succ-k |
                fix s: Seq {
                  @generalize-p-step |
                    fix p: Int {
                      @given-prime-step |
                        assume less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)) {
                          @prime-p |
                            less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
                            [by hypothesis given-prime-step]
                          @given-p-divides-succ-product |
                            assume does_divide(p, productUpTo(s, succ(k))) {
                              @p-divides-succ-product |
                                does_divide(p, productUpTo(s, succ(k)))
                                [by hypothesis given-p-divides-succ-product]
                              @product-of-successor-bound |
                                forall t: Seq; forall j: Int; productUpTo(t, succ(j)) = mul(productUpTo(t, j), at(t, j))
                                [by axiom productUpToSucc]
                              @successor-product-splits |
                                productUpTo(s, succ(k)) = mul(productUpTo(s, k), at(s, k))
                                [by specialize productUpToSucc(s, k)]
                              @p-divides-product-times-last |
                                does_divide(p, mul(productUpTo(s, k), at(s, k)))
                                [by rewrite successor-product-splits p-divides-succ-product]
                              @euclids-lemma |
                                forall a, b, p2: Int;
                                  (less_than(ONE, p2) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p2) -> (d = ONE or d = p2)))
                                  -> does_divide(p2, mul(a, b))
                                  -> (does_divide(p2, a) or does_divide(p2, b))
                                [by theorem euclidsLemma]
                              @euclid-at-product-last-p |
                                (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
                                  -> does_divide(p, mul(productUpTo(s, k), at(s, k)))
                                  -> (does_divide(p, productUpTo(s, k)) or does_divide(p, at(s, k)))
                                [by forall_elim(productUpTo(s, k), at(s, k), p) euclids-lemma]
                              @p-divides-product-or-last |
                                does_divide(p, productUpTo(s, k)) or does_divide(p, at(s, k))
                                [by tautology euclid-at-product-last-p prime-p p-divides-product-times-last]
                              // case 1: p divides the length-k product — the IH gives i < k < succ(k).
                              @when-p-divides-product |
                                assume does_divide(p, productUpTo(s, k)) {
                                  @p-divides-product |
                                    does_divide(p, productUpTo(s, k))
                                    [by hypothesis when-p-divides-product]
                                  @inductive-hypothesis-at-s |
                                    forall p2: Int;
                                      (less_than(ONE, p2) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p2) -> (d = ONE or d = p2)))
                                      -> does_divide(p2, productUpTo(s, k))
                                      -> (exists i: Int; is_nonneg(i) and less_than(i, k) and does_divide(p2, at(s, i)))
                                    [by forall_elim(s) inductive-hypothesis]
                                  @inductive-hypothesis-at-s-p |
                                    (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
                                      -> does_divide(p, productUpTo(s, k))
                                      -> (exists i: Int; is_nonneg(i) and less_than(i, k) and does_divide(p, at(s, i)))
                                    [by forall_elim(p) inductive-hypothesis-at-s]
                                  @earlier-entry-exists |
                                    exists i: Int; is_nonneg(i) and less_than(i, k) and does_divide(p, at(s, i))
                                    [by tautology inductive-hypothesis-at-s-p prime-p p-divides-product]
                                  @with-earlier-entry-index |
                                    unpack earlier: Int from earlier-entry-exists {
                                      @entry-earlier-nonneg-less_than-k-divides |
                                        is_nonneg(earlier) and less_than(earlier, k) and does_divide(p, at(s, earlier))
                                        [by hypothesis with-earlier-entry-index]
                                      @earlier-less_than-k |
                                        less_than(earlier, k)
                                        [by tautology entry-earlier-nonneg-less_than-k-divides]
                                      @earlier-less_than-succ-k |
                                        less_than(earlier, succ(k))
                                        [by specialize lessThanSuccRight(earlier, k) earlier-less_than-k]
                                      @entry-earlier-nonneg-less_than-succ-k-divides |
                                        is_nonneg(earlier) and less_than(earlier, succ(k)) and does_divide(p, at(s, earlier))
                                        [by tautology entry-earlier-nonneg-less_than-k-divides earlier-less_than-succ-k]
                                      @conclusion-earlier-entry-less_than-succ-k |
                                        exists i: Int; is_nonneg(i) and less_than(i, succ(k)) and does_divide(p, at(s, i))
                                        [by exists_intro(earlier) entry-earlier-nonneg-less_than-succ-k-divides]
                                    }
                                  @earlier-entry-less_than-succ-k |
                                    exists i: Int; is_nonneg(i) and less_than(i, succ(k)) and does_divide(p, at(s, i))
                                    [by exists_elim with-earlier-entry-index]
                                }
                              // case 2: p divides the new last entry — witness i = k.
                              @when-p-divides-last |
                                assume does_divide(p, at(s, k)) {
                                  @p-divides-last |
                                    does_divide(p, at(s, k))
                                    [by hypothesis when-p-divides-last]
                                  @k-nonneg-less_than-succ-k-divides |
                                    is_nonneg(k) and less_than(k, succ(k)) and does_divide(p, at(s, k))
                                    [by tautology k-nonneg k-less_than-succ-k p-divides-last]
                                  @conclusion-last-entry-witness |
                                    exists i: Int; is_nonneg(i) and less_than(i, succ(k)) and does_divide(p, at(s, i))
                                    [by exists_intro(k) k-nonneg-less_than-succ-k-divides]
                                }
                              @entry-less_than-succ-k-divides |
                                exists i: Int; is_nonneg(i) and less_than(i, succ(k)) and does_divide(p, at(s, i))
                                [by or_elim p-divides-product-or-last when-p-divides-product when-p-divides-last]
                            }
                          @p-divides-succ-product-gives-witness |
                            does_divide(p, productUpTo(s, succ(k)))
                              -> (exists i: Int; is_nonneg(i) and less_than(i, succ(k)) and does_divide(p, at(s, i)))
                            [by implies_intro given-p-divides-succ-product]
                        }
                      @prime-gives-succ-witness |
                        (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
                          -> does_divide(p, productUpTo(s, succ(k)))
                          -> (exists i: Int; is_nonneg(i) and less_than(i, succ(k)) and does_divide(p, at(s, i)))
                        [by implies_intro given-prime-step]
                    }
                  @succ-product-claim-for-all-p |
                    forall p: Int;
                      (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
                      -> does_divide(p, productUpTo(s, succ(k)))
                      -> (exists i: Int; is_nonneg(i) and less_than(i, succ(k)) and does_divide(p, at(s, i)))
                    [by forall_intro generalize-p-step]
                }
              @claim-at-succ-k-for-all-s |
                forall s: Seq; forall p: Int;
                  (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
                  -> does_divide(p, productUpTo(s, succ(k)))
                  -> (exists i: Int; is_nonneg(i) and less_than(i, succ(k)) and does_divide(p, at(s, i)))
                [by forall_intro claim-at-succ-k]
            }
          @hypothesis-at-k-gives-claim-at-succ-k |
            (forall s: Seq; forall p: Int;
              (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
              -> does_divide(p, productUpTo(s, k))
              -> (exists i: Int; is_nonneg(i) and less_than(i, k) and does_divide(p, at(s, i))))
              -> (forall s: Seq; forall p: Int;
              (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
              -> does_divide(p, productUpTo(s, succ(k)))
              -> (exists i: Int; is_nonneg(i) and less_than(i, succ(k)) and does_divide(p, at(s, i))))
            [by implies_intro given-inductive-hypothesis]
        }
      @induction-step-at-k |
        is_nonneg(k)
          -> (forall s: Seq; forall p: Int;
          (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
          -> does_divide(p, productUpTo(s, k))
          -> (exists i: Int; is_nonneg(i) and less_than(i, k) and does_divide(p, at(s, i))))
          -> (forall s: Seq; forall p: Int;
          (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
          -> does_divide(p, productUpTo(s, succ(k)))
          -> (exists i: Int; is_nonneg(i) and less_than(i, succ(k)) and does_divide(p, at(s, i))))
        [by implies_intro given-k-nonneg]
    }
  @induction-step-for-all-k |
    forall k: Int; is_nonneg(k)
      -> (forall s: Seq; forall p: Int;
      (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
      -> does_divide(p, productUpTo(s, k))
      -> (exists i: Int; is_nonneg(i) and less_than(i, k) and does_divide(p, at(s, i))))
      -> (forall s: Seq; forall p: Int;
      (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
      -> does_divide(p, productUpTo(s, succ(k)))
      -> (exists i: Int; is_nonneg(i) and less_than(i, succ(k)) and does_divide(p, at(s, i))))
    [by forall_intro induction-step]

  @conclusion |
    forall k: Int; is_nonneg(k) ->
      forall s: Seq; forall p: Int;
      (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
      -> does_divide(p, productUpTo(s, k))
      -> (exists i: Int; is_nonneg(i) and less_than(i, k) and does_divide(p, at(s, i)))
    [by instantiate nonnegInduction((fun k: Int => forall s: Seq; forall p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> does_divide(p, productUpTo(s, k)) -> (exists i: Int; is_nonneg(i) and less_than(i, k) and does_divide(p, at(s, i))))) base-case induction-step-for-all-k]
qed
```

### The Infinitude of Primes

Euclid's classical argument (Judson, Theorem 2.12): were there only finitely
many primes `p₁, …, pₙ`, the number `P = p₁·p₂·⋯·pₙ + 1` has a prime factor `p`
(every integer `> 1` does — `primeFactorExists`), and by completeness `p` is one
of the `pᵢ`. Then `p` divides the product `p₁·⋯·pₙ` (`memberDividesProduct`), and
`p` divides `P`, so `p` divides `P − (product) = 1` (`dividesSubtracts`) — forcing
`p ≤ 1`, contradicting `p > 1`. No finite list can hold every prime.

We phrase a finite list of primes as a `Seq primes` together with a bound `count`,
its product `productUpTo(primes, count)`, and the *completeness* hypothesis
"every prime occurs among the first `count` entries" — stated with the membership
predicate `inSeqBefore`. The theorem is the negation: **no such complete finite
list exists**. Its one honest side condition, `productUpTo(primes, count) > 0`,
holds of any list of primes (each factor exceeds `1`); we take it as a hypothesis
rather than re-derive it, since it is manifest and orthogonal to the argument.

The `P > 1` step reads `P = product + 1 > product ≥ 1`; the `P − product = 1`
step is the only elided algebra (a linear identity discharged by `simplify`).

```bpa
theorem infinitudeOfPrimes: forall primes: Seq; forall count: Int;
  is_nonneg(count) -> less_than(ZERO, productUpTo(primes, count))
  -> (not (forall q: Int; (less_than(ONE, q) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBefore(q, primes, count)))
proof
  @generalize-primes |
    fix primes: Seq {
      @generalize-count |
        fix count: Int {
          @given-count-nonneg |
            assume is_nonneg(count) {
              @given-product-positive |
                assume less_than(ZERO, productUpTo(primes, count)) {
                  @product-positive |
                    less_than(ZERO, productUpTo(primes, count))
                    [by hypothesis given-product-positive]
                  // Suppose `primes` (up to `count`) enumerates every prime.
                  @given-list-is-complete |
                    assume forall q: Int; (less_than(ONE, q) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBefore(q, primes, count) {
                      @list-is-complete |
                        forall q: Int; (less_than(ONE, q) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBefore(q, primes, count)
                        [by hypothesis given-list-is-complete]
                      // P = product + 1 exceeds 1: product ≥ 1 and product < product + 1.
                      @one-at-most-positive |
                        forall x: Int; less_than(ZERO, x) -> at_most(ONE, x)
                        [by theorem oneAtMostPositive]
                      @one-at-most-product-if |
                        less_than(ZERO, productUpTo(primes, count)) -> at_most(ONE, productUpTo(primes, count))
                        [by forall_elim(productUpTo(primes, count)) one-at-most-positive]
                      @one-at-most-product |
                        at_most(ONE, productUpTo(primes, count))
                        [by modus_ponens one-at-most-product-if product-positive]
                      @product-less_than-successor |
                        forall n: Int; less_than(n, succ(n))
                        [by theorem lessThanSelf]
                      @product-less_than-succ-product |
                        less_than(productUpTo(primes, count), succ(productUpTo(primes, count)))
                        [by forall_elim(productUpTo(primes, count)) product-less_than-successor]
                      @succ-product-is-product-plus-one |
                        succ(productUpTo(primes, count)) = add(productUpTo(primes, count), ONE)
                        [by simplify oneIsSuccZero addSuccRight addZeroRight]
                      @product-less_than-big-number |
                        less_than(productUpTo(primes, count), add(productUpTo(primes, count), ONE))
                        [by rewrite succ-product-is-product-plus-one product-less_than-succ-product]
                      @at-most-then-less_than |
                        forall x, y, z: Int; at_most(x, y) -> less_than(y, z) -> less_than(x, z)
                        [by theorem atMostThenLessThan]
                      @one-less_than-big-number-if |
                        at_most(ONE, productUpTo(primes, count)) -> less_than(productUpTo(primes, count), add(productUpTo(primes, count), ONE)) -> less_than(ONE, add(productUpTo(primes, count), ONE))
                        [by forall_elim(ONE, productUpTo(primes, count), add(productUpTo(primes, count), ONE)) at-most-then-less_than]
                      @one-less_than-big-number |
                        less_than(ONE, add(productUpTo(primes, count), ONE))
                        [by tautology one-less_than-big-number-if one-at-most-product product-less_than-big-number]
                      // P has a prime factor p.
                      @prime-factor-exists |
                        forall n: Int; less_than(ONE, n) -> exists p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, n)
                        [by theorem primeFactorExists]
                      @big-number-has-prime-factor-if |
                        less_than(ONE, add(productUpTo(primes, count), ONE)) -> exists p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, add(productUpTo(primes, count), ONE))
                        [by forall_elim(add(productUpTo(primes, count), ONE)) prime-factor-exists]
                      @big-number-has-prime-factor |
                        exists p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, add(productUpTo(primes, count), ONE))
                        [by modus_ponens big-number-has-prime-factor-if one-less_than-big-number]
                      @with-prime-factor-p |
                        unpack p: Int from big-number-has-prime-factor {
                          @p-prime-and-divides-big-number |
                            (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, add(productUpTo(primes, count), ONE))
                            [by hypothesis with-prime-factor-p]
                          @p-is-prime |
                            less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
                            [by and_elim_left p-prime-and-divides-big-number]
                          @p-divides-big-number |
                            does_divide(p, add(productUpTo(primes, count), ONE))
                            [by and_elim_right p-prime-and-divides-big-number]
                          @p-above-one |
                            less_than(ONE, p)
                            [by and_elim_left p-is-prime]
                          // p is one of the listed primes, so p | product.
                          @complete-at-p |
                            (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> inSeqBefore(p, primes, count)
                            [by forall_elim(p) list-is-complete]
                          @p-in-list |
                            inSeqBefore(p, primes, count)
                            [by modus_ponens complete-at-p p-is-prime]
                          @p-divides-product |
                            does_divide(p, productUpTo(primes, count))
                            [by specialize memberDividesProduct(primes, count, p) p-in-list]
                          // p | P and p | product give p | (P − product) = p | 1.
                          @divides-subtracts |
                            forall d, a, b: Int; does_divide(d, a) -> does_divide(d, b) -> does_divide(d, sub(a, b))
                            [by theorem dividesSubtracts]
                          @p-divides-difference-if |
                            does_divide(p, add(productUpTo(primes, count), ONE)) -> does_divide(p, productUpTo(primes, count)) -> does_divide(p, sub(add(productUpTo(primes, count), ONE), productUpTo(primes, count)))
                            [by forall_elim(p, add(productUpTo(primes, count), ONE), productUpTo(primes, count)) divides-subtracts]
                          @p-divides-difference |
                            does_divide(p, sub(add(productUpTo(primes, count), ONE), productUpTo(primes, count)))
                            [by tautology p-divides-difference-if p-divides-big-number p-divides-product]
                          // P − product = (product + 1) − product = 1: the linear
                          // identity Judson elides. `simplify` closes it from oriented
                          // (terminating) ring rules — unfold `sub`, the one needed
                          // commutation as a ground fact, reassociate, cancel `P + (−P)`.
                          @difference-unfolds |
                            forall x, y: Int; sub(x, y) = add(x, neg(y))
                            [by axiom subDef]
                          @addition-commutes |
                            forall x, y: Int; add(x, y) = add(y, x)
                            [by theorem addIsCommutative]
                          @product-plus-one-commuted |
                            add(productUpTo(primes, count), ONE) = add(ONE, productUpTo(primes, count))
                            [by forall_elim(productUpTo(primes, count), ONE) addition-commutes]
                          @addition-associates |
                            forall x, y, z: Int; add(add(x, y), z) = add(x, add(y, z))
                            [by theorem addIsAssociative]
                          @cancels-with-negation |
                            forall x: Int; add(x, neg(x)) = ZERO
                            [by theorem addNegRight]
                          @adds-zero-on-the-right |
                            forall x: Int; add(x, ZERO) = x
                            [by theorem addZeroRight]
                          @difference-is-one |
                            sub(add(productUpTo(primes, count), ONE), productUpTo(primes, count)) = ONE
                            [by simplify difference-unfolds product-plus-one-commuted addition-associates cancels-with-negation adds-zero-on-the-right]
                          @p-divides-one |
                            does_divide(p, ONE)
                            [by rewrite difference-is-one p-divides-difference]
                          // p | 1 with p > 1 is impossible: p ≤ 1 < p is less_than itself.
                          @zero-less_than-one |
                            less_than(ZERO, ONE)
                            [by theorem zeroLessThanOne]
                          @less-than-transitive |
                            forall x, y, z: Int; less_than(x, y) -> less_than(y, z) -> less_than(x, z)
                            [by theorem lessThanTransitive]
                          @zero-less_than-p-if-chained |
                            less_than(ZERO, ONE) -> less_than(ONE, p) -> less_than(ZERO, p)
                            [by specialize lessThanTransitive(ZERO, ONE, p)]
                          @p-positive |
                            less_than(ZERO, p)
                            [by tautology zero-less_than-p-if-chained zero-less_than-one p-above-one]
                          @divisor-at-most |
                            forall x, y: Int; less_than(ZERO, x) -> less_than(ZERO, y) -> does_divide(x, y) -> at_most(x, y)
                            [by theorem divisorAtMost]
                          @p-at-most-one |
                            at_most(p, ONE)
                            [by specialize divisorAtMost(p, ONE) p-positive zero-less_than-one p-divides-one]
                          @p-less_than-itself |
                            less_than(p, p)
                            [by specialize atMostThenLessThan(p, ONE, p) p-at-most-one p-above-one]
                          @nothing-less_than-itself |
                            forall x: Int; not less_than(x, x)
                            [by theorem lessThanIrreflexive]
                          @p-not-less_than-itself |
                            not less_than(p, p)
                            [by specialize lessThanIrreflexive(p)]
                          @contradiction |
                            not (forall q: Int; (less_than(ONE, q) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBefore(q, primes, count))
                            [by absurd p-less_than-itself p-not-less_than-itself]
                        }
                      @contradiction |
                        not (forall q: Int; (less_than(ONE, q) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBefore(q, primes, count))
                        [by exists_elim with-prime-factor-p]
                    }
                  @list-complete-is-absurd |
                    (forall q: Int; (less_than(ONE, q) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBefore(q, primes, count)) -> (not (forall q: Int; (less_than(ONE, q) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBefore(q, primes, count)))
                    [by implies_intro given-list-is-complete]
                  @no-complete-finite-list |
                    not (forall q: Int; (less_than(ONE, q) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBefore(q, primes, count))
                    [by tautology list-complete-is-absurd]
                }
              @product-positive-implies-no-complete-list |
                less_than(ZERO, productUpTo(primes, count)) -> (not (forall q: Int; (less_than(ONE, q) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBefore(q, primes, count)))
                [by implies_intro given-product-positive]
            }
          @count-nonneg-implies-no-complete-list |
            is_nonneg(count) -> less_than(ZERO, productUpTo(primes, count)) -> (not (forall q: Int; (less_than(ONE, q) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBefore(q, primes, count)))
            [by implies_intro given-count-nonneg]
        }
      @discharge-count |
        forall count: Int; is_nonneg(count) -> less_than(ZERO, productUpTo(primes, count)) -> (not (forall q: Int; (less_than(ONE, q) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBefore(q, primes, count)))
        [by forall_intro generalize-count]
    }
  @conclusion |
    forall primes: Seq; forall count: Int; is_nonneg(count) -> less_than(ZERO, productUpTo(primes, count)) -> (not (forall q: Int; (less_than(ONE, q) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBefore(q, primes, count)))
    [by forall_intro generalize-primes]
qed
```

The proof reifies Judson's contradiction exactly: `primeFactorExists` supplies the
prime factor `p` of `P`; the completeness hypothesis places `p` in the list;
`memberDividesProduct` makes `p` divide the product; `dividesSubtracts` collapses
`p | P` and `p | product` to `p | 1`; and a positive divisor of `1` is `≤ 1`,
which `p > 1` refutes. The `count` is left unconstrained beyond nonnegativity —
the argument never inspects it — so the theorem denies a complete finite prime
list of *any* length.

## The Euclidean Algorithm

Judson closes §2.2 with the **Euclidean Algorithm**: to compute `gcd(a, b)`
concretely, repeatedly apply the Division Algorithm — `a = bq₁ + r₁`,
`b = r₁q₂ + r₂`, `r₁ = r₂q₃ + r₃`, … — until a zero remainder is reached; the
last nonzero remainder is `gcd(a, b)`, and back-substituting up the chain recovers
the Bézout coefficients `r`, `s` of the previous theorem. This is a *computation*
on particular integers rather than a universally-quantified statement, so we do
not transcribe a numeric instance here; `bezout` already establishes, for every
nonzero `a`, `b`, the existence and defining properties of the gcd that the
algorithm computes.

```bpa
// The Euclidean Algorithm (illustrative, not transcribed):
//   gcd(a, b): while b != 0 { (a, b) := (b, a mod b) }; return a
// It terminates because the nonnegative remainders strictly decrease
// (Well-Ordering), and back-substitution through the division steps yields the
// Bézout coefficients r, s with gcd(a, b) = a*r + b*s established by `bezout`.
```

## The Fundamental Theorem of Arithmetic

> **Theorem 2.15 (Fundamental Theorem of Arithmetic).** Let $n$ be an integer such
> that $n > 1$. Then $n = p_1 p_2 \cdots p_k$, where $p_1, \ldots, p_k$ are primes
> (not necessarily distinct). Furthermore, this factorization is unique; that is,
> if $n = q_1 q_2 \cdots q_l$ is a second factorization into primes, then $k = l$
> and the $q_i$ are, after reordering, the $p_i$.

The book's §2.2 capstone. We prove it in the form the ℤ sequence library makes
expressible: a **prime factorization of `n`** is a finite sequence `s` of length
`k` whose every entry is prime and whose finite product `productUpTo(s, k)` is
`n`. As throughout, "`x` is prime" is spelled inline as
`less_than(ONE, x) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, x) -> (d = ONE or d = x))`.

### Existence

Judson: *every integer `n > 1` is a product of primes.* The argument is strong
induction on `n`. A prime factor `p` splits off (`primeFactorExists` gives a
prime `p` dividing `n`, hence a cofactor `c` with `n = p·c`); the cofactor is
positive, so either `c = 1` — and then `n = p` is itself prime, factored by the
singleton `[p]` — or `c > 1`, and then `c < n`, so the induction hypothesis
factors `c` as a sequence `t`, which we prepend `p` to. We first isolate three
order/arithmetic facts the induction leans on.

A nonnegative index strictly less_than `ONE` must be `ZERO`: `ONE = succ ZERO`, so
`m < ONE` splits (`lessThanSuccSplit`) into `m < ZERO` — impossible for a
nonnegative `m` — or `m = ZERO`.

```bpa
theorem nonnegLessThanOneIsZero: forall m: Int; is_nonneg(m) -> less_than(m, ONE) -> m = ZERO
proof
  @generalize-m |
    fix m: Int {
      @given-m-nonneg |
        assume is_nonneg(m) {
          @given-m-less_than-one |
            assume less_than(m, ONE) {
              @m-nonneg |
                is_nonneg(m)
                [by hypothesis given-m-nonneg]
              @m-less_than-one |
                less_than(m, ONE)
                [by hypothesis given-m-less_than-one]
              @one-is-succ-zero |
                ONE = succ(ZERO)
                [by axiom oneIsSuccZero]
              @m-less_than-succ-zero |
                less_than(m, succ(ZERO))
                [by rewrite one-is-succ-zero m-less_than-one]
              @zero-is-nonneg |
                is_nonneg(ZERO)
                [by axiom nonnegZero]
              @m-less_than-zero-or-equal-zero |
                less_than(m, ZERO) or m = ZERO
                [by specialize lessThanSuccSplit(m, ZERO) m-nonneg zero-is-nonneg m-less_than-succ-zero]
              @m-is-zero |
                m = ZERO
                case m-less_than-zero-or-equal-zero {
                  @when-m-less_than-zero |
                    assume less_than(m, ZERO) {
                      @m-less_than-zero |
                        less_than(m, ZERO)
                        [by hypothesis when-m-less_than-zero]
                      @m-not-less_than-zero |
                        not less_than(m, ZERO)
                        [by specialize nonnegNotLessThanZero(m) m-nonneg]
                      @conclusion-m-is-zero |
                        m = ZERO
                        [by absurd m-less_than-zero m-not-less_than-zero]
                    }
                  @when-m-equals-zero |
                    assume m = ZERO {
                      @conclusion-m-is-zero |
                        m = ZERO
                        [by hypothesis when-m-equals-zero]
                    }
                }
            }
          @less_than-one-implies-zero |
            less_than(m, ONE) -> m = ZERO
            [by implies_intro given-m-less_than-one]
        }
      @nonneg-implies-less_than-one-implies-zero |
        is_nonneg(m) -> less_than(m, ONE) -> m = ZERO
        [by implies_intro given-m-nonneg]
    }
  @conclusion |
    forall m: Int; is_nonneg(m) -> less_than(m, ONE) -> m = ZERO
    [by forall_intro generalize-m]
qed
```

`p > 1` and `c > 0` force `c < p·c`: write `p = 1 + q` with `q = p − 1 > 0`, so
`p·c = c·p = c·1 + c·q = c + c·q` with `c·q > 0`, whence `c < c + c·q = p·c`. This
is what makes the cofactor `c` strictly smaller than `n = p·c`, licensing the
induction hypothesis.

```bpa
theorem lessThanMulGreaterThanOne: forall p, c: Int;
  less_than(ONE, p) -> less_than(ZERO, c) -> less_than(c, mul(p, c))
proof
  @generalize-p |
    fix p: Int {
      @generalize-c |
        fix c: Int {
          @given-p-above-one |
            assume less_than(ONE, p) {
              @given-c-positive |
                assume less_than(ZERO, c) {
                  @p-above-one |
                    less_than(ONE, p)
                    [by hypothesis given-p-above-one]
                  @c-positive |
                    less_than(ZERO, c)
                    [by hypothesis given-c-positive]
                  // q := p − 1 is positive, and 1 + q = p.
                  @one-plus-gap-is-p |
                    add(ONE, sub(p, ONE)) = p
                    [by specialize addDiffReaches(ONE, p)]
                  @add-preserves-order |
                    forall a, b, x: Int; less_than(a, b) -> less_than(add(x, a), add(x, b))
                    [by theorem additionPreservesOrder]
                  @add-zero-right |
                    forall a: Int; add(a, ZERO) = a
                    [by theorem addZeroRight]
                  @one-plus-zero-is-one |
                    add(ONE, ZERO) = ONE
                    [by specialize addZeroRight(ONE)]
                  @one-is-one-plus-zero |
                    ONE = add(ONE, ZERO)
                    [by symmetry one-plus-zero-is-one]
                  @p-is-one-plus-gap |
                    p = add(ONE, sub(p, ONE))
                    [by symmetry one-plus-gap-is-p]
                  // recast 1 < p as (1+0) < (1+q), then cancel the leading 1.
                  @one-recast-less_than-p |
                    less_than(add(ONE, ZERO), p)
                    [by rewrite one-is-one-plus-zero p-above-one]
                  @one-plus-zero-less_than-one-plus-gap |
                    less_than(add(ONE, ZERO), add(ONE, sub(p, ONE)))
                    [by rewrite p-is-one-plus-gap one-recast-less_than-p]
                  @cancel-leading-one |
                    forall x, a, b: Int; less_than(add(x, a), add(x, b)) -> less_than(a, b)
                    [by theorem lessThanAddCancelLeft]
                  @cancel-one-for-gap |
                    less_than(add(ONE, ZERO), add(ONE, sub(p, ONE))) -> less_than(ZERO, sub(p, ONE))
                    [by forall_elim(ONE, ZERO, sub(p, ONE)) cancel-leading-one]
                  @gap-positive |
                    less_than(ZERO, sub(p, ONE))
                    [by modus_ponens cancel-one-for-gap one-plus-zero-less_than-one-plus-gap]
                  // c·q > 0 and c·1 + c·q = c·p.
                  @product-positive |
                    forall a, b: Int; less_than(ZERO, a) -> less_than(ZERO, b) -> less_than(ZERO, mul(a, b))
                    [by theorem productWithPositiveIsPositive]
                  @c-gap-positive-if-both |
                    less_than(ZERO, c) -> less_than(ZERO, sub(p, ONE)) -> less_than(ZERO, mul(c, sub(p, ONE)))
                    [by forall_elim(c, sub(p, ONE)) product-positive]
                  @c-times-gap-positive |
                    less_than(ZERO, mul(c, sub(p, ONE)))
                    [by tautology c-gap-positive-if-both c-positive gap-positive]
                  @distribute |
                    forall a, b, x: Int; mul(a, add(b, x)) = add(mul(a, b), mul(a, x))
                    [by theorem mulAddDistribLeft]
                  @c-times-p-distributes |
                    mul(c, add(ONE, sub(p, ONE))) = add(mul(c, ONE), mul(c, sub(p, ONE)))
                    [by forall_elim(c, ONE, sub(p, ONE)) distribute]
                  @mul-one-right |
                    forall a: Int; mul(a, ONE) = a
                    [by theorem mulOneRight]
                  @c-times-one-is-c |
                    mul(c, ONE) = c
                    [by specialize mulOneRight(c)]
                  @c-times-p-is-c-plus-c-gap |
                    mul(c, p) = add(c, mul(c, sub(p, ONE)))
                    [by chain one-plus-gap-is-p c-times-p-distributes c-times-one-is-c]
                  // c < c + c·q via additionPreservesOrder, then rewrite to c·p, p·c.
                  @c-less_than-c-plus-c-gap-raw |
                    less_than(ZERO, mul(c, sub(p, ONE))) -> less_than(add(c, ZERO), add(c, mul(c, sub(p, ONE))))
                    [by forall_elim(ZERO, mul(c, sub(p, ONE)), c) add-preserves-order]
                  @c-plus-zero-less_than-c-plus-gap |
                    less_than(add(c, ZERO), add(c, mul(c, sub(p, ONE))))
                    [by modus_ponens c-less_than-c-plus-c-gap-raw c-times-gap-positive]
                  @c-plus-zero-is-c |
                    add(c, ZERO) = c
                    [by specialize addZeroRight(c)]
                  @c-less_than-c-plus-c-gap |
                    less_than(c, add(c, mul(c, sub(p, ONE))))
                    [by rewrite c-plus-zero-is-c c-plus-zero-less_than-c-plus-gap]
                  @c-plus-c-gap-is-c-times-p |
                    add(c, mul(c, sub(p, ONE))) = mul(c, p)
                    [by symmetry c-times-p-is-c-plus-c-gap]
                  @c-less_than-c-times-p |
                    less_than(c, mul(c, p))
                    [by rewrite c-plus-c-gap-is-c-times-p c-less_than-c-plus-c-gap]
                  @c-times-p-is-p-times-c |
                    mul(c, p) = mul(p, c)
                    [by specialize mulIsCommutative(c, p)]
                  @c-less_than-p-times-c |
                    less_than(c, mul(p, c))
                    [by rewrite c-times-p-is-p-times-c c-less_than-c-times-p]
                }
              @c-positive-implies-less_than |
                less_than(ZERO, c) -> less_than(c, mul(p, c))
                [by implies_intro given-c-positive]
            }
          @p-above-one-implies-less_than |
            less_than(ONE, p) -> less_than(ZERO, c) -> less_than(c, mul(p, c))
            [by implies_intro given-p-above-one]
        }
      @discharge-c |
        forall c: Int; less_than(ONE, p) -> less_than(ZERO, c) -> less_than(c, mul(p, c))
        [by forall_intro generalize-c]
    }
  @conclusion |
    forall p, c: Int; less_than(ONE, p) -> less_than(ZERO, c) -> less_than(c, mul(p, c))
    [by forall_intro generalize-p]
qed
```

The cofactor of a positive number by a positive factor is itself positive: from
`n = p·c` with `n, p > 0`, the sign of `c` (by `signTrichotomy`) cannot be zero
(that would make `n = 0`) nor negative (that would make `n = −(p·(−c)) ≤ 0`),
both against `n > 0`.

```bpa
theorem positiveCofactor: forall n, p, c: Int;
  less_than(ZERO, n) -> less_than(ZERO, p) -> n = mul(p, c) -> less_than(ZERO, c)
proof
  @generalize-n |
    fix n: Int {
      @generalize-p |
        fix p: Int {
          @generalize-c |
            fix c: Int {
              @given-n-positive |
                assume less_than(ZERO, n) {
                  @given-p-positive |
                    assume less_than(ZERO, p) {
                      @given-n-is-p-times-c |
                        assume n = mul(p, c) {
                          @n-positive |
                            less_than(ZERO, n)
                            [by hypothesis given-n-positive]
                          @p-positive |
                            less_than(ZERO, p)
                            [by hypothesis given-p-positive]
                          @n-is-p-times-c |
                            n = mul(p, c)
                            [by hypothesis given-n-is-p-times-c]
                          @p-nonneg |
                            is_nonneg(p)
                            [by specialize positiveIsNonneg(p) p-positive]
                          @c-sign |
                            less_than(ZERO, c) or c = ZERO or less_than(c, ZERO)
                            [by specialize signTrichotomy(c)]
                          @c-positive |
                            less_than(ZERO, c)
                            case c-sign {
                              @when-c-positive |
                                assume less_than(ZERO, c) {
                                  @conclusion-c-positive |
                                    less_than(ZERO, c)
                                    [by hypothesis when-c-positive]
                                }
                              @when-c-zero |
                                assume c = ZERO {
                                  @c-is-zero |
                                    c = ZERO
                                    [by hypothesis when-c-zero]
                                  @p-times-zero-is-zero |
                                    mul(p, ZERO) = ZERO
                                    [by specialize mulZeroRight(p)]
                                  @n-is-zero |
                                    n = ZERO
                                    [by chain n-is-p-times-c c-is-zero p-times-zero-is-zero]
                                  @zero-less_than-zero |
                                    less_than(ZERO, ZERO)
                                    [by rewrite n-is-zero n-positive]
                                  @nothing-less_than-itself |
                                    forall x: Int; not less_than(x, x)
                                    [by theorem lessThanIrreflexive]
                                  @zero-not-less_than-zero |
                                    not less_than(ZERO, ZERO)
                                    [by specialize lessThanIrreflexive(ZERO)]
                                  @conclusion-c-positive |
                                    less_than(ZERO, c)
                                    [by absurd zero-less_than-zero zero-not-less_than-zero]
                                }
                              @when-c-negative |
                                assume less_than(c, ZERO) {
                                  @c-negative |
                                    less_than(c, ZERO)
                                    [by hypothesis when-c-negative]
                                  @neg-c-nonneg |
                                    is_nonneg(neg(c))
                                    [by specialize negLessThanZero(c) c-negative]
                                  @product-of-nonnegs-nonneg |
                                    forall a, b: Int; is_nonneg(a) -> is_nonneg(b) -> is_nonneg(mul(a, b))
                                    [by theorem nonnegMul]
                                  @p-times-neg-c-nonneg-if |
                                    is_nonneg(p) -> is_nonneg(neg(c)) -> is_nonneg(mul(p, neg(c)))
                                    [by forall_elim(p, neg(c)) product-of-nonnegs-nonneg]
                                  @p-times-neg-c-nonneg |
                                    is_nonneg(mul(p, neg(c)))
                                    [by tautology p-times-neg-c-nonneg-if p-nonneg neg-c-nonneg]
                                  @p-times-neg-c-is-neg-product |
                                    mul(p, neg(c)) = neg(mul(p, c))
                                    [by specialize mulNegRight(p, c)]
                                  // p·(−c) = −(p·c) = −n.
                                  @p-times-neg-c-is-neg-n |
                                    mul(p, neg(c)) = neg(n)
                                    [by chain p-times-neg-c-is-neg-product n-is-p-times-c]
                                  @neg-n-nonneg |
                                    is_nonneg(neg(n))
                                    [by rewrite p-times-neg-c-is-neg-n p-times-neg-c-nonneg]
                                  @add-n-neg-n-is-zero |
                                    add(n, neg(n)) = ZERO
                                    [by specialize addNegRight(n)]
                                  @at-most-intro |
                                    forall a, g, b: Int; is_nonneg(g) -> add(a, g) = b -> at_most(a, b)
                                    [by theorem lessOrEqualIntro]
                                  @n-at-most-zero-if |
                                    is_nonneg(neg(n)) -> add(n, neg(n)) = ZERO -> at_most(n, ZERO)
                                    [by forall_elim(n, neg(n), ZERO) at-most-intro]
                                  @n-at-most-zero |
                                    at_most(n, ZERO)
                                    [by tautology n-at-most-zero-if neg-n-nonneg add-n-neg-n-is-zero]
                                  @zero-at-most-n |
                                    at_most(ZERO, n)
                                    [by specialize lessThanToLessOrEqual(ZERO, n) n-positive]
                                  @n-is-zero |
                                    n = ZERO
                                    [by specialize lessOrEqualAntisymmetric(n, ZERO) n-at-most-zero zero-at-most-n]
                                  @zero-less_than-zero |
                                    less_than(ZERO, ZERO)
                                    [by rewrite n-is-zero n-positive]
                                  @nothing-less_than-itself |
                                    forall x: Int; not less_than(x, x)
                                    [by theorem lessThanIrreflexive]
                                  @zero-not-less_than-zero |
                                    not less_than(ZERO, ZERO)
                                    [by specialize lessThanIrreflexive(ZERO)]
                                  @conclusion-c-positive |
                                    less_than(ZERO, c)
                                    [by absurd zero-less_than-zero zero-not-less_than-zero]
                                }
                            }
                        }
                      @is-p-times-c-implies-c-positive |
                        n = mul(p, c) -> less_than(ZERO, c)
                        [by implies_intro given-n-is-p-times-c]
                    }
                  @p-positive-implies |
                    less_than(ZERO, p) -> n = mul(p, c) -> less_than(ZERO, c)
                    [by implies_intro given-p-positive]
                }
              @n-positive-implies |
                less_than(ZERO, n) -> less_than(ZERO, p) -> n = mul(p, c) -> less_than(ZERO, c)
                [by implies_intro given-n-positive]
            }
          @discharge-c |
            forall c: Int; less_than(ZERO, n) -> less_than(ZERO, p) -> n = mul(p, c) -> less_than(ZERO, c)
            [by forall_intro generalize-c]
        }
      @discharge-p |
        forall p, c: Int; less_than(ZERO, n) -> less_than(ZERO, p) -> n = mul(p, c) -> less_than(ZERO, c)
        [by forall_intro generalize-p]
    }
  @conclusion |
    forall n, p, c: Int; less_than(ZERO, n) -> less_than(ZERO, p) -> n = mul(p, c) -> less_than(ZERO, c)
    [by forall_intro generalize-n]
qed
```

Now the existence half itself. Strong induction on `n` (`nonnegStrongInduction`):
take a prime factor `p | n` and the cofactor `c` with `n = p·c`, positive by
`positiveCofactor`. Split on `c`: when `c = 1`, `n = p` and the singleton `[p]`
(from `seqSingletonExists`) is the factorization; when `c > 1`, `c < n` so the
induction hypothesis gives a prime factorization `t` of length `j` for `c`, and
`seqConcatExists` prepends `[p]` — the resulting sequence has product
`p · productUpTo(t, j) = p·c = n`, and each of its entries is prime (index `0` is
`p`, the shifted indices are `t`'s prime entries). The `c < 1` branch is vacuous
(`c > 0` contradicts `c < 1`).

```bpa
theorem ftaExistence: forall n: Int; less_than(ONE, n) ->
  exists s: Seq; exists k: Int;
  is_nonneg(k)
  and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
  (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
  and n = productUpTo(s, k)
proof
  @zero-less_than-one |
    less_than(ZERO, ONE)
    [by theorem zeroLessThanOne]
  @positive-is-nonneg |
    forall x: Int; less_than(ZERO, x) -> is_nonneg(x)
    [by theorem positiveIsNonneg]
  @one-nonneg |
    is_nonneg(ONE)
    [by specialize positiveIsNonneg(ONE) zero-less_than-one]
  @less-than-transitive |
    forall a, b, c: Int; less_than(a, b) -> less_than(b, c) -> less_than(a, c)
    [by theorem lessThanTransitive]

  @strong-step |
    fix n: Int {
      @given-n-nonneg |
        assume is_nonneg(n) {
          @given-inductive-hypothesis |
            assume forall m: Int; is_nonneg(m) -> less_than(m, n) ->
              (less_than(ONE, m) -> exists s: Seq; exists k: Int;
              is_nonneg(k)
              and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
              (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
              and m = productUpTo(s, k)) {
              @inductive-hypothesis |
                forall m: Int; is_nonneg(m) -> less_than(m, n) ->
                  (less_than(ONE, m) -> exists s: Seq; exists k: Int;
                  is_nonneg(k)
                  and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                  (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                  and m = productUpTo(s, k))
                [by hypothesis given-inductive-hypothesis]
              @given-n-above-one |
                assume less_than(ONE, n) {
                  @n-above-one |
                    less_than(ONE, n)
                    [by hypothesis given-n-above-one]
                  @one-less_than-n-transitive |
                    less_than(ZERO, ONE) -> less_than(ONE, n) -> less_than(ZERO, n)
                    [by specialize lessThanTransitive(ZERO, ONE, n)]
                  @n-positive |
                    less_than(ZERO, n)
                    [by tautology one-less_than-n-transitive zero-less_than-one n-above-one]

                  // ---- get a prime factor p and cofactor c with n = p·c -----
                  @n-has-a-prime-factor |
                    exists p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, n)
                    [by specialize primeFactorExists(n) n-above-one]
                  @with-prime-factor-p |
                    unpack p: Int from n-has-a-prime-factor {
                      @prime-and-divides |
                        (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, n)
                        [by hypothesis with-prime-factor-p]
                      @p-is-prime |
                        less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
                        [by and_elim_left prime-and-divides]
                      @p-above-one |
                        less_than(ONE, p)
                        [by and_elim_left p-is-prime]
                      @p-divides-n |
                        does_divide(p, n)
                        [by and_elim_right prime-and-divides]
                      @p-positive |
                        less_than(ZERO, p)
                        [by specialize lessThanTransitive(ZERO, ONE, p) zero-less_than-one p-above-one]
                      @divides-elim |
                        forall d, x: Int; does_divide(d, x) -> exists k: Int; x = mul(d, k)
                        [by theorem dividesElim]
                      @n-is-p-multiple |
                        exists k: Int; n = mul(p, k)
                        [by specialize dividesElim(p, n) p-divides-n]
                      @with-cofactor-c |
                        unpack c: Int from n-is-p-multiple {
                          @n-is-p-times-c |
                            n = mul(p, c)
                            [by hypothesis with-cofactor-c]
                          @c-positive |
                            less_than(ZERO, c)
                            [by specialize positiveCofactor(n, p, c) n-positive p-positive n-is-p-times-c]

                          // ---- split the cofactor: c = 1 or c > 1 ----------
                          @one-versus-c |
                            less_than(ONE, c) or ONE = c or less_than(c, ONE)
                            [by specialize trichotomy(ONE, c)]
                          @factorization |
                            exists s: Seq; exists k: Int;
                              is_nonneg(k)
                              and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                              (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                              and n = productUpTo(s, k)
                            case one-versus-c {
                              // c > 1: factor c by the IH, then prepend p via concat.
                              @when-c-above-one |
                                assume less_than(ONE, c) {
                                  @c-above-one |
                                    less_than(ONE, c)
                                    [by hypothesis when-c-above-one]
                                  @c-nonneg-if-positive |
                                    less_than(ZERO, c) -> is_nonneg(c)
                                    [by specialize positiveIsNonneg(c)]
                                  @c-nonneg |
                                    is_nonneg(c)
                                    [by modus_ponens c-nonneg-if-positive c-positive]
                                  // c < n = p·c, so the IH applies to c.
                                  @c-less_than-p-times-c |
                                    less_than(c, mul(p, c))
                                    [by specialize lessThanMulGreaterThanOne(p, c) p-above-one c-positive]
                                  @p-times-c-is-n |
                                    mul(p, c) = n
                                    [by symmetry n-is-p-times-c]
                                  @c-less_than-n |
                                    less_than(c, n)
                                    [by rewrite p-times-c-is-n c-less_than-p-times-c]
                                  // apply the IH: c has a prime factorization (t, j).
                                  @ih-at-c |
                                    is_nonneg(c) -> less_than(c, n) ->
                                      (less_than(ONE, c) -> exists s: Seq; exists k: Int;
                                      is_nonneg(k)
                                      and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                      (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                      and c = productUpTo(s, k))
                                    [by forall_elim(c) inductive-hypothesis]
                                  @c-has-factorization |
                                    exists s: Seq; exists k: Int;
                                      is_nonneg(k)
                                      and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                      (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                      and c = productUpTo(s, k)
                                    [by tautology ih-at-c c-nonneg c-less_than-n c-above-one]
                                  @with-factor-sequence |
                                    unpack t: Seq from c-has-factorization {
                                      @factorization-of-c-with-length |
                                        exists k: Int;
                                          is_nonneg(k)
                                          and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                          (less_than(ONE, at(t, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, i)) -> (d = ONE or d = at(t, i)))))
                                          and c = productUpTo(t, k)
                                        [by hypothesis with-factor-sequence]
                                      @with-factor-length |
                                        unpack j: Int from factorization-of-c-with-length {
                                          @factorization-of-c |
                                            is_nonneg(j)
                                              and (forall i: Int; is_nonneg(i) -> less_than(i, j) ->
                                              (less_than(ONE, at(t, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, i)) -> (d = ONE or d = at(t, i)))))
                                              and c = productUpTo(t, j)
                                            [by hypothesis with-factor-length]
                                          @length-and-primes-of-c |
                                            is_nonneg(j)
                                              and (forall i: Int; is_nonneg(i) -> less_than(i, j) ->
                                              (less_than(ONE, at(t, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, i)) -> (d = ONE or d = at(t, i)))))
                                            [by and_elim_left factorization-of-c]
                                          @j-nonneg |
                                            is_nonneg(j)
                                            [by and_elim_left length-and-primes-of-c]
                                          @t-entries-prime |
                                            forall i: Int; is_nonneg(i) -> less_than(i, j) ->
                                              (less_than(ONE, at(t, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, i)) -> (d = ONE or d = at(t, i))))
                                            [by and_elim_right length-and-primes-of-c]
                                          @c-is-t-product |
                                            c = productUpTo(t, j)
                                            [by and_elim_right factorization-of-c]
                                          // singleton [p] to prepend.
                                          @singleton-exists-schema |
                                            forall x: Int; exists s: Seq; at(s, ZERO) = x and productUpTo(s, ONE) = x
                                            [by axiom seqSingletonExists]
                                          @singleton-p-exists |
                                            exists s: Seq; at(s, ZERO) = p and productUpTo(s, ONE) = p
                                            [by specialize seqSingletonExists(p)]
                                          @with-singleton |
                                            unpack sp: Seq from singleton-p-exists {
                                              @singleton-entry-and-product |
                                                at(sp, ZERO) = p and productUpTo(sp, ONE) = p
                                                [by hypothesis with-singleton]
                                              @singleton-entry-is-p |
                                                at(sp, ZERO) = p
                                                [by and_elim_left singleton-entry-and-product]
                                              @singleton-product-is-p |
                                                productUpTo(sp, ONE) = p
                                                [by and_elim_right singleton-entry-and-product]
                                              // concatenate [p] (length 1) with t (length j).
                                              @concat-exists-schema |
                                                forall s: Seq; forall t2: Seq; forall a: Int; forall b: Int;
                                                  is_nonneg(a) -> is_nonneg(b) ->
                                                  exists u: Seq;
                                                  (forall i: Int; is_nonneg(i) -> less_than(i, a) -> at(u, i) = at(s, i))
                                                  and (forall i: Int; is_nonneg(i) -> less_than(i, b) -> at(u, add(a, i)) = at(t2, i))
                                                  and productUpTo(u, add(a, b)) = mul(productUpTo(s, a), productUpTo(t2, b))
                                                [by axiom seqConcatExists]
                                              @concat-sp-t-if |
                                                is_nonneg(ONE) -> is_nonneg(j) ->
                                                  exists u: Seq;
                                                  (forall i: Int; is_nonneg(i) -> less_than(i, ONE) -> at(u, i) = at(sp, i))
                                                  and (forall i: Int; is_nonneg(i) -> less_than(i, j) -> at(u, add(ONE, i)) = at(t, i))
                                                  and productUpTo(u, add(ONE, j)) = mul(productUpTo(sp, ONE), productUpTo(t, j))
                                                [by forall_elim(sp, t, ONE, j) concat-exists-schema]
                                              @concat-sp-t |
                                                exists u: Seq;
                                                  (forall i: Int; is_nonneg(i) -> less_than(i, ONE) -> at(u, i) = at(sp, i))
                                                  and (forall i: Int; is_nonneg(i) -> less_than(i, j) -> at(u, add(ONE, i)) = at(t, i))
                                                  and productUpTo(u, add(ONE, j)) = mul(productUpTo(sp, ONE), productUpTo(t, j))
                                                [by tautology concat-sp-t-if one-nonneg j-nonneg]
                                              @with-concat |
                                                unpack u: Seq from concat-sp-t {
                                                  @concat-clauses |
                                                    (forall i: Int; is_nonneg(i) -> less_than(i, ONE) -> at(u, i) = at(sp, i))
                                                      and (forall i: Int; is_nonneg(i) -> less_than(i, j) -> at(u, add(ONE, i)) = at(t, i))
                                                      and productUpTo(u, add(ONE, j)) = mul(productUpTo(sp, ONE), productUpTo(t, j))
                                                    [by hypothesis with-concat]
                                                  @concat-prefix-and-shift |
                                                    (forall i: Int; is_nonneg(i) -> less_than(i, ONE) -> at(u, i) = at(sp, i))
                                                      and (forall i: Int; is_nonneg(i) -> less_than(i, j) -> at(u, add(ONE, i)) = at(t, i))
                                                    [by and_elim_left concat-clauses]
                                                  @concat-prefix |
                                                    forall i: Int; is_nonneg(i) -> less_than(i, ONE) -> at(u, i) = at(sp, i)
                                                    [by and_elim_left concat-prefix-and-shift]
                                                  @concat-shift |
                                                    forall i: Int; is_nonneg(i) -> less_than(i, j) -> at(u, add(ONE, i)) = at(t, i)
                                                    [by and_elim_right concat-prefix-and-shift]
                                                  @concat-product |
                                                    productUpTo(u, add(ONE, j)) = mul(productUpTo(sp, ONE), productUpTo(t, j))
                                                    [by and_elim_right concat-clauses]
                                                  // length k = 1 + j is nonneg.
                                                  @length-nonneg |
                                                    is_nonneg(add(ONE, j))
                                                    [by specialize nonnegAdd(ONE, j) one-nonneg j-nonneg]
                                                  // product: productUpTo(u, 1+j) = p·(productUpTo t j) = p·c = n.
                                                  @n-is-concat-product |
                                                    n = productUpTo(u, add(ONE, j))
                                                    [by chain p-times-c-is-n c-is-t-product singleton-product-is-p concat-product]
                                                  // every entry of u less_than 1+j is prime.
                                                  @entries-of-concat-prime |
                                                    fix i: Int {
                                                      @given-i-nonneg |
                                                        assume is_nonneg(i) {
                                                          @given-i-less_than-length |
                                                            assume less_than(i, add(ONE, j)) {
                                                              @i-nonneg |
                                                                is_nonneg(i)
                                                                [by hypothesis given-i-nonneg]
                                                              @i-less_than-length |
                                                                less_than(i, add(ONE, j))
                                                                [by hypothesis given-i-less_than-length]
                                                              @less_than-to-at-most |
                                                                forall a, b: Int; less_than(a, b) -> at_most(a, b)
                                                                [by theorem lessThanToLessOrEqual]
                                                              @at-most-elim-at-one-i |
                                                                at_most(ONE, i) -> exists d: Int; is_nonneg(d) and add(ONE, d) = i
                                                                [by specialize lessOrEqualElim(ONE, i)]
                                                              // split i < 1 (the p slot) vs 1 ≤ i (a t slot).
                                                              @i-versus-one |
                                                                less_than(i, ONE) or i = ONE or less_than(ONE, i)
                                                                [by specialize trichotomy(i, ONE)]
                                                              @less_than-or-at-most |
                                                                less_than(i, ONE) or at_most(ONE, i)
                                                                case i-versus-one {
                                                                  @when-i-less_than-one |
                                                                    assume less_than(i, ONE) {
                                                                      @i-less_than-one-here |
                                                                        less_than(i, ONE)
                                                                        [by hypothesis when-i-less_than-one]
                                                                      @conclusion-less_than-or-at-most |
                                                                        less_than(i, ONE) or at_most(ONE, i)
                                                                        [by or_intro_left i-less_than-one-here]
                                                                    }
                                                                  @when-i-equals-one |
                                                                    assume i = ONE {
                                                                      @i-equals-one-here |
                                                                        i = ONE
                                                                        [by hypothesis when-i-equals-one]
                                                                      @one-equals-i |
                                                                        ONE = i
                                                                        [by symmetry i-equals-one-here]
                                                                      @one-at-most-one |
                                                                        at_most(ONE, ONE)
                                                                        [by specialize lessOrEqualRefl(ONE)]
                                                                      @one-at-most-i |
                                                                        at_most(ONE, i)
                                                                        [by rewrite one-equals-i one-at-most-one]
                                                                      @conclusion-less_than-or-at-most |
                                                                        less_than(i, ONE) or at_most(ONE, i)
                                                                        [by or_intro_right one-at-most-i]
                                                                    }
                                                                  @when-one-less_than-i |
                                                                    assume less_than(ONE, i) {
                                                                      @one-less_than-i-here |
                                                                        less_than(ONE, i)
                                                                        [by hypothesis when-one-less_than-i]
                                                                      @one-at-most-i |
                                                                        at_most(ONE, i)
                                                                        [by specialize lessThanToLessOrEqual(ONE, i) one-less_than-i-here]
                                                                      @conclusion-less_than-or-at-most |
                                                                        less_than(i, ONE) or at_most(ONE, i)
                                                                        [by or_intro_right one-at-most-i]
                                                                    }
                                                                }
                                                              @entry-is-prime |
                                                                less_than(ONE, at(u, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))
                                                                case less_than-or-at-most {
                                                                  // index 0: at(u,0) = at(sp,0) = p, prime.
                                                                  @when-i-in-prefix |
                                                                    assume less_than(i, ONE) {
                                                                      @i-less_than-one |
                                                                        less_than(i, ONE)
                                                                        [by hypothesis when-i-in-prefix]
                                                                      @nonneg-less_than-one-is-zero |
                                                                        forall x: Int; is_nonneg(x) -> less_than(x, ONE) -> x = ZERO
                                                                        [by theorem nonnegLessThanOneIsZero]
                                                                      @i-is-zero-if |
                                                                        is_nonneg(i) -> less_than(i, ONE) -> i = ZERO
                                                                        [by specialize nonnegLessThanOneIsZero(i)]
                                                                      @i-is-zero |
                                                                        i = ZERO
                                                                        [by tautology i-is-zero-if i-nonneg i-less_than-one]
                                                                      @concat-prefix-at-i |
                                                                        is_nonneg(i) -> less_than(i, ONE) -> at(u, i) = at(sp, i)
                                                                        [by forall_elim(i) concat-prefix]
                                                                      @u-at-i-is-sp-at-i |
                                                                        at(u, i) = at(sp, i)
                                                                        [by tautology concat-prefix-at-i i-nonneg i-less_than-one]
                                                                      // at(u, i) = at(sp, i) = at(sp, 0) = p.
                                                                      @u-at-i-is-p |
                                                                        at(u, i) = p
                                                                        [by chain u-at-i-is-sp-at-i i-is-zero singleton-entry-is-p]
                                                                      @p-is-u-at-i |
                                                                        p = at(u, i)
                                                                        [by symmetry u-at-i-is-p]
                                                                      @entry-above-one |
                                                                        less_than(ONE, at(u, i))
                                                                        [by rewrite p-is-u-at-i p-above-one]
                                                                      @p-divisor-clause |
                                                                        forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)
                                                                        [by and_elim_right p-is-prime]
                                                                      @entry-divisor-clause |
                                                                        forall d: Int; less_than(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i))
                                                                        [by rewrite p-is-u-at-i p-divisor-clause]
                                                                      @conclusion-entry-is-prime |
                                                                        less_than(ONE, at(u, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))
                                                                        [by and_intro entry-above-one entry-divisor-clause]
                                                                    }
                                                                  // index ≥ 1: at(u,i) = at(t, i-1), prime by t's factorization.
                                                                  @when-i-in-shift |
                                                                    assume at_most(ONE, i) {
                                                                      @one-at-most-i |
                                                                        at_most(ONE, i)
                                                                        [by hypothesis when-i-in-shift]
                                                                      @gap-exists |
                                                                        exists d: Int; is_nonneg(d) and add(ONE, d) = i
                                                                        [by modus_ponens at-most-elim-at-one-i one-at-most-i]
                                                                      @with-gap |
                                                                        unpack g: Int from gap-exists {
                                                                          @gap-nonneg-and-reaches |
                                                                            is_nonneg(g) and add(ONE, g) = i
                                                                            [by hypothesis with-gap]
                                                                          @gap-nonneg |
                                                                            is_nonneg(g)
                                                                            [by and_elim_left gap-nonneg-and-reaches]
                                                                          @one-plus-gap-is-i |
                                                                            add(ONE, g) = i
                                                                            [by and_elim_right gap-nonneg-and-reaches]
                                                                          @i-is-one-plus-gap |
                                                                            i = add(ONE, g)
                                                                            [by symmetry one-plus-gap-is-i]
                                                                          // g < j from add(1,g) = i < add(1,j).
                                                                          @i-less_than-length-recast |
                                                                            less_than(add(ONE, g), add(ONE, j))
                                                                            [by rewrite i-is-one-plus-gap i-less_than-length]
                                                                          @cancel-leading-one |
                                                                            forall x, a, b: Int; less_than(add(x, a), add(x, b)) -> less_than(a, b)
                                                                            [by theorem lessThanAddCancelLeft]
                                                                          @gap-less_than-j |
                                                                            less_than(g, j)
                                                                            [by specialize lessThanAddCancelLeft(ONE, g, j) i-less_than-length-recast]
                                                                          // at(u, i) = at(u, 1+g) = at(t, g).
                                                                          @concat-shift-at-g |
                                                                            is_nonneg(g) -> less_than(g, j) -> at(u, add(ONE, g)) = at(t, g)
                                                                            [by forall_elim(g) concat-shift]
                                                                          @u-at-one-plus-gap-is-t-at-gap |
                                                                            at(u, add(ONE, g)) = at(t, g)
                                                                            [by tautology concat-shift-at-g gap-nonneg gap-less_than-j]
                                                                          @u-at-i-is-t-at-gap |
                                                                            at(u, i) = at(t, g)
                                                                            [by rewrite one-plus-gap-is-i u-at-one-plus-gap-is-t-at-gap]
                                                                          @t-at-gap-is-u-at-i |
                                                                            at(t, g) = at(u, i)
                                                                            [by symmetry u-at-i-is-t-at-gap]
                                                                          // t's g-th entry is prime.
                                                                          @t-entry-prime-at-g |
                                                                            is_nonneg(g) -> less_than(g, j) -> (less_than(ONE, at(t, g)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, g)) -> (d = ONE or d = at(t, g))))
                                                                            [by forall_elim(g) t-entries-prime]
                                                                          @t-gap-entry-prime |
                                                                            less_than(ONE, at(t, g)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, g)) -> (d = ONE or d = at(t, g)))
                                                                            [by tautology t-entry-prime-at-g gap-nonneg gap-less_than-j]
                                                                          @t-gap-above-one |
                                                                            less_than(ONE, at(t, g))
                                                                            [by and_elim_left t-gap-entry-prime]
                                                                          @t-gap-divisor-clause |
                                                                            forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, g)) -> (d = ONE or d = at(t, g))
                                                                            [by and_elim_right t-gap-entry-prime]
                                                                          @entry-above-one |
                                                                            less_than(ONE, at(u, i))
                                                                            [by rewrite t-at-gap-is-u-at-i t-gap-above-one]
                                                                          @entry-divisor-clause |
                                                                            forall d: Int; less_than(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i))
                                                                            [by rewrite t-at-gap-is-u-at-i t-gap-divisor-clause]
                                                                          @conclusion-entry-is-prime |
                                                                            less_than(ONE, at(u, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))
                                                                            [by and_intro entry-above-one entry-divisor-clause]
                                                                        }
                                                                      @conclusion-entry-is-prime |
                                                                        less_than(ONE, at(u, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))
                                                                        [by exists_elim with-gap]
                                                                    }
                                                                }
                                                            }
                                                          @i-less_than-length-implies-prime |
                                                            less_than(i, add(ONE, j)) -> (less_than(ONE, at(u, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i))))
                                                            [by implies_intro given-i-less_than-length]
                                                        }
                                                      @i-nonneg-implies-prime |
                                                        is_nonneg(i) -> less_than(i, add(ONE, j)) -> (less_than(ONE, at(u, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i))))
                                                        [by implies_intro given-i-nonneg]
                                                    }
                                                  @every-concat-entry-prime |
                                                    forall i: Int; is_nonneg(i) -> less_than(i, add(ONE, j)) -> (less_than(ONE, at(u, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i))))
                                                    [by forall_intro entries-of-concat-prime]
                                                  // assemble the existential.
                                                  @length-and-primes |
                                                    is_nonneg(add(ONE, j))
                                                      and (forall i: Int; is_nonneg(i) -> less_than(i, add(ONE, j)) -> (less_than(ONE, at(u, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))))
                                                    [by and_intro length-nonneg every-concat-entry-prime]
                                                  @factorization-body |
                                                    is_nonneg(add(ONE, j))
                                                      and (forall i: Int; is_nonneg(i) -> less_than(i, add(ONE, j)) -> (less_than(ONE, at(u, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))))
                                                      and n = productUpTo(u, add(ONE, j))
                                                    [by and_intro length-and-primes n-is-concat-product]
                                                  @factorization-with-length |
                                                    exists k: Int;
                                                      is_nonneg(k)
                                                      and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                                      (less_than(ONE, at(u, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))))
                                                      and n = productUpTo(u, k)
                                                    [by exists_intro(add(ONE, j)) factorization-body]
                                                  @conclusion-factorization |
                                                    exists s: Seq; exists k: Int;
                                                      is_nonneg(k)
                                                      and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                                      (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                                      and n = productUpTo(s, k)
                                                    [by exists_intro(u) factorization-with-length]
                                                }
                                              @conclusion-concat-factorization |
                                                exists s: Seq; exists k: Int;
                                                  is_nonneg(k)
                                                  and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                                  (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                                  and n = productUpTo(s, k)
                                                [by exists_elim with-concat]
                                            }
                                          @conclusion-singleton-factorization |
                                            exists s: Seq; exists k: Int;
                                              is_nonneg(k)
                                              and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                              (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                              and n = productUpTo(s, k)
                                            [by exists_elim with-singleton]
                                        }
                                      @conclusion-factor-length |
                                        exists s: Seq; exists k: Int;
                                          is_nonneg(k)
                                          and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                          (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                          and n = productUpTo(s, k)
                                        [by exists_elim with-factor-length]
                                    }
                                  @conclusion-factorization |
                                    exists s: Seq; exists k: Int;
                                      is_nonneg(k)
                                      and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                      (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                      and n = productUpTo(s, k)
                                    [by exists_elim with-factor-sequence]
                                }
                              // c = 1: n = p·1 = p is itself prime; use [p].
                              @when-c-equals-one |
                                assume ONE = c {
                                  @one-is-c |
                                    ONE = c
                                    [by hypothesis when-c-equals-one]
                                  @mul-one-right |
                                    forall x: Int; mul(x, ONE) = x
                                    [by theorem mulOneRight]
                                  @p-times-one-is-p |
                                    mul(p, ONE) = p
                                    [by specialize mulOneRight(p)]
                                  // n = p·c = p·1 = p.
                                  @n-is-p |
                                    n = p
                                    [by chain n-is-p-times-c one-is-c p-times-one-is-p]
                                  // build the singleton [p].
                                  @singleton-exists-schema |
                                    forall x: Int; exists s: Seq; at(s, ZERO) = x and productUpTo(s, ONE) = x
                                    [by axiom seqSingletonExists]
                                  @singleton-p-exists |
                                    exists s: Seq; at(s, ZERO) = p and productUpTo(s, ONE) = p
                                    [by specialize seqSingletonExists(p)]
                                  @with-singleton |
                                    unpack sp: Seq from singleton-p-exists {
                                      @singleton-entry-and-product |
                                        at(sp, ZERO) = p and productUpTo(sp, ONE) = p
                                        [by hypothesis with-singleton]
                                      @singleton-entry-is-p |
                                        at(sp, ZERO) = p
                                        [by and_elim_left singleton-entry-and-product]
                                      @singleton-product-is-p |
                                        productUpTo(sp, ONE) = p
                                        [by and_elim_right singleton-entry-and-product]
                                      // the sole entry (index 0) is p, which is prime.
                                      @entries-less_than-one-are-prime |
                                        fix i: Int {
                                          @given-i-nonneg |
                                            assume is_nonneg(i) {
                                              @given-i-less_than-one |
                                                assume less_than(i, ONE) {
                                                  @i-nonneg |
                                                    is_nonneg(i)
                                                    [by hypothesis given-i-nonneg]
                                                  @i-less_than-one |
                                                    less_than(i, ONE)
                                                    [by hypothesis given-i-less_than-one]
                                                  @nonneg-less_than-one-is-zero |
                                                    forall x: Int; is_nonneg(x) -> less_than(x, ONE) -> x = ZERO
                                                    [by theorem nonnegLessThanOneIsZero]
                                                  @i-is-zero-if |
                                                    is_nonneg(i) -> less_than(i, ONE) -> i = ZERO
                                                    [by specialize nonnegLessThanOneIsZero(i)]
                                                  @i-is-zero |
                                                    i = ZERO
                                                    [by tautology i-is-zero-if i-nonneg i-less_than-one]
                                                  // at(sp, i) = at(sp, 0) = p.
                                                  @entry-at-i-is-p |
                                                    at(sp, i) = p
                                                    [by chain i-is-zero singleton-entry-is-p]
                                                  @p-is-entry-at-i |
                                                    p = at(sp, i)
                                                    [by symmetry entry-at-i-is-p]
                                                  @entry-above-one |
                                                    less_than(ONE, at(sp, i))
                                                    [by rewrite p-is-entry-at-i p-above-one]
                                                  @p-divisor-clause |
                                                    forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)
                                                    [by and_elim_right p-is-prime]
                                                  @entry-divisor-clause |
                                                    forall d: Int; less_than(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i))
                                                    [by rewrite p-is-entry-at-i p-divisor-clause]
                                                  @conclusion-entry-is-prime |
                                                    less_than(ONE, at(sp, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i)))
                                                    [by and_intro entry-above-one entry-divisor-clause]
                                                }
                                              @i-less_than-one-implies-prime |
                                                less_than(i, ONE) -> (less_than(ONE, at(sp, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i))))
                                                [by implies_intro given-i-less_than-one]
                                            }
                                          @i-nonneg-implies-prime |
                                            is_nonneg(i) -> less_than(i, ONE) -> (less_than(ONE, at(sp, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i))))
                                            [by implies_intro given-i-nonneg]
                                        }
                                      @every-entry-is-prime |
                                        forall i: Int; is_nonneg(i) -> less_than(i, ONE) -> (less_than(ONE, at(sp, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i))))
                                        [by forall_intro entries-less_than-one-are-prime]
                                      // n = p = productUpTo(sp, 1).
                                      @n-is-singleton-product |
                                        n = productUpTo(sp, ONE)
                                        [by chain n-is-p singleton-product-is-p]
                                      @length-and-primes |
                                        is_nonneg(ONE)
                                          and (forall i: Int; is_nonneg(i) -> less_than(i, ONE) -> (less_than(ONE, at(sp, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i)))))
                                        [by and_intro one-nonneg every-entry-is-prime]
                                      @factorization-body |
                                        is_nonneg(ONE)
                                          and (forall i: Int; is_nonneg(i) -> less_than(i, ONE) -> (less_than(ONE, at(sp, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i)))))
                                          and n = productUpTo(sp, ONE)
                                        [by and_intro length-and-primes n-is-singleton-product]
                                      @factorization-with-length |
                                        exists k: Int;
                                          is_nonneg(k)
                                          and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                          (less_than(ONE, at(sp, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i)))))
                                          and n = productUpTo(sp, k)
                                        [by exists_intro(ONE) factorization-body]
                                      @conclusion-factorization |
                                        exists s: Seq; exists k: Int;
                                          is_nonneg(k)
                                          and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                          (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                          and n = productUpTo(s, k)
                                        [by exists_intro(sp) factorization-with-length]
                                    }
                                  @conclusion-singleton-factorization |
                                    exists s: Seq; exists k: Int;
                                      is_nonneg(k)
                                      and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                      (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                      and n = productUpTo(s, k)
                                    [by exists_elim with-singleton]
                                }
                              @when-c-less_than-one |
                                assume less_than(c, ONE) {
                                  @c-nonneg-if-positive |
                                    less_than(ZERO, c) -> is_nonneg(c)
                                    [by specialize positiveIsNonneg(c)]
                                  @c-nonneg |
                                    is_nonneg(c)
                                    [by modus_ponens c-nonneg-if-positive c-positive]
                                  @c-less_than-one |
                                    less_than(c, ONE)
                                    [by hypothesis when-c-less_than-one]
                                  @nonneg-less_than-one-is-zero |
                                    forall x: Int; is_nonneg(x) -> less_than(x, ONE) -> x = ZERO
                                    [by theorem nonnegLessThanOneIsZero]
                                  @c-is-zero |
                                    c = ZERO
                                    [by specialize nonnegLessThanOneIsZero(c) c-nonneg c-less_than-one]
                                  @zero-less_than-zero |
                                    less_than(ZERO, ZERO)
                                    [by rewrite c-is-zero c-positive]
                                  @nothing-less_than-itself |
                                    forall x: Int; not less_than(x, x)
                                    [by theorem lessThanIrreflexive]
                                  @zero-not-less_than-zero |
                                    not less_than(ZERO, ZERO)
                                    [by specialize lessThanIrreflexive(ZERO)]
                                  @conclusion-factorization |
                                    exists s: Seq; exists k: Int;
                                      is_nonneg(k)
                                      and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                                      (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                      and n = productUpTo(s, k)
                                    [by absurd zero-less_than-zero zero-not-less_than-zero]
                                }
                            }
                        }
                      @conclusion-with-prime |
                        exists s: Seq; exists k: Int;
                          is_nonneg(k)
                          and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                          (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                          and n = productUpTo(s, k)
                        [by exists_elim with-cofactor-c]
                    }
                  @conclusion-factorization |
                    exists s: Seq; exists k: Int;
                      is_nonneg(k)
                      and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                      (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                      and n = productUpTo(s, k)
                    [by exists_elim with-prime-factor-p]
                }
              @above-one-implies-factorization |
                less_than(ONE, n) -> exists s: Seq; exists k: Int;
                  is_nonneg(k)
                  and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
                  (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                  and n = productUpTo(s, k)
                [by implies_intro given-n-above-one]
            }
          @ih-implies-factorization |
            (forall m: Int; is_nonneg(m) -> less_than(m, n) ->
              (less_than(ONE, m) -> exists s: Seq; exists k: Int;
              is_nonneg(k)
              and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
              (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
              and m = productUpTo(s, k)))
              -> (less_than(ONE, n) -> exists s: Seq; exists k: Int;
              is_nonneg(k)
              and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
              (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
              and n = productUpTo(s, k))
            [by implies_intro given-inductive-hypothesis]
        }
      @strong-step-at-n |
        is_nonneg(n) ->
          (forall m: Int; is_nonneg(m) -> less_than(m, n) ->
          (less_than(ONE, m) -> exists s: Seq; exists k: Int;
          is_nonneg(k)
          and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
          (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
          and m = productUpTo(s, k)))
          -> (less_than(ONE, n) -> exists s: Seq; exists k: Int;
          is_nonneg(k)
          and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
          (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
          and n = productUpTo(s, k))
        [by implies_intro given-n-nonneg]
    }
  @strong-step-for-all-n |
    forall n: Int; is_nonneg(n) ->
      (forall m: Int; is_nonneg(m) -> less_than(m, n) ->
      (less_than(ONE, m) -> exists s: Seq; exists k: Int;
      is_nonneg(k)
      and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
      (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
      and m = productUpTo(s, k)))
      -> (less_than(ONE, n) -> exists s: Seq; exists k: Int;
      is_nonneg(k)
      and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
      (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
      and n = productUpTo(s, k))
    [by forall_intro strong-step]
  @factorization-for-nonneg |
    forall n: Int; is_nonneg(n) ->
      (less_than(ONE, n) -> exists s: Seq; exists k: Int;
      is_nonneg(k)
      and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
      (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
      and n = productUpTo(s, k))
    [by instantiate nonnegStrongInduction((fun n: Int => less_than(ONE, n) -> exists s: Seq; exists k: Int; is_nonneg(k) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) and n = productUpTo(s, k))) strong-step-for-all-n]
  @discharge-less_than-one |
    fix n: Int {
      @given-n-above-one |
        assume less_than(ONE, n) {
          @n-above-one |
            less_than(ONE, n)
            [by hypothesis given-n-above-one]
          @one-less_than-n-transitive |
            less_than(ZERO, ONE) -> less_than(ONE, n) -> less_than(ZERO, n)
            [by specialize lessThanTransitive(ZERO, ONE, n)]
          @n-positive |
            less_than(ZERO, n)
            [by tautology one-less_than-n-transitive zero-less_than-one n-above-one]
          @n-nonneg |
            is_nonneg(n)
            [by specialize positiveIsNonneg(n) n-positive]
          @factorization-at-n |
            is_nonneg(n) ->
              (less_than(ONE, n) -> exists s: Seq; exists k: Int;
              is_nonneg(k)
              and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
              (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
              and n = productUpTo(s, k))
            [by forall_elim(n) factorization-for-nonneg]
          @conclusion-factorization |
            exists s: Seq; exists k: Int;
              is_nonneg(k)
              and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
              (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
              and n = productUpTo(s, k)
            [by tautology factorization-at-n n-nonneg n-above-one]
        }
      @above-one-gives-factorization |
        less_than(ONE, n) ->
          exists s: Seq; exists k: Int;
          is_nonneg(k)
          and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
          (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
          and n = productUpTo(s, k)
        [by implies_intro given-n-above-one]
    }
  @conclusion |
    forall n: Int; less_than(ONE, n) ->
      exists s: Seq; exists k: Int;
      is_nonneg(k)
      and (forall i: Int; is_nonneg(i) -> less_than(i, k) ->
      (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
      and n = productUpTo(s, k)
    [by forall_intro discharge-less_than-one]
qed
```

### Factorizations of the same product have equal length

Before the divides-based uniqueness above, we record the sharper statement that
underlies the Fundamental Theorem: two prime factorizations of the *same* product
have **the same length**. The argument is an induction on the length `k` of the
first factorization. A supporting fact we isolate first: no prime divides `1` (a
prime `p > 1` dividing `1` would force `p ≤ 1` by `divisorAtMost`, contradicting
`p > 1`) — the same reasoning already used in the base cases of
`primeDividesProductImpliesMember` and `infinitudeOfPrimes`, packaged as a lemma.

We also need that `less_than` is preserved by `succ` on both sides — if `a < b`
then `succ(a) < succ(b)` — since the induction shifts indices past a removed entry.
Read off the gap `d` witnessing `a < b` (`add(a, succ(d)) = b`), push a `succ`
through the left summand (`addSuccLeft`), and re-read it with `lessThanIntro`.

```bpa
theorem succMonotone: forall a, b: Int; less_than(a, b) -> less_than(succ(a), succ(b))
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @given-a-less_than-b |
            assume less_than(a, b) {
              @a-less_than-b |
                less_than(a, b)
                [by hypothesis given-a-less_than-b]
              @gap-exists |
                exists d: Int; is_nonneg(d) and add(a, succ(d)) = b
                [by specialize lessThanElim(a, b) a-less_than-b]
              @with-gap-d |
                unpack d: Int from gap-exists {
                  @gap-nonneg-and-sums |
                    is_nonneg(d) and add(a, succ(d)) = b
                    [by hypothesis with-gap-d]
                  @gap-nonneg |
                    is_nonneg(d)
                    [by and_elim_left gap-nonneg-and-sums]
                  @gap-sums-to-b |
                    add(a, succ(d)) = b
                    [by and_elim_right gap-nonneg-and-sums]
                  @add-succ-left |
                    forall a2, b2: Int; add(succ(a2), b2) = succ(add(a2, b2))
                    [by axiom addSuccLeft]
                  @succ-a-plus-gap |
                    add(succ(a), succ(d)) = succ(add(a, succ(d)))
                    [by forall_elim(a, succ(d)) add-succ-left]
                  @succ-a-plus-gap-is-succ-b |
                    add(succ(a), succ(d)) = succ(b)
                    [by rewrite gap-sums-to-b succ-a-plus-gap]
                  @less_than-intro |
                    forall a2, d2, b2: Int; is_nonneg(d2) -> add(a2, succ(d2)) = b2 -> less_than(a2, b2)
                    [by axiom lessThanIntro]
                  @succ-a-less_than-succ-b-conditions |
                    is_nonneg(d) -> add(succ(a), succ(d)) = succ(b) -> less_than(succ(a), succ(b))
                    [by forall_elim(succ(a), d, succ(b)) less_than-intro]
                  @conclusion-succ-a-less_than-succ-b |
                    less_than(succ(a), succ(b))
                    [by tautology succ-a-less_than-succ-b-conditions gap-nonneg succ-a-plus-gap-is-succ-b]
                }
              @succ-a-less_than-succ-b |
                less_than(succ(a), succ(b))
                [by exists_elim with-gap-d]
            }
          @a-less_than-b-gives |
            less_than(a, b) -> less_than(succ(a), succ(b))
            [by implies_intro given-a-less_than-b]
        }
      @discharge-b |
        forall b: Int; less_than(a, b) -> less_than(succ(a), succ(b))
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b: Int; less_than(a, b) -> less_than(succ(a), succ(b))
    [by forall_intro generalize-a]
qed
```

```bpa
theorem primeDoesNotDivideOne: forall p: Int;
  (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
  -> (not does_divide(p, ONE))
proof
  @generalize-p |
    fix p: Int {
      @given-p-prime |
        assume less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)) {
          @p-prime |
            less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
            [by hypothesis given-p-prime]
          @one-less_than-p |
            less_than(ONE, p)
            [by tautology p-prime]
          @assume-p-divides-one |
            assume does_divide(p, ONE) {
              @p-divides-one |
                does_divide(p, ONE)
                [by hypothesis assume-p-divides-one]
              @zero-less_than-one |
                less_than(ZERO, ONE)
                [by theorem zeroLessThanOne]
              @less_than-is-transitive |
                forall a, b, c: Int; less_than(a, b) -> less_than(b, c) -> less_than(a, c)
                [by theorem lessThanTransitive]
              @zero-less_than-one-less_than-p |
                less_than(ZERO, ONE) -> less_than(ONE, p) -> less_than(ZERO, p)
                [by specialize lessThanTransitive(ZERO, ONE, p)]
              @zero-less_than-p |
                less_than(ZERO, p)
                [by tautology zero-less_than-one-less_than-p zero-less_than-one one-less_than-p]
              @divisor-at-most |
                forall d, n: Int; less_than(ZERO, d) -> less_than(ZERO, n) -> does_divide(d, n) -> at_most(d, n)
                [by theorem divisorAtMost]
              @p-at-most-one-conditions |
                less_than(ZERO, p) -> less_than(ZERO, ONE) -> does_divide(p, ONE) -> at_most(p, ONE)
                [by specialize divisorAtMost(p, ONE)]
              @p-at-most-one |
                at_most(p, ONE)
                [by tautology p-at-most-one-conditions zero-less_than-p zero-less_than-one p-divides-one]
              @at-most-splits |
                forall a, b: Int; at_most(a, b) -> (less_than(a, b) or a = b)
                [by theorem lessOrEqualSplit]
              @p-less_than-one-or-equals-one-conditions |
                at_most(p, ONE) -> (less_than(p, ONE) or p = ONE)
                [by specialize lessOrEqualSplit(p, ONE)]
              @p-less_than-one-or-equals-one |
                less_than(p, ONE) or p = ONE
                [by modus_ponens p-less_than-one-or-equals-one-conditions p-at-most-one]
              @less_than-is-irreflexive |
                forall n: Int; not less_than(n, n)
                [by theorem lessThanIrreflexive]
              @one-not-less_than-one |
                not less_than(ONE, ONE)
                [by specialize lessThanIrreflexive(ONE)]
              // both branches contradict less_than(ONE, p).
              @when-p-less_than-one |
                assume less_than(p, ONE) {
                  @p-less_than-one |
                    less_than(p, ONE)
                    [by hypothesis when-p-less_than-one]
                  @one-less_than-p-less_than-one |
                    less_than(ONE, p) -> less_than(p, ONE) -> less_than(ONE, ONE)
                    [by specialize lessThanTransitive(ONE, p, ONE)]
                  @one-less_than-one |
                    less_than(ONE, ONE)
                    [by tautology one-less_than-p-less_than-one one-less_than-p p-less_than-one]
                  @contradiction-p-below-one |
                    less_than(ONE, ONE)
                    [by tautology one-less_than-one]
                }
              @when-p-equals-one |
                assume p = ONE {
                  @p-equals-one |
                    p = ONE
                    [by hypothesis when-p-equals-one]
                  @one-less_than-one-from-p |
                    less_than(ONE, ONE)
                    [by rewrite p-equals-one one-less_than-p]
                  @contradiction-p-equals-one |
                    less_than(ONE, ONE)
                    [by tautology one-less_than-one-from-p]
                }
              @one-less_than-one-either-way |
                less_than(ONE, ONE)
                [by or_elim p-less_than-one-or-equals-one when-p-less_than-one when-p-equals-one]
              @contradiction |
                not does_divide(p, ONE)
                [by absurd one-less_than-one-either-way one-not-less_than-one]
            }
          @p-does-not-divide-one |
            does_divide(p, ONE) -> (not does_divide(p, ONE))
            [by implies_intro assume-p-divides-one]
          @not-p-divides-one |
            not does_divide(p, ONE)
            [by tautology p-does-not-divide-one]
        }
      @prime-implies-not-divides-one |
        (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
          -> (not does_divide(p, ONE))
        [by implies_intro given-p-prime]
    }
  @conclusion |
    forall p: Int;
      (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
      -> (not does_divide(p, ONE))
    [by forall_intro generalize-p]
qed
```

The **peel step** shared by both uniqueness inductions. Given two prime
factorizations of the same product — `s` of length `succ(k)` and `t` of length
`l` — it peels the last factor `at(s, k)`: that factor is prime and divides
`productUpTo(s, succ k) = productUpTo(t, l)`, so by
`primeDividesProductImpliesMember` it divides some prime `at(t, m)`, forcing
`at(s, k) = at(t, m)`. Then `l = succ(l')` (it is positive, as it contains the
index `m`), and removing entry `m` (`seqRemoveExists`) yields `t'` of length `l'`
with `productUpTo(s, k) = productUpTo(t', l')` (cancel `at(s, k)` by
`mulCancelRight`) whose entries below `l'` are still prime, and whose entries
agree with `t` (copied below `m`, shifted above). Both `factorizationLengthUnique`
and `factorizationPermutationExists` open their inductive step by citing this.

```bpa
theorem factorizationPeelStep: forall k: Int; is_nonneg(k) ->
  forall s: Seq; forall t: Seq; forall l: Int;
  is_nonneg(l)
  -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
  -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))))
  -> productUpTo(s, succ(k)) = productUpTo(t, l)
  -> (exists lp: Int; exists mm: Int; exists tt: Seq;
  is_nonneg(lp)
  and l = succ(lp)
  and is_nonneg(mm)
  and less_than(mm, l)
  and at(s, k) = at(t, mm)
  and productUpTo(s, k) = productUpTo(tt, lp)
  and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j)))))
  and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i))
  and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i))))
proof
  // peel the last factor and match it to at(t,m); remove m from t to get t2.
  @peel-step-proof |
    fix k: Int {
      @given-k-nonneg |
        assume is_nonneg(k) {
          @k-nonneg |
            is_nonneg(k)
            [by hypothesis given-k-nonneg]
          @successor-case-proof |
            fix s: Seq {
              @generalize-t-step |
                fix t: Seq {
                  @generalize-l-step |
                    fix l: Int {
                      @given-l-nonneg-step |
                        assume is_nonneg(l) {
                          @given-s-primes-step |
                            assume forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))) {
                              @given-t-primes-step |
                                assume forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))) {
                                  @given-equal-products-step |
                                    assume productUpTo(s, succ(k)) = productUpTo(t, l) {
                                      @l-nonneg |
                                        is_nonneg(l)
                                        [by hypothesis given-l-nonneg-step]
                                      @s-primes-below-succ-k |
                                        forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                        [by hypothesis given-s-primes-step]
                                      @t-primes-below-l |
                                        forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))
                                        [by hypothesis given-t-primes-step]
                                      @equal-products |
                                        productUpTo(s, succ(k)) = productUpTo(t, l)
                                        [by hypothesis given-equal-products-step]

                                      // shared: k < succ k, transitivity, positivity of at(s,k).
                                      @below-successor-lemma |
                                        forall k2: Int; less_than(k2, succ(k2))
                                        [by theorem lessThanSuccSelf]
                                      @k-below-succ-k |
                                        less_than(k, succ(k))
                                        [by specialize lessThanSuccSelf(k)]
                                      @less_than-is-transitive |
                                        forall a, b, c: Int; less_than(a, b) -> less_than(b, c) -> less_than(a, c)
                                        [by theorem lessThanTransitive]

                                      // the last factor at(s,k) is prime.
                                      @sk-prime-conditions |
                                        is_nonneg(k) -> less_than(k, succ(k)) -> (less_than(ONE, at(s, k)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, k)) -> (d = ONE or d = at(s, k))))
                                        [by forall_elim(k) s-primes-below-succ-k]
                                      @sk-prime |
                                        less_than(ONE, at(s, k)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, k)) -> (d = ONE or d = at(s, k)))
                                        [by tautology sk-prime-conditions k-nonneg k-below-succ-k]
                                      @sk-above-one |
                                        less_than(ONE, at(s, k))
                                        [by and_elim_left sk-prime]
                                      @zero-less_than-one |
                                        less_than(ZERO, ONE)
                                        [by theorem zeroLessThanOne]
                                      @zero-below-sk-conditions |
                                        less_than(ZERO, ONE) -> less_than(ONE, at(s, k)) -> less_than(ZERO, at(s, k))
                                        [by forall_elim(ZERO, ONE, at(s, k)) less_than-is-transitive]
                                      @sk-above-zero |
                                        less_than(ZERO, at(s, k))
                                        [by tautology zero-below-sk-conditions zero-less_than-one sk-above-one]

                                      // at(s,k) is the last factor, so it divides productUpTo(s, succ k) = productUpTo(t, l).
                                      @last-entry-divides-succ-product-lemma |
                                        forall u: Seq; forall k2: Int; does_divide(at(u, k2), productUpTo(u, succ(k2)))
                                        [by theorem lastEntryDividesProduct]
                                      @sk-divides-succ-product |
                                        does_divide(at(s, k), productUpTo(s, succ(k)))
                                        [by specialize lastEntryDividesProduct(s, k)]
                                      @sk-divides-prod-t |
                                        does_divide(at(s, k), productUpTo(t, l))
                                        [by rewrite equal-products sk-divides-succ-product]

                                      // crux: at(s,k) divides some entry at(t,m).
                                      @prime-divides-member-l |
                                        forall ss: Seq; forall p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> does_divide(p, productUpTo(ss, l)) -> (exists i2: Int; is_nonneg(i2) and less_than(i2, l) and does_divide(p, at(ss, i2)))
                                        [by specialize primeDividesProductImpliesMember(l) l-nonneg]
                                      @prime-divides-member-t-sk |
                                        (less_than(ONE, at(s, k)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, k)) -> (d = ONE or d = at(s, k)))) -> does_divide(at(s, k), productUpTo(t, l)) -> (exists i2: Int; is_nonneg(i2) and less_than(i2, l) and does_divide(at(s, k), at(t, i2)))
                                        [by forall_elim(t, at(s, k)) prime-divides-member-l]
                                      @member-exists |
                                        exists i2: Int; is_nonneg(i2) and less_than(i2, l) and does_divide(at(s, k), at(t, i2))
                                        [by tautology prime-divides-member-t-sk sk-prime sk-divides-prod-t]

                                      @with-member-m |
                                        unpack m: Int from member-exists {
                                          @m-props |
                                            is_nonneg(m) and less_than(m, l) and does_divide(at(s, k), at(t, m))
                                            [by hypothesis with-member-m]
                                          @m-nonneg |
                                            is_nonneg(m)
                                            [by tautology m-props]
                                          @m-below-l |
                                            less_than(m, l)
                                            [by tautology m-props]
                                          @sk-divides-tm |
                                            does_divide(at(s, k), at(t, m))
                                            [by tautology m-props]

                                          // at(t,m) is prime; at(s,k) | at(t,m) with at(s,k) > 1 forces at(s,k) = at(t,m).
                                          @tm-prime-conditions |
                                            is_nonneg(m) -> less_than(m, l) -> (less_than(ONE, at(t, m)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, m)) -> (d = ONE or d = at(t, m))))
                                            [by forall_elim(m) t-primes-below-l]
                                          @tm-prime |
                                            less_than(ONE, at(t, m)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, m)) -> (d = ONE or d = at(t, m)))
                                            [by tautology tm-prime-conditions m-nonneg m-below-l]
                                          @tm-divisor-clause |
                                            forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, m)) -> (d = ONE or d = at(t, m))
                                            [by and_elim_right tm-prime]
                                          @clause-at-sk |
                                            less_than(ZERO, at(s, k)) -> does_divide(at(s, k), at(t, m)) -> (at(s, k) = ONE or at(s, k) = at(t, m))
                                            [by forall_elim(at(s, k)) tm-divisor-clause]
                                          @sk-one-or-eq |
                                            at(s, k) = ONE or at(s, k) = at(t, m)
                                            [by tautology clause-at-sk sk-above-zero sk-divides-tm]
                                          @less_than-is-irreflexive |
                                            forall a: Int; not less_than(a, a)
                                            [by theorem lessThanIrreflexive]
                                          @one-not-below-one |
                                            not less_than(ONE, ONE)
                                            [by specialize lessThanIrreflexive(ONE)]
                                          @sk-equals-tm |
                                            at(s, k) = at(t, m)
                                            case sk-one-or-eq {
                                              @when-sk-one |
                                                assume at(s, k) = ONE {
                                                  @sk-is-one |
                                                    at(s, k) = ONE
                                                    [by hypothesis when-sk-one]
                                                  @one-below-one |
                                                    less_than(ONE, ONE)
                                                    [by rewrite sk-is-one sk-above-one]
                                                  @sk-equals-tm-absurd |
                                                    at(s, k) = at(t, m)
                                                    [by absurd one-below-one one-not-below-one]
                                                }
                                              @when-sk-eq |
                                                assume at(s, k) = at(t, m) {
                                                  @sk-equals-tm-direct |
                                                    at(s, k) = at(t, m)
                                                    [by hypothesis when-sk-eq]
                                                }
                                            }
                                          @tm-equals-sk |
                                            at(t, m) = at(s, k)
                                            [by symmetry sk-equals-tm]

                                          // l > 0, so l = succ(lprev) for nonneg lprev; and m < succ(lprev).
                                          @nonneg-cases |
                                            forall n: Int; is_nonneg(n) -> (n = ZERO or (exists mm: Int; is_nonneg(mm) and n = succ(mm)))
                                            [by theorem nonnegCases]
                                          @l-cases-if-nonneg |
                                            is_nonneg(l) -> (l = ZERO or (exists mm: Int; is_nonneg(mm) and l = succ(mm)))
                                            [by specialize nonnegCases(l)]
                                          @l-zero-or-successor |
                                            l = ZERO or (exists mm: Int; is_nonneg(mm) and l = succ(mm))
                                            [by modus_ponens l-cases-if-nonneg l-nonneg]
                                          @nothing-nonneg-below-zero |
                                            forall n: Int; is_nonneg(n) -> (not less_than(n, ZERO))
                                            [by theorem nonnegNotLessThanZero]
                                          @m-not-below-zero |
                                            is_nonneg(m) -> (not less_than(m, ZERO))
                                            [by specialize nonnegNotLessThanZero(m)]
                                          @l-is-successor-exists |
                                            exists mm: Int; is_nonneg(mm) and l = succ(mm)
                                            case l-zero-or-successor {
                                              @when-l-zero |
                                                assume l = ZERO {
                                                  @l-is-zero |
                                                    l = ZERO
                                                    [by hypothesis when-l-zero]
                                                  @m-below-zero |
                                                    less_than(m, ZERO)
                                                    [by rewrite l-is-zero m-below-l]
                                                  @m-not-below-zero-derived |
                                                    not less_than(m, ZERO)
                                                    [by modus_ponens m-not-below-zero m-nonneg]
                                                  @l-successor-absurd |
                                                    exists mm: Int; is_nonneg(mm) and l = succ(mm)
                                                    [by absurd m-below-zero m-not-below-zero-derived]
                                                }
                                              @when-l-successor |
                                                assume exists mm: Int; is_nonneg(mm) and l = succ(mm) {
                                                  @l-successor-direct |
                                                    exists mm: Int; is_nonneg(mm) and l = succ(mm)
                                                    [by hypothesis when-l-successor]
                                                }
                                            }

                                          @with-predecessor-lprev |
                                            unpack lprev: Int from l-is-successor-exists {
                                              @lprev-nonneg-and-l-successor |
                                                is_nonneg(lprev) and l = succ(lprev)
                                                [by hypothesis with-predecessor-lprev]
                                              @lprev-nonneg |
                                                is_nonneg(lprev)
                                                [by and_elim_left lprev-nonneg-and-l-successor]
                                              @l-is-succ-lprev |
                                                l = succ(lprev)
                                                [by and_elim_right lprev-nonneg-and-l-successor]
                                              @succ-lprev-is-l |
                                                succ(lprev) = l
                                                [by symmetry l-is-succ-lprev]
                                              @m-below-succ-lprev |
                                                less_than(m, succ(lprev))
                                                [by rewrite l-is-succ-lprev m-below-l]

                                              // remove entry m from t: get t2 of length lprev.
                                              @removed-exists |
                                                exists r: Seq; mul(productUpTo(r, lprev), at(t, m)) = productUpTo(t, succ(lprev)) and (forall i: Int; is_nonneg(i) -> less_than(i, m) -> at(r, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(m, i) -> less_than(i, lprev) -> at(r, i) = at(t, succ(i)))
                                                [by specialize seqRemoveExists(t, lprev, m) lprev-nonneg m-nonneg m-below-succ-lprev]

                                              @with-removed-t2 |
                                                unpack t2: Seq from removed-exists {
                                                  @t2-clauses |
                                                    mul(productUpTo(t2, lprev), at(t, m)) = productUpTo(t, succ(lprev)) and (forall i: Int; is_nonneg(i) -> less_than(i, m) -> at(t2, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(m, i) -> less_than(i, lprev) -> at(t2, i) = at(t, succ(i)))
                                                    [by hypothesis with-removed-t2]
                                                  @t2-product-times-tm |
                                                    mul(productUpTo(t2, lprev), at(t, m)) = productUpTo(t, succ(lprev))
                                                    [by tautology t2-clauses]
                                                  @t2-agrees-below-m |
                                                    forall i: Int; is_nonneg(i) -> less_than(i, m) -> at(t2, i) = at(t, i)
                                                    [by tautology t2-clauses]
                                                  @t2-agrees-above-m |
                                                    forall i: Int; is_nonneg(i) -> at_most(m, i) -> less_than(i, lprev) -> at(t2, i) = at(t, succ(i))
                                                    [by tautology t2-clauses]

                                                  // productUpTo(t, succ lprev) = productUpTo(t, l) = productUpTo(s, succ k)
                                                  //   = mul(productUpTo(s,k), at(s,k)); rewrite at(t,m) = at(s,k), cancel at(s,k).
                                                  @t2-product-times-tm-is-l-product |
                                                    mul(productUpTo(t2, lprev), at(t, m)) = productUpTo(t, l)
                                                    [by rewrite succ-lprev-is-l t2-product-times-tm]
                                                  @product-of-successor-bound |
                                                    forall u: Seq; forall k2: Int; productUpTo(u, succ(k2)) = mul(productUpTo(u, k2), at(u, k2))
                                                    [by axiom productUpToSucc]
                                                  @s-succ-product-splits |
                                                    productUpTo(s, succ(k)) = mul(productUpTo(s, k), at(s, k))
                                                    [by specialize productUpToSucc(s, k)]
                                                  @s-product-equals-t2-product-times-sk |
                                                    mul(productUpTo(s, k), at(s, k)) = mul(productUpTo(t2, lprev), at(s, k))
                                                    [by chain s-succ-product-splits equal-products t2-product-times-tm-is-l-product tm-equals-sk]

                                                  // cancel the positive factor at(s,k).
                                                  @cancel-right |
                                                    forall a, x, y: Int; less_than(ZERO, a) -> mul(x, a) = mul(y, a) -> x = y
                                                    [by theorem mulCancelRight]
                                                  @cancel-at-sk |
                                                    less_than(ZERO, at(s, k)) -> mul(productUpTo(s, k), at(s, k)) = mul(productUpTo(t2, lprev), at(s, k)) -> productUpTo(s, k) = productUpTo(t2, lprev)
                                                    [by forall_elim(at(s, k), productUpTo(s, k), productUpTo(t2, lprev)) cancel-right]
                                                  @s-product-equals-t2-product |
                                                    productUpTo(s, k) = productUpTo(t2, lprev)
                                                    [by tautology cancel-at-sk sk-above-zero s-product-equals-t2-product-times-sk]

                                                  // t2's entries below lprev are prime (agreement clauses transfer t's primality).
                                                  @lprev-below-succ-lprev |
                                                    less_than(lprev, succ(lprev))
                                                    [by specialize lessThanSuccSelf(lprev)]
                                                  @lprev-below-l |
                                                    less_than(lprev, l)
                                                    [by rewrite succ-lprev-is-l lprev-below-succ-lprev]
                                                  @order-is-trichotomous |
                                                    forall a, b: Int; less_than(a, b) or a = b or less_than(b, a)
                                                    [by theorem trichotomy]
                                                  @less_than-to-at-most |
                                                    forall a, b: Int; less_than(a, b) -> at_most(a, b)
                                                    [by theorem lessThanToLessOrEqual]
                                                  @at-most-is-reflexive |
                                                    forall a: Int; at_most(a, a)
                                                    [by theorem lessOrEqualRefl]
                                                  @t2-entries-prime-proof |
                                                    fix j: Int {
                                                      @given-j-nonneg |
                                                        assume is_nonneg(j) {
                                                          @given-j-below-lprev |
                                                            assume less_than(j, lprev) {
                                                              @j-nonneg |
                                                                is_nonneg(j)
                                                                [by hypothesis given-j-nonneg]
                                                              @j-below-lprev |
                                                                less_than(j, lprev)
                                                                [by hypothesis given-j-below-lprev]
                                                              @j-below-l |
                                                                less_than(j, l)
                                                                [by specialize lessThanTransitive(j, lprev, l) j-below-lprev lprev-below-l]
                                                              @j-vs-m |
                                                                less_than(j, m) or j = m or less_than(m, j)
                                                                [by specialize trichotomy(j, m)]
                                                              // collapse the trichotomy to a binary split: below m, or m-at-most-j.
                                                              @j-below-m-or-m-at-most-j |
                                                                less_than(j, m) or at_most(m, j)
                                                                case j-vs-m {
                                                                  @when-j-strictly-below-m |
                                                                    assume less_than(j, m) {
                                                                      @j-strictly-below-m |
                                                                        less_than(j, m)
                                                                        [by hypothesis when-j-strictly-below-m]
                                                                      @below-m-left |
                                                                        less_than(j, m) or at_most(m, j)
                                                                        [by or_intro_left j-strictly-below-m]
                                                                    }
                                                                  @when-j-equals-m |
                                                                    assume j = m {
                                                                      @j-equals-m |
                                                                        j = m
                                                                        [by hypothesis when-j-equals-m]
                                                                      @m-at-most-m |
                                                                        at_most(m, m)
                                                                        [by specialize lessOrEqualRefl(m)]
                                                                      @m-equals-j |
                                                                        m = j
                                                                        [by symmetry j-equals-m]
                                                                      @m-at-most-j-from-equal |
                                                                        at_most(m, j)
                                                                        [by rewrite m-equals-j m-at-most-m]
                                                                      @at-most-right-from-equal |
                                                                        less_than(j, m) or at_most(m, j)
                                                                        [by or_intro_right m-at-most-j-from-equal]
                                                                    }
                                                                  @when-m-below-j |
                                                                    assume less_than(m, j) {
                                                                      @m-below-j |
                                                                        less_than(m, j)
                                                                        [by hypothesis when-m-below-j]
                                                                      @m-at-most-j-from-strict |
                                                                        at_most(m, j)
                                                                        [by specialize lessThanToLessOrEqual(m, j) m-below-j]
                                                                      @at-most-right-from-strict |
                                                                        less_than(j, m) or at_most(m, j)
                                                                        [by or_intro_right m-at-most-j-from-strict]
                                                                    }
                                                                }
                                                              @t2-entry-prime |
                                                                less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j)))
                                                                case j-below-m-or-m-at-most-j {
                                                                  @when-j-below-m |
                                                                    assume less_than(j, m) {
                                                                      @j-below-m |
                                                                        less_than(j, m)
                                                                        [by hypothesis when-j-below-m]
                                                                      @t2-agrees-at-j-conditions |
                                                                        is_nonneg(j) -> less_than(j, m) -> at(t2, j) = at(t, j)
                                                                        [by forall_elim(j) t2-agrees-below-m]
                                                                      @t2-agrees-at-j |
                                                                        at(t2, j) = at(t, j)
                                                                        [by tautology t2-agrees-at-j-conditions j-nonneg j-below-m]
                                                                      @tj-prime-conditions |
                                                                        is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))
                                                                        [by forall_elim(j) t-primes-below-l]
                                                                      @tj-prime |
                                                                        less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))
                                                                        [by tautology tj-prime-conditions j-nonneg j-below-l]
                                                                      @tj-is-t2-at-j |
                                                                        at(t, j) = at(t2, j)
                                                                        [by symmetry t2-agrees-at-j]
                                                                      @t2-entry-prime-below-m |
                                                                        less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j)))
                                                                        [by rewrite tj-is-t2-at-j tj-prime]
                                                                    }
                                                                  @when-m-at-most-j |
                                                                    assume at_most(m, j) {
                                                                      @m-at-most-j |
                                                                        at_most(m, j)
                                                                        [by hypothesis when-m-at-most-j]
                                                                      @t2-agrees-at-shifted-conditions |
                                                                        is_nonneg(j) -> at_most(m, j) -> less_than(j, lprev) -> at(t2, j) = at(t, succ(j))
                                                                        [by forall_elim(j) t2-agrees-above-m]
                                                                      @t2-agrees-at-shifted-j |
                                                                        at(t2, j) = at(t, succ(j))
                                                                        [by tautology t2-agrees-at-shifted-conditions j-nonneg m-at-most-j j-below-lprev]
                                                                      @succ-j-nonneg |
                                                                        is_nonneg(succ(j))
                                                                        [by specialize nonnegSucc(j) j-nonneg]
                                                                      @succ-j-below-succ-lprev |
                                                                        less_than(succ(j), succ(lprev))
                                                                        [by specialize succMonotone(j, lprev) j-below-lprev]
                                                                      @succ-j-below-l |
                                                                        less_than(succ(j), l)
                                                                        [by rewrite succ-lprev-is-l succ-j-below-succ-lprev]
                                                                      @t-shifted-prime-conditions |
                                                                        is_nonneg(succ(j)) -> less_than(succ(j), l) -> (less_than(ONE, at(t, succ(j))) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, succ(j))) -> (d = ONE or d = at(t, succ(j)))))
                                                                        [by forall_elim(succ(j)) t-primes-below-l]
                                                                      @t-shifted-prime |
                                                                        less_than(ONE, at(t, succ(j))) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, succ(j))) -> (d = ONE or d = at(t, succ(j))))
                                                                        [by tautology t-shifted-prime-conditions succ-j-nonneg succ-j-below-l]
                                                                      @t-shifted-is-t2-at-j |
                                                                        at(t, succ(j)) = at(t2, j)
                                                                        [by symmetry t2-agrees-at-shifted-j]
                                                                      @t2-entry-prime-above-m |
                                                                        less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j)))
                                                                        [by rewrite t-shifted-is-t2-at-j t-shifted-prime]
                                                                    }
                                                                }
                                                            }
                                                          @j-below-lprev-gives-prime |
                                                            less_than(j, lprev) -> (less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j))))
                                                            [by implies_intro given-j-below-lprev]
                                                        }
                                                      @j-nonneg-gives-prime |
                                                        is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j))))
                                                        [by implies_intro given-j-nonneg]
                                                    }
                                                  @t2-primes-below-lprev |
                                                    forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j))))
                                                    [by forall_intro t2-entries-prime-proof]

                                                  // package the peeled data as the existential.
                                                  @peel-conjunction |
                                                    is_nonneg(lprev) and l = succ(lprev) and is_nonneg(m) and less_than(m, l) and at(s, k) = at(t, m) and productUpTo(s, k) = productUpTo(t2, lprev) and (forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, m) -> at(t2, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(m, i) -> less_than(i, lprev) -> at(t2, i) = at(t, succ(i)))
                                                    [by tautology lprev-nonneg l-is-succ-lprev m-nonneg m-below-l sk-equals-tm s-product-equals-t2-product t2-primes-below-lprev t2-agrees-below-m t2-agrees-above-m]
                                                  @peel-exists-t2 |
                                                    exists tt: Seq; is_nonneg(lprev) and l = succ(lprev) and is_nonneg(m) and less_than(m, l) and at(s, k) = at(t, m) and productUpTo(s, k) = productUpTo(tt, lprev) and (forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, m) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(m, i) -> less_than(i, lprev) -> at(tt, i) = at(t, succ(i)))
                                                    [by exists_intro(t2) peel-conjunction]
                                                  @peel-exists-m-t2 |
                                                    exists mm: Int; exists tt: Seq; is_nonneg(lprev) and l = succ(lprev) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lprev) and (forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lprev) -> at(tt, i) = at(t, succ(i)))
                                                    [by exists_intro(m) peel-exists-t2]
                                                  @peel-exists-all |
                                                    exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i)))
                                                    [by exists_intro(lprev) peel-exists-m-t2]
                                                }
                                              @peel-exists-from-removal |
                                                exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i)))
                                                [by exists_elim with-removed-t2]
                                            }
                                          @peel-exists-from-predecessor |
                                            exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i)))
                                            [by exists_elim with-predecessor-lprev]
                                        }
                                      @conclusion-peel-exists |
                                        exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i)))
                                        [by exists_elim with-member-m]
                                    }
                                  @equal-products-give-peel |
                                    productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i))))
                                    [by implies_intro given-equal-products-step]
                                }
                              @t-primes-give-peel |
                                (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i))))
                                [by implies_intro given-t-primes-step]
                            }
                          @s-primes-give-peel |
                            (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i))))
                            [by implies_intro given-s-primes-step]
                        }
                      @l-nonneg-gives-peel |
                        is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i))))
                        [by implies_intro given-l-nonneg-step]
                    }
                  @discharge-l-step |
                    forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i))))
                    [by forall_intro generalize-l-step]
                }
              @discharge-t-step |
                forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i))))
                [by forall_intro generalize-t-step]
            }
          @peel-step-for-s |
            forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i))))
            [by forall_intro successor-case-proof]
        }
      @peel-step-if-k-nonneg |
        is_nonneg(k) -> (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i)))))
        [by implies_intro given-k-nonneg]
    }
  @conclusion |
    forall k: Int; is_nonneg(k) -> (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i)))))
    [by forall_intro peel-step-proof]
qed
```

The length statement itself. We induct on the length `k` of the first
factorization `s`. **Base (`k = 0`):** the product is `1`; if `t` had positive
length `l = succ(l')`, its last entry `at(t, l')` is a prime dividing the product
`1` (`lastEntryDividesProduct`), impossible by `primeDoesNotDivideOne`; hence
`l = 0`. **Step (`P(k) ⇒ P(succ k)`):** the last factor `at(s, k)` is prime and
divides `productUpTo(s, succ k) = productUpTo(t, l)`, so by
`primeDividesProductImpliesMember` it divides some prime `at(t, m)`, forcing
`at(s, k) = at(t, m)`. Remove that entry from `t` (`seqRemoveExists`) to get `t'`
of length `l' = l − 1` whose product is `productUpTo(t, l)` with one factor of
`at(t, m)` peeled off; cancelling `at(s, k) = at(t, m)` (`mulCancelRight`) equates
`productUpTo(s, k) = productUpTo(t', l')`. The removed sequence's entries below
`l'` are still prime, so the inductive hypothesis gives `k = l'`, whence
`succ k = succ l' = l`.

```bpa
theorem factorizationLengthUnique: forall k: Int; is_nonneg(k) ->
  forall s: Seq; forall t: Seq; forall l: Int;
  is_nonneg(l)
  -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
  -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))))
  -> productUpTo(s, k) = productUpTo(t, l)
  -> k = l
proof
  // base case: k = 0. The product is ONE. If t had a last entry at(t, l'), it
  // would be a prime dividing ONE — impossible — so l = 0.
  @base-case-proof |
    fix s: Seq {
      @generalize-t-base |
        fix t: Seq {
          @generalize-l-base |
            fix l: Int {
              @given-l-nonneg-base |
                assume is_nonneg(l) {
                  @given-s-primes-base |
                    assume forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))) {
                      @given-t-primes-base |
                        assume forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))) {
                          @given-equal-products-base |
                            assume productUpTo(s, ZERO) = productUpTo(t, l) {
                              @l-nonneg |
                                is_nonneg(l)
                                [by hypothesis given-l-nonneg-base]
                              @t-primes |
                                forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))
                                [by hypothesis given-t-primes-base]
                              @equal-products |
                                productUpTo(s, ZERO) = productUpTo(t, l)
                                [by hypothesis given-equal-products-base]
                              @empty-product-is-one-for-all |
                                forall u: Seq; productUpTo(u, ZERO) = ONE
                                [by axiom productUpToZero]
                              @empty-product-is-one |
                                productUpTo(s, ZERO) = ONE
                                [by specialize productUpToZero(s)]
                              @t-product-is-one |
                                productUpTo(t, l) = ONE
                                [by rewrite equal-products empty-product-is-one]

                              // suppose l = succ(l'); derive a contradiction.
                              @nonneg-cases |
                                forall n: Int; is_nonneg(n) -> (n = ZERO or (exists m: Int; is_nonneg(m) and n = succ(m)))
                                [by theorem nonnegCases]
                              @l-cases-if-nonneg |
                                is_nonneg(l) -> (l = ZERO or (exists m: Int; is_nonneg(m) and l = succ(m)))
                                [by specialize nonnegCases(l)]
                              @l-zero-or-successor |
                                l = ZERO or (exists m: Int; is_nonneg(m) and l = succ(m))
                                [by modus_ponens l-cases-if-nonneg l-nonneg]
                              @l-is-zero |
                                l = ZERO
                                case l-zero-or-successor {
                                  @when-l-is-zero |
                                    assume l = ZERO {
                                      @conclusion-l-is-zero |
                                        l = ZERO
                                        [by hypothesis when-l-is-zero]
                                    }
                                  @when-l-is-successor |
                                    assume exists m: Int; is_nonneg(m) and l = succ(m) {
                                      @successor-exists |
                                        exists m: Int; is_nonneg(m) and l = succ(m)
                                        [by hypothesis when-l-is-successor]
                                      @with-predecessor-l-prime |
                                        unpack l_prime: Int from successor-exists {
                                          @predecessor-nonneg-and-l-successor |
                                            is_nonneg(l_prime) and l = succ(l_prime)
                                            [by hypothesis with-predecessor-l-prime]
                                          @predecessor-nonneg |
                                            is_nonneg(l_prime)
                                            [by and_elim_left predecessor-nonneg-and-l-successor]
                                          @l-is-successor |
                                            l = succ(l_prime)
                                            [by and_elim_right predecessor-nonneg-and-l-successor]
                                          @predecessor-less_than-its-successor |
                                            less_than(l_prime, succ(l_prime))
                                            [by specialize lessThanSuccSelf(l_prime)]
                                          @successor-is-l |
                                            succ(l_prime) = l
                                            [by symmetry l-is-successor]
                                          @predecessor-less_than-l |
                                            less_than(l_prime, l)
                                            [by rewrite successor-is-l predecessor-less_than-its-successor]
                                          @last-entry-prime-conditions |
                                            is_nonneg(l_prime) -> less_than(l_prime, l) -> (less_than(ONE, at(t, l_prime)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, l_prime)) -> (d = ONE or d = at(t, l_prime))))
                                            [by forall_elim(l_prime) t-primes]
                                          @last-entry-prime |
                                            less_than(ONE, at(t, l_prime)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, l_prime)) -> (d = ONE or d = at(t, l_prime)))
                                            [by tautology last-entry-prime-conditions predecessor-nonneg predecessor-less_than-l]
                                          // at(t, l') divides productUpTo(t, succ l') = productUpTo(t, l) = ONE.
                                          @last-entry-divides-succ-product-lemma |
                                            forall u: Seq; forall k2: Int; does_divide(at(u, k2), productUpTo(u, succ(k2)))
                                            [by theorem lastEntryDividesProduct]
                                          @last-entry-divides-succ-product |
                                            does_divide(at(t, l_prime), productUpTo(t, succ(l_prime)))
                                            [by specialize lastEntryDividesProduct(t, l_prime)]
                                          @last-entry-divides-t-product |
                                            does_divide(at(t, l_prime), productUpTo(t, l))
                                            [by rewrite successor-is-l last-entry-divides-succ-product]
                                          @last-entry-divides-one |
                                            does_divide(at(t, l_prime), ONE)
                                            [by rewrite t-product-is-one last-entry-divides-t-product]
                                          @prime-does-not-divide-one |
                                            forall p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> (not does_divide(p, ONE))
                                            [by theorem primeDoesNotDivideOne]
                                          @last-entry-does-not-divide-one |
                                            (less_than(ONE, at(t, l_prime)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, l_prime)) -> (d = ONE or d = at(t, l_prime)))) -> (not does_divide(at(t, l_prime), ONE))
                                            [by forall_elim(at(t, l_prime)) prime-does-not-divide-one]
                                          @not-last-entry-divides-one |
                                            not does_divide(at(t, l_prime), ONE)
                                            [by modus_ponens last-entry-does-not-divide-one last-entry-prime]
                                          @conclusion-l-is-zero |
                                            l = ZERO
                                            [by absurd last-entry-divides-one not-last-entry-divides-one]
                                        }
                                      @conclusion-l-is-zero |
                                        l = ZERO
                                        [by exists_elim with-predecessor-l-prime]
                                    }
                                }
                              @zero-equals-l |
                                ZERO = l
                                [by symmetry l-is-zero]
                            }
                          @equal-products-give-base |
                            productUpTo(s, ZERO) = productUpTo(t, l) -> ZERO = l
                            [by implies_intro given-equal-products-base]
                        }
                      @t-primes-give-base |
                        (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, ZERO) = productUpTo(t, l) -> ZERO = l
                        [by implies_intro given-t-primes-base]
                    }
                  @s-primes-give-base |
                    (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, ZERO) = productUpTo(t, l) -> ZERO = l
                    [by implies_intro given-s-primes-base]
                }
              @l-nonneg-gives-base |
                is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, ZERO) = productUpTo(t, l) -> ZERO = l
                [by implies_intro given-l-nonneg-base]
            }
          @discharge-l-base |
            forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, ZERO) = productUpTo(t, l) -> ZERO = l
            [by forall_intro generalize-l-base]
        }
      @discharge-t-base |
        forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, ZERO) = productUpTo(t, l) -> ZERO = l
        [by forall_intro generalize-t-base]
    }
  @base-case |
    forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, ZERO) = productUpTo(t, l) -> ZERO = l
    [by forall_intro base-case-proof]

  // inductive step: from P(k) derive P(succ k).
  @induction-step |
    fix k: Int {
      @given-k-nonneg |
        assume is_nonneg(k) {
          @k-nonneg |
            is_nonneg(k)
            [by hypothesis given-k-nonneg]
          @given-inductive-hypothesis |
            assume forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> k = l {
              @inductive-hypothesis |
                forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> k = l
                [by hypothesis given-inductive-hypothesis]

              // prove P(succ k): fix s, t, l with the primality/product hypotheses at length succ k.
              @successor-case-proof |
                fix s: Seq {
                  @generalize-t-step |
                    fix t: Seq {
                      @generalize-l-step |
                        fix l: Int {
                          @given-l-nonneg-step |
                            assume is_nonneg(l) {
                              @given-s-primes-step |
                                assume forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))) {
                                  @given-t-primes-step |
                                    assume forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))) {
                                      @given-equal-products-step |
                                        assume productUpTo(s, succ(k)) = productUpTo(t, l) {
                                          @l-nonneg |
                                            is_nonneg(l)
                                            [by hypothesis given-l-nonneg-step]
                                          @s-primes-below-succ-k |
                                            forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                            [by hypothesis given-s-primes-step]
                                          @t-primes-below-l |
                                            forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))
                                            [by hypothesis given-t-primes-step]
                                          @equal-products |
                                            productUpTo(s, succ(k)) = productUpTo(t, l)
                                            [by hypothesis given-equal-products-step]

                                          // peel the last factor at(s,k): match it to at(t,m), remove m from t to get t2.
                                          @peel-step |
                                            forall kfix: Int; is_nonneg(kfix) -> (forall s0: Seq; forall t0: Seq; forall l0: Int; is_nonneg(l0) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(kfix)) -> (less_than(ONE, at(s0, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s0, i)) -> (d = ONE or d = at(s0, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l0) -> (less_than(ONE, at(t0, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t0, j)) -> (d = ONE or d = at(t0, j))))) -> productUpTo(s0, succ(kfix)) = productUpTo(t0, l0) -> (exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l0 = succ(lp) and is_nonneg(mm) and less_than(mm, l0) and at(s0, kfix) = at(t0, mm) and productUpTo(s0, kfix) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t0, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t0, succ(i)))))
                                            [by theorem factorizationPeelStep]
                                          // apply the shared peel step at all four binders in one
                                          // interleaved specialize (k↦∀k, k-nonneg↦guard, then s,t,l
                                          // ↦∀s,t,l and the four antecedents).
                                          @peeled-exists |
                                            exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i)))
                                            [by specialize factorizationPeelStep(k, s, t, l) k-nonneg l-nonneg s-primes-below-succ-k t-primes-below-l equal-products]

                                          @with-predecessor-lprev |
                                            unpack lprev: Int from peeled-exists {
                                              @inner-exists-m-t2 |
                                                exists mm: Int; exists tt: Seq; is_nonneg(lprev) and l = succ(lprev) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lprev) and (forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lprev) -> at(tt, i) = at(t, succ(i)))
                                                [by hypothesis with-predecessor-lprev]
                                              @with-matched-m |
                                                unpack m: Int from inner-exists-m-t2 {
                                                  @inner-exists-t2 |
                                                    exists tt: Seq; is_nonneg(lprev) and l = succ(lprev) and is_nonneg(m) and less_than(m, l) and at(s, k) = at(t, m) and productUpTo(s, k) = productUpTo(tt, lprev) and (forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, m) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(m, i) -> less_than(i, lprev) -> at(tt, i) = at(t, succ(i)))
                                                    [by hypothesis with-matched-m]
                                                  @with-removed-t2 |
                                                    unpack t2: Seq from inner-exists-t2 {
                                                      @peel-facts |
                                                        is_nonneg(lprev) and l = succ(lprev) and is_nonneg(m) and less_than(m, l) and at(s, k) = at(t, m) and productUpTo(s, k) = productUpTo(t2, lprev) and (forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, m) -> at(t2, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(m, i) -> less_than(i, lprev) -> at(t2, i) = at(t, succ(i)))
                                                        [by hypothesis with-removed-t2]
                                                      @lprev-nonneg |
                                                        is_nonneg(lprev)
                                                        [by tautology peel-facts]
                                                      @l-is-succ-lprev |
                                                        l = succ(lprev)
                                                        [by tautology peel-facts]
                                                      @s-product-equals-t2-product |
                                                        productUpTo(s, k) = productUpTo(t2, lprev)
                                                        [by tautology peel-facts]
                                                      @t2-primes-below-lprev |
                                                        forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j))))
                                                        [by tautology peel-facts]
                                                      @succ-lprev-is-l |
                                                        succ(lprev) = l
                                                        [by symmetry l-is-succ-lprev]

                                                      // s's entries below k are prime (restrict the below-succ-k hypothesis).
                                                      @below-successor-lemma |
                                                        forall k2: Int; less_than(k2, succ(k2))
                                                        [by theorem lessThanSuccSelf]
                                                      @k-below-succ-k |
                                                        less_than(k, succ(k))
                                                        [by specialize lessThanSuccSelf(k)]
                                                      @less_than-is-transitive |
                                                        forall a, b, c: Int; less_than(a, b) -> less_than(b, c) -> less_than(a, c)
                                                        [by theorem lessThanTransitive]
                                                      @s-primes-below-k-proof |
                                                        fix i: Int {
                                                          @given-i-nonneg |
                                                            assume is_nonneg(i) {
                                                              @given-i-below-k |
                                                                assume less_than(i, k) {
                                                                  @i-nonneg |
                                                                    is_nonneg(i)
                                                                    [by hypothesis given-i-nonneg]
                                                                  @i-below-k |
                                                                    less_than(i, k)
                                                                    [by hypothesis given-i-below-k]
                                                                  @i-below-succ-k-conditions |
                                                                    less_than(i, k) -> less_than(k, succ(k)) -> less_than(i, succ(k))
                                                                    [by forall_elim(i, k, succ(k)) less_than-is-transitive]
                                                                  @i-below-succ-k |
                                                                    less_than(i, succ(k))
                                                                    [by tautology i-below-succ-k-conditions i-below-k k-below-succ-k]
                                                                  @si-prime-conditions |
                                                                    is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                                                    [by forall_elim(i) s-primes-below-succ-k]
                                                                  @si-prime-below-k |
                                                                    less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))
                                                                    [by tautology si-prime-conditions i-nonneg i-below-succ-k]
                                                                }
                                                              @i-below-k-gives-prime |
                                                                less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                                                [by implies_intro given-i-below-k]
                                                            }
                                                          @i-nonneg-gives-prime |
                                                            is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                                            [by implies_intro given-i-nonneg]
                                                        }
                                                      @s-primes-below-k |
                                                        forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                                        [by forall_intro s-primes-below-k-proof]

                                                      // apply the inductive hypothesis to s, t2, lprev: k = lprev.
                                                      @ih-at-s |
                                                        forall t3: Seq; forall l2: Int; is_nonneg(l2) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l2) -> (less_than(ONE, at(t3, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t3, j)) -> (d = ONE or d = at(t3, j))))) -> productUpTo(s, k) = productUpTo(t3, l2) -> k = l2
                                                        [by forall_elim(s) inductive-hypothesis]
                                                      @ih-at-s-t2-lprev |
                                                        is_nonneg(lprev) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j))))) -> productUpTo(s, k) = productUpTo(t2, lprev) -> k = lprev
                                                        [by forall_elim(t2, lprev) ih-at-s]
                                                      @k-equals-lprev |
                                                        k = lprev
                                                        [by tautology ih-at-s-t2-lprev lprev-nonneg s-primes-below-k t2-primes-below-lprev s-product-equals-t2-product]

                                                      // succ(k) = succ(lprev) = l.
                                                      @succ-k-equals-l |
                                                        succ(k) = l
                                                        [by chain k-equals-lprev succ-lprev-is-l]
                                                    }
                                                  @conclusion-succ-k-equals-l-from-removal |
                                                    succ(k) = l
                                                    [by exists_elim with-removed-t2]
                                                }
                                              @conclusion-succ-k-equals-l-from-matched |
                                                succ(k) = l
                                                [by exists_elim with-matched-m]
                                            }
                                          @conclusion-succ-k-equals-l |
                                            succ(k) = l
                                            [by exists_elim with-predecessor-lprev]
                                        }
                                      @equal-products-give-step |
                                        productUpTo(s, succ(k)) = productUpTo(t, l) -> succ(k) = l
                                        [by implies_intro given-equal-products-step]
                                    }
                                  @t-primes-give-step |
                                    (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> succ(k) = l
                                    [by implies_intro given-t-primes-step]
                                }
                              @s-primes-give-step |
                                (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> succ(k) = l
                                [by implies_intro given-s-primes-step]
                            }
                          @l-nonneg-gives-step |
                            is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> succ(k) = l
                            [by implies_intro given-l-nonneg-step]
                        }
                      @discharge-l-step |
                        forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> succ(k) = l
                        [by forall_intro generalize-l-step]
                    }
                  @discharge-t-step |
                    forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> succ(k) = l
                    [by forall_intro generalize-t-step]
                }
              @p-succ-k |
                forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> succ(k) = l
                [by forall_intro successor-case-proof]
            }
          @inductive-hypothesis-gives-p-succ-k |
            (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> k = l) -> (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> succ(k) = l)
            [by implies_intro given-inductive-hypothesis]
        }
      @induction-step-at-k |
        is_nonneg(k) -> (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> k = l) -> (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> succ(k) = l)
        [by implies_intro given-k-nonneg]
    }
  @induction-step-for-all-k |
    forall k: Int; is_nonneg(k) -> (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> k = l) -> (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> succ(k) = l)
    [by forall_intro induction-step]

  @conclusion |
    forall k: Int; is_nonneg(k) ->
      forall s: Seq; forall t: Seq; forall l: Int;
      is_nonneg(l)
      -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
      -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))))
      -> productUpTo(s, k) = productUpTo(t, l)
      -> k = l
    [by instantiate nonnegInduction((fun k: Int => forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> k = l)) base-case induction-step-for-all-k]
qed
```

The injectivity of the index map needs one small arithmetic fact: `succ` is
injective — `succ(a) = succ(b)` forces `a = b` — since `prev` inverts `succ`
(`prevSucc`). Apply `prev` to both sides and collapse each `prev(succ(·))`.

```bpa
theorem succInjective: forall a, b: Int; succ(a) = succ(b) -> a = b
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @given-succ-a-equals-succ-b |
            assume succ(a) = succ(b) {
              @succ-a-equals-succ-b |
                succ(a) = succ(b)
                [by hypothesis given-succ-a-equals-succ-b]
              @prev-succ-a-is-a |
                prev(succ(a)) = a
                [by specialize prevSucc(a)]
              @prev-succ-b-is-b |
                prev(succ(b)) = b
                [by specialize prevSucc(b)]
              // a = prev(succ a) = prev(succ b) = b.
              @conclusion-a-equals-b |
                a = b
                [by chain prev-succ-a-is-a succ-a-equals-succ-b prev-succ-b-is-b]
            }
          @succ-equal-implies-equal |
            succ(a) = succ(b) -> a = b
            [by implies_intro given-succ-a-equals-succ-b]
        }
      @discharge-b |
        forall b: Int; succ(a) = succ(b) -> a = b
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b: Int; succ(a) = succ(b) -> a = b
    [by forall_intro generalize-a]
qed
```

Two pure-integer facts about the `unremove` reindex `c ↦ (c < m ? c : succ c)`
used throughout the permutation induction below, extracted so the three
σ-clauses (range, injectivity, match) cite them instead of re-deriving. First,
the trichotomy on a value versus the removal threshold `m` only ever needs its
two collapsed branches `v < m` or `m ≤ v`:

```bpa
theorem lessThanOrAtMost: forall a, b: Int; less_than(a, b) or at_most(b, a)
proof
  @order-is-trichotomous |
    forall c, d: Int; less_than(c, d) or c = d or less_than(d, c)
    [by theorem trichotomy]
  @at-most-is-reflexive |
    forall c: Int; at_most(c, c)
    [by theorem lessOrEqualRefl]
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @a-vs-b |
            less_than(a, b) or a = b or less_than(b, a)
            [by specialize trichotomy(a, b)]
          @a-below-b-or-b-at-most-a |
            less_than(a, b) or at_most(b, a)
            case a-vs-b {
              @when-a-below-b |
                assume less_than(a, b) {
                  @a-below-b-hypothesis |
                    less_than(a, b)
                    [by hypothesis when-a-below-b]
                  @a-below-b-on-the-left |
                    less_than(a, b) or at_most(b, a)
                    [by or_intro_left a-below-b-hypothesis]
                }
              @when-a-equals-b |
                assume a = b {
                  @a-equals-b-hypothesis |
                    a = b
                    [by hypothesis when-a-equals-b]
                  @b-at-most-b |
                    at_most(b, b)
                    [by specialize lessOrEqualRefl(b)]
                  @b-is-a |
                    b = a
                    [by symmetry a-equals-b-hypothesis]
                  @b-at-most-a |
                    at_most(b, a)
                    [by rewrite b-is-a b-at-most-b]
                  @equal-case-on-the-right |
                    less_than(a, b) or at_most(b, a)
                    [by or_intro_right b-at-most-a]
                }
              @when-b-below-a |
                assume less_than(b, a) {
                  @b-below-a-hypothesis |
                    less_than(b, a)
                    [by hypothesis when-b-below-a]
                  @b-at-most-a-from-strict |
                    at_most(b, a)
                    [by specialize lessThanToLessOrEqual(b, a) b-below-a-hypothesis]
                  @b-below-a-on-the-right |
                    less_than(a, b) or at_most(b, a)
                    [by or_intro_right b-at-most-a-from-strict]
                }
            }
        }
      @discharge-b |
        forall b: Int; less_than(a, b) or at_most(b, a)
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b: Int; less_than(a, b) or at_most(b, a)
    [by forall_intro generalize-a]
qed
```

Second, when a value `b` lies at or above the threshold `a`, its `unremove`
image `succ(b)` overshoots `a` strictly (`a ≤ b < succ b`) — this is why a
shifted tail entry can never equal, and never falls below, the threshold `m`:

```bpa
theorem atMostThenBelowSucc: forall a, b: Int; at_most(a, b) -> less_than(a, succ(b))
proof
  @at-most-then-below |
    forall x, y, z: Int; at_most(x, y) -> less_than(y, z) -> less_than(x, z)
    [by theorem atMostThenLessThan]
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @given-a-at-most-b |
            assume at_most(a, b) {
              @a-at-most-b |
                at_most(a, b)
                [by hypothesis given-a-at-most-b]
              @b-below-its-successor |
                less_than(b, succ(b))
                [by specialize lessThanSuccSelf(b)]
              @a-below-succ-b-conditions |
                at_most(a, b) -> less_than(b, succ(b)) -> less_than(a, succ(b))
                [by forall_elim(a, b, succ(b)) at-most-then-below]
              @a-below-succ-b |
                less_than(a, succ(b))
                [by tautology a-below-succ-b-conditions a-at-most-b b-below-its-successor]
            }
          @a-at-most-b-implies-below-succ |
            at_most(a, b) -> less_than(a, succ(b))
            [by implies_intro given-a-at-most-b]
        }
      @discharge-b |
        forall b: Int; at_most(a, b) -> less_than(a, succ(b))
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b: Int; at_most(a, b) -> less_than(a, succ(b))
    [by forall_intro generalize-a]
qed
```

The permutation itself. We induct on the length `k` of `s`, building the index
map σ explicitly. **Base (`k = 0`):** all three σ-clauses (range, injectivity,
match) quantify over `i < 0`, so they hold vacuously for any witness sequence.
**Step (`P(k) ⇒ P(succ k)`):** exactly as in `factorizationLengthUnique`, the
last factor `at(s, k)` matches some prime `at(t, m)`; remove `m` from `t` to get
`t2` of length `lprev = l − 1` with `productUpTo(s, k) = productUpTo(t2, lprev)`,
whose entries stay prime. The inductive hypothesis yields an index map `sigma2`
matching `s`'s first `k` entries into `t2`. To lift `sigma2` from `t2`'s index
space back to `t`'s, we compose it with the *inverse* of the removal — the
`unremove` map `c ↦ (c < m ? c : succ c)` — realized as a Seq `r` by
`seqShiftPastExists` (shift every value `≥ m` up by one). Then σ is `r` with `m`
appended at index `k` (`seqConcatExists` of `r` with the singleton `[m]`):
index `i < k ↦ unremove(at(sigma2, i))` (matching `at(s, i)` through the
removal-agreement clauses), and index `k ↦ m` (matching `at(s, k) = at(t, m)`).
An index `i < succ k` splits (`lessThanSuccSplit`) into `i < k` (the reindexed
`sigma2` part) or `i = k` (the appended `m`). Range holds because `unremove`
maps `{0..lprev−1}` into `{0..l−1}\{m}` and `m < l`; injectivity because
`unremove` is injective, never hits `m`, and `sigma2` is injective.

```bpa
theorem factorizationPermutationExists: forall k: Int; is_nonneg(k) ->
  forall s: Seq; forall t: Seq; forall l: Int;
  is_nonneg(l)
  -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
  -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))))
  -> productUpTo(s, k) = productUpTo(t, l)
  -> (exists sigma: Seq;
  (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l)))
  and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2)
  and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i))))
proof
  // base case: k = 0. All three sigma-clauses are vacuous (nothing nonneg is
  // less_than ZERO), so ANY sequence works as the witness sigma.
  @base-case-proof |
    fix s: Seq {
      @generalize-t-base |
        fix t: Seq {
          @generalize-l-base |
            fix l: Int {
              @given-l-nonneg-base |
                assume is_nonneg(l) {
                  @given-s-primes-base |
                    assume forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))) {
                      @given-t-primes-base |
                        assume forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))) {
                          @given-equal-products-base |
                            assume productUpTo(s, ZERO) = productUpTo(t, l) {
                              @nothing-nonneg-below-zero |
                                forall n: Int; is_nonneg(n) -> (not less_than(n, ZERO))
                                [by theorem nonnegNotLessThanZero]

                              // a witness sequence exists (the singleton [ZERO]).
                              @some-singleton-exists |
                                exists sing: Seq; at(sing, ZERO) = ZERO and productUpTo(sing, ONE) = ZERO
                                [by specialize seqSingletonExists(ZERO)]
                              @with-witness-sigma |
                                unpack sing: Seq from some-singleton-exists {
                                  // RANGE: vacuous — no nonneg i is less_than ZERO.
                                  @range-proof |
                                    fix i: Int {
                                      @given-i-nonneg-range |
                                        assume is_nonneg(i) {
                                          @given-i-below-zero-range |
                                            assume less_than(i, ZERO) {
                                              @i-nonneg-range |
                                                is_nonneg(i)
                                                [by hypothesis given-i-nonneg-range]
                                              @i-below-zero-range |
                                                less_than(i, ZERO)
                                                [by hypothesis given-i-below-zero-range]
                                              @i-not-below-zero-range |
                                                not less_than(i, ZERO)
                                                [by specialize nonnegNotLessThanZero(i) i-nonneg-range]
                                              @range-vacuous |
                                                is_nonneg(at(sing, i)) and less_than(at(sing, i), l)
                                                [by absurd i-below-zero-range i-not-below-zero-range]
                                            }
                                          @i-below-zero-gives-range |
                                            less_than(i, ZERO) -> (is_nonneg(at(sing, i)) and less_than(at(sing, i), l))
                                            [by implies_intro given-i-below-zero-range]
                                        }
                                      @i-nonneg-gives-range |
                                        is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sing, i)) and less_than(at(sing, i), l))
                                        [by implies_intro given-i-nonneg-range]
                                    }
                                  @sigma-range |
                                    forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sing, i)) and less_than(at(sing, i), l))
                                    [by forall_intro range-proof]

                                  // INJECTIVE: vacuous — no nonneg i is less_than ZERO.
                                  @injective-proof |
                                    fix i: Int {
                                      @generalize-i2-inj |
                                        fix i2: Int {
                                          @given-i-nonneg-inj |
                                            assume is_nonneg(i) {
                                              @given-i-below-zero-inj |
                                                assume less_than(i, ZERO) {
                                                  @i-nonneg-inj |
                                                    is_nonneg(i)
                                                    [by hypothesis given-i-nonneg-inj]
                                                  @i-below-zero-inj |
                                                    less_than(i, ZERO)
                                                    [by hypothesis given-i-below-zero-inj]
                                                  @i-not-below-zero-inj |
                                                    not less_than(i, ZERO)
                                                    [by specialize nonnegNotLessThanZero(i) i-nonneg-inj]
                                                  @injective-vacuous |
                                                    is_nonneg(i2) -> less_than(i2, ZERO) -> at(sing, i) = at(sing, i2) -> i = i2
                                                    [by absurd i-below-zero-inj i-not-below-zero-inj]
                                                }
                                              @i-below-zero-gives-inj |
                                                less_than(i, ZERO) -> (is_nonneg(i2) -> less_than(i2, ZERO) -> at(sing, i) = at(sing, i2) -> i = i2)
                                                [by implies_intro given-i-below-zero-inj]
                                            }
                                          @i-nonneg-gives-inj |
                                            is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sing, i) = at(sing, i2) -> i = i2
                                            [by implies_intro given-i-nonneg-inj]
                                        }
                                      @discharge-i2-inj |
                                        forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sing, i) = at(sing, i2) -> i = i2
                                        [by forall_intro generalize-i2-inj]
                                    }
                                  @sigma-injective |
                                    forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sing, i) = at(sing, i2) -> i = i2
                                    [by forall_intro injective-proof]

                                  // MATCH: vacuous — no nonneg i is less_than ZERO.
                                  @match-proof |
                                    fix i: Int {
                                      @given-i-nonneg-match |
                                        assume is_nonneg(i) {
                                          @given-i-below-zero-match |
                                            assume less_than(i, ZERO) {
                                              @i-nonneg-match |
                                                is_nonneg(i)
                                                [by hypothesis given-i-nonneg-match]
                                              @i-below-zero-match |
                                                less_than(i, ZERO)
                                                [by hypothesis given-i-below-zero-match]
                                              @i-not-below-zero-match |
                                                not less_than(i, ZERO)
                                                [by specialize nonnegNotLessThanZero(i) i-nonneg-match]
                                              @match-vacuous |
                                                at(s, i) = at(t, at(sing, i))
                                                [by absurd i-below-zero-match i-not-below-zero-match]
                                            }
                                          @i-below-zero-gives-match |
                                            less_than(i, ZERO) -> at(s, i) = at(t, at(sing, i))
                                            [by implies_intro given-i-below-zero-match]
                                        }
                                      @i-nonneg-gives-match |
                                        is_nonneg(i) -> less_than(i, ZERO) -> at(s, i) = at(t, at(sing, i))
                                        [by implies_intro given-i-nonneg-match]
                                    }
                                  @sigma-match |
                                    forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> at(s, i) = at(t, at(sing, i))
                                    [by forall_intro match-proof]

                                  @sigma-range-and-injective |
                                    (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sing, i)) and less_than(at(sing, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sing, i) = at(sing, i2) -> i = i2)
                                    [by and_intro sigma-range sigma-injective]
                                  @sigma-clauses |
                                    (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sing, i)) and less_than(at(sing, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sing, i) = at(sing, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> at(s, i) = at(t, at(sing, i)))
                                    [by and_intro sigma-range-and-injective sigma-match]
                                  @sigma-witnesses-base |
                                    exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> at(s, i) = at(t, at(sigma, i)))
                                    [by exists_intro(sing) sigma-clauses]
                                }
                              @sigma-exists-base |
                                exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> at(s, i) = at(t, at(sigma, i)))
                                [by exists_elim with-witness-sigma]
                            }
                          @equal-products-give-base |
                            productUpTo(s, ZERO) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> at(s, i) = at(t, at(sigma, i))))
                            [by implies_intro given-equal-products-base]
                        }
                      @t-primes-give-base |
                        (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, ZERO) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> at(s, i) = at(t, at(sigma, i))))
                        [by implies_intro given-t-primes-base]
                    }
                  @s-primes-give-base |
                    (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, ZERO) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> at(s, i) = at(t, at(sigma, i))))
                    [by implies_intro given-s-primes-base]
                }
              @l-nonneg-gives-base |
                is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, ZERO) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> at(s, i) = at(t, at(sigma, i))))
                [by implies_intro given-l-nonneg-base]
            }
          @discharge-l-base |
            forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, ZERO) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> at(s, i) = at(t, at(sigma, i))))
            [by forall_intro generalize-l-base]
        }
      @discharge-t-base |
        forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, ZERO) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> at(s, i) = at(t, at(sigma, i))))
        [by forall_intro generalize-t-base]
    }
  @base-case |
    forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, ZERO) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, ZERO) -> is_nonneg(i2) -> less_than(i2, ZERO) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, ZERO) -> at(s, i) = at(t, at(sigma, i))))
    [by forall_intro base-case-proof]

  @induction-step |
    fix k: Int {
      @given-k-nonneg |
        assume is_nonneg(k) {
          @k-nonneg |
            is_nonneg(k)
            [by hypothesis given-k-nonneg]
          @given-inductive-hypothesis |
            assume forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i)))) {
              @inductive-hypothesis |
                forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i))))
                [by hypothesis given-inductive-hypothesis]

              // prove P(succ k): fix s, t, l with the primality/product hypotheses at length succ k.
              @successor-case-proof |
                fix s: Seq {
                  @generalize-t-step |
                    fix t: Seq {
                      @generalize-l-step |
                        fix l: Int {
                          @given-l-nonneg-step |
                            assume is_nonneg(l) {
                              @given-s-primes-step |
                                assume forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))) {
                                  @given-t-primes-step |
                                    assume forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))) {
                                      @given-equal-products-step |
                                        assume productUpTo(s, succ(k)) = productUpTo(t, l) {
                                          @l-nonneg |
                                            is_nonneg(l)
                                            [by hypothesis given-l-nonneg-step]
                                          @s-primes-below-succ-k |
                                            forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                            [by hypothesis given-s-primes-step]
                                          @t-primes-below-l |
                                            forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))
                                            [by hypothesis given-t-primes-step]
                                          @equal-products |
                                            productUpTo(s, succ(k)) = productUpTo(t, l)
                                            [by hypothesis given-equal-products-step]

                                          // peel the last factor at(s,k): match it to at(t,m), remove m from t to get t2.
                                          @peel-step |
                                            forall kfix: Int; is_nonneg(kfix) -> (forall s0: Seq; forall t0: Seq; forall l0: Int; is_nonneg(l0) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(kfix)) -> (less_than(ONE, at(s0, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s0, i)) -> (d = ONE or d = at(s0, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l0) -> (less_than(ONE, at(t0, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t0, j)) -> (d = ONE or d = at(t0, j))))) -> productUpTo(s0, succ(kfix)) = productUpTo(t0, l0) -> (exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l0 = succ(lp) and is_nonneg(mm) and less_than(mm, l0) and at(s0, kfix) = at(t0, mm) and productUpTo(s0, kfix) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t0, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t0, succ(i)))))
                                            [by theorem factorizationPeelStep]
                                          // apply the shared peel step at all four binders in one
                                          // interleaved specialize (k↦∀k, k-nonneg↦guard, then s,t,l
                                          // ↦∀s,t,l and the four antecedents).
                                          @peeled-exists |
                                            exists lp: Int; exists mm: Int; exists tt: Seq; is_nonneg(lp) and l = succ(lp) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lp) and (forall j: Int; is_nonneg(j) -> less_than(j, lp) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lp) -> at(tt, i) = at(t, succ(i)))
                                            [by specialize factorizationPeelStep(k, s, t, l) k-nonneg l-nonneg s-primes-below-succ-k t-primes-below-l equal-products]

                                          @with-predecessor-lprev |
                                            unpack lprev: Int from peeled-exists {
                                              @inner-exists-m-t2 |
                                                exists mm: Int; exists tt: Seq; is_nonneg(lprev) and l = succ(lprev) and is_nonneg(mm) and less_than(mm, l) and at(s, k) = at(t, mm) and productUpTo(s, k) = productUpTo(tt, lprev) and (forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, mm) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(mm, i) -> less_than(i, lprev) -> at(tt, i) = at(t, succ(i)))
                                                [by hypothesis with-predecessor-lprev]
                                              @with-matched-m |
                                                unpack m: Int from inner-exists-m-t2 {
                                                  @inner-exists-t2 |
                                                    exists tt: Seq; is_nonneg(lprev) and l = succ(lprev) and is_nonneg(m) and less_than(m, l) and at(s, k) = at(t, m) and productUpTo(s, k) = productUpTo(tt, lprev) and (forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(tt, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(tt, j)) -> (d = ONE or d = at(tt, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, m) -> at(tt, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(m, i) -> less_than(i, lprev) -> at(tt, i) = at(t, succ(i)))
                                                    [by hypothesis with-matched-m]
                                                  @with-removed-t2 |
                                                    unpack t2: Seq from inner-exists-t2 {
                                                      @peel-facts |
                                                        is_nonneg(lprev) and l = succ(lprev) and is_nonneg(m) and less_than(m, l) and at(s, k) = at(t, m) and productUpTo(s, k) = productUpTo(t2, lprev) and (forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j))))) and (forall i: Int; is_nonneg(i) -> less_than(i, m) -> at(t2, i) = at(t, i)) and (forall i: Int; is_nonneg(i) -> at_most(m, i) -> less_than(i, lprev) -> at(t2, i) = at(t, succ(i)))
                                                        [by hypothesis with-removed-t2]
                                                      @lprev-nonneg |
                                                        is_nonneg(lprev)
                                                        [by tautology peel-facts]
                                                      @l-is-succ-lprev |
                                                        l = succ(lprev)
                                                        [by tautology peel-facts]
                                                      @m-nonneg |
                                                        is_nonneg(m)
                                                        [by tautology peel-facts]
                                                      @m-below-l |
                                                        less_than(m, l)
                                                        [by tautology peel-facts]
                                                      @sk-equals-tm |
                                                        at(s, k) = at(t, m)
                                                        [by tautology peel-facts]
                                                      @s-product-equals-t2-product |
                                                        productUpTo(s, k) = productUpTo(t2, lprev)
                                                        [by tautology peel-facts]
                                                      @t2-primes-below-lprev |
                                                        forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j))))
                                                        [by tautology peel-facts]
                                                      @t2-agrees-below-m |
                                                        forall i: Int; is_nonneg(i) -> less_than(i, m) -> at(t2, i) = at(t, i)
                                                        [by tautology peel-facts]
                                                      @t2-agrees-above-m |
                                                        forall i: Int; is_nonneg(i) -> at_most(m, i) -> less_than(i, lprev) -> at(t2, i) = at(t, succ(i))
                                                        [by tautology peel-facts]
                                                      @succ-lprev-is-l |
                                                        succ(lprev) = l
                                                        [by symmetry l-is-succ-lprev]

                                                      // facts for restricting s's primes below k and reindexing sigma.
                                                      @below-successor-lemma |
                                                        forall k2: Int; less_than(k2, succ(k2))
                                                        [by theorem lessThanSuccSelf]
                                                      @k-below-succ-k |
                                                        less_than(k, succ(k))
                                                        [by specialize lessThanSuccSelf(k)]
                                                      @less_than-is-transitive |
                                                        forall a, b, c: Int; less_than(a, b) -> less_than(b, c) -> less_than(a, c)
                                                        [by theorem lessThanTransitive]
                                                      // s's entries below k are prime (restrict the below-succ-k hypothesis).
                                                      @s-primes-below-k-proof |
                                                        fix i: Int {
                                                          @given-i-nonneg |
                                                            assume is_nonneg(i) {
                                                              @given-i-below-k |
                                                                assume less_than(i, k) {
                                                                  @i-nonneg |
                                                                    is_nonneg(i)
                                                                    [by hypothesis given-i-nonneg]
                                                                  @i-below-k |
                                                                    less_than(i, k)
                                                                    [by hypothesis given-i-below-k]
                                                                  @i-below-succ-k-conditions |
                                                                    less_than(i, k) -> less_than(k, succ(k)) -> less_than(i, succ(k))
                                                                    [by forall_elim(i, k, succ(k)) less_than-is-transitive]
                                                                  @i-below-succ-k |
                                                                    less_than(i, succ(k))
                                                                    [by tautology i-below-succ-k-conditions i-below-k k-below-succ-k]
                                                                  @si-prime-conditions |
                                                                    is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                                                    [by forall_elim(i) s-primes-below-succ-k]
                                                                  @si-prime-below-k |
                                                                    less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))
                                                                    [by tautology si-prime-conditions i-nonneg i-below-succ-k]
                                                                }
                                                              @i-below-k-gives-prime |
                                                                less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                                                [by implies_intro given-i-below-k]
                                                            }
                                                          @i-nonneg-gives-prime |
                                                            is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                                            [by implies_intro given-i-nonneg]
                                                        }
                                                      @s-primes-below-k |
                                                        forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                                        [by forall_intro s-primes-below-k-proof]

                                                      // apply the inductive hypothesis to s, t2, lprev: an index map sigma2
                                                      // matching s's first k entries into t2 (length lprev).
                                                      @ih-at-s |
                                                        forall t3: Seq; forall l2: Int; is_nonneg(l2) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l2) -> (less_than(ONE, at(t3, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t3, j)) -> (d = ONE or d = at(t3, j))))) -> productUpTo(s, k) = productUpTo(t3, l2) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l2))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t3, at(sigma, i))))
                                                        [by forall_elim(s) inductive-hypothesis]
                                                      @ih-at-s-t2-lprev |
                                                        is_nonneg(lprev) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, lprev) -> (less_than(ONE, at(t2, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t2, j)) -> (d = ONE or d = at(t2, j))))) -> productUpTo(s, k) = productUpTo(t2, lprev) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), lprev))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t2, at(sigma, i))))
                                                        [by forall_elim(t2, lprev) ih-at-s]
                                                      @sigma2-exists |
                                                        exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), lprev))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t2, at(sigma, i)))
                                                        [by tautology ih-at-s-t2-lprev lprev-nonneg s-primes-below-k t2-primes-below-lprev s-product-equals-t2-product]

                                                      @with-index-map-sigma2 |
                                                        unpack sigma2: Seq from sigma2-exists {
                                                          @sigma2-clauses |
                                                            (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma2, i)) and less_than(at(sigma2, i), lprev))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma2, i) = at(sigma2, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t2, at(sigma2, i)))
                                                            [by hypothesis with-index-map-sigma2]
                                                          @sigma2-range |
                                                            forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma2, i)) and less_than(at(sigma2, i), lprev))
                                                            [by tautology sigma2-clauses]
                                                          @sigma2-injective |
                                                            forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma2, i) = at(sigma2, i2) -> i = i2
                                                            [by tautology sigma2-clauses]
                                                          @sigma2-match |
                                                            forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t2, at(sigma2, i))
                                                            [by tautology sigma2-clauses]

                                                          // reindex sigma2 through the inverse of the removal: r bumps every value
                                                          // >= m up by one (values < m are copied). r = unremove . sigma2.
                                                          @reindexed-exists |
                                                            exists r: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> less_than(at(sigma2, i), m) -> at(r, i) = at(sigma2, i)) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at_most(m, at(sigma2, i)) -> at(r, i) = succ(at(sigma2, i)))
                                                            [by specialize seqShiftPastExists(sigma2, k, m) k-nonneg m-nonneg]

                                                          @with-reindexed-r |
                                                            unpack r: Seq from reindexed-exists {
                                                              @r-clauses |
                                                                (forall i: Int; is_nonneg(i) -> less_than(i, k) -> less_than(at(sigma2, i), m) -> at(r, i) = at(sigma2, i)) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at_most(m, at(sigma2, i)) -> at(r, i) = succ(at(sigma2, i)))
                                                                [by hypothesis with-reindexed-r]
                                                              @r-copies-below-m |
                                                                forall i: Int; is_nonneg(i) -> less_than(i, k) -> less_than(at(sigma2, i), m) -> at(r, i) = at(sigma2, i)
                                                                [by tautology r-clauses]
                                                              @r-shifts-above-m |
                                                                forall i: Int; is_nonneg(i) -> less_than(i, k) -> at_most(m, at(sigma2, i)) -> at(r, i) = succ(at(sigma2, i))
                                                                [by tautology r-clauses]

                                                              // sigma = r ++ [m]: append the matched index m at position k.
                                                              @singleton-m-at-m |
                                                                exists sm: Seq; at(sm, ZERO) = m and productUpTo(sm, ONE) = m
                                                                [by specialize seqSingletonExists(m)]
                                                              @with-singleton-m |
                                                                unpack sm: Seq from singleton-m-at-m {
                                                                  @singleton-m-clauses |
                                                                    at(sm, ZERO) = m and productUpTo(sm, ONE) = m
                                                                    [by hypothesis with-singleton-m]
                                                                  @singleton-head-is-m |
                                                                    at(sm, ZERO) = m
                                                                    [by and_elim_left singleton-m-clauses]
                                                                  @one-is-succ-zero |
                                                                    ONE = succ(ZERO)
                                                                    [by axiom oneIsSuccZero]
                                                                  @zero-nonneg |
                                                                    is_nonneg(ZERO)
                                                                    [by axiom nonnegZero]
                                                                  @succ-zero-nonneg |
                                                                    is_nonneg(succ(ZERO))
                                                                    [by specialize nonnegSucc(ZERO) zero-nonneg]
                                                                  @succ-zero-is-one |
                                                                    succ(ZERO) = ONE
                                                                    [by symmetry one-is-succ-zero]
                                                                  @one-nonneg |
                                                                    is_nonneg(ONE)
                                                                    [by rewrite succ-zero-is-one succ-zero-nonneg]
                                                                  @appended-exists |
                                                                    exists u: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(u, i) = at(r, i)) and (forall i: Int; is_nonneg(i) -> less_than(i, ONE) -> at(u, add(k, i)) = at(sm, i)) and productUpTo(u, add(k, ONE)) = mul(productUpTo(r, k), productUpTo(sm, ONE))
                                                                    [by specialize seqConcatExists(r, sm, k, ONE) k-nonneg one-nonneg]

                                                                  @with-permutation-sigma |
                                                                    unpack sigmaf: Seq from appended-exists {
                                                                      @sigma-concat-clauses |
                                                                        (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(sigmaf, i) = at(r, i)) and (forall i: Int; is_nonneg(i) -> less_than(i, ONE) -> at(sigmaf, add(k, i)) = at(sm, i)) and productUpTo(sigmaf, add(k, ONE)) = mul(productUpTo(r, k), productUpTo(sm, ONE))
                                                                        [by hypothesis with-permutation-sigma]
                                                                      @sigma-copies-r-below-k |
                                                                        forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(sigmaf, i) = at(r, i)
                                                                        [by tautology sigma-concat-clauses]
                                                                      @sigma-append-clause |
                                                                        forall i: Int; is_nonneg(i) -> less_than(i, ONE) -> at(sigmaf, add(k, i)) = at(sm, i)
                                                                        [by tautology sigma-concat-clauses]
                                                                      // pin at(sigmaf, k) = m.
                                                                      @zero-below-one-append |
                                                                        less_than(ZERO, ONE)
                                                                        [by theorem zeroLessThanOne]
                                                                      @sigma-at-k-zero-conditions |
                                                                        is_nonneg(ZERO) -> less_than(ZERO, ONE) -> at(sigmaf, add(k, ZERO)) = at(sm, ZERO)
                                                                        [by forall_elim(ZERO) sigma-append-clause]
                                                                      @sigma-at-k-plus-zero-is-singleton |
                                                                        at(sigmaf, add(k, ZERO)) = at(sm, ZERO)
                                                                        [by tautology sigma-at-k-zero-conditions zero-nonneg zero-below-one-append]
                                                                      @add-k-zero-is-k |
                                                                        add(k, ZERO) = k
                                                                        [by specialize addZeroRight(k)]
                                                                      // σ(k) = σ(k+0) = sm(0) = m.
                                                                      @sigma-at-k-is-m |
                                                                        at(sigmaf, k) = m
                                                                        [by chain add-k-zero-is-k sigma-at-k-plus-zero-is-singleton singleton-head-is-m]

                                                                      // shared facts for verifying sigma's three clauses over indices < succ(k).
                                                                      @add-zero-right |
                                                                        forall n: Int; add(n, ZERO) = n
                                                                        [by theorem addZeroRight]
                                                                      @succ-preserves-order-sigma |
                                                                        forall a, b: Int; less_than(a, b) -> less_than(succ(a), succ(b))
                                                                        [by theorem succMonotone]
                                                                      @successor-of-nonneg-sigma |
                                                                        forall n: Int; is_nonneg(n) -> is_nonneg(succ(n))
                                                                        [by axiom nonnegSucc]
                                                                      @less_than-is-transitive-sigma |
                                                                        forall a, b, c: Int; less_than(a, b) -> less_than(b, c) -> less_than(a, c)
                                                                        [by theorem lessThanTransitive]
                                                                      @less_than-is-irreflexive-sigma |
                                                                        forall a: Int; not less_than(a, a)
                                                                        [by theorem lessThanIrreflexive]
                                                                      @less-than-or-at-most |
                                                                        forall a, b: Int; less_than(a, b) or at_most(b, a)
                                                                        [by theorem lessThanOrAtMost]
                                                                      @at-most-then-below-succ |
                                                                        forall a, b: Int; at_most(a, b) -> less_than(a, succ(b))
                                                                        [by theorem atMostThenBelowSucc]
                                                                      @m-below-l-copy |
                                                                        less_than(m, l)
                                                                        [by tautology m-below-l]

                                                                      // MATCH: at(s, i) = at(t, at(sigmaf, i)) for i < succ(k).
                                                                      @sigma-match-succ-proof |
                                                                        fix i: Int {
                                                                          @given-i-nonneg-match |
                                                                            assume is_nonneg(i) {
                                                                              @given-i-below-succ-k-match |
                                                                                assume less_than(i, succ(k)) {
                                                                                  @i-nonneg-match |
                                                                                    is_nonneg(i)
                                                                                    [by hypothesis given-i-nonneg-match]
                                                                                  @i-below-succ-k-match |
                                                                                    less_than(i, succ(k))
                                                                                    [by hypothesis given-i-below-succ-k-match]
                                                                                  @i-below-k-or-equal-match |
                                                                                    less_than(i, k) or i = k
                                                                                    [by specialize lessThanSuccSplit(i, k) i-nonneg-match k-nonneg i-below-succ-k-match]
                                                                                  @s-at-i-matches |
                                                                                    at(s, i) = at(t, at(sigmaf, i))
                                                                                    case i-below-k-or-equal-match {
                                                                                      @when-i-below-k-match |
                                                                                        assume less_than(i, k) {
                                                                                          @i-below-k-match |
                                                                                            less_than(i, k)
                                                                                            [by hypothesis when-i-below-k-match]
                                                                                          @sigma-at-i-is-r-conditions-match |
                                                                                            is_nonneg(i) -> less_than(i, k) -> at(sigmaf, i) = at(r, i)
                                                                                            [by forall_elim(i) sigma-copies-r-below-k]
                                                                                          @sigma-at-i-is-r-match |
                                                                                            at(sigmaf, i) = at(r, i)
                                                                                            [by tautology sigma-at-i-is-r-conditions-match i-nonneg-match i-below-k-match]
                                                                                          @sigma2-at-i-range-conditions-match |
                                                                                            is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma2, i)) and less_than(at(sigma2, i), lprev))
                                                                                            [by forall_elim(i) sigma2-range]
                                                                                          @sigma2-at-i-in-range-match |
                                                                                            is_nonneg(at(sigma2, i)) and less_than(at(sigma2, i), lprev)
                                                                                            [by tautology sigma2-at-i-range-conditions-match i-nonneg-match i-below-k-match]
                                                                                          @sigma2-at-i-nonneg-match |
                                                                                            is_nonneg(at(sigma2, i))
                                                                                            [by and_elim_left sigma2-at-i-in-range-match]
                                                                                          @sigma2-at-i-below-lprev-match |
                                                                                            less_than(at(sigma2, i), lprev)
                                                                                            [by and_elim_right sigma2-at-i-in-range-match]
                                                                                          @s-at-i-is-t2-conditions-match |
                                                                                            is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t2, at(sigma2, i))
                                                                                            [by forall_elim(i) sigma2-match]
                                                                                          @s-at-i-is-t2-match |
                                                                                            at(s, i) = at(t2, at(sigma2, i))
                                                                                            [by tautology s-at-i-is-t2-conditions-match i-nonneg-match i-below-k-match]
                                                                                          // split on sigma2(i) vs m to relate at(t2, sigma2(i)) to at(t, at(r,i)).
                                                                                          @sigma2-i-below-m-or-at-most-match |
                                                                                            less_than(at(sigma2, i), m) or at_most(m, at(sigma2, i))
                                                                                            [by forall_elim(at(sigma2, i), m) less-than-or-at-most]
                                                                                          @s-at-i-is-t-at-r |
                                                                                            at(s, i) = at(t, at(r, i))
                                                                                            case sigma2-i-below-m-or-at-most-match {
                                                                                              @when-sigma2-below-m-match |
                                                                                                assume less_than(at(sigma2, i), m) {
                                                                                                  @sigma2-below-m-match |
                                                                                                    less_than(at(sigma2, i), m)
                                                                                                    [by hypothesis when-sigma2-below-m-match]
                                                                                                  @t2-agrees-below-m-conditions-match |
                                                                                                    is_nonneg(at(sigma2, i)) -> less_than(at(sigma2, i), m) -> at(t2, at(sigma2, i)) = at(t, at(sigma2, i))
                                                                                                    [by forall_elim(at(sigma2, i)) t2-agrees-below-m]
                                                                                                  @t2-at-sigma2-is-t-at-sigma2 |
                                                                                                    at(t2, at(sigma2, i)) = at(t, at(sigma2, i))
                                                                                                    [by tautology t2-agrees-below-m-conditions-match sigma2-at-i-nonneg-match sigma2-below-m-match]
                                                                                                  @s-at-i-is-t-at-sigma2-below |
                                                                                                    at(s, i) = at(t, at(sigma2, i))
                                                                                                    [by rewrite t2-at-sigma2-is-t-at-sigma2 s-at-i-is-t2-match]
                                                                                                  @r-at-i-conditions-match |
                                                                                                    is_nonneg(i) -> less_than(i, k) -> less_than(at(sigma2, i), m) -> at(r, i) = at(sigma2, i)
                                                                                                    [by forall_elim(i) r-copies-below-m]
                                                                                                  @r-at-i-is-sigma2-match |
                                                                                                    at(r, i) = at(sigma2, i)
                                                                                                    [by tautology r-at-i-conditions-match i-nonneg-match i-below-k-match sigma2-below-m-match]
                                                                                                  @conclusion-s-t-r-below-m |
                                                                                                    at(s, i) = at(t, at(r, i))
                                                                                                    [by chain s-at-i-is-t-at-sigma2-below r-at-i-is-sigma2-match]
                                                                                                }
                                                                                              @when-sigma2-at-most-m-match |
                                                                                                assume at_most(m, at(sigma2, i)) {
                                                                                                  @sigma2-at-most-m-match |
                                                                                                    at_most(m, at(sigma2, i))
                                                                                                    [by hypothesis when-sigma2-at-most-m-match]
                                                                                                  @t2-agrees-above-m-conditions-match |
                                                                                                    is_nonneg(at(sigma2, i)) -> at_most(m, at(sigma2, i)) -> less_than(at(sigma2, i), lprev) -> at(t2, at(sigma2, i)) = at(t, succ(at(sigma2, i)))
                                                                                                    [by forall_elim(at(sigma2, i)) t2-agrees-above-m]
                                                                                                  @t2-at-sigma2-is-t-at-succ |
                                                                                                    at(t2, at(sigma2, i)) = at(t, succ(at(sigma2, i)))
                                                                                                    [by tautology t2-agrees-above-m-conditions-match sigma2-at-i-nonneg-match sigma2-at-most-m-match sigma2-at-i-below-lprev-match]
                                                                                                  @s-at-i-is-t-at-succ-above |
                                                                                                    at(s, i) = at(t, succ(at(sigma2, i)))
                                                                                                    [by rewrite t2-at-sigma2-is-t-at-succ s-at-i-is-t2-match]
                                                                                                  @r-at-i-shift-conditions-match |
                                                                                                    is_nonneg(i) -> less_than(i, k) -> at_most(m, at(sigma2, i)) -> at(r, i) = succ(at(sigma2, i))
                                                                                                    [by forall_elim(i) r-shifts-above-m]
                                                                                                  @r-at-i-is-succ-sigma2-match |
                                                                                                    at(r, i) = succ(at(sigma2, i))
                                                                                                    [by tautology r-at-i-shift-conditions-match i-nonneg-match i-below-k-match sigma2-at-most-m-match]
                                                                                                  @conclusion-s-t-r-above-m |
                                                                                                    at(s, i) = at(t, at(r, i))
                                                                                                    [by chain s-at-i-is-t-at-succ-above r-at-i-is-succ-sigma2-match]
                                                                                                }
                                                                                            }
                                                                                          @conclusion-match-below-k |
                                                                                            at(s, i) = at(t, at(sigmaf, i))
                                                                                            [by chain s-at-i-is-t-at-r sigma-at-i-is-r-match]
                                                                                        }
                                                                                      @when-i-equals-k-match |
                                                                                        assume i = k {
                                                                                          @i-is-k-match |
                                                                                            i = k
                                                                                            [by hypothesis when-i-equals-k-match]
                                                                                          @sigma-at-k-is-m-copy |
                                                                                            at(sigmaf, k) = m
                                                                                            [by tautology sigma-at-k-is-m]
                                                                                          @sk-is-t-at-m |
                                                                                            at(s, k) = at(t, m)
                                                                                            [by tautology sk-equals-tm]
                                                                                          @sk-is-t-at-sigma-k |
                                                                                            at(s, k) = at(t, at(sigmaf, k))
                                                                                            [by chain sk-is-t-at-m sigma-at-k-is-m-copy]
                                                                                          @conclusion-match-at-k |
                                                                                            at(s, i) = at(t, at(sigmaf, i))
                                                                                            [by chain i-is-k-match sk-is-t-at-sigma-k]
                                                                                        }
                                                                                    }
                                                                                }
                                                                              @i-below-succ-k-gives-match |
                                                                                less_than(i, succ(k)) -> at(s, i) = at(t, at(sigmaf, i))
                                                                                [by implies_intro given-i-below-succ-k-match]
                                                                            }
                                                                          @i-nonneg-gives-match |
                                                                            is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigmaf, i))
                                                                            [by implies_intro given-i-nonneg-match]
                                                                        }
                                                                      @sigma-match-succ |
                                                                        forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigmaf, i))
                                                                        [by forall_intro sigma-match-succ-proof]

                                                                      // RANGE: at(sigmaf, i) is nonneg and < l for i < succ(k).
                                                                      @sigma-range-succ-proof |
                                                                        fix i: Int {
                                                                          @given-i-nonneg-range |
                                                                            assume is_nonneg(i) {
                                                                              @given-i-below-succ-k-range |
                                                                                assume less_than(i, succ(k)) {
                                                                                  @i-nonneg-range |
                                                                                    is_nonneg(i)
                                                                                    [by hypothesis given-i-nonneg-range]
                                                                                  @i-below-succ-k-range |
                                                                                    less_than(i, succ(k))
                                                                                    [by hypothesis given-i-below-succ-k-range]
                                                                                  @i-below-k-or-equal-range |
                                                                                    less_than(i, k) or i = k
                                                                                    [by specialize lessThanSuccSplit(i, k) i-nonneg-range k-nonneg i-below-succ-k-range]
                                                                                  @sigma-at-i-in-range |
                                                                                    is_nonneg(at(sigmaf, i)) and less_than(at(sigmaf, i), l)
                                                                                    case i-below-k-or-equal-range {
                                                                                      @when-i-below-k-range |
                                                                                        assume less_than(i, k) {
                                                                                          @i-below-k-range |
                                                                                            less_than(i, k)
                                                                                            [by hypothesis when-i-below-k-range]
                                                                                          @sigma-at-i-is-r-conditions-range |
                                                                                            is_nonneg(i) -> less_than(i, k) -> at(sigmaf, i) = at(r, i)
                                                                                            [by forall_elim(i) sigma-copies-r-below-k]
                                                                                          @sigma-at-i-is-r-range |
                                                                                            at(sigmaf, i) = at(r, i)
                                                                                            [by tautology sigma-at-i-is-r-conditions-range i-nonneg-range i-below-k-range]
                                                                                          @sigma2-at-i-range-conditions-range |
                                                                                            is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma2, i)) and less_than(at(sigma2, i), lprev))
                                                                                            [by forall_elim(i) sigma2-range]
                                                                                          @sigma2-at-i-in-range-range |
                                                                                            is_nonneg(at(sigma2, i)) and less_than(at(sigma2, i), lprev)
                                                                                            [by tautology sigma2-at-i-range-conditions-range i-nonneg-range i-below-k-range]
                                                                                          @sigma2-at-i-nonneg-range |
                                                                                            is_nonneg(at(sigma2, i))
                                                                                            [by and_elim_left sigma2-at-i-in-range-range]
                                                                                          @sigma2-at-i-below-lprev-range |
                                                                                            less_than(at(sigma2, i), lprev)
                                                                                            [by and_elim_right sigma2-at-i-in-range-range]
                                                                                          @sigma2-i-below-m-or-at-most-range |
                                                                                            less_than(at(sigma2, i), m) or at_most(m, at(sigma2, i))
                                                                                            [by forall_elim(at(sigma2, i), m) less-than-or-at-most]
                                                                                          @sigma-at-i-in-range-below-k |
                                                                                            is_nonneg(at(sigmaf, i)) and less_than(at(sigmaf, i), l)
                                                                                            case sigma2-i-below-m-or-at-most-range {
                                                                                              @when-sigma2-below-m-range |
                                                                                                assume less_than(at(sigma2, i), m) {
                                                                                                  @sigma2-below-m-range |
                                                                                                    less_than(at(sigma2, i), m)
                                                                                                    [by hypothesis when-sigma2-below-m-range]
                                                                                                  @r-at-i-conditions-range |
                                                                                                    is_nonneg(i) -> less_than(i, k) -> less_than(at(sigma2, i), m) -> at(r, i) = at(sigma2, i)
                                                                                                    [by forall_elim(i) r-copies-below-m]
                                                                                                  @r-at-i-is-sigma2-range |
                                                                                                    at(r, i) = at(sigma2, i)
                                                                                                    [by tautology r-at-i-conditions-range i-nonneg-range i-below-k-range sigma2-below-m-range]
                                                                                                  @sigma-at-i-is-sigma2-range |
                                                                                                    at(sigmaf, i) = at(sigma2, i)
                                                                                                    [by rewrite r-at-i-is-sigma2-range sigma-at-i-is-r-range]
                                                                                                  @sigma2-below-l-transitivity-range |
                                                                                                    less_than(at(sigma2, i), m) -> less_than(m, l) -> less_than(at(sigma2, i), l)
                                                                                                    [by forall_elim(at(sigma2, i), m, l) less_than-is-transitive-sigma]
                                                                                                  @sigma2-below-l-range |
                                                                                                    less_than(at(sigma2, i), l)
                                                                                                    [by tautology sigma2-below-l-transitivity-range sigma2-below-m-range m-below-l-copy]
                                                                                                  @sigma2-in-l-range |
                                                                                                    is_nonneg(at(sigma2, i)) and less_than(at(sigma2, i), l)
                                                                                                    [by and_intro sigma2-at-i-nonneg-range sigma2-below-l-range]
                                                                                                  @sigma2-is-sigma-at-i-range |
                                                                                                    at(sigma2, i) = at(sigmaf, i)
                                                                                                    [by symmetry sigma-at-i-is-sigma2-range]
                                                                                                  @conclusion-range-below-m |
                                                                                                    is_nonneg(at(sigmaf, i)) and less_than(at(sigmaf, i), l)
                                                                                                    [by rewrite sigma2-is-sigma-at-i-range sigma2-in-l-range]
                                                                                                }
                                                                                              @when-sigma2-at-most-m-range |
                                                                                                assume at_most(m, at(sigma2, i)) {
                                                                                                  @sigma2-at-most-m-range |
                                                                                                    at_most(m, at(sigma2, i))
                                                                                                    [by hypothesis when-sigma2-at-most-m-range]
                                                                                                  @r-at-i-shift-conditions-range |
                                                                                                    is_nonneg(i) -> less_than(i, k) -> at_most(m, at(sigma2, i)) -> at(r, i) = succ(at(sigma2, i))
                                                                                                    [by forall_elim(i) r-shifts-above-m]
                                                                                                  @r-at-i-is-succ-sigma2-range |
                                                                                                    at(r, i) = succ(at(sigma2, i))
                                                                                                    [by tautology r-at-i-shift-conditions-range i-nonneg-range i-below-k-range sigma2-at-most-m-range]
                                                                                                  @sigma-at-i-is-succ-sigma2-range |
                                                                                                    at(sigmaf, i) = succ(at(sigma2, i))
                                                                                                    [by rewrite r-at-i-is-succ-sigma2-range sigma-at-i-is-r-range]
                                                                                                  @succ-sigma2-nonneg-conditions-range |
                                                                                                    is_nonneg(at(sigma2, i)) -> is_nonneg(succ(at(sigma2, i)))
                                                                                                    [by forall_elim(at(sigma2, i)) successor-of-nonneg-sigma]
                                                                                                  @succ-sigma2-nonneg-range |
                                                                                                    is_nonneg(succ(at(sigma2, i)))
                                                                                                    [by modus_ponens succ-sigma2-nonneg-conditions-range sigma2-at-i-nonneg-range]
                                                                                                  @succ-sigma2-below-succ-lprev-conditions-range |
                                                                                                    less_than(at(sigma2, i), lprev) -> less_than(succ(at(sigma2, i)), succ(lprev))
                                                                                                    [by forall_elim(at(sigma2, i), lprev) succ-preserves-order-sigma]
                                                                                                  @succ-sigma2-below-succ-lprev-range |
                                                                                                    less_than(succ(at(sigma2, i)), succ(lprev))
                                                                                                    [by modus_ponens succ-sigma2-below-succ-lprev-conditions-range sigma2-at-i-below-lprev-range]
                                                                                                  @succ-sigma2-below-l-range |
                                                                                                    less_than(succ(at(sigma2, i)), l)
                                                                                                    [by rewrite succ-lprev-is-l succ-sigma2-below-succ-lprev-range]
                                                                                                  @succ-sigma2-in-l-range |
                                                                                                    is_nonneg(succ(at(sigma2, i))) and less_than(succ(at(sigma2, i)), l)
                                                                                                    [by and_intro succ-sigma2-nonneg-range succ-sigma2-below-l-range]
                                                                                                  @succ-sigma2-is-sigma-at-i-range |
                                                                                                    succ(at(sigma2, i)) = at(sigmaf, i)
                                                                                                    [by symmetry sigma-at-i-is-succ-sigma2-range]
                                                                                                  @conclusion-range-above-m |
                                                                                                    is_nonneg(at(sigmaf, i)) and less_than(at(sigmaf, i), l)
                                                                                                    [by rewrite succ-sigma2-is-sigma-at-i-range succ-sigma2-in-l-range]
                                                                                                }
                                                                                            }
                                                                                          @conclusion-range-below-k |
                                                                                            is_nonneg(at(sigmaf, i)) and less_than(at(sigmaf, i), l)
                                                                                            [by tautology sigma-at-i-in-range-below-k]
                                                                                        }
                                                                                      @when-i-equals-k-range |
                                                                                        assume i = k {
                                                                                          @i-is-k-range |
                                                                                            i = k
                                                                                            [by hypothesis when-i-equals-k-range]
                                                                                          @sigma-at-k-is-m-range |
                                                                                            at(sigmaf, k) = m
                                                                                            [by tautology sigma-at-k-is-m]
                                                                                          @m-in-l-range |
                                                                                            is_nonneg(m) and less_than(m, l)
                                                                                            [by and_intro m-nonneg m-below-l-copy]
                                                                                          @m-is-sigma-at-k-range |
                                                                                            m = at(sigmaf, k)
                                                                                            [by symmetry sigma-at-k-is-m-range]
                                                                                          @sigma-at-k-in-range |
                                                                                            is_nonneg(at(sigmaf, k)) and less_than(at(sigmaf, k), l)
                                                                                            [by rewrite m-is-sigma-at-k-range m-in-l-range]
                                                                                          @k-is-i-range |
                                                                                            k = i
                                                                                            [by symmetry i-is-k-range]
                                                                                          @conclusion-range-at-k |
                                                                                            is_nonneg(at(sigmaf, i)) and less_than(at(sigmaf, i), l)
                                                                                            [by rewrite k-is-i-range sigma-at-k-in-range]
                                                                                        }
                                                                                    }
                                                                                }
                                                                              @i-below-succ-k-gives-range |
                                                                                less_than(i, succ(k)) -> (is_nonneg(at(sigmaf, i)) and less_than(at(sigmaf, i), l))
                                                                                [by implies_intro given-i-below-succ-k-range]
                                                                            }
                                                                          @i-nonneg-gives-range |
                                                                            is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigmaf, i)) and less_than(at(sigmaf, i), l))
                                                                            [by implies_intro given-i-nonneg-range]
                                                                        }
                                                                      @sigma-range-succ |
                                                                        forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigmaf, i)) and less_than(at(sigmaf, i), l))
                                                                        [by forall_intro sigma-range-succ-proof]

                                                                      // INJECTIVE: at(sigmaf, a) = at(sigmaf, b) with a, b < succ(k) forces a = b.
                                                                      // helper: for x < k, at(sigmaf, x) equals unremove(at(sigma2, x)), which is
                                                                      // never equal to m (values < m stay < m; values >= m become succ(...) > m).
                                                                      @sigma-tail-avoids-m-proof |
                                                                        fix x: Int {
                                                                          @given-x-nonneg-avoid |
                                                                            assume is_nonneg(x) {
                                                                              @given-x-below-k-avoid |
                                                                                assume less_than(x, k) {
                                                                                  @x-nonneg-avoid |
                                                                                    is_nonneg(x)
                                                                                    [by hypothesis given-x-nonneg-avoid]
                                                                                  @x-below-k-avoid |
                                                                                    less_than(x, k)
                                                                                    [by hypothesis given-x-below-k-avoid]
                                                                                  @sigma-at-x-is-r-conditions-avoid |
                                                                                    is_nonneg(x) -> less_than(x, k) -> at(sigmaf, x) = at(r, x)
                                                                                    [by forall_elim(x) sigma-copies-r-below-k]
                                                                                  @sigma-at-x-is-r-avoid |
                                                                                    at(sigmaf, x) = at(r, x)
                                                                                    [by tautology sigma-at-x-is-r-conditions-avoid x-nonneg-avoid x-below-k-avoid]
                                                                                  @sigma2-at-x-range-conditions-avoid |
                                                                                    is_nonneg(x) -> less_than(x, k) -> (is_nonneg(at(sigma2, x)) and less_than(at(sigma2, x), lprev))
                                                                                    [by forall_elim(x) sigma2-range]
                                                                                  @sigma2-at-x-in-range-avoid |
                                                                                    is_nonneg(at(sigma2, x)) and less_than(at(sigma2, x), lprev)
                                                                                    [by tautology sigma2-at-x-range-conditions-avoid x-nonneg-avoid x-below-k-avoid]
                                                                                  @sigma2-at-x-nonneg-avoid |
                                                                                    is_nonneg(at(sigma2, x))
                                                                                    [by and_elim_left sigma2-at-x-in-range-avoid]
                                                                                  @sigma2-x-below-m-or-at-most-avoid |
                                                                                    less_than(at(sigma2, x), m) or at_most(m, at(sigma2, x))
                                                                                    [by forall_elim(at(sigma2, x), m) less-than-or-at-most]
                                                                                  @sigma-at-x-below-m-relation |
                                                                                    less_than(at(sigmaf, x), m) or less_than(m, at(sigmaf, x))
                                                                                    case sigma2-x-below-m-or-at-most-avoid {
                                                                                      @when-sigma2-below-m-avoid |
                                                                                        assume less_than(at(sigma2, x), m) {
                                                                                          @sigma2-below-m-avoid |
                                                                                            less_than(at(sigma2, x), m)
                                                                                            [by hypothesis when-sigma2-below-m-avoid]
                                                                                          @r-at-x-conditions-avoid |
                                                                                            is_nonneg(x) -> less_than(x, k) -> less_than(at(sigma2, x), m) -> at(r, x) = at(sigma2, x)
                                                                                            [by forall_elim(x) r-copies-below-m]
                                                                                          @r-at-x-is-sigma2-avoid |
                                                                                            at(r, x) = at(sigma2, x)
                                                                                            [by tautology r-at-x-conditions-avoid x-nonneg-avoid x-below-k-avoid sigma2-below-m-avoid]
                                                                                          @sigma-at-x-is-sigma2-avoid |
                                                                                            at(sigmaf, x) = at(sigma2, x)
                                                                                            [by rewrite r-at-x-is-sigma2-avoid sigma-at-x-is-r-avoid]
                                                                                          @sigma2-is-sigma-at-x-avoid |
                                                                                            at(sigma2, x) = at(sigmaf, x)
                                                                                            [by symmetry sigma-at-x-is-sigma2-avoid]
                                                                                          @sigma-at-x-below-m-avoid |
                                                                                            less_than(at(sigmaf, x), m)
                                                                                            [by rewrite sigma2-is-sigma-at-x-avoid sigma2-below-m-avoid]
                                                                                          @sigma-at-x-below-m-left-avoid |
                                                                                            less_than(at(sigmaf, x), m) or less_than(m, at(sigmaf, x))
                                                                                            [by or_intro_left sigma-at-x-below-m-avoid]
                                                                                        }
                                                                                      @when-sigma2-at-most-m-avoid |
                                                                                        assume at_most(m, at(sigma2, x)) {
                                                                                          @sigma2-at-most-m-avoid |
                                                                                            at_most(m, at(sigma2, x))
                                                                                            [by hypothesis when-sigma2-at-most-m-avoid]
                                                                                          @r-at-x-shift-conditions-avoid |
                                                                                            is_nonneg(x) -> less_than(x, k) -> at_most(m, at(sigma2, x)) -> at(r, x) = succ(at(sigma2, x))
                                                                                            [by forall_elim(x) r-shifts-above-m]
                                                                                          @r-at-x-is-succ-sigma2-avoid |
                                                                                            at(r, x) = succ(at(sigma2, x))
                                                                                            [by tautology r-at-x-shift-conditions-avoid x-nonneg-avoid x-below-k-avoid sigma2-at-most-m-avoid]
                                                                                          @sigma-at-x-is-succ-sigma2-avoid |
                                                                                            at(sigmaf, x) = succ(at(sigma2, x))
                                                                                            [by rewrite r-at-x-is-succ-sigma2-avoid sigma-at-x-is-r-avoid]
                                                                                          // m <= sigma2(x) < succ(sigma2(x)) = sigma(x), so m < sigma(x).
                                                                                          @m-below-succ-sigma2-conditions-avoid |
                                                                                            at_most(m, at(sigma2, x)) -> less_than(m, succ(at(sigma2, x)))
                                                                                            [by forall_elim(m, at(sigma2, x)) at-most-then-below-succ]
                                                                                          @m-below-succ-sigma2-avoid |
                                                                                            less_than(m, succ(at(sigma2, x)))
                                                                                            [by modus_ponens m-below-succ-sigma2-conditions-avoid sigma2-at-most-m-avoid]
                                                                                          @succ-sigma2-is-sigma-at-x-avoid |
                                                                                            succ(at(sigma2, x)) = at(sigmaf, x)
                                                                                            [by symmetry sigma-at-x-is-succ-sigma2-avoid]
                                                                                          @m-below-sigma-at-x-avoid |
                                                                                            less_than(m, at(sigmaf, x))
                                                                                            [by rewrite succ-sigma2-is-sigma-at-x-avoid m-below-succ-sigma2-avoid]
                                                                                          @m-below-sigma-at-x-right-avoid |
                                                                                            less_than(at(sigmaf, x), m) or less_than(m, at(sigmaf, x))
                                                                                            [by or_intro_right m-below-sigma-at-x-avoid]
                                                                                        }
                                                                                    }
                                                                                }
                                                                              @x-below-k-gives-relation |
                                                                                less_than(x, k) -> (less_than(at(sigmaf, x), m) or less_than(m, at(sigmaf, x)))
                                                                                [by implies_intro given-x-below-k-avoid]
                                                                            }
                                                                          @x-nonneg-gives-relation |
                                                                            is_nonneg(x) -> less_than(x, k) -> (less_than(at(sigmaf, x), m) or less_than(m, at(sigmaf, x)))
                                                                            [by implies_intro given-x-nonneg-avoid]
                                                                        }
                                                                      @sigma-tail-avoids-m |
                                                                        forall x: Int; is_nonneg(x) -> less_than(x, k) -> (less_than(at(sigmaf, x), m) or less_than(m, at(sigmaf, x)))
                                                                        [by forall_intro sigma-tail-avoids-m-proof]

                                                                      // helper: for x < k, sigma(x) is the injective image of sigma2(x) under unremove.
                                                                      // We recover sigma2(x) from sigma(x) via the two shift cases (used to prove
                                                                      // injectivity below by composing with sigma2's injectivity).
                                                                      @succ-is-injective |
                                                                        forall a, b: Int; succ(a) = succ(b) -> a = b
                                                                        [by theorem succInjective]

                                                                      // main injectivity argument.
                                                                      @sigma-injective-succ-proof |
                                                                        fix a: Int {
                                                                          @generalize-b-inj |
                                                                            fix b: Int {
                                                                              @given-a-nonneg-inj |
                                                                                assume is_nonneg(a) {
                                                                                  @given-a-below-succ-k-inj |
                                                                                    assume less_than(a, succ(k)) {
                                                                                      @given-b-nonneg-inj |
                                                                                        assume is_nonneg(b) {
                                                                                          @given-b-below-succ-k-inj |
                                                                                            assume less_than(b, succ(k)) {
                                                                                              @given-sigma-a-equals-sigma-b |
                                                                                                assume at(sigmaf, a) = at(sigmaf, b) {
                                                                                                  @a-nonneg-inj |
                                                                                                    is_nonneg(a)
                                                                                                    [by hypothesis given-a-nonneg-inj]
                                                                                                  @a-below-succ-k-inj |
                                                                                                    less_than(a, succ(k))
                                                                                                    [by hypothesis given-a-below-succ-k-inj]
                                                                                                  @b-nonneg-inj |
                                                                                                    is_nonneg(b)
                                                                                                    [by hypothesis given-b-nonneg-inj]
                                                                                                  @b-below-succ-k-inj |
                                                                                                    less_than(b, succ(k))
                                                                                                    [by hypothesis given-b-below-succ-k-inj]
                                                                                                  @sigma-a-equals-sigma-b |
                                                                                                    at(sigmaf, a) = at(sigmaf, b)
                                                                                                    [by hypothesis given-sigma-a-equals-sigma-b]
                                                                                                  @a-below-k-or-equal-inj |
                                                                                                    less_than(a, k) or a = k
                                                                                                    [by specialize lessThanSuccSplit(a, k) a-nonneg-inj k-nonneg a-below-succ-k-inj]
                                                                                                  @b-below-k-or-equal-inj |
                                                                                                    less_than(b, k) or b = k
                                                                                                    [by specialize lessThanSuccSplit(b, k) b-nonneg-inj k-nonneg b-below-succ-k-inj]
                                                                                                  @a-equals-b |
                                                                                                    a = b
                                                                                                    case a-below-k-or-equal-inj {
                                                                                                      @when-a-below-k-inj |
                                                                                                        assume less_than(a, k) {
                                                                                                          @a-below-k-inj |
                                                                                                            less_than(a, k)
                                                                                                            [by hypothesis when-a-below-k-inj]
                                                                                                          @a-equals-b-from-a-below |
                                                                                                            a = b
                                                                                                            case b-below-k-or-equal-inj {
                                                                                                              @when-b-below-k-inj |
                                                                                                                assume less_than(b, k) {
                                                                                                                  @b-below-k-inj |
                                                                                                                    less_than(b, k)
                                                                                                                    [by hypothesis when-b-below-k-inj]
                                                                                                                  // both a, b < k: recover sigma2(a) = sigma2(b) from at(sigmaf,a)=at(sigmaf,b)
                                                                                                                  // (unremove is injective), then sigma2's injectivity gives a = b.
                                                                                                                  @sigma-a-is-r-conditions-core |
                                                                                                                    is_nonneg(a) -> less_than(a, k) -> at(sigmaf, a) = at(r, a)
                                                                                                                    [by forall_elim(a) sigma-copies-r-below-k]
                                                                                                                  @sigma-a-is-r-core |
                                                                                                                    at(sigmaf, a) = at(r, a)
                                                                                                                    [by tautology sigma-a-is-r-conditions-core a-nonneg-inj a-below-k-inj]
                                                                                                                  @sigma-b-is-r-conditions-core |
                                                                                                                    is_nonneg(b) -> less_than(b, k) -> at(sigmaf, b) = at(r, b)
                                                                                                                    [by forall_elim(b) sigma-copies-r-below-k]
                                                                                                                  @sigma-b-is-r-core |
                                                                                                                    at(sigmaf, b) = at(r, b)
                                                                                                                    [by tautology sigma-b-is-r-conditions-core b-nonneg-inj b-below-k-inj]
                                                                                                                  // r(a) = σ(a) = σ(b) = r(b).
                                                                                                                  @r-a-equals-r-b-core |
                                                                                                                    at(r, a) = at(r, b)
                                                                                                                    [by chain sigma-a-is-r-core sigma-a-equals-sigma-b sigma-b-is-r-core]
                                                                                                                  // sigma2 range facts for a and b.
                                                                                                                  @sigma2-a-range-conditions-core |
                                                                                                                    is_nonneg(a) -> less_than(a, k) -> (is_nonneg(at(sigma2, a)) and less_than(at(sigma2, a), lprev))
                                                                                                                    [by forall_elim(a) sigma2-range]
                                                                                                                  @sigma2-a-in-range-core |
                                                                                                                    is_nonneg(at(sigma2, a)) and less_than(at(sigma2, a), lprev)
                                                                                                                    [by tautology sigma2-a-range-conditions-core a-nonneg-inj a-below-k-inj]
                                                                                                                  @sigma2-a-nonneg-core |
                                                                                                                    is_nonneg(at(sigma2, a))
                                                                                                                    [by and_elim_left sigma2-a-in-range-core]
                                                                                                                  @sigma2-b-range-conditions-core |
                                                                                                                    is_nonneg(b) -> less_than(b, k) -> (is_nonneg(at(sigma2, b)) and less_than(at(sigma2, b), lprev))
                                                                                                                    [by forall_elim(b) sigma2-range]
                                                                                                                  @sigma2-b-in-range-core |
                                                                                                                    is_nonneg(at(sigma2, b)) and less_than(at(sigma2, b), lprev)
                                                                                                                    [by tautology sigma2-b-range-conditions-core b-nonneg-inj b-below-k-inj]
                                                                                                                  @sigma2-b-nonneg-core |
                                                                                                                    is_nonneg(at(sigma2, b))
                                                                                                                    [by and_elim_left sigma2-b-in-range-core]
                                                                                                                  // classify sigma2(a) and sigma2(b) relative to m.
                                                                                                                  @sigma2-a-below-or-at-most-core |
                                                                                                                    less_than(at(sigma2, a), m) or at_most(m, at(sigma2, a))
                                                                                                                    [by forall_elim(at(sigma2, a), m) less-than-or-at-most]
                                                                                                                  @sigma2-b-below-or-at-most-core |
                                                                                                                    less_than(at(sigma2, b), m) or at_most(m, at(sigma2, b))
                                                                                                                    [by forall_elim(at(sigma2, b), m) less-than-or-at-most]
                                                                                                                  // evaluate r at a and b in each case, equate, and recover sigma2(a)=sigma2(b).
                                                                                                                  @sigma2-a-equals-sigma2-b-core |
                                                                                                                    at(sigma2, a) = at(sigma2, b)
                                                                                                                    case sigma2-a-below-or-at-most-core {
                                                                                                                      @when-a-below-m-final |
                                                                                                                        assume less_than(at(sigma2, a), m) {
                                                                                                                          @a-below-m-final |
                                                                                                                            less_than(at(sigma2, a), m)
                                                                                                                            [by hypothesis when-a-below-m-final]
                                                                                                                          @r-a-copies-conditions |
                                                                                                                            is_nonneg(a) -> less_than(a, k) -> less_than(at(sigma2, a), m) -> at(r, a) = at(sigma2, a)
                                                                                                                            [by forall_elim(a) r-copies-below-m]
                                                                                                                          @r-a-is-sigma2-a-final |
                                                                                                                            at(r, a) = at(sigma2, a)
                                                                                                                            [by tautology r-a-copies-conditions a-nonneg-inj a-below-k-inj a-below-m-final]
                                                                                                                          @sigma2-a-equals-sigma2-b-a-below |
                                                                                                                            at(sigma2, a) = at(sigma2, b)
                                                                                                                            case sigma2-b-below-or-at-most-core {
                                                                                                                              @when-b-below-m-final-ab |
                                                                                                                                assume less_than(at(sigma2, b), m) {
                                                                                                                                  @b-below-m-final-ab |
                                                                                                                                    less_than(at(sigma2, b), m)
                                                                                                                                    [by hypothesis when-b-below-m-final-ab]
                                                                                                                                  @r-b-copies-conditions-ab |
                                                                                                                                    is_nonneg(b) -> less_than(b, k) -> less_than(at(sigma2, b), m) -> at(r, b) = at(sigma2, b)
                                                                                                                                    [by forall_elim(b) r-copies-below-m]
                                                                                                                                  @r-b-is-sigma2-b-ab |
                                                                                                                                    at(r, b) = at(sigma2, b)
                                                                                                                                    [by tautology r-b-copies-conditions-ab b-nonneg-inj b-below-k-inj b-below-m-final-ab]
                                                                                                                                  // σ2(a) = r(a) = r(b) = σ2(b).
                                                                                                                                  @conclusion-both-below |
                                                                                                                                    at(sigma2, a) = at(sigma2, b)
                                                                                                                                    [by chain r-a-is-sigma2-a-final r-a-equals-r-b-core r-b-is-sigma2-b-ab]
                                                                                                                                }
                                                                                                                              @when-b-at-most-m-final-ab |
                                                                                                                                assume at_most(m, at(sigma2, b)) {
                                                                                                                                  // a below m but b at-least m: r(a)=sigma2(a)<m, r(b)=succ(sigma2(b))>m; equal is impossible.
                                                                                                                                  @b-at-most-m-final-ab |
                                                                                                                                    at_most(m, at(sigma2, b))
                                                                                                                                    [by hypothesis when-b-at-most-m-final-ab]
                                                                                                                                  @r-b-shift-conditions-ab |
                                                                                                                                    is_nonneg(b) -> less_than(b, k) -> at_most(m, at(sigma2, b)) -> at(r, b) = succ(at(sigma2, b))
                                                                                                                                    [by forall_elim(b) r-shifts-above-m]
                                                                                                                                  @r-b-is-succ-sigma2-b-ab |
                                                                                                                                    at(r, b) = succ(at(sigma2, b))
                                                                                                                                    [by tautology r-b-shift-conditions-ab b-nonneg-inj b-below-k-inj b-at-most-m-final-ab]
                                                                                                                                  @sigma2-a-is-r-a-ab |
                                                                                                                                    at(sigma2, a) = at(r, a)
                                                                                                                                    [by symmetry r-a-is-sigma2-a-final]
                                                                                                                                  @r-a-is-sigma2-a-below-m |
                                                                                                                                    less_than(at(r, a), m)
                                                                                                                                    [by rewrite sigma2-a-is-r-a-ab a-below-m-final]
                                                                                                                                  @m-below-succ-sigma2-b-conditions |
                                                                                                                                    at_most(m, at(sigma2, b)) -> less_than(m, succ(at(sigma2, b)))
                                                                                                                                    [by forall_elim(m, at(sigma2, b)) at-most-then-below-succ]
                                                                                                                                  @m-below-succ-sigma2-b-ab |
                                                                                                                                    less_than(m, succ(at(sigma2, b)))
                                                                                                                                    [by modus_ponens m-below-succ-sigma2-b-conditions b-at-most-m-final-ab]
                                                                                                                                  @succ-sigma2-b-is-r-b-ab |
                                                                                                                                    succ(at(sigma2, b)) = at(r, b)
                                                                                                                                    [by symmetry r-b-is-succ-sigma2-b-ab]
                                                                                                                                  @m-below-r-b-ab |
                                                                                                                                    less_than(m, at(r, b))
                                                                                                                                    [by rewrite succ-sigma2-b-is-r-b-ab m-below-succ-sigma2-b-ab]
                                                                                                                                  @r-a-below-m-then-r-b-transitivity |
                                                                                                                                    less_than(at(r, a), m) -> less_than(m, at(r, b)) -> less_than(at(r, a), at(r, b))
                                                                                                                                    [by forall_elim(at(r, a), m, at(r, b)) less_than-is-transitive-sigma]
                                                                                                                                  @r-a-below-r-b-ab |
                                                                                                                                    less_than(at(r, a), at(r, b))
                                                                                                                                    [by tautology r-a-below-m-then-r-b-transitivity r-a-is-sigma2-a-below-m m-below-r-b-ab]
                                                                                                                                  @r-b-equals-r-a-ab |
                                                                                                                                    at(r, b) = at(r, a)
                                                                                                                                    [by symmetry r-a-equals-r-b-core]
                                                                                                                                  @r-a-below-r-a-ab |
                                                                                                                                    less_than(at(r, a), at(r, a))
                                                                                                                                    [by rewrite r-b-equals-r-a-ab r-a-below-r-b-ab]
                                                                                                                                  @r-a-not-below-r-a-ab |
                                                                                                                                    not less_than(at(r, a), at(r, a))
                                                                                                                                    [by forall_elim(at(r, a)) less_than-is-irreflexive-sigma]
                                                                                                                                  @conclusion-a-below-b-above |
                                                                                                                                    at(sigma2, a) = at(sigma2, b)
                                                                                                                                    [by absurd r-a-below-r-a-ab r-a-not-below-r-a-ab]
                                                                                                                                }
                                                                                                                            }
                                                                                                                        }
                                                                                                                      @when-a-at-most-m-final |
                                                                                                                        assume at_most(m, at(sigma2, a)) {
                                                                                                                          @a-at-most-m-final |
                                                                                                                            at_most(m, at(sigma2, a))
                                                                                                                            [by hypothesis when-a-at-most-m-final]
                                                                                                                          @r-a-shift-conditions |
                                                                                                                            is_nonneg(a) -> less_than(a, k) -> at_most(m, at(sigma2, a)) -> at(r, a) = succ(at(sigma2, a))
                                                                                                                            [by forall_elim(a) r-shifts-above-m]
                                                                                                                          @r-a-is-succ-sigma2-a-final |
                                                                                                                            at(r, a) = succ(at(sigma2, a))
                                                                                                                            [by tautology r-a-shift-conditions a-nonneg-inj a-below-k-inj a-at-most-m-final]
                                                                                                                          @sigma2-a-equals-sigma2-b-a-above |
                                                                                                                            at(sigma2, a) = at(sigma2, b)
                                                                                                                            case sigma2-b-below-or-at-most-core {
                                                                                                                              @when-b-below-m-final-ba |
                                                                                                                                assume less_than(at(sigma2, b), m) {
                                                                                                                                  // a at-least m but b below m: r(a)=succ(sigma2(a))>m, r(b)=sigma2(b)<m; impossible.
                                                                                                                                  @b-below-m-final-ba |
                                                                                                                                    less_than(at(sigma2, b), m)
                                                                                                                                    [by hypothesis when-b-below-m-final-ba]
                                                                                                                                  @r-b-copies-conditions-ba |
                                                                                                                                    is_nonneg(b) -> less_than(b, k) -> less_than(at(sigma2, b), m) -> at(r, b) = at(sigma2, b)
                                                                                                                                    [by forall_elim(b) r-copies-below-m]
                                                                                                                                  @r-b-is-sigma2-b-ba |
                                                                                                                                    at(r, b) = at(sigma2, b)
                                                                                                                                    [by tautology r-b-copies-conditions-ba b-nonneg-inj b-below-k-inj b-below-m-final-ba]
                                                                                                                                  @sigma2-b-is-r-b-ba |
                                                                                                                                    at(sigma2, b) = at(r, b)
                                                                                                                                    [by symmetry r-b-is-sigma2-b-ba]
                                                                                                                                  @r-b-below-m-ba |
                                                                                                                                    less_than(at(r, b), m)
                                                                                                                                    [by rewrite sigma2-b-is-r-b-ba b-below-m-final-ba]
                                                                                                                                  @m-below-succ-sigma2-a-conditions |
                                                                                                                                    at_most(m, at(sigma2, a)) -> less_than(m, succ(at(sigma2, a)))
                                                                                                                                    [by forall_elim(m, at(sigma2, a)) at-most-then-below-succ]
                                                                                                                                  @m-below-succ-sigma2-a-ba |
                                                                                                                                    less_than(m, succ(at(sigma2, a)))
                                                                                                                                    [by modus_ponens m-below-succ-sigma2-a-conditions a-at-most-m-final]
                                                                                                                                  @succ-sigma2-a-is-r-a-ba |
                                                                                                                                    succ(at(sigma2, a)) = at(r, a)
                                                                                                                                    [by symmetry r-a-is-succ-sigma2-a-final]
                                                                                                                                  @m-below-r-a-ba |
                                                                                                                                    less_than(m, at(r, a))
                                                                                                                                    [by rewrite succ-sigma2-a-is-r-a-ba m-below-succ-sigma2-a-ba]
                                                                                                                                  @r-b-below-m-then-r-a-transitivity |
                                                                                                                                    less_than(at(r, b), m) -> less_than(m, at(r, a)) -> less_than(at(r, b), at(r, a))
                                                                                                                                    [by forall_elim(at(r, b), m, at(r, a)) less_than-is-transitive-sigma]
                                                                                                                                  @r-b-below-r-a-ba |
                                                                                                                                    less_than(at(r, b), at(r, a))
                                                                                                                                    [by tautology r-b-below-m-then-r-a-transitivity r-b-below-m-ba m-below-r-a-ba]
                                                                                                                                  @r-b-below-r-b-ba |
                                                                                                                                    less_than(at(r, b), at(r, b))
                                                                                                                                    [by rewrite r-a-equals-r-b-core r-b-below-r-a-ba]
                                                                                                                                  @r-b-not-below-r-b-ba |
                                                                                                                                    not less_than(at(r, b), at(r, b))
                                                                                                                                    [by forall_elim(at(r, b)) less_than-is-irreflexive-sigma]
                                                                                                                                  @conclusion-a-above-b-below |
                                                                                                                                    at(sigma2, a) = at(sigma2, b)
                                                                                                                                    [by absurd r-b-below-r-b-ba r-b-not-below-r-b-ba]
                                                                                                                                }
                                                                                                                              @when-b-at-most-m-final-ba |
                                                                                                                                assume at_most(m, at(sigma2, b)) {
                                                                                                                                  @b-at-most-m-final-ba |
                                                                                                                                    at_most(m, at(sigma2, b))
                                                                                                                                    [by hypothesis when-b-at-most-m-final-ba]
                                                                                                                                  @r-b-shift-conditions-ba |
                                                                                                                                    is_nonneg(b) -> less_than(b, k) -> at_most(m, at(sigma2, b)) -> at(r, b) = succ(at(sigma2, b))
                                                                                                                                    [by forall_elim(b) r-shifts-above-m]
                                                                                                                                  @r-b-is-succ-sigma2-b-ba |
                                                                                                                                    at(r, b) = succ(at(sigma2, b))
                                                                                                                                    [by tautology r-b-shift-conditions-ba b-nonneg-inj b-below-k-inj b-at-most-m-final-ba]
                                                                                                                                  // succ(σ2 a) = r(a) = r(b) = succ(σ2 b).
                                                                                                                                  @succ-sigma2-a-equals-succ-sigma2-b-ba |
                                                                                                                                    succ(at(sigma2, a)) = succ(at(sigma2, b))
                                                                                                                                    [by chain r-a-is-succ-sigma2-a-final r-a-equals-r-b-core r-b-is-succ-sigma2-b-ba]
                                                                                                                                  @succ-inj-at-sigma2-conditions |
                                                                                                                                    succ(at(sigma2, a)) = succ(at(sigma2, b)) -> at(sigma2, a) = at(sigma2, b)
                                                                                                                                    [by forall_elim(at(sigma2, a), at(sigma2, b)) succ-is-injective]
                                                                                                                                  @conclusion-both-above |
                                                                                                                                    at(sigma2, a) = at(sigma2, b)
                                                                                                                                    [by modus_ponens succ-inj-at-sigma2-conditions succ-sigma2-a-equals-succ-sigma2-b-ba]
                                                                                                                                }
                                                                                                                            }
                                                                                                                        }
                                                                                                                    }
                                                                                                                  // sigma2 is injective, so a = b.
                                                                                                                  @sigma2-injective-at-a-b |
                                                                                                                    is_nonneg(a) -> less_than(a, k) -> is_nonneg(b) -> less_than(b, k) -> at(sigma2, a) = at(sigma2, b) -> a = b
                                                                                                                    [by forall_elim(a, b) sigma2-injective]
                                                                                                                  @a-equals-b-both-below |
                                                                                                                    a = b
                                                                                                                    [by tautology sigma2-injective-at-a-b a-nonneg-inj a-below-k-inj b-nonneg-inj b-below-k-inj sigma2-a-equals-sigma2-b-core]

                                                                                                                }
                                                                                                              @when-b-equals-k-inj |
                                                                                                                assume b = k {
                                                                                                                  @b-is-k-inj |
                                                                                                                    b = k
                                                                                                                    [by hypothesis when-b-equals-k-inj]
                                                                                                                  // σ(a) = σ(b) = σ(k) = m.
                                                                                                                  @sigma-a-is-m-inj |
                                                                                                                    at(sigmaf, a) = m
                                                                                                                    [by chain sigma-a-equals-sigma-b b-is-k-inj sigma-at-k-is-m]
                                                                                                                  @a-relation-conditions-inj |
                                                                                                                    is_nonneg(a) -> less_than(a, k) -> (less_than(at(sigmaf, a), m) or less_than(m, at(sigmaf, a)))
                                                                                                                    [by forall_elim(a) sigma-tail-avoids-m]
                                                                                                                  @a-relation-inj |
                                                                                                                    less_than(at(sigmaf, a), m) or less_than(m, at(sigmaf, a))
                                                                                                                    [by tautology a-relation-conditions-inj a-nonneg-inj a-below-k-inj]
                                                                                                                  @m-below-m-from-a |
                                                                                                                    less_than(m, m) or less_than(m, m)
                                                                                                                    [by rewrite sigma-a-is-m-inj a-relation-inj]
                                                                                                                  @m-strictly-below-m-a |
                                                                                                                    less_than(m, m)
                                                                                                                    [by tautology m-below-m-from-a]
                                                                                                                  @m-not-below-m-a |
                                                                                                                    not less_than(m, m)
                                                                                                                    [by specialize lessThanIrreflexive(m)]
                                                                                                                  @a-equals-b-absurd-1 |
                                                                                                                    a = b
                                                                                                                    [by absurd m-strictly-below-m-a m-not-below-m-a]
                                                                                                                }
                                                                                                            }
                                                                                                        }
                                                                                                      @when-a-equals-k-inj |
                                                                                                        assume a = k {
                                                                                                          @a-is-k-inj |
                                                                                                            a = k
                                                                                                            [by hypothesis when-a-equals-k-inj]
                                                                                                          @a-equals-b-from-a-equals |
                                                                                                            a = b
                                                                                                            case b-below-k-or-equal-inj {
                                                                                                              @when-b-below-k-inj-2 |
                                                                                                                assume less_than(b, k) {
                                                                                                                  @b-below-k-inj-2 |
                                                                                                                    less_than(b, k)
                                                                                                                    [by hypothesis when-b-below-k-inj-2]
                                                                                                                  // σ(b) = σ(a) = σ(k) = m.
                                                                                                                  @sigma-b-is-m-inj-2 |
                                                                                                                    at(sigmaf, b) = m
                                                                                                                    [by chain sigma-a-equals-sigma-b a-is-k-inj sigma-at-k-is-m]
                                                                                                                  @b-relation-conditions-inj-2 |
                                                                                                                    is_nonneg(b) -> less_than(b, k) -> (less_than(at(sigmaf, b), m) or less_than(m, at(sigmaf, b)))
                                                                                                                    [by forall_elim(b) sigma-tail-avoids-m]
                                                                                                                  @b-relation-inj-2 |
                                                                                                                    less_than(at(sigmaf, b), m) or less_than(m, at(sigmaf, b))
                                                                                                                    [by tautology b-relation-conditions-inj-2 b-nonneg-inj b-below-k-inj-2]
                                                                                                                  @m-below-m-from-b |
                                                                                                                    less_than(m, m) or less_than(m, m)
                                                                                                                    [by rewrite sigma-b-is-m-inj-2 b-relation-inj-2]
                                                                                                                  @m-strictly-below-m-b |
                                                                                                                    less_than(m, m)
                                                                                                                    [by tautology m-below-m-from-b]
                                                                                                                  @m-not-below-m-b |
                                                                                                                    not less_than(m, m)
                                                                                                                    [by specialize lessThanIrreflexive(m)]
                                                                                                                  @a-equals-b-absurd-2 |
                                                                                                                    a = b
                                                                                                                    [by absurd m-strictly-below-m-b m-not-below-m-b]
                                                                                                                }
                                                                                                              @when-b-equals-k-inj-2 |
                                                                                                                assume b = k {
                                                                                                                  @b-is-k-inj-2 |
                                                                                                                    b = k
                                                                                                                    [by hypothesis when-b-equals-k-inj-2]
                                                                                                                  // a = k = b.
                                                                                                                  @a-equals-b-both-k |
                                                                                                                    a = b
                                                                                                                    [by chain a-is-k-inj b-is-k-inj-2]
                                                                                                                }
                                                                                                            }
                                                                                                        }
                                                                                                    }
                                                                                                }
                                                                                              @sigma-equal-implies-index-equal |
                                                                                                at(sigmaf, a) = at(sigmaf, b) -> a = b
                                                                                                [by implies_intro given-sigma-a-equals-sigma-b]
                                                                                            }
                                                                                          @b-below-succ-k-gives-inj |
                                                                                            less_than(b, succ(k)) -> at(sigmaf, a) = at(sigmaf, b) -> a = b
                                                                                            [by implies_intro given-b-below-succ-k-inj]
                                                                                        }
                                                                                      @b-nonneg-gives-inj |
                                                                                        is_nonneg(b) -> less_than(b, succ(k)) -> at(sigmaf, a) = at(sigmaf, b) -> a = b
                                                                                        [by implies_intro given-b-nonneg-inj]
                                                                                    }
                                                                                  @a-below-succ-k-gives-inj |
                                                                                    less_than(a, succ(k)) -> is_nonneg(b) -> less_than(b, succ(k)) -> at(sigmaf, a) = at(sigmaf, b) -> a = b
                                                                                    [by implies_intro given-a-below-succ-k-inj]
                                                                                }
                                                                              @a-nonneg-gives-inj |
                                                                                is_nonneg(a) -> less_than(a, succ(k)) -> is_nonneg(b) -> less_than(b, succ(k)) -> at(sigmaf, a) = at(sigmaf, b) -> a = b
                                                                                [by implies_intro given-a-nonneg-inj]
                                                                            }
                                                                          @discharge-b-inj |
                                                                            forall b: Int; is_nonneg(a) -> less_than(a, succ(k)) -> is_nonneg(b) -> less_than(b, succ(k)) -> at(sigmaf, a) = at(sigmaf, b) -> a = b
                                                                            [by forall_intro generalize-b-inj]
                                                                        }
                                                                      @sigma-injective-succ |
                                                                        forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigmaf, i) = at(sigmaf, i2) -> i = i2
                                                                        [by forall_intro sigma-injective-succ-proof]

                                                                      // assemble the three clauses (top-level and is left-associative: (RANGE and INJ) and MATCH).
                                                                      @sigma-range-and-injective |
                                                                        (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigmaf, i)) and less_than(at(sigmaf, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigmaf, i) = at(sigmaf, i2) -> i = i2)
                                                                        [by and_intro sigma-range-succ sigma-injective-succ]
                                                                      @sigma-all-clauses |
                                                                        (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigmaf, i)) and less_than(at(sigmaf, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigmaf, i) = at(sigmaf, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigmaf, i)))
                                                                        [by and_intro sigma-range-and-injective sigma-match-succ]
                                                                      @sigma-witnesses-succ |
                                                                        exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i)))
                                                                        [by exists_intro(sigmaf) sigma-all-clauses]

                                                                    }
                                                                  @sigma-exists-from-append |
                                                                    (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                                                                    [by exists_elim with-permutation-sigma]
                                                                }
                                                              @sigma-exists-from-singleton |
                                                                (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                                                                [by exists_elim with-singleton-m]
                                                            }
                                                          @sigma-exists-from-reindex |
                                                            (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                                                            [by exists_elim with-reindexed-r]
                                                        }
                                                      @sigma-exists-from-index-map |
                                                        (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                                                        [by exists_elim with-index-map-sigma2]

                                                    }
                                                  @conclusion-sigma-exists-from-removal |
                                                    (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                                                    [by exists_elim with-removed-t2]
                                                }
                                              @conclusion-sigma-exists-from-predecessor |
                                                (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                                                [by exists_elim with-matched-m]
                                            }
                                          @conclusion-sigma-exists |
                                            (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                                            [by exists_elim with-predecessor-lprev]
                                        }
                                      @equal-products-give-step |
                                        productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                                        [by implies_intro given-equal-products-step]
                                    }
                                  @t-primes-give-step |
                                    (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                                    [by implies_intro given-t-primes-step]
                                }
                              @s-primes-give-step |
                                (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                                [by implies_intro given-s-primes-step]
                            }
                          @l-nonneg-gives-step |
                            is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                            [by implies_intro given-l-nonneg-step]
                        }
                      @discharge-l-step |
                        forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                        [by forall_intro generalize-l-step]
                    }
                  @discharge-t-step |
                    forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                    [by forall_intro generalize-t-step]
                }
              @p-succ-k |
                forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i))))
                [by forall_intro successor-case-proof]
            }
          @inductive-hypothesis-gives-p-succ-k |
            (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i))))) -> (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i)))))
            [by implies_intro given-inductive-hypothesis]
        }
      @induction-step-at-k |
        is_nonneg(k) -> (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i))))) -> (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i)))))
        [by implies_intro given-k-nonneg]
    }
  @induction-step-for-all-k |
    forall k: Int; is_nonneg(k) -> (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i))))) -> (forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, succ(k)) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, succ(k)) -> is_nonneg(i2) -> less_than(i2, succ(k)) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, succ(k)) -> at(s, i) = at(t, at(sigma, i)))))
    [by forall_intro induction-step]

  @conclusion |
    forall k: Int; is_nonneg(k) ->
      forall s: Seq; forall t: Seq; forall l: Int;
      is_nonneg(l)
      -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
      -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))))
      -> productUpTo(s, k) = productUpTo(t, l)
      -> (exists sigma: Seq;
      (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l)))
      and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2)
      and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i))))
    [by instantiate nonnegInduction((fun k: Int => forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i)))))) base-case induction-step-for-all-k]
qed
```


### Uniqueness

Judson's uniqueness — "if `n = q₁⋯qₗ` is a second factorization into primes, then
`k = l` and the `qᵢ` are, after reordering, the `pᵢ`" — is proved **in full**, in
three parts culminating in `ftaUniquenessFull` below.

The core is that **every prime in one factorization appears in the other**: given
two prime factorizations of the same product `s` (length `k`) and `t` (length
`l`), each entry `at(s, i)` is prime and divides `productUpTo(s, k) =
productUpTo(t, l)` (`everyEntryDividesProduct`), so by
`primeDividesProductImpliesMember` it divides some `at(t, j)`; and a prime
dividing a prime equals it (the primality clause of `at(t, j)`, since
`at(s, i) > 1`). Hence `at(s, i) = at(t, j)` — each factor of `s` is a factor of
`t` (theorem `ftaUniqueness`).

That recurrence is strengthened two ways, both by induction on `k` that peels the
last factor `at(s, k)`, matches it to some `at(t, m)` via the crux, deletes that
entry with `seqRemoveExists`, and recurses on the shortened `t`:

- **equal length** — `k = l` (theorem `factorizationLengthUnique`): the peeled
  primes cancel one-for-one (`mulCancelRight`), so the shorter products stay equal
  and the induction bottoms out only when both lengths hit zero.
- **an explicit reordering** — a bijection `σ` (a `Seq` of indices) with `at(s, i)
  = at(t, σ(i))`, injective and into `t`'s index range (theorem
  `factorizationPermutationExists`): the recursion records the matched index at
  each step, lifting the sub-factorization's map back through the deletion with
  `seqShiftPastExists` (the removal-inverse reindex) and appending the new match.

The permutation `σ` is exactly Judson's "after reordering." Together with `k = l`
it is multiset equality: the two factorizations are the same primes with the same
multiplicities, related by a concrete index bijection — no `Seq`-permutation
*predicate* is needed, since `σ` is produced as an ordinary existentially-bound
sequence.

```bpa
theorem ftaUniqueness: forall s: Seq; forall k: Int; forall t: Seq; forall l: Int;
  is_nonneg(k) -> is_nonneg(l)
  -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
  -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))))
  -> productUpTo(s, k) = productUpTo(t, l)
  -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (exists j: Int; is_nonneg(j) and less_than(j, l) and at(s, i) = at(t, j)))
proof
  @generalize-s |
    fix s: Seq {
      @generalize-k |
        fix k: Int {
          @generalize-t |
            fix t: Seq {
              @generalize-l |
                fix l: Int {
                  @given-k-nonneg |
                    assume is_nonneg(k) {
                      @given-l-nonneg |
                        assume is_nonneg(l) {
                          @given-s-primes |
                            assume forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))) {
                              @given-t-primes |
                                assume forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))) {
                                  @given-equal-products |
                                    assume productUpTo(s, k) = productUpTo(t, l) {
                                      @per-index |
                                        fix i: Int {
                                          @given-i-nonneg |
                                            assume is_nonneg(i) {
                                              @given-i-less_than-k |
                                                assume less_than(i, k) {
                                                  @k-nonneg |
                                                    is_nonneg(k)
                                                    [by hypothesis given-k-nonneg]
                                                  @l-nonneg |
                                                    is_nonneg(l)
                                                    [by hypothesis given-l-nonneg]
                                                  @i-nonneg |
                                                    is_nonneg(i)
                                                    [by hypothesis given-i-nonneg]
                                                  @i-less_than-k |
                                                    less_than(i, k)
                                                    [by hypothesis given-i-less_than-k]
                                                  @s-primes |
                                                    forall i2: Int; is_nonneg(i2) -> less_than(i2, k) -> (less_than(ONE, at(s, i2)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i2)) -> (d = ONE or d = at(s, i2))))
                                                    [by hypothesis given-s-primes]
                                                  @t-primes |
                                                    forall j2: Int; is_nonneg(j2) -> less_than(j2, l) -> (less_than(ONE, at(t, j2)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j2)) -> (d = ONE or d = at(t, j2))))
                                                    [by hypothesis given-t-primes]
                                                  @equal-products |
                                                    productUpTo(s, k) = productUpTo(t, l)
                                                    [by hypothesis given-equal-products]

                                                  // at(s,i) is prime
                                                  @si-prime-at-i |
                                                    is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                                    [by forall_elim(i) s-primes]
                                                  @si-prime |
                                                    less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))
                                                    [by tautology si-prime-at-i i-nonneg i-less_than-k]
                                                  @si-above-one |
                                                    less_than(ONE, at(s, i))
                                                    [by and_elim_left si-prime]

                                                  // at(s,i) divides productUpTo(s,k), hence productUpTo(t,l)
                                                  @entries-of-s-divide-2 |
                                                    forall i2: Int; is_nonneg(i2) -> less_than(i2, k) -> does_divide(at(s, i2), productUpTo(s, k))
                                                    [by specialize everyEntryDividesProduct(s, k) k-nonneg]
                                                  @si-divides-at-i |
                                                    is_nonneg(i) -> less_than(i, k) -> does_divide(at(s, i), productUpTo(s, k))
                                                    [by forall_elim(i) entries-of-s-divide-2]
                                                  @si-divides-prod-s |
                                                    does_divide(at(s, i), productUpTo(s, k))
                                                    [by tautology si-divides-at-i i-nonneg i-less_than-k]
                                                  @si-divides-prod-t |
                                                    does_divide(at(s, i), productUpTo(t, l))
                                                    [by rewrite equal-products si-divides-prod-s]

                                                  // crux: at(s,i) divides some entry at(t,j)
                                                  @crux |
                                                    forall kk: Int; is_nonneg(kk) -> forall ss: Seq; forall p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> does_divide(p, productUpTo(ss, kk)) -> (exists i2: Int; is_nonneg(i2) and less_than(i2, kk) and does_divide(p, at(ss, i2)))
                                                    [by theorem primeDividesProductImpliesMember]
                                                  @crux-at-l |
                                                    is_nonneg(l) -> forall ss: Seq; forall p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> does_divide(p, productUpTo(ss, l)) -> (exists i2: Int; is_nonneg(i2) and less_than(i2, l) and does_divide(p, at(ss, i2)))
                                                    [by forall_elim(l) crux]
                                                  @crux-l |
                                                    forall ss: Seq; forall p: Int; (less_than(ONE, p) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> does_divide(p, productUpTo(ss, l)) -> (exists i2: Int; is_nonneg(i2) and less_than(i2, l) and does_divide(p, at(ss, i2)))
                                                    [by modus_ponens crux-at-l l-nonneg]
                                                  @crux-t-si |
                                                    (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))) -> does_divide(at(s, i), productUpTo(t, l)) -> (exists i2: Int; is_nonneg(i2) and less_than(i2, l) and does_divide(at(s, i), at(t, i2)))
                                                    [by forall_elim(t, at(s, i)) crux-l]
                                                  @member-exists |
                                                    exists i2: Int; is_nonneg(i2) and less_than(i2, l) and does_divide(at(s, i), at(t, i2))
                                                    [by tautology crux-t-si si-prime si-divides-prod-t]

                                                  @with-member-j |
                                                    unpack j: Int from member-exists {
                                                      @j-props |
                                                        is_nonneg(j) and less_than(j, l) and does_divide(at(s, i), at(t, j))
                                                        [by hypothesis with-member-j]
                                                      @j-nonneg |
                                                        is_nonneg(j)
                                                        [by tautology j-props]
                                                      @j-less_than-l |
                                                        less_than(j, l)
                                                        [by tautology j-props]
                                                      @si-divides-tj |
                                                        does_divide(at(s, i), at(t, j))
                                                        [by tautology j-props]

                                                      // at(t,j) is prime; at(s,i) | at(t,j) with at(s,i) > 1 forces at(s,i) = at(t,j)
                                                      @tj-prime-at-j |
                                                        is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))
                                                        [by forall_elim(j) t-primes]
                                                      @tj-prime |
                                                        less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))
                                                        [by tautology tj-prime-at-j j-nonneg j-less_than-l]
                                                      @tj-divisor-clause |
                                                        forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))
                                                        [by and_elim_right tj-prime]
                                                      @zero-less_than-one |
                                                        less_than(ZERO, ONE)
                                                        [by theorem zeroLessThanOne]
                                                      @lt-trans |
                                                        forall a, b, c: Int; less_than(a, b) -> less_than(b, c) -> less_than(a, c)
                                                        [by theorem lessThanTransitive]
                                                      @lt-trans-si |
                                                        less_than(ZERO, ONE) -> less_than(ONE, at(s, i)) -> less_than(ZERO, at(s, i))
                                                        [by forall_elim(ZERO, ONE, at(s, i)) lt-trans]
                                                      @si-above-zero |
                                                        less_than(ZERO, at(s, i))
                                                        [by tautology lt-trans-si zero-less_than-one si-above-one]
                                                      @clause-at-si |
                                                        less_than(ZERO, at(s, i)) -> does_divide(at(s, i), at(t, j)) -> (at(s, i) = ONE or at(s, i) = at(t, j))
                                                        [by forall_elim(at(s, i)) tj-divisor-clause]
                                                      @si-one-or-eq |
                                                        at(s, i) = ONE or at(s, i) = at(t, j)
                                                        [by tautology clause-at-si si-above-zero si-divides-tj]
                                                      // at(s,i) > 1 rules out at(s,i) = ONE (would give ONE < ONE, false by irreflexivity)
                                                      @not-one-less_than-one |
                                                        not less_than(ONE, ONE)
                                                        [by specialize lessThanIrreflexive(ONE)]
                                                      // case-split the disjunction: at(s,i)=ONE contradicts at(s,i)>1 (rewrite
                                                      // less_than(ONE, at(s,i)) to less_than(ONE, ONE), refuted); else at(s,i)=at(t,j).
                                                      @si-eq-tj |
                                                        at(s, i) = at(t, j)
                                                        case si-one-or-eq {
                                                          @when-si-one |
                                                            assume at(s, i) = ONE {
                                                              @si-is-one |
                                                                at(s, i) = ONE
                                                                [by hypothesis when-si-one]
                                                              @one-less_than-one |
                                                                less_than(ONE, ONE)
                                                                [by rewrite si-is-one si-above-one]
                                                              @si-eq-tj-absurd |
                                                                at(s, i) = at(t, j)
                                                                [by absurd one-less_than-one not-one-less_than-one]
                                                            }
                                                          @when-si-eq |
                                                            assume at(s, i) = at(t, j) {
                                                              @si-eq-tj-direct |
                                                                at(s, i) = at(t, j)
                                                                [by hypothesis when-si-eq]
                                                            }
                                                        }

                                                      @j-witnesses |
                                                        is_nonneg(j) and less_than(j, l) and at(s, i) = at(t, j)
                                                        [by tautology j-nonneg j-less_than-l si-eq-tj]
                                                      @found-j |
                                                        exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2)
                                                        [by exists_intro(j) j-witnesses]
                                                    }
                                                  @member-found |
                                                    exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2)
                                                    [by exists_elim with-member-j]
                                                }
                                              @less_than-k-gives |
                                                less_than(i, k) -> (exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2))
                                                [by implies_intro given-i-less_than-k]
                                            }
                                          @i-nonneg-gives |
                                            is_nonneg(i) -> less_than(i, k) -> (exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2))
                                            [by implies_intro given-i-nonneg]
                                        }
                                      @all-i |
                                        forall i: Int; is_nonneg(i) -> less_than(i, k) -> (exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2))
                                        [by forall_intro per-index]
                                    }
                                  @products-give |
                                    productUpTo(s, k) = productUpTo(t, l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2)))
                                    [by implies_intro given-equal-products]
                                }
                              @t-primes-give |
                                (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2)))
                                [by implies_intro given-t-primes]
                            }
                          @s-primes-give |
                            (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2)))
                            [by implies_intro given-s-primes]
                        }
                      @l-nonneg-gives |
                        is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2)))
                        [by implies_intro given-l-nonneg]
                    }
                  @k-nonneg-gives |
                    is_nonneg(k) -> is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2)))
                    [by implies_intro given-k-nonneg]
                }
              @discharge-l |
                forall l: Int; is_nonneg(k) -> is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2)))
                [by forall_intro generalize-l]
            }
          @discharge-t |
            forall t: Seq; forall l: Int; is_nonneg(k) -> is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2)))
            [by forall_intro generalize-t]
        }
      @discharge-k |
        forall k: Int; forall t: Seq; forall l: Int; is_nonneg(k) -> is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2)))
        [by forall_intro generalize-k]
    }
  @conclusion |
    forall s: Seq; forall k: Int; forall t: Seq; forall l: Int; is_nonneg(k) -> is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (exists j2: Int; is_nonneg(j2) and less_than(j2, l) and at(s, i) = at(t, j2)))
    [by forall_intro generalize-s]
qed
```

The capstone conjoins the two strengthenings: equal length **and** an explicit
matching bijection. Both are established theorems, cited once up front, then
specialized and applied under the shared hypotheses — the whole of Judson's
"unique: `k = l` and the `qᵢ` are, after reordering, the `pᵢ`."

```bpa
theorem ftaUniquenessFull: forall k: Int; is_nonneg(k) ->
  forall s: Seq; forall t: Seq; forall l: Int;
  is_nonneg(l)
  -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
  -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))))
  -> productUpTo(s, k) = productUpTo(t, l)
  -> (k = l and (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i)))))
proof
  @generalize-k |
    fix k: Int {
      @given-k-nonneg |
        assume is_nonneg(k) {
          @generalize-s-seq |
            fix s: Seq {
              @generalize-t-seq |
                fix t: Seq {
                  @generalize-l |
                    fix l: Int {
                      @given-l-nonneg |
                        assume is_nonneg(l) {
                          @given-s-primes |
                            assume forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))) {
                              @given-t-primes |
                                assume forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j)))) {
                                  @given-equal-products |
                                    assume productUpTo(s, k) = productUpTo(t, l) {
                                      @k-nonneg |
                                        is_nonneg(k)
                                        [by hypothesis given-k-nonneg]
                                      @l-nonneg |
                                        is_nonneg(l)
                                        [by hypothesis given-l-nonneg]
                                      @s-primes |
                                        forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))
                                        [by hypothesis given-s-primes]
                                      @t-primes |
                                        forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))
                                        [by hypothesis given-t-primes]
                                      @equal-products |
                                        productUpTo(s, k) = productUpTo(t, l)
                                        [by hypothesis given-equal-products]
                                      // apply factorizationLengthUnique at all four
                                      // binders in one interleaved specialize:
                                      // k↦∀k, k-nonneg↦is_nonneg(k), then
                                      // s/t/l↦∀s,t,l and the four antecedents.
                                      @lengths-equal |
                                        k = l
                                        [by specialize factorizationLengthUnique(k, s, t, l) k-nonneg l-nonneg s-primes t-primes equal-products]
                                      @permutation-exists |
                                        (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i))))
                                        [by specialize factorizationPermutationExists(k, s, t, l) k-nonneg l-nonneg s-primes t-primes equal-products]
                                      @length-and-permutation |
                                        k = l and (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i))))
                                        [by and_intro lengths-equal permutation-exists]
                                    }
                                  @products-give |
                                    productUpTo(s, k) = productUpTo(t, l) -> (k = l and (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i)))))
                                    [by implies_intro given-equal-products]
                                }
                              @t-primes-give |
                                (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (k = l and (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i)))))
                                [by implies_intro given-t-primes]
                            }
                          @s-primes-give |
                            (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (k = l and (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i)))))
                            [by implies_intro given-s-primes]
                        }
                      @l-nonneg-gives |
                        is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (k = l and (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i)))))
                        [by implies_intro given-l-nonneg]
                    }
                  @discharge-l |
                    forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (k = l and (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i)))))
                    [by forall_intro generalize-l]
                }
              @discharge-t-seq |
                forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (k = l and (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i)))))
                [by forall_intro generalize-t-seq]
            }
          @discharge-s-seq |
            forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (k = l and (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i)))))
            [by forall_intro generalize-s-seq]
        }
      @k-nonneg-gives |
        is_nonneg(k) -> forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (k = l and (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i)))))
        [by implies_intro given-k-nonneg]
    }
  @conclusion |
    forall k: Int; is_nonneg(k) -> forall s: Seq; forall t: Seq; forall l: Int; is_nonneg(l) -> (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (less_than(ONE, at(s, i)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) -> (forall j: Int; is_nonneg(j) -> less_than(j, l) -> (less_than(ONE, at(t, j)) and (forall d: Int; less_than(ZERO, d) -> does_divide(d, at(t, j)) -> (d = ONE or d = at(t, j))))) -> productUpTo(s, k) = productUpTo(t, l) -> (k = l and (exists sigma: Seq; (forall i: Int; is_nonneg(i) -> less_than(i, k) -> (is_nonneg(at(sigma, i)) and less_than(at(sigma, i), l))) and (forall i: Int; forall i2: Int; is_nonneg(i) -> less_than(i, k) -> is_nonneg(i2) -> less_than(i2, k) -> at(sigma, i) = at(sigma, i2) -> i = i2) and (forall i: Int; is_nonneg(i) -> less_than(i, k) -> at(s, i) = at(t, at(sigma, i)))))
    [by forall_intro generalize-k]
qed
```
