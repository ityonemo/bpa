# The Integers — Primes

A literate `bpa` transliteration of **Chapter 2 ("The Integers"), §2.2 (Greatest
Common Divisors and Primes)** of Thomas W. Judson's *Abstract Algebra* (source:
`vendor/aata/src/integers.xml`; © 1997–2026 Judson, GFDL 1.3). Run
`bpa check aata/2.3-primes.md`.

This section proves the **GCD / Bézout theorem** and its **coprime corollary**,
following Judson's argument. They build on the Division Algorithm proved in
`std/integer-division.bpa` (§2.2). We bring in that library, the ℤ order,
divisibility, and the Well-Ordering schema, naming the arithmetic vocabulary
this section will use. Because there is no `gcd` function, we *characterize* the
greatest common divisor by its defining properties (positive, a common divisor,
divisible by every common divisor).

```bpa
import division <<< "std/integer-division.bpa"
import order <<< "std/integer-order.bpa"
import divides <<< "std/integer-divides.bpa"
import ring <<< "std/integer-ring.bpa"
import wellordering <<< "std/integer-wellordering.bpa"

sort Int = division.Int
const ZERO = division.ZERO
const ONE = division.ONE
func succ = division.succ
func add = division.add
func mul = division.mul
func sub = division.sub
func neg = division.neg
pred below = division.below
pred at_most = division.at_most
pred is_nonneg = division.is_nonneg
pred does_divide = division.does_divide

theorem divisionAlgorithmExists = division.divisionAlgorithmExists
theorem divisionAlgorithmUnique = division.divisionAlgorithmUnique
axiom oneIsSuccZero = division.oneIsSuccZero

// -- order and nonneg vocabulary the argument names explicitly --------------
theorem trichotomy = order.trichotomy
theorem lessThanIrreflexive = order.lessThanIrreflexive
theorem lessThanTransitive = order.lessThanTransitive
theorem lessThanToLessOrEqual = order.lessThanToLessOrEqual
theorem lessOrEqualSplit = order.lessOrEqualSplit
theorem negBelowZero = order.negBelowZero
theorem nonnegCases = order.nonnegCases
axiom lessThanElim = order.lessThanElim
axiom lessThanIntro = order.lessThanIntro
axiom nonnegZero = order.nonnegZero
axiom nonnegSucc = order.nonnegSucc
axiom addZeroLeft = order.addZeroLeft

// -- divisibility intro/elim and the closure lemmas -------------------------
// `does_divide` is now a transparent `define` (= `divides.divides(d, n)` = the
// existential `exists k; n = mul(d, k)`), so the old `dividesIntro`/`dividesElim`
// AXIOMS are gone. Recover them here as local THEOREMS proved from the definition:
// intro is `exists_intro`, elim is the identity (the pred IS the existential).
theorem dividesMul = divides.dividesMul
theorem dividesAdd = divides.dividesAdd

theorem dividesIntro: forall n, d, k: Int; n = mul(d, k) -> does_divide(d, n)
proof
  @generalize-n |
    fix n: Int {
      @generalize-d |
        fix d: Int {
          @generalize-k |
            fix k: Int {
              @given-n-is-dk |
                assume n = mul(d, k) {
                  @n-is-dk |
                    n = mul(d, k)
                    [by hypothesis given-n-is-dk]
                  @some-k-works |
                    does_divide(d, n)
                    [by exists_intro(k) n-is-dk]
                }
              @intro-at-n-d-k |
                n = mul(d, k) -> does_divide(d, n)
                [by implies_intro given-n-is-dk]
            }
          @discharge-k |
            forall k: Int; n = mul(d, k) -> does_divide(d, n)
            [by forall_intro generalize-k]
        }
      @discharge-d |
        forall d, k: Int; n = mul(d, k) -> does_divide(d, n)
        [by forall_intro generalize-d]
    }
  @conclusion |
    forall n, d, k: Int; n = mul(d, k) -> does_divide(d, n)
    [by forall_intro generalize-n]
qed

theorem dividesElim: forall d, m: Int; does_divide(d, m) -> exists k: Int; m = mul(d, k)
proof
  @generalize-d |
    fix d: Int {
      @generalize-m |
        fix m: Int {
          @given-d-divides-m |
            assume does_divide(d, m) {
              @the-existential |
                exists k: Int; m = mul(d, k)
                [by hypothesis given-d-divides-m]
            }
          @elim-at-d-m |
            does_divide(d, m) -> exists k: Int; m = mul(d, k)
            [by implies_intro given-d-divides-m]
        }
      @discharge-m |
        forall m: Int; does_divide(d, m) -> exists k: Int; m = mul(d, k)
        [by forall_intro generalize-m]
    }
  @conclusion |
    forall d, m: Int; does_divide(d, m) -> exists k: Int; m = mul(d, k)
    [by forall_intro generalize-d]
qed

// -- ring rearrangements the book elides ------------------------------------
axiom subDef = order.subDef
theorem mulOneRight = ring.mulOneRight
theorem mulZeroRight = ring.mulZeroRight
theorem mulNegRight = ring.mulNegRight
theorem mulAddDistribLeft = ring.mulAddDistribLeft
theorem mulAddDistribRight = ring.mulAddDistribRight
theorem mulIsAssociative = ring.mulIsAssociative
theorem addIsCommutative = ring.addIsCommutative
theorem addIsAssociative = ring.addIsAssociative
theorem addZeroRight = ring.addZeroRight
theorem addSuccRight = ring.addSuccRight
theorem addNegRight = ring.addNegRight
theorem addNegLeft = ring.addNegLeft
theorem negNeg = ring.negNeg
theorem negAdd = ring.negAdd
theorem addDiffReaches = order.addDiffReaches
theorem addCancelLeft = order.addCancelLeft

// -- the Well-Ordering schema ------------------------------------------------
theorem nonnegWellOrdering = wellordering.nonnegWellOrdering
```

A positive integer is in particular nonnegative — the fact that lets the
Well-Ordering schema's guard (`is_nonneg`) be discharged from `below(ZERO, x)`.
Unfolding `below(ZERO, x)` gives a nonnegative gap `d` with `ZERO + succ(d) = x`;
since `add(ZERO, succ(d)) = succ(d)` and the successor of a nonnegative is
nonnegative, `x` itself is nonnegative.

```bpa
theorem positiveIsNonneg: forall x: Int; below(ZERO, x) -> is_nonneg(x)
proof
  @generalize-x |
    fix x: Int {
      @given-x-positive |
        assume below(ZERO, x) {
          @x-positive |
            below(ZERO, x)
            [by hypothesis given-x-positive]
          @less-than-elim |
            forall a, b: Int; below(a, b) -> exists d: Int; is_nonneg(d) and add(a, succ(d)) = b
            [by axiom lessThanElim]
          @less-than-elim-at-zero-x |
            below(ZERO, x) -> exists d: Int; is_nonneg(d) and add(ZERO, succ(d)) = x
            [by forall_elim(ZERO, x) less-than-elim]
          @gap-exists |
            exists d: Int; is_nonneg(d) and add(ZERO, succ(d)) = x
            [by modus_ponens less-than-elim-at-zero-x x-positive]
          @with-gap-d |
            unpack d: Int from gap-exists {
              @gap-nonneg-and-sums |
                is_nonneg(d) and add(ZERO, succ(d)) = x
                [by hypothesis with-gap-d]
              @gap-nonneg |
                is_nonneg(d)
                [by and_elim_left gap-nonneg-and-sums]
              @gap-sums-to-x |
                add(ZERO, succ(d)) = x
                [by and_elim_right gap-nonneg-and-sums]
              @add-zero-left |
                forall n: Int; add(ZERO, n) = n
                [by axiom addZeroLeft]
              @zero-plus-succ-is-succ |
                add(ZERO, succ(d)) = succ(d)
                [by forall_elim(succ(d)) add-zero-left]
              @succ-gap-is-x |
                succ(d) = x
                [by rewrite zero-plus-succ-is-succ gap-sums-to-x]
              @nonneg-succ |
                forall n: Int; is_nonneg(n) -> is_nonneg(succ(n))
                [by axiom nonnegSucc]
              @succ-of-nonneg-is-nonneg |
                is_nonneg(d) -> is_nonneg(succ(d))
                [by forall_elim(d) nonneg-succ]
              @succ-gap-nonneg |
                is_nonneg(succ(d))
                [by modus_ponens succ-of-nonneg-is-nonneg gap-nonneg]
              @conclusion-x-nonneg |
                is_nonneg(x)
                [by rewrite succ-gap-is-x succ-gap-nonneg]
            }
          @x-nonneg |
            is_nonneg(x)
            [by exists_elim with-gap-d]
        }
      @positive-implies-nonneg |
        below(ZERO, x) -> is_nonneg(x)
        [by implies_intro given-x-positive]
    }
  @conclusion |
    forall x: Int; below(ZERO, x) -> is_nonneg(x)
    [by forall_intro generalize-x]
qed
```

The Bézout argument will, at its crux, learn of a remainder `r'` that is both
nonnegative and *not* positive, and must conclude `r' = ZERO`. A nonnegative
integer splits (`nonnegCases`) into `ZERO` or a successor `succ(m)` of a
nonnegative `m`; a successor is positive (`add(ZERO, succ(m)) = succ(m)`, so
`lessThanIntro` gives `below(ZERO, succ(m))`), so if the integer is *not*
positive the successor case is impossible and it must be `ZERO`.

```bpa
theorem nonnegNotPositiveIsZero: forall x: Int; is_nonneg(x) -> (not below(ZERO, x)) -> x = ZERO
proof
  @generalize-x |
    fix x: Int {
      @given-x-nonneg |
        assume is_nonneg(x) {
          @given-x-not-positive |
            assume not below(ZERO, x) {
              @x-nonneg |
                is_nonneg(x)
                [by hypothesis given-x-nonneg]
              @x-not-positive |
                not below(ZERO, x)
                [by hypothesis given-x-not-positive]
              @nonneg-cases |
                forall n: Int; is_nonneg(n) -> (n = ZERO or (exists m: Int; is_nonneg(m) and n = succ(m)))
                [by theorem nonnegCases]
              @x-cases-if-nonneg |
                is_nonneg(x) -> (x = ZERO or (exists m: Int; is_nonneg(m) and x = succ(m)))
                [by forall_elim(x) nonneg-cases]
              @x-zero-or-successor |
                x = ZERO or (exists m: Int; is_nonneg(m) and x = succ(m))
                [by modus_ponens x-cases-if-nonneg x-nonneg]
              @conclusion-x-is-zero |
                x = ZERO
                case on x-zero-or-successor {
                  @when-x-is-zero |
                    assume x = ZERO {
                      @conclusion-x-is-zero |
                        x = ZERO
                        [by hypothesis when-x-is-zero]
                    }
                  @when-x-is-successor |
                    assume exists m: Int; is_nonneg(m) and x = succ(m) {
                      @successor-exists |
                        exists m: Int; is_nonneg(m) and x = succ(m)
                        [by hypothesis when-x-is-successor]
                      @with-predecessor-m |
                        unpack m: Int from successor-exists {
                          @gap-nonneg-and-x-is-successor |
                            is_nonneg(m) and x = succ(m)
                            [by hypothesis with-predecessor-m]
                          @gap-nonneg |
                            is_nonneg(m)
                            [by and_elim_left gap-nonneg-and-x-is-successor]
                          @x-is-successor |
                            x = succ(m)
                            [by and_elim_right gap-nonneg-and-x-is-successor]
                          @add-zero-left |
                            forall n: Int; add(ZERO, n) = n
                            [by axiom addZeroLeft]
                          @zero-plus-successor-is-successor |
                            add(ZERO, succ(m)) = succ(m)
                            [by forall_elim(succ(m)) add-zero-left]
                          @successor-is-x |
                            succ(m) = x
                            [by symmetry x-is-successor]
                          @zero-plus-successor-is-x |
                            add(ZERO, succ(m)) = x
                            [by rewrite successor-is-x zero-plus-successor-is-successor]
                          @less-than-intro |
                            forall a, d, b: Int; is_nonneg(d) -> add(a, succ(d)) = b -> below(a, b)
                            [by axiom lessThanIntro]
                          @successor-is-positive-if-summed |
                            is_nonneg(m) -> add(ZERO, succ(m)) = x -> below(ZERO, x)
                            [by forall_elim(ZERO, m, x) less-than-intro]
                          @x-is-positive |
                            below(ZERO, x)
                            [by tautology successor-is-positive-if-summed gap-nonneg zero-plus-successor-is-x]
                          @conclusion-x-is-zero |
                            x = ZERO
                            [by absurd x-is-positive x-not-positive]
                        }
                      @conclusion-x-is-zero |
                        x = ZERO
                        [by exists_elim with-predecessor-m]
                    }
                }
            }
          @not-positive-implies-zero |
            (not below(ZERO, x)) -> x = ZERO
            [by implies_intro given-x-not-positive]
        }
      @nonneg-implies-not-positive-implies-zero |
        is_nonneg(x) -> (not below(ZERO, x)) -> x = ZERO
        [by implies_intro given-x-nonneg]
    }
  @conclusion |
    forall x: Int; is_nonneg(x) -> (not below(ZERO, x)) -> x = ZERO
    [by forall_intro generalize-x]
qed
```

Judson's nonemptiness argument splits on the sign of `a`: when `a` is positive
the witness `a·1 + b·0 = a` is a positive member; when `a` is negative the
witness `a·(−1) + b·0 = −a` is, and this needs that the negation of a negative
integer is positive. Unfolding `below(a, ZERO)` gives a nonnegative gap `d` with
`add(a, succ(d)) = ZERO`; adding `−a` on the left and cancelling `a` shows
`succ(d) = −a`, and `lessThanIntro` reads `below(ZERO, −a)` off that.

```bpa
theorem negativeNegIsPositive: forall a: Int; below(a, ZERO) -> below(ZERO, neg(a))
proof
  @generalize-a |
    fix a: Int {
      @given-a-negative |
        assume below(a, ZERO) {
          @a-negative |
            below(a, ZERO)
            [by hypothesis given-a-negative]
          @less-than-elim |
            forall x, y: Int; below(x, y) -> exists d: Int; is_nonneg(d) and add(x, succ(d)) = y
            [by axiom lessThanElim]
          @gap-below-a-zero |
            below(a, ZERO) -> exists d: Int; is_nonneg(d) and add(a, succ(d)) = ZERO
            [by forall_elim(a, ZERO) less-than-elim]
          @gap-exists |
            exists d: Int; is_nonneg(d) and add(a, succ(d)) = ZERO
            [by modus_ponens gap-below-a-zero a-negative]
          @with-gap-d |
            unpack d: Int from gap-exists {
              @gap-nonneg-and-sums |
                is_nonneg(d) and add(a, succ(d)) = ZERO
                [by hypothesis with-gap-d]
              @gap-nonneg |
                is_nonneg(d)
                [by and_elim_left gap-nonneg-and-sums]
              @a-plus-successor-is-zero |
                add(a, succ(d)) = ZERO
                [by and_elim_right gap-nonneg-and-sums]
              // succ(d) = -a: add -a on the left and cancel a.
              @neg-a-sum-reflexive |
                add(neg(a), add(a, succ(d))) = add(neg(a), add(a, succ(d)))
                [by reflexivity]
              @neg-a-times-sum-is-neg-a-times-zero |
                add(neg(a), add(a, succ(d))) = add(neg(a), ZERO)
                [by rewrite a-plus-successor-is-zero neg-a-sum-reflexive]
              @add-is-associative |
                forall x, y, z: Int; add(add(x, y), z) = add(x, add(y, z))
                [by theorem addIsAssociative]
              @regroup |
                add(add(neg(a), a), succ(d)) = add(neg(a), add(a, succ(d)))
                [by forall_elim(neg(a), a, succ(d)) add-is-associative]
              @add-neg-left |
                forall n: Int; add(neg(n), n) = ZERO
                [by theorem addNegLeft]
              @neg-a-plus-a-is-zero |
                add(neg(a), a) = ZERO
                [by forall_elim(a) add-neg-left]
              @regroup-with-zero |
                add(ZERO, succ(d)) = add(neg(a), add(a, succ(d)))
                [by rewrite neg-a-plus-a-is-zero regroup]
              @add-zero-left-lemma |
                forall n: Int; add(ZERO, n) = n
                [by axiom addZeroLeft]
              @zero-plus-successor-collapses |
                add(ZERO, succ(d)) = succ(d)
                [by forall_elim(succ(d)) add-zero-left-lemma]
              @cancelled-sum-is-successor |
                add(neg(a), add(a, succ(d))) = succ(d)
                [by rewrite regroup-with-zero zero-plus-successor-collapses]
              @add-zero-right-lemma |
                forall n: Int; add(n, ZERO) = n
                [by theorem addZeroRight]
              @neg-a-plus-zero-is-neg-a |
                add(neg(a), ZERO) = neg(a)
                [by forall_elim(neg(a)) add-zero-right-lemma]
              @successor-is-neg-a-step |
                succ(d) = add(neg(a), ZERO)
                [by rewrite cancelled-sum-is-successor neg-a-times-sum-is-neg-a-times-zero]
              @successor-is-neg-a |
                succ(d) = neg(a)
                [by rewrite neg-a-plus-zero-is-neg-a successor-is-neg-a-step]
              // read below(ZERO, -a) off add(ZERO, succ(d)) = -a.
              @zero-plus-successor-is-neg-a |
                add(ZERO, succ(d)) = neg(a)
                [by rewrite successor-is-neg-a zero-plus-successor-collapses]
              @less-than-intro |
                forall x, e, y: Int; is_nonneg(e) -> add(x, succ(e)) = y -> below(x, y)
                [by axiom lessThanIntro]
              @neg-a-positive-if-summed |
                is_nonneg(d) -> add(ZERO, succ(d)) = neg(a) -> below(ZERO, neg(a))
                [by forall_elim(ZERO, d, neg(a)) less-than-intro]
              @conclusion-neg-a-positive |
                below(ZERO, neg(a))
                [by tautology neg-a-positive-if-summed gap-nonneg zero-plus-successor-is-neg-a]
            }
          @neg-a-positive |
            below(ZERO, neg(a))
            [by exists_elim with-gap-d]
        }
      @negative-implies-neg-positive |
        below(a, ZERO) -> below(ZERO, neg(a))
        [by implies_intro given-a-negative]
    }
  @conclusion |
    forall a: Int; below(a, ZERO) -> below(ZERO, neg(a))
    [by forall_intro generalize-a]
qed
```

The heart of Judson's argument that `d = ar + bs` divides `a` runs the Division
Algorithm to write `a = dq + r'` and shows the remainder `r'` is *itself* a
member of `S`: substituting `d = ar + bs`, Judson computes `r' = a − dq =
a − (ar + bs)q = a(1 − rq) + b(−sq)` in one line. We isolate that elided ring
identity here. From `a = dq + r'` we recover `r' = a − dq` by cancelling `dq`
(via `addDiffReaches` and `addCancelLeft`); substituting `d` and rearranging by
distributing and collecting the `a` and `b` coefficients (`simplify` over the
ring lemmas) gives the member form `r' = a(1 − rq) + b(−sq)`.

```bpa
theorem remainderIsMember(a: Int, b: Int, d: Int, r: Int, s: Int, q: Int, remainder: Int):
  a = add(mul(d, q), remainder) -> d = add(mul(a, r), mul(b, s))
  -> remainder = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
proof
  @given-a-decomposes |
    assume a = add(mul(d, q), remainder) {
      @given-d-value |
        assume d = add(mul(a, r), mul(b, s)) {
          @a-decomposes |
            a = add(mul(d, q), remainder)
            [by hypothesis given-a-decomposes]
          @d-value |
            d = add(mul(a, r), mul(b, s))
            [by hypothesis given-d-value]
          // remainder = a - dq, by cancelling dq off a = dq + remainder.
          @difference-reaches |
            forall x, y: Int; add(x, sub(y, x)) = y
            [by theorem addDiffReaches]
          @dq-plus-difference-is-a |
            add(mul(d, q), sub(a, mul(d, q))) = a
            [by forall_elim(mul(d, q), a) difference-reaches]
          @dq-plus-difference-is-decomposition |
            add(mul(d, q), sub(a, mul(d, q))) = add(mul(d, q), remainder)
            [by rewrite a-decomposes dq-plus-difference-is-a]
          @cancel-left |
            forall c, x, y: Int; add(c, x) = add(c, y) -> x = y
            [by theorem addCancelLeft]
          @cancel-the-quotient-term |
            add(mul(d, q), sub(a, mul(d, q))) = add(mul(d, q), remainder) -> sub(a, mul(d, q)) = remainder
            [by forall_elim(mul(d, q), sub(a, mul(d, q)), remainder) cancel-left]
          @difference-is-remainder |
            sub(a, mul(d, q)) = remainder
            [by modus_ponens cancel-the-quotient-term dq-plus-difference-is-decomposition]
          @remainder-is-difference |
            remainder = sub(a, mul(d, q))
            [by symmetry difference-is-remainder]
          // substitute d = ar + bs into the difference a - dq.
          @remainder-with-d-expanded |
            remainder = sub(a, mul(add(mul(a, r), mul(b, s)), q))
            [by rewrite d-value remainder-is-difference]
          // the elided algebra: a - (ar + bs)q = a(1 - rq) + b(-sq).
          @difference-collects-coefficients |
            sub(a, mul(add(mul(a, r), mul(b, s)), q)) = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
            [by simplify subDef mulAddDistribLeft mulAddDistribRight mulNegRight mulOneRight mulIsAssociative addIsAssociative negAdd]
          @conclusion-remainder-is-member |
            remainder = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
            [by rewrite difference-collects-coefficients remainder-with-d-expanded]
        }
      @d-value-implies-member |
        d = add(mul(a, r), mul(b, s)) -> remainder = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
        [by implies_intro given-d-value]
    }
  @conclusion |
    a = add(mul(d, q), remainder) -> d = add(mul(a, r), mul(b, s)) -> remainder = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
    [by implies_intro given-a-decomposes]
qed
```

The coprime corollary will produce a positive common divisor `d` that must equal
`ONE` or `−ONE`; the second alternative is ruled out because `−1` is *below*
zero, so it cannot also be positive. We record that `−1 < 0` here: `−1` is
nonnegatively-gapped below `0` by `succ(ZERO) = ONE`, since
`add(neg(ONE), succ(ZERO)) = add(neg(ONE), ONE) = ZERO`, and `lessThanIntro`
reads `below(neg(ONE), ZERO)` off that.

```bpa
theorem negOneBelowZero: below(neg(ONE), ZERO)
proof
  @one-is-succ-zero |
    ONE = succ(ZERO)
    [by axiom oneIsSuccZero]
  @add-neg-left |
    forall n: Int; add(neg(n), n) = ZERO
    [by theorem addNegLeft]
  @neg-one-plus-one-is-zero |
    add(neg(ONE), ONE) = ZERO
    [by forall_elim(ONE) add-neg-left]
  @neg-one-plus-succ-zero-is-zero |
    add(neg(ONE), succ(ZERO)) = ZERO
    [by rewrite one-is-succ-zero neg-one-plus-one-is-zero]
  @zero-is-nonneg |
    is_nonneg(ZERO)
    [by axiom nonnegZero]
  @less-than-intro |
    forall a, d, b: Int; is_nonneg(d) -> add(a, succ(d)) = b -> below(a, b)
    [by axiom lessThanIntro]
  @neg-one-negative-if-summed |
    is_nonneg(ZERO) -> add(neg(ONE), succ(ZERO)) = ZERO -> below(neg(ONE), ZERO)
    [by forall_elim(neg(ONE), ZERO, ZERO) less-than-intro]
  @conclusion |
    below(neg(ONE), ZERO)
    [by tautology neg-one-negative-if-summed zero-is-nonneg neg-one-plus-succ-zero-is-zero]
qed
```

Before invoking Well-Ordering, Judson must exhibit *some* positive member of
`S = {am + bn : am + bn > 0}` — the set is nonempty because `|a| ∈ S`. Since
`a ≠ 0`, trichotomy splits `a` into positive or negative (the `a = 0` case is
excluded). If `a > 0`, the combination `a·1 + b·0 = a` is a positive member; if
`a < 0`, then `−a > 0` (`negativeNegIsPositive`) and `a·(−1) + b·0 = −a` is a
positive member. Either way the witness is positive, hence nonnegative
(`positiveIsNonneg`), discharging the schema's guard. We package this nonemptiness
as a schema in `a`, `b`, so its conclusion matches the exact shape the
Well-Ordering instance below consumes.

```bpa
theorem combinationSetNonempty(a: Int, b: Int):
  a != ZERO
  -> exists n: Int; is_nonneg(n) and (below(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
proof
  @given-a-nonzero |
    assume a != ZERO {
      @a-nonzero |
        a != ZERO
        [by hypothesis given-a-nonzero]
      @order-is-trichotomous |
        forall x, y: Int; below(x, y) or x = y or below(y, x)
        [by theorem trichotomy]
      @a-sign-split |
        below(a, ZERO) or a = ZERO or below(ZERO, a)
        [by forall_elim(a, ZERO) order-is-trichotomous]
      @conclusion-set-nonempty |
        exists n: Int; is_nonneg(n) and (below(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
        case on a-sign-split {
          @when-a-negative |
            assume below(a, ZERO) {
              @a-negative |
                below(a, ZERO)
                [by hypothesis when-a-negative]
              @negative-neg-is-positive |
                forall x: Int; below(x, ZERO) -> below(ZERO, neg(x))
                [by theorem negativeNegIsPositive]
              @negative-neg-is-positive-at-a |
                below(a, ZERO) -> below(ZERO, neg(a))
                [by forall_elim(a) negative-neg-is-positive]
              @neg-a-positive |
                below(ZERO, neg(a))
                [by modus_ponens negative-neg-is-positive-at-a a-negative]
              @positive-is-nonneg |
                forall x: Int; below(ZERO, x) -> is_nonneg(x)
                [by theorem positiveIsNonneg]
              @positive-is-nonneg-at-neg-a |
                below(ZERO, neg(a)) -> is_nonneg(neg(a))
                [by forall_elim(neg(a)) positive-is-nonneg]
              @neg-a-nonneg |
                is_nonneg(neg(a))
                [by modus_ponens positive-is-nonneg-at-neg-a neg-a-positive]
              @neg-a-is-combination |
                neg(a) = add(mul(a, neg(ONE)), mul(b, ZERO))
                [by simplify mulNegRight mulOneRight mulZeroRight addZeroRight]
              @neg-a-is-some-combination-in-nn |
                exists nn: Int; neg(a) = add(mul(a, neg(ONE)), mul(b, nn))
                [by exists_intro(ZERO) neg-a-is-combination]
              @neg-a-is-a-combination |
                exists mm: Int; exists nn: Int; neg(a) = add(mul(a, mm), mul(b, nn))
                [by exists_intro(neg(ONE)) neg-a-is-some-combination-in-nn]
              @neg-a-positive-and-combination |
                below(ZERO, neg(a)) and (exists mm: Int; exists nn: Int; neg(a) = add(mul(a, mm), mul(b, nn)))
                [by and_intro neg-a-positive neg-a-is-a-combination]
              @neg-a-is-nonneg-member |
                is_nonneg(neg(a)) and (below(ZERO, neg(a)) and (exists mm: Int; exists nn: Int; neg(a) = add(mul(a, mm), mul(b, nn))))
                [by and_intro neg-a-nonneg neg-a-positive-and-combination]
              @conclusion-set-nonempty |
                exists n: Int; is_nonneg(n) and (below(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
                [by exists_intro(neg(a)) neg-a-is-nonneg-member]
            }
          @when-a-zero |
            assume a = ZERO {
              @a-is-zero |
                a = ZERO
                [by hypothesis when-a-zero]
              @conclusion-set-nonempty |
                exists n: Int; is_nonneg(n) and (below(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
                [by absurd a-is-zero a-nonzero]
            }
          @when-a-positive |
            assume below(ZERO, a) {
              @a-positive |
                below(ZERO, a)
                [by hypothesis when-a-positive]
              @positive-is-nonneg |
                forall x: Int; below(ZERO, x) -> is_nonneg(x)
                [by theorem positiveIsNonneg]
              @positive-is-nonneg-at-a |
                below(ZERO, a) -> is_nonneg(a)
                [by forall_elim(a) positive-is-nonneg]
              @a-nonneg |
                is_nonneg(a)
                [by modus_ponens positive-is-nonneg-at-a a-positive]
              @a-is-combination |
                a = add(mul(a, ONE), mul(b, ZERO))
                [by simplify mulOneRight mulZeroRight addZeroRight]
              @a-is-some-combination-in-nn |
                exists nn: Int; a = add(mul(a, ONE), mul(b, nn))
                [by exists_intro(ZERO) a-is-combination]
              @a-is-a-combination |
                exists mm: Int; exists nn: Int; a = add(mul(a, mm), mul(b, nn))
                [by exists_intro(ONE) a-is-some-combination-in-nn]
              @a-positive-and-combination |
                below(ZERO, a) and (exists mm: Int; exists nn: Int; a = add(mul(a, mm), mul(b, nn)))
                [by and_intro a-positive a-is-a-combination]
              @a-is-nonneg-member |
                is_nonneg(a) and (below(ZERO, a) and (exists mm: Int; exists nn: Int; a = add(mul(a, mm), mul(b, nn))))
                [by and_intro a-nonneg a-positive-and-combination]
              @conclusion-set-nonempty |
                exists n: Int; is_nonneg(n) and (below(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
                [by exists_intro(a) a-is-nonneg-member]
            }
        }
    }
  @conclusion |
    a != ZERO
      -> exists n: Int; is_nonneg(n) and (below(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
    [by implies_intro given-a-nonzero]
qed
```

## The GCD / Bézout Theorem

> **Theorem 2.10.** Let $a$ and $b$ be nonzero integers. Then there exist integers
> $r$ and $s$ such that $\gcd(a,b) = ar + bs$. Furthermore, the greatest common
> divisor of $a$ and $b$ is unique.

There is no `gcd` *function* in this development, so we prove the theorem in the
form that *characterizes* the gcd by its defining properties. `bezout` asserts a
single positive integer `d` that (i) divides `a` and `b`, (ii) is divisible by
every common divisor `e`, and (iii) is an integer combination `d = ar + bs`.
Uniqueness is then automatic: any two integers with property (ii) divide each
other, and — being positive — are equal; and (iii) is Bézout's identity itself.

Judson's proof: let `S = {am + bn : am + bn > 0}`. `S` is nonempty
(`combinationSetNonempty`), and every member is positive hence nonnegative, so
Well-Ordering hands us a *least* member `d = ar + bs`. We claim `d = gcd(a,b)`.
Running the Division Algorithm on `a` by `d` gives `a = dq + r'` with
`0 ≤ r' < d`. Were `r' > 0`, the identity `r' = a(1 − rq) + b(−sq)`
(`remainderIsMember`) would make `r'` a *member of `S` smaller than `d`*,
contradicting minimality; so `r' = 0` and `d ∣ a`. Symmetrically `d ∣ b`. Finally
any common divisor `e` of `a`, `b` divides `ar + bs = d`, so `d` is the greatest.

```bpa
theorem bezout: forall a, b: Int; a != ZERO -> b != ZERO ->
  exists d: Int;
  below(ZERO, d)
  and does_divide(d, a) and does_divide(d, b)
  and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d))
  and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @given-a-nonzero |
            assume a != ZERO {
              @given-b-nonzero |
                assume b != ZERO {
                  @a-nonzero |
                    a != ZERO
                    [by hypothesis given-a-nonzero]
                  // Nonemptiness of S = {am + bn > 0}, from combinationSetNonempty.
                  @set-nonempty-if-a-nonzero |
                    a != ZERO
                      -> exists n: Int; is_nonneg(n) and (below(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
                    [by instantiate combinationSetNonempty(a, b)]
                  @set-is-nonempty |
                    exists n: Int; is_nonneg(n) and (below(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn))))
                    [by modus_ponens set-nonempty-if-a-nonzero a-nonzero]
                  // Well-Ordering: S has a least member d.
                  @well-ordering-on-S |
                    (exists n: Int; is_nonneg(n) and (below(ZERO, n) and (exists mm: Int; exists nn: Int; n = add(mul(a, mm), mul(b, nn)))))
                      -> exists least: Int; is_nonneg(least) and (below(ZERO, least) and (exists mm: Int; exists nn: Int; least = add(mul(a, mm), mul(b, nn)))) and (forall m: Int; is_nonneg(m) -> (below(ZERO, m) and (exists mm: Int; exists nn: Int; m = add(mul(a, mm), mul(b, nn)))) -> at_most(least, m))
                    [by instantiate nonnegWellOrdering((fun x: Int => below(ZERO, x) and (exists mm: Int; exists nn: Int; x = add(mul(a, mm), mul(b, nn)))))]
                  @least-member-exists |
                    exists least: Int; is_nonneg(least) and (below(ZERO, least) and (exists mm: Int; exists nn: Int; least = add(mul(a, mm), mul(b, nn)))) and (forall m: Int; is_nonneg(m) -> (below(ZERO, m) and (exists mm: Int; exists nn: Int; m = add(mul(a, mm), mul(b, nn)))) -> at_most(least, m))
                    [by modus_ponens well-ordering-on-S set-is-nonempty]
                  @with-least-member-d |
                    unpack d: Int from least-member-exists {
                      @d-is-least-member |
                        is_nonneg(d) and (below(ZERO, d) and (exists mm: Int; exists nn: Int; d = add(mul(a, mm), mul(b, nn)))) and (forall m: Int; is_nonneg(m) -> (below(ZERO, m) and (exists mm: Int; exists nn: Int; m = add(mul(a, mm), mul(b, nn)))) -> at_most(d, m))
                        [by hypothesis with-least-member-d]
                      @d-is-positive |
                        below(ZERO, d)
                        [by tautology d-is-least-member]
                      @d-is-a-combination |
                        exists mm: Int; exists nn: Int; d = add(mul(a, mm), mul(b, nn))
                        [by tautology d-is-least-member]
                      @d-is-least |
                        forall m: Int; is_nonneg(m) -> (below(ZERO, m) and (exists mm: Int; exists nn: Int; m = add(mul(a, mm), mul(b, nn)))) -> at_most(d, m)
                        [by tautology d-is-least-member]
                      // name the coefficients: d = ar + bs.
                      @with-coefficient-r |
                        unpack r: Int from d-is-a-combination {
                          @d-is-combination-in-r |
                            exists nn: Int; d = add(mul(a, r), mul(b, nn))
                            [by hypothesis with-coefficient-r]
                          @with-coefficient-s |
                            unpack s: Int from d-is-combination-in-r {
                              @d-equals-combination |
                                d = add(mul(a, r), mul(b, s))
                                [by hypothesis with-coefficient-s]
                              // d | a: run the Division Algorithm a = dq + r'.
                              @division-of-a-by-d |
                                forall x, y: Int; below(ZERO, y) -> exists q: Int; exists rem: Int; x = add(mul(y, q), rem) and is_nonneg(rem) and below(rem, y)
                                [by theorem divisionAlgorithmExists]
                              @division-of-a-by-d-exists |
                                below(ZERO, d) -> exists q: Int; exists rem: Int; a = add(mul(d, q), rem) and is_nonneg(rem) and below(rem, d)
                                [by forall_elim(a, d) division-of-a-by-d]
                              @a-divided-by-d |
                                exists q: Int; exists rem: Int; a = add(mul(d, q), rem) and is_nonneg(rem) and below(rem, d)
                                [by modus_ponens division-of-a-by-d-exists d-is-positive]
                              @with-quotient-q |
                                unpack q: Int from a-divided-by-d {
                                  @a-remainder-exists |
                                    exists rem: Int; a = add(mul(d, q), rem) and is_nonneg(rem) and below(rem, d)
                                    [by hypothesis with-quotient-q]
                                  @with-remainder |
                                    unpack rem: Int from a-remainder-exists {
                                      @a-decomposes-and-remainder-bounded |
                                        a = add(mul(d, q), rem) and is_nonneg(rem) and below(rem, d)
                                        [by hypothesis with-remainder]
                                      @a-decomposes |
                                        a = add(mul(d, q), rem)
                                        [by tautology a-decomposes-and-remainder-bounded]
                                      @remainder-nonneg |
                                        is_nonneg(rem)
                                        [by tautology a-decomposes-and-remainder-bounded]
                                      @remainder-below-d |
                                        below(rem, d)
                                        [by tautology a-decomposes-and-remainder-bounded]
                                      // a positive remainder would be a member of S smaller than d.
                                      @less-than-is-irreflexive |
                                        forall n: Int; not below(n, n)
                                        [by theorem lessThanIrreflexive]
                                      @d-not-below-itself |
                                        not below(d, d)
                                        [by forall_elim(d) less-than-is-irreflexive]
                                      @suppose-remainder-positive |
                                        assume below(ZERO, rem) {
                                          @remainder-positive |
                                            below(ZERO, rem)
                                            [by hypothesis suppose-remainder-positive]
                                          @remainder-is-member-identity |
                                            a = add(mul(d, q), rem) -> d = add(mul(a, r), mul(b, s)) -> rem = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
                                            [by instantiate remainderIsMember(a, b, d, r, s, q, rem)]
                                          @remainder-is-member-form |
                                            rem = add(mul(a, sub(ONE, mul(r, q))), mul(b, neg(mul(s, q))))
                                            [by tautology remainder-is-member-identity a-decomposes d-equals-combination]
                                          @remainder-is-some-combination-in-nn |
                                            exists nn: Int; rem = add(mul(a, sub(ONE, mul(r, q))), mul(b, nn))
                                            [by exists_intro(neg(mul(s, q))) remainder-is-member-form]
                                          @remainder-is-a-combination |
                                            exists mm: Int; exists nn: Int; rem = add(mul(a, mm), mul(b, nn))
                                            [by exists_intro(sub(ONE, mul(r, q))) remainder-is-some-combination-in-nn]
                                          @remainder-is-positive-combination |
                                            below(ZERO, rem) and (exists mm: Int; exists nn: Int; rem = add(mul(a, mm), mul(b, nn)))
                                            [by and_intro remainder-positive remainder-is-a-combination]
                                          @remainder-least-comparison |
                                            is_nonneg(rem) -> (below(ZERO, rem) and (exists mm: Int; exists nn: Int; rem = add(mul(a, mm), mul(b, nn)))) -> at_most(d, rem)
                                            [by forall_elim(rem) d-is-least]
                                          @d-at-most-remainder |
                                            at_most(d, rem)
                                            [by tautology remainder-least-comparison remainder-nonneg remainder-is-positive-combination]
                                          @less-or-equal-split |
                                            forall x, y: Int; at_most(x, y) -> (below(x, y) or x = y)
                                            [by theorem lessOrEqualSplit]
                                          @less-or-equal-split-at-d-remainder |
                                            at_most(d, rem) -> (below(d, rem) or d = rem)
                                            [by forall_elim(d, rem) less-or-equal-split]
                                          @d-below-or-equal-remainder |
                                            below(d, rem) or d = rem
                                            [by modus_ponens less-or-equal-split-at-d-remainder d-at-most-remainder]
                                          @remainder-would-be-a-smaller-member |
                                            below(d, d)
                                            case on d-below-or-equal-remainder {
                                              @when-d-below-remainder |
                                                assume below(d, rem) {
                                                  @d-below-remainder |
                                                    below(d, rem)
                                                    [by hypothesis when-d-below-remainder]
                                                  @less-than-transitive |
                                                    forall x, y, z: Int; below(x, y) -> below(y, z) -> below(x, z)
                                                    [by theorem lessThanTransitive]
                                                  @transitive-at-d-remainder-d |
                                                    below(d, rem) -> below(rem, d) -> below(d, d)
                                                    [by forall_elim(d, rem, d) less-than-transitive]
                                                  @conclusion-d-below-itself |
                                                    below(d, d)
                                                    [by tautology transitive-at-d-remainder-d d-below-remainder remainder-below-d]
                                                }
                                              @when-d-equals-remainder |
                                                assume d = rem {
                                                  @d-equals-remainder |
                                                    d = rem
                                                    [by hypothesis when-d-equals-remainder]
                                                  @remainder-equals-d |
                                                    rem = d
                                                    [by symmetry d-equals-remainder]
                                                  @conclusion-d-below-itself |
                                                    below(d, d)
                                                    [by rewrite remainder-equals-d remainder-below-d]
                                                }
                                            }
                                        }
                                      @remainder-not-positive |
                                        not below(ZERO, rem)
                                        [by not_intro suppose-remainder-positive remainder-would-be-a-smaller-member d-not-below-itself]
                                      // nonneg and not positive forces the remainder to be zero.
                                      @nonneg-not-positive-forces-zero |
                                        forall x: Int; is_nonneg(x) -> (not below(ZERO, x)) -> x = ZERO
                                        [by theorem nonnegNotPositiveIsZero]
                                      @nonneg-not-positive-is-zero |
                                        is_nonneg(rem) -> (not below(ZERO, rem)) -> rem = ZERO
                                        [by forall_elim(rem) nonneg-not-positive-forces-zero]
                                      @remainder-is-zero |
                                        rem = ZERO
                                        [by tautology nonneg-not-positive-is-zero remainder-nonneg remainder-not-positive]
                                      @a-is-dq-plus-zero |
                                        a = add(mul(d, q), ZERO)
                                        [by rewrite remainder-is-zero a-decomposes]
                                      @add-zero-right |
                                        forall n: Int; add(n, ZERO) = n
                                        [by theorem addZeroRight]
                                      @dq-plus-zero-is-dq |
                                        add(mul(d, q), ZERO) = mul(d, q)
                                        [by forall_elim(mul(d, q)) add-zero-right]
                                      @a-is-dq |
                                        a = mul(d, q)
                                        [by rewrite dq-plus-zero-is-dq a-is-dq-plus-zero]
                                      @divides-intro |
                                        forall n, dd, k: Int; n = mul(dd, k) -> does_divide(dd, n)
                                        [by theorem dividesIntro]
                                      @d-divides-a-if-product |
                                        a = mul(d, q) -> does_divide(d, a)
                                        [by forall_elim(a, d, q) divides-intro]
                                      @conclusion-d-divides-a |
                                        does_divide(d, a)
                                        [by modus_ponens d-divides-a-if-product a-is-dq]
                                    }
                                  @conclusion-d-divides-a |
                                    does_divide(d, a)
                                    [by exists_elim with-remainder]
                                }
                              @d-divides-a |
                                does_divide(d, a)
                                [by exists_elim with-quotient-q]
                              // d | b: symmetric, dividing b by d.
                              @add-is-commutative |
                                forall x, y: Int; add(x, y) = add(y, x)
                                [by theorem addIsCommutative]
                              @combination-commutes |
                                add(mul(a, r), mul(b, s)) = add(mul(b, s), mul(a, r))
                                [by forall_elim(mul(a, r), mul(b, s)) add-is-commutative]
                              @d-equals-combination-swapped |
                                d = add(mul(b, s), mul(a, r))
                                [by rewrite combination-commutes d-equals-combination]
                              @division-of-b-by-d |
                                forall x, y: Int; below(ZERO, y) -> exists q: Int; exists rem: Int; x = add(mul(y, q), rem) and is_nonneg(rem) and below(rem, y)
                                [by theorem divisionAlgorithmExists]
                              @division-of-b-by-d-exists |
                                below(ZERO, d) -> exists q: Int; exists rem: Int; b = add(mul(d, q), rem) and is_nonneg(rem) and below(rem, d)
                                [by forall_elim(b, d) division-of-b-by-d]
                              @b-divided-by-d |
                                exists q: Int; exists rem: Int; b = add(mul(d, q), rem) and is_nonneg(rem) and below(rem, d)
                                [by modus_ponens division-of-b-by-d-exists d-is-positive]
                              @with-quotient-q2 |
                                unpack q2: Int from b-divided-by-d {
                                  @b-remainder-exists |
                                    exists rem: Int; b = add(mul(d, q2), rem) and is_nonneg(rem) and below(rem, d)
                                    [by hypothesis with-quotient-q2]
                                  @with-remainder-of-b |
                                    unpack rem2: Int from b-remainder-exists {
                                      @b-decomposes-and-remainder-bounded |
                                        b = add(mul(d, q2), rem2) and is_nonneg(rem2) and below(rem2, d)
                                        [by hypothesis with-remainder-of-b]
                                      @b-decomposes |
                                        b = add(mul(d, q2), rem2)
                                        [by tautology b-decomposes-and-remainder-bounded]
                                      @remainder-nonneg |
                                        is_nonneg(rem2)
                                        [by tautology b-decomposes-and-remainder-bounded]
                                      @remainder-below-d |
                                        below(rem2, d)
                                        [by tautology b-decomposes-and-remainder-bounded]
                                      @less-than-is-irreflexive |
                                        forall n: Int; not below(n, n)
                                        [by theorem lessThanIrreflexive]
                                      @d-not-below-itself |
                                        not below(d, d)
                                        [by forall_elim(d) less-than-is-irreflexive]
                                      @suppose-remainder-positive |
                                        assume below(ZERO, rem2) {
                                          @remainder-positive |
                                            below(ZERO, rem2)
                                            [by hypothesis suppose-remainder-positive]
                                          @remainder-is-member-identity |
                                            b = add(mul(d, q2), rem2) -> d = add(mul(b, s), mul(a, r)) -> rem2 = add(mul(b, sub(ONE, mul(s, q2))), mul(a, neg(mul(r, q2))))
                                            [by instantiate remainderIsMember(b, a, d, s, r, q2, rem2)]
                                          @remainder-is-member-form-swapped |
                                            rem2 = add(mul(b, sub(ONE, mul(s, q2))), mul(a, neg(mul(r, q2))))
                                            [by tautology remainder-is-member-identity b-decomposes d-equals-combination-swapped]
                                          @member-form-commutes |
                                            add(mul(b, sub(ONE, mul(s, q2))), mul(a, neg(mul(r, q2)))) = add(mul(a, neg(mul(r, q2))), mul(b, sub(ONE, mul(s, q2))))
                                            [by forall_elim(mul(b, sub(ONE, mul(s, q2))), mul(a, neg(mul(r, q2)))) add-is-commutative]
                                          @remainder-is-member-form |
                                            rem2 = add(mul(a, neg(mul(r, q2))), mul(b, sub(ONE, mul(s, q2))))
                                            [by rewrite member-form-commutes remainder-is-member-form-swapped]
                                          @remainder-is-some-combination-in-nn |
                                            exists nn: Int; rem2 = add(mul(a, neg(mul(r, q2))), mul(b, nn))
                                            [by exists_intro(sub(ONE, mul(s, q2))) remainder-is-member-form]
                                          @remainder-is-a-combination |
                                            exists mm: Int; exists nn: Int; rem2 = add(mul(a, mm), mul(b, nn))
                                            [by exists_intro(neg(mul(r, q2))) remainder-is-some-combination-in-nn]
                                          @remainder-is-positive-combination |
                                            below(ZERO, rem2) and (exists mm: Int; exists nn: Int; rem2 = add(mul(a, mm), mul(b, nn)))
                                            [by and_intro remainder-positive remainder-is-a-combination]
                                          @remainder-least-comparison |
                                            is_nonneg(rem2) -> (below(ZERO, rem2) and (exists mm: Int; exists nn: Int; rem2 = add(mul(a, mm), mul(b, nn)))) -> at_most(d, rem2)
                                            [by forall_elim(rem2) d-is-least]
                                          @d-at-most-remainder |
                                            at_most(d, rem2)
                                            [by tautology remainder-least-comparison remainder-nonneg remainder-is-positive-combination]
                                          @less-or-equal-split |
                                            forall x, y: Int; at_most(x, y) -> (below(x, y) or x = y)
                                            [by theorem lessOrEqualSplit]
                                          @less-or-equal-split-at-d-remainder |
                                            at_most(d, rem2) -> (below(d, rem2) or d = rem2)
                                            [by forall_elim(d, rem2) less-or-equal-split]
                                          @d-below-or-equal-remainder |
                                            below(d, rem2) or d = rem2
                                            [by modus_ponens less-or-equal-split-at-d-remainder d-at-most-remainder]
                                          @remainder-would-be-a-smaller-member |
                                            below(d, d)
                                            case on d-below-or-equal-remainder {
                                              @when-d-below-remainder |
                                                assume below(d, rem2) {
                                                  @d-below-remainder |
                                                    below(d, rem2)
                                                    [by hypothesis when-d-below-remainder]
                                                  @less-than-transitive |
                                                    forall x, y, z: Int; below(x, y) -> below(y, z) -> below(x, z)
                                                    [by theorem lessThanTransitive]
                                                  @transitive-at-d-remainder-d |
                                                    below(d, rem2) -> below(rem2, d) -> below(d, d)
                                                    [by forall_elim(d, rem2, d) less-than-transitive]
                                                  @conclusion-d-below-itself |
                                                    below(d, d)
                                                    [by tautology transitive-at-d-remainder-d d-below-remainder remainder-below-d]
                                                }
                                              @when-d-equals-remainder |
                                                assume d = rem2 {
                                                  @d-equals-remainder |
                                                    d = rem2
                                                    [by hypothesis when-d-equals-remainder]
                                                  @remainder-equals-d |
                                                    rem2 = d
                                                    [by symmetry d-equals-remainder]
                                                  @conclusion-d-below-itself |
                                                    below(d, d)
                                                    [by rewrite remainder-equals-d remainder-below-d]
                                                }
                                            }
                                        }
                                      @remainder-not-positive |
                                        not below(ZERO, rem2)
                                        [by not_intro suppose-remainder-positive remainder-would-be-a-smaller-member d-not-below-itself]
                                      @nonneg-not-positive-forces-zero |
                                        forall x: Int; is_nonneg(x) -> (not below(ZERO, x)) -> x = ZERO
                                        [by theorem nonnegNotPositiveIsZero]
                                      @nonneg-not-positive-is-zero |
                                        is_nonneg(rem2) -> (not below(ZERO, rem2)) -> rem2 = ZERO
                                        [by forall_elim(rem2) nonneg-not-positive-forces-zero]
                                      @remainder-is-zero |
                                        rem2 = ZERO
                                        [by tautology nonneg-not-positive-is-zero remainder-nonneg remainder-not-positive]
                                      @b-is-dq-plus-zero |
                                        b = add(mul(d, q2), ZERO)
                                        [by rewrite remainder-is-zero b-decomposes]
                                      @add-zero-right |
                                        forall n: Int; add(n, ZERO) = n
                                        [by theorem addZeroRight]
                                      @dq-plus-zero-is-dq |
                                        add(mul(d, q2), ZERO) = mul(d, q2)
                                        [by forall_elim(mul(d, q2)) add-zero-right]
                                      @b-is-dq |
                                        b = mul(d, q2)
                                        [by rewrite dq-plus-zero-is-dq b-is-dq-plus-zero]
                                      @divides-intro |
                                        forall n, dd, k: Int; n = mul(dd, k) -> does_divide(dd, n)
                                        [by theorem dividesIntro]
                                      @d-divides-b-if-product |
                                        b = mul(d, q2) -> does_divide(d, b)
                                        [by forall_elim(b, d, q2) divides-intro]
                                      @conclusion-d-divides-b |
                                        does_divide(d, b)
                                        [by modus_ponens d-divides-b-if-product b-is-dq]
                                    }
                                  @conclusion-d-divides-b |
                                    does_divide(d, b)
                                    [by exists_elim with-remainder-of-b]
                                }
                              @d-divides-b |
                                does_divide(d, b)
                                [by exists_elim with-quotient-q2]
                              // any common divisor e divides d = ar + bs.
                              @generalize-e |
                                fix e: Int {
                                  @given-e-divides-a |
                                    assume does_divide(e, a) {
                                      @given-e-divides-b |
                                        assume does_divide(e, b) {
                                          @e-divides-a |
                                            does_divide(e, a)
                                            [by hypothesis given-e-divides-a]
                                          @e-divides-b |
                                            does_divide(e, b)
                                            [by hypothesis given-e-divides-b]
                                          @divides-mul |
                                            forall dd, x, c: Int; does_divide(dd, x) -> does_divide(dd, mul(x, c))
                                            [by theorem dividesMul]
                                          @e-divides-a-times-r |
                                            does_divide(e, a) -> does_divide(e, mul(a, r))
                                            [by forall_elim(e, a, r) divides-mul]
                                          @e-divides-ar |
                                            does_divide(e, mul(a, r))
                                            [by modus_ponens e-divides-a-times-r e-divides-a]
                                          @e-divides-b-times-s |
                                            does_divide(e, b) -> does_divide(e, mul(b, s))
                                            [by forall_elim(e, b, s) divides-mul]
                                          @e-divides-bs |
                                            does_divide(e, mul(b, s))
                                            [by modus_ponens e-divides-b-times-s e-divides-b]
                                          @divides-add |
                                            forall dd, x, y: Int; does_divide(dd, x) -> does_divide(dd, y) -> does_divide(dd, add(x, y))
                                            [by theorem dividesAdd]
                                          @e-divides-sum-if-parts |
                                            does_divide(e, mul(a, r)) -> does_divide(e, mul(b, s)) -> does_divide(e, add(mul(a, r), mul(b, s)))
                                            [by forall_elim(e, mul(a, r), mul(b, s)) divides-add]
                                          @e-divides-combination |
                                            does_divide(e, add(mul(a, r), mul(b, s)))
                                            [by tautology e-divides-sum-if-parts e-divides-ar e-divides-bs]
                                          @combination-is-d |
                                            add(mul(a, r), mul(b, s)) = d
                                            [by symmetry d-equals-combination]
                                          @conclusion-e-divides-d |
                                            does_divide(e, d)
                                            [by rewrite combination-is-d e-divides-combination]
                                        }
                                      @e-divides-b-implies-e-divides-d |
                                        does_divide(e, b) -> does_divide(e, d)
                                        [by implies_intro given-e-divides-b]
                                    }
                                  @e-divides-a-implies-rest |
                                    does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)
                                    [by implies_intro given-e-divides-a]
                                }
                              @common-divisor-divides-d |
                                forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)
                                [by forall_intro generalize-e]
                              // assemble the five defining properties of the gcd.
                              @d-is-some-combination-in-s |
                                exists s2: Int; d = add(mul(a, r), mul(b, s2))
                                [by exists_intro(s) d-equals-combination]
                              @d-is-a-combination-again |
                                exists r2: Int; exists s2: Int; d = add(mul(a, r2), mul(b, s2))
                                [by exists_intro(r) d-is-some-combination-in-s]
                              @d-positive-and-divides-a |
                                below(ZERO, d) and does_divide(d, a)
                                [by and_intro d-is-positive d-divides-a]
                              @d-divides-a-and-b |
                                below(ZERO, d) and does_divide(d, a) and does_divide(d, b)
                                [by and_intro d-positive-and-divides-a d-divides-b]
                              @d-divides-and-greatest |
                                below(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d))
                                [by and_intro d-divides-a-and-b common-divisor-divides-d]
                              @d-has-all-gcd-properties |
                                below(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r2: Int; exists s2: Int; d = add(mul(a, r2), mul(b, s2)))
                                [by and_intro d-divides-and-greatest d-is-a-combination-again]
                              @conclusion-bezout |
                                exists d2: Int; below(ZERO, d2) and does_divide(d2, a) and does_divide(d2, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d2)) and (exists r2: Int; exists s2: Int; d2 = add(mul(a, r2), mul(b, s2)))
                                [by exists_intro(d) d-has-all-gcd-properties]
                            }
                          @conclusion-bezout |
                            exists d2: Int; below(ZERO, d2) and does_divide(d2, a) and does_divide(d2, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d2)) and (exists r2: Int; exists s2: Int; d2 = add(mul(a, r2), mul(b, s2)))
                            [by exists_elim with-coefficient-s]
                        }
                      @conclusion-bezout |
                        exists d2: Int; below(ZERO, d2) and does_divide(d2, a) and does_divide(d2, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d2)) and (exists r2: Int; exists s2: Int; d2 = add(mul(a, r2), mul(b, s2)))
                        [by exists_elim with-coefficient-r]
                    }
                  @conclusion-bezout |
                    exists d2: Int; below(ZERO, d2) and does_divide(d2, a) and does_divide(d2, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d2)) and (exists r2: Int; exists s2: Int; d2 = add(mul(a, r2), mul(b, s2)))
                    [by exists_elim with-least-member-d]
                }
              @b-nonzero-gives-bezout |
                (b != ZERO)
                  -> exists d: Int; below(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
                [by implies_intro given-b-nonzero]
            }
          @a-nonzero-gives-bezout |
            (a != ZERO)
              -> (b != ZERO) -> exists d: Int; below(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
            [by implies_intro given-a-nonzero]
        }
      @discharge-b |
        forall b: Int; (a != ZERO) -> (b != ZERO) -> exists d: Int; below(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b: Int; (a != ZERO) -> (b != ZERO) -> exists d: Int; below(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
    [by forall_intro generalize-a]
qed
```

## The Coprime Corollary

> **Corollary 2.11.** Let $a$ and $b$ be two integers that are relatively prime.
> Then there exist integers $r$ and $s$ such that $ar + bs = 1$.

If `a` and `b` are coprime — every common divisor is `±1` — Bézout's positive
common divisor `d = gcd(a,b)` must itself be `1` or `−1`. It is positive
(`below(ZERO, d)`), and `−1` is *not* positive (`negOneBelowZero` puts `−1 < 0`,
which together with `0 < −1` and transitivity would give `0 < 0`), so `d = 1` and
Bézout's identity `d = ar + bs` becomes `ar + bs = 1`.

```bpa
theorem coprimeBezout: forall a, b: Int; a != ZERO -> b != ZERO ->
  (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE)))
  -> exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @given-a-nonzero |
            assume a != ZERO {
              @given-b-nonzero |
                assume b != ZERO {
                  @given-coprime |
                    assume forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE)) {
                      @a-nonzero |
                        a != ZERO
                        [by hypothesis given-a-nonzero]
                      @b-nonzero |
                        b != ZERO
                        [by hypothesis given-b-nonzero]
                      @coprime |
                        forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE))
                        [by hypothesis given-coprime]
                      // Bézout supplies a positive gcd d = ar + bs dividing a and b.
                      @bezout-for-all |
                        forall a2, b2: Int; a2 != ZERO -> b2 != ZERO -> exists d: Int; below(ZERO, d) and does_divide(d, a2) and does_divide(d, b2) and (forall e: Int; does_divide(e, a2) -> does_divide(e, b2) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a2, r), mul(b2, s)))
                        [by theorem bezout]
                      @bezout-at-a-b |
                        a != ZERO -> b != ZERO -> exists d: Int; below(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
                        [by forall_elim(a, b) bezout-for-all]
                      @gcd-exists |
                        exists d: Int; below(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
                        [by tautology bezout-at-a-b a-nonzero b-nonzero]
                      @with-gcd-d |
                        unpack d: Int from gcd-exists {
                          @d-has-all-gcd-properties |
                            below(ZERO, d) and does_divide(d, a) and does_divide(d, b) and (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> does_divide(e, d)) and (exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s)))
                            [by hypothesis with-gcd-d]
                          @d-is-positive |
                            below(ZERO, d)
                            [by tautology d-has-all-gcd-properties]
                          @d-divides-a |
                            does_divide(d, a)
                            [by tautology d-has-all-gcd-properties]
                          @d-divides-b |
                            does_divide(d, b)
                            [by tautology d-has-all-gcd-properties]
                          @d-is-a-combination |
                            exists r: Int; exists s: Int; d = add(mul(a, r), mul(b, s))
                            [by tautology d-has-all-gcd-properties]
                          // coprimality forces the gcd to be 1 or -1.
                          @coprime-at-d |
                            does_divide(d, a) -> does_divide(d, b) -> (d = ONE or d = neg(ONE))
                            [by forall_elim(d) coprime]
                          @d-is-one-or-neg-one |
                            d = ONE or d = neg(ONE)
                            [by tautology coprime-at-d d-divides-a d-divides-b]
                          @conclusion-bezout-identity |
                            exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
                            case on d-is-one-or-neg-one {
                              @when-d-is-one |
                                assume d = ONE {
                                  @d-is-one |
                                    d = ONE
                                    [by hypothesis when-d-is-one]
                                  @with-coefficient-r |
                                    unpack r: Int from d-is-a-combination {
                                      @d-is-combination-in-r |
                                        exists s: Int; d = add(mul(a, r), mul(b, s))
                                        [by hypothesis with-coefficient-r]
                                      @with-coefficient-s |
                                        unpack s: Int from d-is-combination-in-r {
                                          @d-equals-combination |
                                            d = add(mul(a, r), mul(b, s))
                                            [by hypothesis with-coefficient-s]
                                          @one-equals-combination |
                                            ONE = add(mul(a, r), mul(b, s))
                                            [by rewrite d-is-one d-equals-combination]
                                          @combination-equals-one |
                                            add(mul(a, r), mul(b, s)) = ONE
                                            [by symmetry one-equals-combination]
                                          @combination-in-s-is-one |
                                            exists s2: Int; add(mul(a, r), mul(b, s2)) = ONE
                                            [by exists_intro(s) combination-equals-one]
                                          @conclusion-bezout-identity |
                                            exists r2: Int; exists s2: Int; add(mul(a, r2), mul(b, s2)) = ONE
                                            [by exists_intro(r) combination-in-s-is-one]
                                        }
                                      @conclusion-bezout-identity |
                                        exists r2: Int; exists s2: Int; add(mul(a, r2), mul(b, s2)) = ONE
                                        [by exists_elim with-coefficient-s]
                                    }
                                  @conclusion-bezout-identity |
                                    exists r2: Int; exists s2: Int; add(mul(a, r2), mul(b, s2)) = ONE
                                    [by exists_elim with-coefficient-r]
                                }
                              @when-d-is-neg-one |
                                assume d = neg(ONE) {
                                  @d-is-neg-one |
                                    d = neg(ONE)
                                    [by hypothesis when-d-is-neg-one]
                                  // d positive but d = -1 < 0 is impossible.
                                  @neg-one-is-positive |
                                    below(ZERO, neg(ONE))
                                    [by rewrite d-is-neg-one d-is-positive]
                                  @neg-one-is-negative |
                                    below(neg(ONE), ZERO)
                                    [by theorem negOneBelowZero]
                                  @less-than-transitive |
                                    forall x, y, z: Int; below(x, y) -> below(y, z) -> below(x, z)
                                    [by theorem lessThanTransitive]
                                  @transitive-through-neg-one |
                                    below(ZERO, neg(ONE)) -> below(neg(ONE), ZERO) -> below(ZERO, ZERO)
                                    [by forall_elim(ZERO, neg(ONE), ZERO) less-than-transitive]
                                  @zero-below-itself |
                                    below(ZERO, ZERO)
                                    [by tautology transitive-through-neg-one neg-one-is-positive neg-one-is-negative]
                                  @less-than-is-irreflexive |
                                    forall n: Int; not below(n, n)
                                    [by theorem lessThanIrreflexive]
                                  @zero-not-below-itself |
                                    not below(ZERO, ZERO)
                                    [by forall_elim(ZERO) less-than-is-irreflexive]
                                  @conclusion-bezout-identity |
                                    exists r2: Int; exists s2: Int; add(mul(a, r2), mul(b, s2)) = ONE
                                    [by absurd zero-below-itself zero-not-below-itself]
                                }
                            }
                        }
                      @conclusion-bezout-identity |
                        exists r2: Int; exists s2: Int; add(mul(a, r2), mul(b, s2)) = ONE
                        [by exists_elim with-gcd-d]
                    }
                  @coprime-implies-bezout-identity |
                    (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE)))
                      -> exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
                    [by implies_intro given-coprime]
                }
              @b-nonzero-implies-coprime-bezout |
                (b != ZERO)
                  -> (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE)))
                  -> exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
                [by implies_intro given-b-nonzero]
            }
          @a-nonzero-implies-coprime-bezout |
            (a != ZERO)
              -> (b != ZERO)
              -> (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE)))
              -> exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
            [by implies_intro given-a-nonzero]
        }
      @discharge-b |
        forall b: Int; (a != ZERO) -> (b != ZERO) -> (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE))) -> exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b: Int; (a != ZERO) -> (b != ZERO) -> (forall e: Int; does_divide(e, a) -> does_divide(e, b) -> (e = ONE or e = neg(ONE))) -> exists r: Int; exists s: Int; add(mul(a, r), mul(b, s)) = ONE
    [by forall_intro generalize-a]
qed
```

## Prime Numbers

A **prime** is an integer `p > 1` whose only *positive* divisors are `1` and `p`
itself (Judson, Definition 2.10). Since `bpa` predicates are opaque, we spell the
condition out inline as the conjunction

```
below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
```

wherever "`p` is prime" is needed. This section proves the two theorems that make
primes the multiplicative atoms of `ℤ`: **Euclid's Lemma** — a prime dividing a
product divides one of the factors — and, as the classical corollary, a note on
the **infinitude of primes**. Euclid's Lemma rests on `coprimeBezout` (proved
above) and on the ℤ number-theory library; we bring in the last few names the
argument spells out, plus the finite-product sequence library for the
infinitude discussion.

```bpa
import sequence <<< "std/integer-sequence.bpa"

axiom mulZeroLeft = ring.mulZeroLeft
axiom mulSuccLeft = ring.mulSuccLeft
axiom negZero = division.negZero
theorem mulIsCommutative = order.mulIsCommutative
theorem mulNegLeft = order.mulNegLeft
theorem nonnegNotBelowZero = wellordering.nonnegNotBelowZero
theorem dividesRefl = divides.dividesRefl

sort Seq = sequence.Seq
func at = sequence.at
func productUpTo = sequence.productUpTo
pred inSeqBelow = sequence.inSeqBelow
axiom productUpToZero = sequence.productUpToZero
axiom productUpToSucc = sequence.productUpToSucc
theorem everyEntryDividesProduct = sequence.everyEntryDividesProduct
theorem memberDividesProduct = sequence.memberDividesProduct
theorem primeFactorExists = sequence.primeFactorExists
theorem dividesSubtracts = sequence.dividesSubtracts
theorem oneAtMostPositive = sequence.oneAtMostPositive
theorem divisorAtMost = sequence.divisorAtMost
theorem atMostThenBelow = division.atMostThenBelow
theorem lessThanSelf = wellordering.lessThanSelf

// -- sequence-construction axioms + the order/arith facts the Fundamental
//    Theorem of Arithmetic (below) needs but the sections above did not ------
axiom seqSingletonExists = sequence.seqSingletonExists
axiom seqConcatExists = sequence.seqConcatExists
axiom seqRemoveExists = sequence.seqRemoveExists
theorem nonnegStrongInduction = division.nonnegStrongInduction
theorem productWithPositiveIsPositive = division.productWithPositiveIsPositive
theorem additionPreservesOrder = order.additionPreservesOrder
theorem lessThanAddCancelLeft = order.lessThanAddCancelLeft
theorem lessThanSuccSplit = wellordering.lessThanSuccSplit
theorem signTrichotomy = order.signTrichotomy
theorem nonnegMul = order.nonnegMul
theorem nonnegAdd = order.nonnegAdd
theorem lessOrEqualRefl = order.lessOrEqualRefl
theorem lessOrEqualAntisymmetric = order.lessOrEqualAntisymmetric
// `at_most` (= less_or_equal) is now `less_than or =`, so its old gap-existential
// intro/elim AXIOMS are gone. `lessOrEqualIsGapExistential` proves at_most(a,b) iff
// (exists d; is_nonneg(d) and add(a,d)=b) — recover intro/elim as local theorems by
// projecting each direction of that iff.
theorem lessOrEqualIsGapExistential = order.lessOrEqualIsGapExistential
```

```bpa
theorem lessOrEqualElim: forall a, b: Int; at_most(a, b) -> exists d: Int; is_nonneg(d) and add(a, d) = b
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @gap-iff |
            forall a2, b2: Int; at_most(a2, b2) iff (exists d: Int; is_nonneg(d) and add(a2, d) = b2)
            [by theorem lessOrEqualIsGapExistential]
          @gap-iff-at-a-b |
            at_most(a, b) iff (exists d: Int; is_nonneg(d) and add(a, d) = b)
            [by forall_elim(a, b) gap-iff]
          @elim-at-a-b |
            at_most(a, b) -> exists d: Int; is_nonneg(d) and add(a, d) = b
            [by iff_elim_forward gap-iff-at-a-b]
        }
      @discharge-b |
        forall b: Int; at_most(a, b) -> exists d: Int; is_nonneg(d) and add(a, d) = b
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b: Int; at_most(a, b) -> exists d: Int; is_nonneg(d) and add(a, d) = b
    [by forall_intro generalize-a]
qed

theorem lessOrEqualIntro: forall a, g, b: Int; is_nonneg(g) -> add(a, g) = b -> at_most(a, b)
proof
  @generalize-a |
    fix a: Int {
      @generalize-g |
        fix g: Int {
          @generalize-b |
            fix b: Int {
              @given-nonneg-g |
                assume is_nonneg(g) {
                  @given-gap |
                    assume add(a, g) = b {
                      @nonneg-g |
                        is_nonneg(g)
                        [by hypothesis given-nonneg-g]
                      @the-gap |
                        add(a, g) = b
                        [by hypothesis given-gap]
                      @nonneg-and-gap |
                        is_nonneg(g) and add(a, g) = b
                        [by and_intro nonneg-g the-gap]
                      @gap-exists |
                        exists d: Int; is_nonneg(d) and add(a, d) = b
                        [by exists_intro(g) nonneg-and-gap]
                      @gap-iff |
                        forall a2, b2: Int; at_most(a2, b2) iff (exists d: Int; is_nonneg(d) and add(a2, d) = b2)
                        [by theorem lessOrEqualIsGapExistential]
                      @gap-iff-at-a-b |
                        at_most(a, b) iff (exists d: Int; is_nonneg(d) and add(a, d) = b)
                        [by forall_elim(a, b) gap-iff]
                      @gap-gives-at-most |
                        (exists d: Int; is_nonneg(d) and add(a, d) = b) -> at_most(a, b)
                        [by iff_elim_backward gap-iff-at-a-b]
                      @a-at-most-b |
                        at_most(a, b)
                        [by modus_ponens gap-gives-at-most gap-exists]
                    }
                  @gap-gives-le |
                    add(a, g) = b -> at_most(a, b)
                    [by implies_intro given-gap]
                }
              @nonneg-gives |
                is_nonneg(g) -> add(a, g) = b -> at_most(a, b)
                [by implies_intro given-nonneg-g]
            }
          @discharge-b |
            forall b: Int; is_nonneg(g) -> add(a, g) = b -> at_most(a, b)
            [by forall_intro generalize-b]
        }
      @discharge-g |
        forall g, b: Int; is_nonneg(g) -> add(a, g) = b -> at_most(a, b)
        [by forall_intro generalize-g]
    }
  @conclusion |
    forall a, g, b: Int; is_nonneg(g) -> add(a, g) = b -> at_most(a, b)
    [by forall_intro generalize-a]
qed
```

The elided ring algebra Euclid's Lemma needs — collapsing `b·(ar+ps)` to a
sum of two `p`-related products — is a nonlinear identity handled by the
`polynomial` tactic. That tactic expects the ring's associativity/commutativity
lemmas *and their left-swap forms* in scope; ℤ has the swaps only implicitly, so
we prove the three names it looks up (`mulOneLeft`, `mulLeftSwap`, `addLeftSwap`)
first. `mul(1, n) = n` mirrors `mulOneRight` on the left, unfolding `1 = succ 0`
through `mulSuccLeft`.

```bpa
theorem mulOneLeft: forall n: Int; mul(ONE, n) = n
proof
  @generalize-n |
    fix n: Int {
      @one-is-succ-zero |
        ONE = succ(ZERO)
        [by axiom oneIsSuccZero]
      @succ-zero-is-one |
        succ(ZERO) = ONE
        [by symmetry one-is-succ-zero]
      @mul-succ-left |
        forall a, b: Int; mul(succ(a), b) = add(mul(a, b), b)
        [by axiom mulSuccLeft]
      @mul-succ-zero-n |
        mul(succ(ZERO), n) = add(mul(ZERO, n), n)
        [by forall_elim(ZERO, n) mul-succ-left]
      @mul-one-n-expanded |
        mul(ONE, n) = add(mul(ZERO, n), n)
        [by rewrite succ-zero-is-one mul-succ-zero-n]
      @mul-zero-left |
        forall b: Int; mul(ZERO, b) = ZERO
        [by axiom mulZeroLeft]
      @mul-zero-n-is-zero |
        mul(ZERO, n) = ZERO
        [by forall_elim(n) mul-zero-left]
      @mul-one-n-is-zero-plus-n |
        mul(ONE, n) = add(ZERO, n)
        [by rewrite mul-zero-n-is-zero mul-one-n-expanded]
      @add-zero-left |
        forall b: Int; add(ZERO, b) = b
        [by axiom addZeroLeft]
      @zero-plus-n-is-n |
        add(ZERO, n) = n
        [by forall_elim(n) add-zero-left]
      @conclusion-mul-one-left |
        mul(ONE, n) = n
        [by rewrite zero-plus-n-is-n mul-one-n-is-zero-plus-n]
    }
  @conclusion |
    forall n: Int; mul(ONE, n) = n
    [by forall_intro generalize-n]
qed
```

The left-swap forms `a·(b·c) = b·(a·c)` and `a+(b+c) = b+(a+c)` reassociate,
commute the exposed pair, then reassociate back.

```bpa
theorem mulLeftSwap: forall a, b, c: Int; mul(a, mul(b, c)) = mul(b, mul(a, c))
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @generalize-c |
            fix c: Int {
              @mul-associative |
                forall x, y, z: Int; mul(mul(x, y), z) = mul(x, mul(y, z))
                [by theorem mulIsAssociative]
              @abc-regroups-flat |
                mul(mul(a, b), c) = mul(a, mul(b, c))
                [by forall_elim(a, b, c) mul-associative]
              @a-bc-regroups |
                mul(a, mul(b, c)) = mul(mul(a, b), c)
                [by symmetry abc-regroups-flat]
              @mul-commutative |
                forall x, y: Int; mul(x, y) = mul(y, x)
                [by theorem mulIsCommutative]
              @a-b-swaps |
                mul(a, b) = mul(b, a)
                [by forall_elim(a, b) mul-commutative]
              @a-bc-is-ba-grouped |
                mul(a, mul(b, c)) = mul(mul(b, a), c)
                [by rewrite a-b-swaps a-bc-regroups]
              @ba-c-regroups |
                mul(mul(b, a), c) = mul(b, mul(a, c))
                [by forall_elim(b, a, c) mul-associative]
              @conclusion-mul-left-swap |
                mul(a, mul(b, c)) = mul(b, mul(a, c))
                [by rewrite ba-c-regroups a-bc-is-ba-grouped]
            }
          @discharge-c |
            forall c: Int; mul(a, mul(b, c)) = mul(b, mul(a, c))
            [by forall_intro generalize-c]
        }
      @discharge-b |
        forall b, c: Int; mul(a, mul(b, c)) = mul(b, mul(a, c))
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b, c: Int; mul(a, mul(b, c)) = mul(b, mul(a, c))
    [by forall_intro generalize-a]
qed
```

The additive left-swap is the identical argument on `add`.

```bpa
theorem addLeftSwap: forall a, b, c: Int; add(a, add(b, c)) = add(b, add(a, c))
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @generalize-c |
            fix c: Int {
              @add-associative |
                forall x, y, z: Int; add(add(x, y), z) = add(x, add(y, z))
                [by theorem addIsAssociative]
              @abc-regroups-flat |
                add(add(a, b), c) = add(a, add(b, c))
                [by forall_elim(a, b, c) add-associative]
              @a-bc-regroups |
                add(a, add(b, c)) = add(add(a, b), c)
                [by symmetry abc-regroups-flat]
              @add-commutative |
                forall x, y: Int; add(x, y) = add(y, x)
                [by theorem addIsCommutative]
              @a-b-swaps |
                add(a, b) = add(b, a)
                [by forall_elim(a, b) add-commutative]
              @a-bc-is-ba-grouped |
                add(a, add(b, c)) = add(add(b, a), c)
                [by rewrite a-b-swaps a-bc-regroups]
              @ba-c-regroups |
                add(add(b, a), c) = add(b, add(a, c))
                [by forall_elim(b, a, c) add-associative]
              @conclusion-add-left-swap |
                add(a, add(b, c)) = add(b, add(a, c))
                [by rewrite ba-c-regroups a-bc-is-ba-grouped]
            }
          @discharge-c |
            forall c: Int; add(a, add(b, c)) = add(b, add(a, c))
            [by forall_intro generalize-c]
        }
      @discharge-b |
        forall b, c: Int; add(a, add(b, c)) = add(b, add(a, c))
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b, c: Int; add(a, add(b, c)) = add(b, add(a, c))
    [by forall_intro generalize-a]
qed
```

A small positivity fact used repeatedly: `0 < 1`, the gap being `0`, since
`add(0, succ 0) = succ 0 = 1`.

```bpa
theorem zeroBelowOne: below(ZERO, ONE)
proof
  @one-is-succ-zero |
    ONE = succ(ZERO)
    [by axiom oneIsSuccZero]
  @add-zero-left |
    forall b: Int; add(ZERO, b) = b
    [by axiom addZeroLeft]
  @zero-plus-one-is-one |
    add(ZERO, ONE) = ONE
    [by forall_elim(ONE) add-zero-left]
  @zero-plus-succ-zero-is-one |
    add(ZERO, succ(ZERO)) = ONE
    [by rewrite one-is-succ-zero zero-plus-one-is-one]
  @zero-is-nonneg |
    is_nonneg(ZERO)
    [by axiom nonnegZero]
  @less-than-intro |
    forall a, d, b: Int; is_nonneg(d) -> add(a, succ(d)) = b -> below(a, b)
    [by axiom lessThanIntro]
  @zero-below-one-if-summed |
    is_nonneg(ZERO) -> add(ZERO, succ(ZERO)) = ONE -> below(ZERO, ONE)
    [by forall_elim(ZERO, ZERO, ONE) less-than-intro]
  @conclusion |
    below(ZERO, ONE)
    [by tautology zero-below-one-if-summed zero-is-nonneg zero-plus-succ-zero-is-one]
qed
```

`neg(e)` divides whatever `e` divides: unfolding `n = e·k` gives
`n = neg(e)·neg(k)` (`mulNegLeft`, `mulNegRight`, `negNeg` cancel the two sign
flips), so `neg(e) | n`. This lets the coprimality argument reduce a negative
common divisor to its positive counterpart.

```bpa
theorem negDivides: forall e, n: Int; does_divide(e, n) -> does_divide(neg(e), n)
proof
  @generalize-e |
    fix e: Int {
      @generalize-n |
        fix n: Int {
          @given-e-divides-n |
            assume does_divide(e, n) {
              @e-divides-n |
                does_divide(e, n)
                [by hypothesis given-e-divides-n]
              @divides-elim |
                forall d, m: Int; does_divide(d, m) -> exists k: Int; m = mul(d, k)
                [by theorem dividesElim]
              @quotient-of-n-by-e-exists |
                does_divide(e, n) -> exists k: Int; n = mul(e, k)
                [by forall_elim(e, n) divides-elim]
              @quotient-exists |
                exists k: Int; n = mul(e, k)
                [by modus_ponens quotient-of-n-by-e-exists e-divides-n]
              @with-quotient-k |
                unpack k: Int from quotient-exists {
                  @n-is-e-times-k |
                    n = mul(e, k)
                    [by hypothesis with-quotient-k]
                  @e-times-k-is-neg-e-times-neg-k |
                    mul(e, k) = mul(neg(e), neg(k))
                    [by simplify mulNegLeft mulNegRight negNeg]
                  @n-is-neg-e-times-neg-k |
                    n = mul(neg(e), neg(k))
                    [by rewrite e-times-k-is-neg-e-times-neg-k n-is-e-times-k]
                  @divides-intro |
                    forall m, d, j: Int; m = mul(d, j) -> does_divide(d, m)
                    [by theorem dividesIntro]
                  @neg-e-divides-n-if-product |
                    n = mul(neg(e), neg(k)) -> does_divide(neg(e), n)
                    [by forall_elim(n, neg(e), neg(k)) divides-intro]
                  @neg-e-divides-n |
                    does_divide(neg(e), n)
                    [by modus_ponens neg-e-divides-n-if-product n-is-neg-e-times-neg-k]
                }
              @neg-e-divides-n |
                does_divide(neg(e), n)
                [by exists_elim with-quotient-k]
            }
          @e-divides-implies-neg-e-divides |
            does_divide(e, n) -> does_divide(neg(e), n)
            [by implies_intro given-e-divides-n]
        }
      @discharge-n |
        forall n: Int; does_divide(e, n) -> does_divide(neg(e), n)
        [by forall_intro generalize-n]
    }
  @conclusion |
    forall e, n: Int; does_divide(e, n) -> does_divide(neg(e), n)
    [by forall_intro generalize-e]
qed
```

A negative integer negates to a positive one: `negBelowZero` gives
`neg(e) ≥ 0`, and `neg(e)` is not `0` (that would force `e = 0`), so `nonnegCases`
exhibits it as a successor `succ(m)` of a nonnegative `m`, whence `0 < succ(m) =
neg(e)`.

```bpa
theorem positiveOfNegative: forall e: Int; below(e, ZERO) -> below(ZERO, neg(e))
proof
  @generalize-e |
    fix e: Int {
      @given-e-negative |
        assume below(e, ZERO) {
          @e-negative |
            below(e, ZERO)
            [by hypothesis given-e-negative]
          @neg-nonneg-if-negative |
            forall n: Int; below(n, ZERO) -> is_nonneg(neg(n))
            [by theorem negBelowZero]
          @neg-e-nonneg-if-e-negative |
            below(e, ZERO) -> is_nonneg(neg(e))
            [by forall_elim(e) neg-nonneg-if-negative]
          @neg-e-is-nonneg |
            is_nonneg(neg(e))
            [by modus_ponens neg-e-nonneg-if-e-negative e-negative]
          @nonneg-splits |
            forall n: Int; is_nonneg(n) -> (n = ZERO or (exists m: Int; is_nonneg(m) and n = succ(m)))
            [by theorem nonnegCases]
          @neg-e-is-zero-or-successor |
            is_nonneg(neg(e)) -> (neg(e) = ZERO or (exists m: Int; is_nonneg(m) and neg(e) = succ(m)))
            [by forall_elim(neg(e)) nonneg-splits]
          @neg-e-zero-or-successor |
            neg(e) = ZERO or (exists m: Int; is_nonneg(m) and neg(e) = succ(m))
            [by modus_ponens neg-e-is-zero-or-successor neg-e-is-nonneg]
          @neg-e-positive |
            below(ZERO, neg(e))
            case on neg-e-zero-or-successor {
              @when-neg-e-zero |
                assume neg(e) = ZERO {
                  // neg(e)=0 forces e=neg(neg(e))=0, contradicting e<0.
                  @neg-e-is-zero |
                    neg(e) = ZERO
                    [by hypothesis when-neg-e-zero]
                  @neg-neg-is-identity |
                    forall n: Int; neg(neg(n)) = n
                    [by theorem negNeg]
                  @e-is-neg-neg-e |
                    neg(neg(e)) = e
                    [by forall_elim(e) neg-neg-is-identity]
                  @neg-zero-is-e |
                    neg(ZERO) = e
                    [by rewrite neg-e-is-zero e-is-neg-neg-e]
                  @neg-of-zero-is-zero |
                    neg(ZERO) = ZERO
                    [by axiom negZero]
                  @zero-is-e |
                    ZERO = e
                    [by rewrite neg-of-zero-is-zero neg-zero-is-e]
                  @e-is-zero |
                    e = ZERO
                    [by symmetry zero-is-e]
                  @zero-is-negative |
                    below(ZERO, ZERO)
                    [by rewrite e-is-zero e-negative]
                  @less-than-irreflexive |
                    forall n: Int; not below(n, n)
                    [by theorem lessThanIrreflexive]
                  @zero-not-below-zero |
                    not below(ZERO, ZERO)
                    [by forall_elim(ZERO) less-than-irreflexive]
                  @neg-e-positive |
                    below(ZERO, neg(e))
                    [by absurd zero-is-negative zero-not-below-zero]
                }
              @when-neg-e-successor |
                assume exists m: Int; is_nonneg(m) and neg(e) = succ(m) {
                  @neg-e-is-a-successor |
                    exists m: Int; is_nonneg(m) and neg(e) = succ(m)
                    [by hypothesis when-neg-e-successor]
                  @with-predecessor-m |
                    unpack m: Int from neg-e-is-a-successor {
                      @m-nonneg-and-neg-e-is-succ |
                        is_nonneg(m) and neg(e) = succ(m)
                        [by hypothesis with-predecessor-m]
                      @m-is-nonneg |
                        is_nonneg(m)
                        [by and_elim_left m-nonneg-and-neg-e-is-succ]
                      @neg-e-is-succ-m |
                        neg(e) = succ(m)
                        [by and_elim_right m-nonneg-and-neg-e-is-succ]
                      @add-zero-left |
                        forall b: Int; add(ZERO, b) = b
                        [by axiom addZeroLeft]
                      @zero-plus-succ-m-is-succ-m |
                        add(ZERO, succ(m)) = succ(m)
                        [by forall_elim(succ(m)) add-zero-left]
                      @less-than-intro |
                        forall a, d, b: Int; is_nonneg(d) -> add(a, succ(d)) = b -> below(a, b)
                        [by axiom lessThanIntro]
                      @zero-below-succ-m-if-summed |
                        is_nonneg(m) -> add(ZERO, succ(m)) = succ(m) -> below(ZERO, succ(m))
                        [by forall_elim(ZERO, m, succ(m)) less-than-intro]
                      @zero-below-succ-m |
                        below(ZERO, succ(m))
                        [by tautology zero-below-succ-m-if-summed m-is-nonneg zero-plus-succ-m-is-succ-m]
                      @succ-m-is-neg-e |
                        succ(m) = neg(e)
                        [by symmetry neg-e-is-succ-m]
                      @neg-e-positive |
                        below(ZERO, neg(e))
                        [by rewrite succ-m-is-neg-e zero-below-succ-m]
                    }
                  @neg-e-positive |
                    below(ZERO, neg(e))
                    [by exists_elim with-predecessor-m]
                }
            }
        }
      @e-negative-implies-neg-e-positive |
        below(e, ZERO) -> below(ZERO, neg(e))
        [by implies_intro given-e-negative]
    }
  @conclusion |
    forall e: Int; below(e, ZERO) -> below(ZERO, neg(e))
    [by forall_intro generalize-e]
qed
```

Euclid's Lemma proceeds by cases on whether `p | a`, and `ℤ`'s intuitionistic
kernel offers no free excluded middle — so we first establish that divisibility
by a positive integer *is decidable*. Run the Division Algorithm: `a = pq + r`
with `0 ≤ r < p`. By trichotomy the remainder `r` is `< 0` (impossible, `r` is
nonnegative), `= 0` (then `a = p·q`, so `p | a`), or `> 0`. In the last case,
were `p | a` we would have a second decomposition `a = p·m + 0`; the Division
Algorithm's *uniqueness* forces `r = 0`, contradicting `r > 0` — so `p ∤ a`.

```bpa
theorem dividesDecidable: forall p, a: Int; below(ZERO, p) -> (does_divide(p, a) or (not does_divide(p, a)))
proof
  @generalize-p |
    fix p: Int {
      @generalize-a |
        fix a: Int {
          @given-p-positive |
            assume below(ZERO, p) {
              @p-positive |
                below(ZERO, p)
                [by hypothesis given-p-positive]
              @division-exists |
                forall x, y: Int; below(ZERO, y) -> exists q: Int; exists r: Int; x = add(mul(y, q), r) and is_nonneg(r) and below(r, y)
                [by theorem divisionAlgorithmExists]
              @division-of-a-by-p |
                below(ZERO, p) -> exists q: Int; exists r: Int; a = add(mul(p, q), r) and is_nonneg(r) and below(r, p)
                [by forall_elim(a, p) division-exists]
              @quotient-remainder-exist |
                exists q: Int; exists r: Int; a = add(mul(p, q), r) and is_nonneg(r) and below(r, p)
                [by modus_ponens division-of-a-by-p p-positive]
              @with-quotient-q |
                unpack q: Int from quotient-remainder-exist {
                  @remainder-exists |
                    exists r: Int; a = add(mul(p, q), r) and is_nonneg(r) and below(r, p)
                    [by hypothesis with-quotient-q]
                  @with-remainder-r |
                    unpack r: Int from remainder-exists {
                      @decomposition-and-bounds |
                        a = add(mul(p, q), r) and is_nonneg(r) and below(r, p)
                        [by hypothesis with-remainder-r]
                      @decomposition-and-remainder-nonneg |
                        a = add(mul(p, q), r) and is_nonneg(r)
                        [by and_elim_left decomposition-and-bounds]
                      @decomposition |
                        a = add(mul(p, q), r)
                        [by and_elim_left decomposition-and-remainder-nonneg]
                      @remainder-nonneg |
                        is_nonneg(r)
                        [by and_elim_right decomposition-and-remainder-nonneg]
                      @remainder-below-p |
                        below(r, p)
                        [by and_elim_right decomposition-and-bounds]
                      @trichotomy-of-r |
                        forall x, y: Int; below(x, y) or x = y or below(y, x)
                        [by theorem trichotomy]
                      @r-against-zero |
                        below(r, ZERO) or r = ZERO or below(ZERO, r)
                        [by forall_elim(r, ZERO) trichotomy-of-r]
                      @divides-decidable |
                        does_divide(p, a) or (not does_divide(p, a))
                        case on r-against-zero {
                          @when-remainder-below-zero |
                            assume below(r, ZERO) {
                              @remainder-below-zero |
                                below(r, ZERO)
                                [by hypothesis when-remainder-below-zero]
                              @nonneg-not-below-zero |
                                forall m: Int; is_nonneg(m) -> (not below(m, ZERO))
                                [by theorem nonnegNotBelowZero]
                              @remainder-not-below-zero-if-nonneg |
                                is_nonneg(r) -> (not below(r, ZERO))
                                [by forall_elim(r) nonneg-not-below-zero]
                              @remainder-not-below-zero |
                                not below(r, ZERO)
                                [by modus_ponens remainder-not-below-zero-if-nonneg remainder-nonneg]
                              @divides-decidable |
                                does_divide(p, a) or (not does_divide(p, a))
                                [by absurd remainder-below-zero remainder-not-below-zero]
                            }
                          @when-remainder-zero |
                            assume r = ZERO {
                              @remainder-zero |
                                r = ZERO
                                [by hypothesis when-remainder-zero]
                              @decomposition-with-zero |
                                a = add(mul(p, q), ZERO)
                                [by rewrite remainder-zero decomposition]
                              @add-zero-right |
                                forall n: Int; add(n, ZERO) = n
                                [by theorem addZeroRight]
                              @product-plus-zero |
                                add(mul(p, q), ZERO) = mul(p, q)
                                [by forall_elim(mul(p, q)) add-zero-right]
                              @a-is-p-times-q |
                                a = mul(p, q)
                                [by rewrite product-plus-zero decomposition-with-zero]
                              @divides-intro |
                                forall n, d, k: Int; n = mul(d, k) -> does_divide(d, n)
                                [by theorem dividesIntro]
                              @p-divides-a-if-product |
                                a = mul(p, q) -> does_divide(p, a)
                                [by forall_elim(a, p, q) divides-intro]
                              @p-divides-a |
                                does_divide(p, a)
                                [by modus_ponens p-divides-a-if-product a-is-p-times-q]
                              @divides-decidable |
                                does_divide(p, a) or (not does_divide(p, a))
                                [by or_intro_left p-divides-a]
                            }
                          @when-remainder-positive |
                            assume below(ZERO, r) {
                              @remainder-positive |
                                below(ZERO, r)
                                [by hypothesis when-remainder-positive]
                              @given-p-divides-a |
                                assume does_divide(p, a) {
                                  @p-divides-a |
                                    does_divide(p, a)
                                    [by hypothesis given-p-divides-a]
                                  @divides-elim |
                                    forall d, n: Int; does_divide(d, n) -> exists k: Int; n = mul(d, k)
                                    [by theorem dividesElim]
                                  @quotient-of-a-exists |
                                    does_divide(p, a) -> exists k: Int; a = mul(p, k)
                                    [by forall_elim(p, a) divides-elim]
                                  @exact-quotient-exists |
                                    exists k: Int; a = mul(p, k)
                                    [by modus_ponens quotient-of-a-exists p-divides-a]
                                  @with-exact-quotient-m |
                                    unpack m: Int from exact-quotient-exists {
                                      @a-is-p-times-m |
                                        a = mul(p, m)
                                        [by hypothesis with-exact-quotient-m]
                                      @add-zero-right |
                                        forall n: Int; add(n, ZERO) = n
                                        [by theorem addZeroRight]
                                      @product-m-plus-zero |
                                        add(mul(p, m), ZERO) = mul(p, m)
                                        [by forall_elim(mul(p, m)) add-zero-right]
                                      @product-m-is-sum |
                                        mul(p, m) = add(mul(p, m), ZERO)
                                        [by symmetry product-m-plus-zero]
                                      @a-is-p-times-m-plus-zero |
                                        a = add(mul(p, m), ZERO)
                                        [by rewrite product-m-is-sum a-is-p-times-m]
                                      @zero-is-nonneg |
                                        is_nonneg(ZERO)
                                        [by axiom nonnegZero]
                                      @uniqueness |
                                        forall x, y, q1, r1, q2, r2: Int; below(ZERO, y) -> x = add(mul(y, q1), r1) -> is_nonneg(r1) -> below(r1, y) -> x = add(mul(y, q2), r2) -> is_nonneg(r2) -> below(r2, y) -> (q1 = q2 and r1 = r2)
                                        [by theorem divisionAlgorithmUnique]
                                      @uniqueness-for-a |
                                        below(ZERO, p) -> a = add(mul(p, q), r) -> is_nonneg(r) -> below(r, p) -> a = add(mul(p, m), ZERO) -> is_nonneg(ZERO) -> below(ZERO, p) -> (q = m and r = ZERO)
                                        [by forall_elim(a, p, q, r, m, ZERO) uniqueness]
                                      @quotient-and-remainder-agree |
                                        q = m and r = ZERO
                                        [by tautology uniqueness-for-a p-positive decomposition remainder-nonneg remainder-below-p a-is-p-times-m-plus-zero zero-is-nonneg]
                                      @remainder-is-zero |
                                        r = ZERO
                                        [by and_elim_right quotient-and-remainder-agree]
                                      @zero-below-zero |
                                        below(ZERO, ZERO)
                                        [by rewrite remainder-is-zero remainder-positive]
                                      @less-than-irreflexive |
                                        forall n: Int; not below(n, n)
                                        [by theorem lessThanIrreflexive]
                                      @zero-not-below-zero |
                                        not below(ZERO, ZERO)
                                        [by forall_elim(ZERO) less-than-irreflexive]
                                      @contradiction |
                                        not does_divide(p, a)
                                        [by absurd zero-below-zero zero-not-below-zero]
                                    }
                                  @p-does-not-divide-a-inner |
                                    not does_divide(p, a)
                                    [by exists_elim with-exact-quotient-m]
                                }
                              @p-does-not-divide-a |
                                not does_divide(p, a)
                                [by not_intro given-p-divides-a p-divides-a p-does-not-divide-a-inner]
                              @divides-decidable |
                                does_divide(p, a) or (not does_divide(p, a))
                                [by or_intro_right p-does-not-divide-a]
                            }
                        }
                    }
                  @divides-decidable |
                    does_divide(p, a) or (not does_divide(p, a))
                    [by exists_elim with-remainder-r]
                }
              @divides-decidable |
                does_divide(p, a) or (not does_divide(p, a))
                [by exists_elim with-quotient-q]
            }
          @positive-p-gives-decidable |
            below(ZERO, p) -> (does_divide(p, a) or (not does_divide(p, a)))
            [by implies_intro given-p-positive]
        }
      @discharge-a |
        forall a: Int; below(ZERO, p) -> (does_divide(p, a) or (not does_divide(p, a)))
        [by forall_intro generalize-a]
    }
  @conclusion |
    forall p, a: Int; below(ZERO, p) -> (does_divide(p, a) or (not does_divide(p, a)))
    [by forall_intro generalize-p]
qed
```

The bridge from primality to Bézout's coprimality hypothesis. Judson takes
`gcd(a, p) = 1` for granted when `p ∤ a`; over `ℤ` the common divisors come with
signs, so we prove it: any common divisor `e` of `a` and `p` is `±1`. Trichotomy
splits `e`. If `e > 0`, primality gives `e = 1` or `e = p`, and `e = p` would
make `p | a` (via `e | a`), against the hypothesis — so `e = 1`. If `e < 0`,
`neg(e) > 0` divides both (`negDivides`), so the same argument gives `neg(e) = 1`,
i.e. `e = -1`. And `e = 0` is impossible, since `0 | p` forces `p = 0 < p`.

```bpa
theorem coprimalityFromPrime: forall p, a: Int;
  below(ZERO, p)
  -> (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
  -> (not does_divide(p, a))
  -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
proof
  @generalize-p |
    fix p: Int {
      @generalize-a |
        fix a: Int {
          @given-p-positive |
            assume below(ZERO, p) {
              @given-p-prime |
                assume forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p) {
                  @given-p-not-divide-a |
                    assume not does_divide(p, a) {
                      @p-positive |
                        below(ZERO, p)
                        [by hypothesis given-p-positive]
                      @only-positive-divisors-are-one-and-p |
                        forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)
                        [by hypothesis given-p-prime]
                      @p-does-not-divide-a |
                        not does_divide(p, a)
                        [by hypothesis given-p-not-divide-a]
                      @common-divisors-are-units |
                        fix e: Int {
                          @given-e-divides-a |
                            assume does_divide(e, a) {
                              @given-e-divides-p |
                                assume does_divide(e, p) {
                                  @e-divides-a |
                                    does_divide(e, a)
                                    [by hypothesis given-e-divides-a]
                                  @e-divides-p |
                                    does_divide(e, p)
                                    [by hypothesis given-e-divides-p]
                                  @trichotomy-of-e |
                                    forall x, y: Int; below(x, y) or x = y or below(y, x)
                                    [by theorem trichotomy]
                                  @e-against-zero |
                                    below(e, ZERO) or e = ZERO or below(ZERO, e)
                                    [by forall_elim(e, ZERO) trichotomy-of-e]
                                  @e-is-unit |
                                    e = ONE or e = neg(ONE)
                                    case on e-against-zero {
                                      @when-e-negative |
                                        assume below(e, ZERO) {
                                          @e-negative |
                                            below(e, ZERO)
                                            [by hypothesis when-e-negative]
                                          @negation-of-negative-is-positive |
                                            forall x: Int; below(x, ZERO) -> below(ZERO, neg(x))
                                            [by theorem positiveOfNegative]
                                          @neg-e-positive-if-e-negative |
                                            below(e, ZERO) -> below(ZERO, neg(e))
                                            [by forall_elim(e) negation-of-negative-is-positive]
                                          @neg-e-is-positive |
                                            below(ZERO, neg(e))
                                            [by modus_ponens neg-e-positive-if-e-negative e-negative]
                                          @neg-divides |
                                            forall x, n: Int; does_divide(x, n) -> does_divide(neg(x), n)
                                            [by theorem negDivides]
                                          @neg-e-divides-p-if-e-divides |
                                            does_divide(e, p) -> does_divide(neg(e), p)
                                            [by forall_elim(e, p) neg-divides]
                                          @neg-e-divides-p |
                                            does_divide(neg(e), p)
                                            [by modus_ponens neg-e-divides-p-if-e-divides e-divides-p]
                                          @neg-e-divides-a-if-e-divides |
                                            does_divide(e, a) -> does_divide(neg(e), a)
                                            [by forall_elim(e, a) neg-divides]
                                          @neg-e-divides-a |
                                            does_divide(neg(e), a)
                                            [by modus_ponens neg-e-divides-a-if-e-divides e-divides-a]
                                          @primality-at-neg-e |
                                            below(ZERO, neg(e)) -> does_divide(neg(e), p) -> (neg(e) = ONE or neg(e) = p)
                                            [by forall_elim(neg(e)) only-positive-divisors-are-one-and-p]
                                          @neg-e-is-one-or-p |
                                            neg(e) = ONE or neg(e) = p
                                            [by tautology primality-at-neg-e neg-e-is-positive neg-e-divides-p]
                                          @e-is-unit |
                                            e = ONE or e = neg(ONE)
                                            case on neg-e-is-one-or-p {
                                              @when-neg-e-is-one |
                                                assume neg(e) = ONE {
                                                  // neg(e)=1 => e = neg(neg(e)) = neg(1).
                                                  @neg-e-is-one |
                                                    neg(e) = ONE
                                                    [by hypothesis when-neg-e-is-one]
                                                  @neg-neg-is-identity |
                                                    forall n: Int; neg(neg(n)) = n
                                                    [by theorem negNeg]
                                                  @e-is-neg-neg-e |
                                                    neg(neg(e)) = e
                                                    [by forall_elim(e) neg-neg-is-identity]
                                                  @neg-one-is-e |
                                                    neg(ONE) = e
                                                    [by rewrite neg-e-is-one e-is-neg-neg-e]
                                                  @e-is-neg-one |
                                                    e = neg(ONE)
                                                    [by symmetry neg-one-is-e]
                                                  @e-is-unit |
                                                    e = ONE or e = neg(ONE)
                                                    [by or_intro_right e-is-neg-one]
                                                }
                                              @when-neg-e-is-p |
                                                assume neg(e) = p {
                                                  // neg(e)=p and neg(e)|a => p|a, contradiction.
                                                  @neg-e-is-p |
                                                    neg(e) = p
                                                    [by hypothesis when-neg-e-is-p]
                                                  @p-divides-a |
                                                    does_divide(p, a)
                                                    [by rewrite neg-e-is-p neg-e-divides-a]
                                                  @e-is-unit |
                                                    e = ONE or e = neg(ONE)
                                                    [by absurd p-divides-a p-does-not-divide-a]
                                                }
                                            }
                                        }
                                      @when-e-zero |
                                        assume e = ZERO {
                                          // e=0 and e|p => p = 0*k = 0, but p>0 is irreflexive-absurd.
                                          @e-is-zero |
                                            e = ZERO
                                            [by hypothesis when-e-zero]
                                          @zero-divides-p |
                                            does_divide(ZERO, p)
                                            [by rewrite e-is-zero e-divides-p]
                                          @divides-elim |
                                            forall d, n: Int; does_divide(d, n) -> exists k: Int; n = mul(d, k)
                                            [by theorem dividesElim]
                                          @quotient-of-p-by-zero-exists |
                                            does_divide(ZERO, p) -> exists k: Int; p = mul(ZERO, k)
                                            [by forall_elim(ZERO, p) divides-elim]
                                          @zero-quotient-exists |
                                            exists k: Int; p = mul(ZERO, k)
                                            [by modus_ponens quotient-of-p-by-zero-exists zero-divides-p]
                                          @with-zero-quotient-k |
                                            unpack k: Int from zero-quotient-exists {
                                              @p-is-zero-times-k |
                                                p = mul(ZERO, k)
                                                [by hypothesis with-zero-quotient-k]
                                              @mul-zero-left |
                                                forall b: Int; mul(ZERO, b) = ZERO
                                                [by axiom mulZeroLeft]
                                              @zero-times-k-is-zero |
                                                mul(ZERO, k) = ZERO
                                                [by forall_elim(k) mul-zero-left]
                                              @p-is-zero |
                                                p = ZERO
                                                [by rewrite zero-times-k-is-zero p-is-zero-times-k]
                                              @zero-is-positive |
                                                below(ZERO, ZERO)
                                                [by rewrite p-is-zero p-positive]
                                              @less-than-irreflexive |
                                                forall n: Int; not below(n, n)
                                                [by theorem lessThanIrreflexive]
                                              @zero-not-below-zero |
                                                not below(ZERO, ZERO)
                                                [by forall_elim(ZERO) less-than-irreflexive]
                                              @e-is-unit |
                                                e = ONE or e = neg(ONE)
                                                [by absurd zero-is-positive zero-not-below-zero]
                                            }
                                          @e-is-unit |
                                            e = ONE or e = neg(ONE)
                                            [by exists_elim with-zero-quotient-k]
                                        }
                                      @when-e-positive |
                                        assume below(ZERO, e) {
                                          @e-positive |
                                            below(ZERO, e)
                                            [by hypothesis when-e-positive]
                                          @primality-at-e |
                                            below(ZERO, e) -> does_divide(e, p) -> (e = ONE or e = p)
                                            [by forall_elim(e) only-positive-divisors-are-one-and-p]
                                          @e-is-one-or-p |
                                            e = ONE or e = p
                                            [by tautology primality-at-e e-positive e-divides-p]
                                          @e-is-unit |
                                            e = ONE or e = neg(ONE)
                                            case on e-is-one-or-p {
                                              @when-e-is-one |
                                                assume e = ONE {
                                                  @e-is-one |
                                                    e = ONE
                                                    [by hypothesis when-e-is-one]
                                                  @e-is-unit |
                                                    e = ONE or e = neg(ONE)
                                                    [by or_intro_left e-is-one]
                                                }
                                              @when-e-is-p |
                                                assume e = p {
                                                  @e-is-p |
                                                    e = p
                                                    [by hypothesis when-e-is-p]
                                                  @p-divides-a |
                                                    does_divide(p, a)
                                                    [by rewrite e-is-p e-divides-a]
                                                  @e-is-unit |
                                                    e = ONE or e = neg(ONE)
                                                    [by absurd p-divides-a p-does-not-divide-a]
                                                }
                                            }
                                        }
                                    }
                                }
                              @e-divides-p-gives-unit |
                                does_divide(e, p) -> (e = ONE or e = neg(ONE))
                                [by implies_intro given-e-divides-p]
                            }
                          @e-divides-a-gives-unit |
                            does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE))
                            [by implies_intro given-e-divides-a]
                        }
                      @common-divisors-are-units-for-all |
                        forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE))
                        [by forall_intro common-divisors-are-units]
                    }
                  @not-divide-gives-coprime |
                    (not does_divide(p, a))
                      -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
                    [by implies_intro given-p-not-divide-a]
                }
              @prime-gives-coprime |
                (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
                  -> (not does_divide(p, a))
                  -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
                [by implies_intro given-p-prime]
            }
          @positive-p-gives-coprime |
            below(ZERO, p)
              -> (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
              -> (not does_divide(p, a))
              -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
            [by implies_intro given-p-positive]
        }
      @discharge-a |
        forall a: Int; below(ZERO, p)
          -> (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
          -> (not does_divide(p, a))
          -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
        [by forall_intro generalize-a]
    }
  @conclusion |
    forall p, a: Int; below(ZERO, p)
      -> (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
      -> (not does_divide(p, a))
      -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
    [by forall_intro generalize-p]
qed
```

**Euclid's Lemma** (Judson, Lemma 2.11). Suppose the prime `p` divides `a·b`.
If `p | a` we are done; otherwise `p ∤ a`, so `a` and `p` are coprime and
`coprimeBezout` yields `r, s` with `a·r + p·s = 1` (the hypotheses `a ≠ 0` and
`p ≠ 0` come from `p ∤ a` — `p | 0` always holds — and from `p > 0`). Multiplying
by `b`, `b = b·1 = b·(a·r + p·s) = (a·b)·r + p·(b·s)`, the last identity the only
elided ring algebra (discharged by `polynomial`). Now `p | (a·b)·r` because
`p | a·b`, and `p | p·(b·s)` because `p | p`; by `dividesAdd` their sum, which is
`b`, is divisible by `p`. So `p | b`.

```bpa
theorem euclidsLemma: forall a, b, p: Int;
  (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
  -> does_divide(p, mul(a, b))
  -> (does_divide(p, a) or does_divide(p, b))
proof
  @generalize-a |
    fix a: Int {
      @generalize-b |
        fix b: Int {
          @generalize-p |
            fix p: Int {
              @given-p-prime |
                assume below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)) {
                  @given-p-divides-product |
                    assume does_divide(p, mul(a, b)) {
                      @prime-conjunction |
                        below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
                        [by hypothesis given-p-prime]
                      @one-below-p |
                        below(ONE, p)
                        [by and_elim_left prime-conjunction]
                      @only-positive-divisors-are-one-and-p |
                        forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)
                        [by and_elim_right prime-conjunction]
                      @p-divides-product |
                        does_divide(p, mul(a, b))
                        [by hypothesis given-p-divides-product]
                      // p > 0, from 0 < 1 < p.
                      @zero-below-one |
                        below(ZERO, ONE)
                        [by theorem zeroBelowOne]
                      @less-than-transitive |
                        forall x, y, z: Int; below(x, y) -> below(y, z) -> below(x, z)
                        [by theorem lessThanTransitive]
                      @zero-below-p-if-chained |
                        below(ZERO, ONE) -> below(ONE, p) -> below(ZERO, p)
                        [by forall_elim(ZERO, ONE, p) less-than-transitive]
                      @p-positive |
                        below(ZERO, p)
                        [by tautology zero-below-p-if-chained zero-below-one one-below-p]
                      // decide whether p | a.
                      @divides-decidable |
                        forall x, y: Int; below(ZERO, x) -> (does_divide(x, y) or (not does_divide(x, y)))
                        [by theorem dividesDecidable]
                      @p-divides-a-decidable |
                        below(ZERO, p) -> (does_divide(p, a) or (not does_divide(p, a)))
                        [by forall_elim(p, a) divides-decidable]
                      @p-divides-a-or-not |
                        does_divide(p, a) or (not does_divide(p, a))
                        [by modus_ponens p-divides-a-decidable p-positive]
                      @p-divides-a-or-b |
                        does_divide(p, a) or does_divide(p, b)
                        case on p-divides-a-or-not {
                          @when-p-divides-a |
                            assume does_divide(p, a) {
                              @p-divides-a |
                                does_divide(p, a)
                                [by hypothesis when-p-divides-a]
                              @p-divides-a-or-b |
                                does_divide(p, a) or does_divide(p, b)
                                [by or_intro_left p-divides-a]
                            }
                          @when-p-does-not-divide-a |
                            assume not does_divide(p, a) {
                              @p-does-not-divide-a |
                                not does_divide(p, a)
                                [by hypothesis when-p-does-not-divide-a]
                              // a != 0: else p | 0 = a, against the branch.
                              @given-a-zero |
                                assume a = ZERO {
                                  @a-is-zero |
                                    a = ZERO
                                    [by hypothesis given-a-zero]
                                  @zero-is-p-times-zero |
                                    ZERO = mul(p, ZERO)
                                    [by simplify mulZeroRight]
                                  @a-is-p-times-zero |
                                    a = mul(p, ZERO)
                                    [by rewrite zero-is-p-times-zero a-is-zero]
                                  @divides-intro |
                                    forall n, d, k: Int; n = mul(d, k) -> does_divide(d, n)
                                    [by theorem dividesIntro]
                                  @p-divides-a-if-product |
                                    a = mul(p, ZERO) -> does_divide(p, a)
                                    [by forall_elim(a, p, ZERO) divides-intro]
                                  @p-divides-a |
                                    does_divide(p, a)
                                    [by modus_ponens p-divides-a-if-product a-is-p-times-zero]
                                  @a-not-zero-inner |
                                    a != ZERO
                                    [by absurd p-divides-a p-does-not-divide-a]
                                }
                              @a-not-zero |
                                a != ZERO
                                [by not_intro given-a-zero a-is-zero a-not-zero-inner]
                              // p != 0, from p > 0.
                              @given-p-zero |
                                assume p = ZERO {
                                  @p-is-zero |
                                    p = ZERO
                                    [by hypothesis given-p-zero]
                                  @zero-below-zero |
                                    below(ZERO, ZERO)
                                    [by rewrite p-is-zero p-positive]
                                  @less-than-irreflexive |
                                    forall n: Int; not below(n, n)
                                    [by theorem lessThanIrreflexive]
                                  @zero-not-below-zero |
                                    not below(ZERO, ZERO)
                                    [by forall_elim(ZERO) less-than-irreflexive]
                                  @p-not-zero-inner |
                                    p != ZERO
                                    [by absurd zero-below-zero zero-not-below-zero]
                                }
                              @p-not-zero |
                                p != ZERO
                                [by not_intro given-p-zero p-is-zero p-not-zero-inner]
                              // a and p are coprime.
                              @coprimality-from-prime |
                                forall q, x: Int; below(ZERO, q) -> (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q)) -> (not does_divide(q, x)) -> (forall e: Int; does_divide(e, x) -> does_divide(e, q) -> (e = ONE or e = neg(ONE)))
                                [by theorem coprimalityFromPrime]
                              @coprimality-for-a-and-p |
                                below(ZERO, p) -> (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)) -> (not does_divide(p, a)) -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE)))
                                [by forall_elim(p, a) coprimality-from-prime]
                              @a-and-p-coprime |
                                forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE))
                                [by tautology coprimality-for-a-and-p p-positive only-positive-divisors-are-one-and-p p-does-not-divide-a]
                              // Bezout: a*r + p*s = 1.
                              @bezout-identity |
                                forall x, y: Int; x != ZERO -> y != ZERO -> (forall e: Int; does_divide(e, x) -> does_divide(e, y) -> (e = ONE or e = neg(ONE))) -> exists r: Int; exists s: Int; add(mul(x, r), mul(y, s)) = ONE
                                [by theorem coprimeBezout]
                              @bezout-for-a-and-p |
                                a != ZERO -> p != ZERO -> (forall e: Int; does_divide(e, a) -> does_divide(e, p) -> (e = ONE or e = neg(ONE))) -> exists r: Int; exists s: Int; add(mul(a, r), mul(p, s)) = ONE
                                [by forall_elim(a, p) bezout-identity]
                              @bezout-coefficients-exist |
                                exists r: Int; exists s: Int; add(mul(a, r), mul(p, s)) = ONE
                                [by tautology bezout-for-a-and-p a-not-zero p-not-zero a-and-p-coprime]
                              @with-bezout-r |
                                unpack r: Int from bezout-coefficients-exist {
                                  @bezout-s-exists |
                                    exists s: Int; add(mul(a, r), mul(p, s)) = ONE
                                    [by hypothesis with-bezout-r]
                                  @with-bezout-s |
                                    unpack s: Int from bezout-s-exists {
                                      @bezout-holds |
                                        add(mul(a, r), mul(p, s)) = ONE
                                        [by hypothesis with-bezout-s]
                                      // b = b*1 = b*(a*r + p*s) = (a*b)*r + p*(b*s).
                                      @mul-one-right |
                                        forall n: Int; mul(n, ONE) = n
                                        [by theorem mulOneRight]
                                      @b-times-one-is-b |
                                        mul(b, ONE) = b
                                        [by forall_elim(b) mul-one-right]
                                      @b-is-b-times-one |
                                        b = mul(b, ONE)
                                        [by symmetry b-times-one-is-b]
                                      @one-is-bezout |
                                        ONE = add(mul(a, r), mul(p, s))
                                        [by symmetry bezout-holds]
                                      @b-is-b-times-bezout |
                                        b = mul(b, add(mul(a, r), mul(p, s)))
                                        [by rewrite one-is-bezout b-is-b-times-one]
                                      @product-distributes |
                                        mul(b, add(mul(a, r), mul(p, s))) = add(mul(mul(a, b), r), mul(p, mul(b, s)))
                                        [by polynomial]
                                      @b-splits-into-product-and-p-multiple |
                                        b = add(mul(mul(a, b), r), mul(p, mul(b, s)))
                                        [by rewrite product-distributes b-is-b-times-bezout]
                                      // p | (a*b)*r, since p | a*b.
                                      @divides-mul |
                                        forall d, x, c: Int; does_divide(d, x) -> does_divide(d, mul(x, c))
                                        [by theorem dividesMul]
                                      @p-divides-product-times-r-if |
                                        does_divide(p, mul(a, b)) -> does_divide(p, mul(mul(a, b), r))
                                        [by forall_elim(p, mul(a, b), r) divides-mul]
                                      @p-divides-product-times-r |
                                        does_divide(p, mul(mul(a, b), r))
                                        [by modus_ponens p-divides-product-times-r-if p-divides-product]
                                      // p | p*(b*s), since p | p.
                                      @divides-refl |
                                        forall d: Int; does_divide(d, d)
                                        [by theorem dividesRefl]
                                      @p-divides-p |
                                        does_divide(p, p)
                                        [by forall_elim(p) divides-refl]
                                      @p-divides-p-times-bs-if |
                                        does_divide(p, p) -> does_divide(p, mul(p, mul(b, s)))
                                        [by forall_elim(p, p, mul(b, s)) divides-mul]
                                      @p-divides-p-times-bs |
                                        does_divide(p, mul(p, mul(b, s)))
                                        [by modus_ponens p-divides-p-times-bs-if p-divides-p]
                                      // p | their sum, which is b.
                                      @divides-add |
                                        forall d, x, y: Int; does_divide(d, x) -> does_divide(d, y) -> does_divide(d, add(x, y))
                                        [by theorem dividesAdd]
                                      @p-divides-sum-if |
                                        does_divide(p, mul(mul(a, b), r)) -> does_divide(p, mul(p, mul(b, s))) -> does_divide(p, add(mul(mul(a, b), r), mul(p, mul(b, s))))
                                        [by forall_elim(p, mul(mul(a, b), r), mul(p, mul(b, s))) divides-add]
                                      @p-divides-sum |
                                        does_divide(p, add(mul(mul(a, b), r), mul(p, mul(b, s))))
                                        [by tautology p-divides-sum-if p-divides-product-times-r p-divides-p-times-bs]
                                      @sum-is-b |
                                        add(mul(mul(a, b), r), mul(p, mul(b, s))) = b
                                        [by symmetry b-splits-into-product-and-p-multiple]
                                      @p-divides-b |
                                        does_divide(p, b)
                                        [by rewrite sum-is-b p-divides-sum]
                                      @p-divides-a-or-b |
                                        does_divide(p, a) or does_divide(p, b)
                                        [by or_intro_right p-divides-b]
                                    }
                                  @p-divides-a-or-b |
                                    does_divide(p, a) or does_divide(p, b)
                                    [by exists_elim with-bezout-s]
                                }
                              @p-divides-a-or-b |
                                does_divide(p, a) or does_divide(p, b)
                                [by exists_elim with-bezout-r]
                            }
                        }
                    }
                  @product-implies-divides-factor |
                    does_divide(p, mul(a, b)) -> (does_divide(p, a) or does_divide(p, b))
                    [by implies_intro given-p-divides-product]
                }
              @prime-implies-divides-factor |
                (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)))
                  -> does_divide(p, mul(a, b)) -> (does_divide(p, a) or does_divide(p, b))
                [by implies_intro given-p-prime]
            }
          @discharge-p |
            forall p: Int; (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> does_divide(p, mul(a, b)) -> (does_divide(p, a) or does_divide(p, b))
            [by forall_intro generalize-p]
        }
      @discharge-b |
        forall b, p: Int; (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> does_divide(p, mul(a, b)) -> (does_divide(p, a) or does_divide(p, b))
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b, p: Int; (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> does_divide(p, mul(a, b)) -> (does_divide(p, a) or does_divide(p, b))
    [by forall_intro generalize-a]
qed
```

### The Infinitude of Primes

Euclid's classical argument (Judson, Theorem 2.12): were there only finitely
many primes `p₁, …, pₙ`, the number `P = p₁·p₂·⋯·pₙ + 1` has a prime factor `p`
(every integer `> 1` does — `primeFactorExists`), and by completeness `p` is one
of the `pᵢ`. Then `p` divides the product `p₁·⋯·pₙ` (`memberDividesProduct`), and
`p` divides `P`, so `p` divides `P − (product) = 1` (`dividesSubtracts`) — forcing
`p ≤ 1`, contradicting `p > 1`. No finite list can hold every prime.

We phrase a finite list of primes as a `Seq primes` together with a bound `count`,
its product `productUpTo(primes, count)`, and the *completeness* hypothesis
"every prime occurs among the first `count` entries" — stated with the membership
predicate `inSeqBelow`. The theorem is the negation: **no such complete finite
list exists**. Its one honest side condition, `productUpTo(primes, count) > 0`,
holds of any list of primes (each factor exceeds `1`); we take it as a hypothesis
rather than re-derive it, since it is manifest and orthogonal to the argument.

The `P > 1` step reads `P = product + 1 > product ≥ 1`; the `P − product = 1`
step is the only elided algebra (a linear identity discharged by `simplify`).

```bpa
theorem infinitudeOfPrimes: forall primes: Seq; forall count: Int;
  is_nonneg(count) -> below(ZERO, productUpTo(primes, count))
  -> (not (forall q: Int; (below(ONE, q) and (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBelow(q, primes, count)))
proof
  @generalize-primes |
    fix primes: Seq {
      @generalize-count |
        fix count: Int {
          @given-count-nonneg |
            assume is_nonneg(count) {
              @given-product-positive |
                assume below(ZERO, productUpTo(primes, count)) {
                  @product-positive |
                    below(ZERO, productUpTo(primes, count))
                    [by hypothesis given-product-positive]
                  // Suppose `primes` (up to `count`) enumerates every prime.
                  @given-list-is-complete |
                    assume forall q: Int; (below(ONE, q) and (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBelow(q, primes, count) {
                      @list-is-complete |
                        forall q: Int; (below(ONE, q) and (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBelow(q, primes, count)
                        [by hypothesis given-list-is-complete]
                      // P = product + 1 exceeds 1: product ≥ 1 and product < product + 1.
                      @one-at-most-positive |
                        forall x: Int; below(ZERO, x) -> at_most(ONE, x)
                        [by theorem oneAtMostPositive]
                      @one-at-most-product-if |
                        below(ZERO, productUpTo(primes, count)) -> at_most(ONE, productUpTo(primes, count))
                        [by forall_elim(productUpTo(primes, count)) one-at-most-positive]
                      @one-at-most-product |
                        at_most(ONE, productUpTo(primes, count))
                        [by modus_ponens one-at-most-product-if product-positive]
                      @product-below-successor |
                        forall n: Int; below(n, succ(n))
                        [by theorem lessThanSelf]
                      @product-below-succ-product |
                        below(productUpTo(primes, count), succ(productUpTo(primes, count)))
                        [by forall_elim(productUpTo(primes, count)) product-below-successor]
                      @succ-product-is-product-plus-one |
                        succ(productUpTo(primes, count)) = add(productUpTo(primes, count), ONE)
                        [by simplify oneIsSuccZero addSuccRight addZeroRight]
                      @product-below-big-number |
                        below(productUpTo(primes, count), add(productUpTo(primes, count), ONE))
                        [by rewrite succ-product-is-product-plus-one product-below-succ-product]
                      @at-most-then-below |
                        forall x, y, z: Int; at_most(x, y) -> below(y, z) -> below(x, z)
                        [by theorem atMostThenBelow]
                      @one-below-big-number-if |
                        at_most(ONE, productUpTo(primes, count)) -> below(productUpTo(primes, count), add(productUpTo(primes, count), ONE)) -> below(ONE, add(productUpTo(primes, count), ONE))
                        [by forall_elim(ONE, productUpTo(primes, count), add(productUpTo(primes, count), ONE)) at-most-then-below]
                      @one-below-big-number |
                        below(ONE, add(productUpTo(primes, count), ONE))
                        [by tautology one-below-big-number-if one-at-most-product product-below-big-number]
                      // P has a prime factor p.
                      @prime-factor-exists |
                        forall n: Int; below(ONE, n) -> exists p: Int; (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, n)
                        [by theorem primeFactorExists]
                      @big-number-has-prime-factor-if |
                        below(ONE, add(productUpTo(primes, count), ONE)) -> exists p: Int; (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, add(productUpTo(primes, count), ONE))
                        [by forall_elim(add(productUpTo(primes, count), ONE)) prime-factor-exists]
                      @big-number-has-prime-factor |
                        exists p: Int; (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, add(productUpTo(primes, count), ONE))
                        [by modus_ponens big-number-has-prime-factor-if one-below-big-number]
                      @with-prime-factor-p |
                        unpack p: Int from big-number-has-prime-factor {
                          @p-prime-and-divides-big-number |
                            (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, add(productUpTo(primes, count), ONE))
                            [by hypothesis with-prime-factor-p]
                          @p-is-prime |
                            below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
                            [by and_elim_left p-prime-and-divides-big-number]
                          @p-divides-big-number |
                            does_divide(p, add(productUpTo(primes, count), ONE))
                            [by and_elim_right p-prime-and-divides-big-number]
                          @p-above-one |
                            below(ONE, p)
                            [by and_elim_left p-is-prime]
                          // p is one of the listed primes, so p | product.
                          @complete-at-p |
                            (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) -> inSeqBelow(p, primes, count)
                            [by forall_elim(p) list-is-complete]
                          @p-in-list |
                            inSeqBelow(p, primes, count)
                            [by modus_ponens complete-at-p p-is-prime]
                          @member-divides-product |
                            forall s: Seq; forall k: Int; forall x: Int; inSeqBelow(x, s, k) -> does_divide(x, productUpTo(s, k))
                            [by theorem memberDividesProduct]
                          @p-divides-product-if-member |
                            inSeqBelow(p, primes, count) -> does_divide(p, productUpTo(primes, count))
                            [by forall_elim(primes, count, p) member-divides-product]
                          @p-divides-product |
                            does_divide(p, productUpTo(primes, count))
                            [by modus_ponens p-divides-product-if-member p-in-list]
                          // p | P and p | product give p | (P − product) = p | 1.
                          @divides-subtracts |
                            forall d, a, b: Int; does_divide(d, a) -> does_divide(d, b) -> does_divide(d, sub(a, b))
                            [by theorem dividesSubtracts]
                          @p-divides-difference-if |
                            does_divide(p, add(productUpTo(primes, count), ONE)) -> does_divide(p, productUpTo(primes, count)) -> does_divide(p, sub(add(productUpTo(primes, count), ONE), productUpTo(primes, count)))
                            [by forall_elim(p, add(productUpTo(primes, count), ONE), productUpTo(primes, count)) divides-subtracts]
                          @p-divides-difference |
                            does_divide(p, sub(add(productUpTo(primes, count), ONE), productUpTo(primes, count)))
                            [by tautology p-divides-difference-if p-divides-big-number p-divides-product]
                          // P − product = (product + 1) − product = 1, step by step.
                          @difference-unfolds |
                            forall x, y: Int; sub(x, y) = add(x, neg(y))
                            [by axiom subDef]
                          @difference-is-sum-with-negation |
                            sub(add(productUpTo(primes, count), ONE), productUpTo(primes, count)) = add(add(productUpTo(primes, count), ONE), neg(productUpTo(primes, count)))
                            [by forall_elim(add(productUpTo(primes, count), ONE), productUpTo(primes, count)) difference-unfolds]
                          @product-plus-one-commutes |
                            forall x, y: Int; add(x, y) = add(y, x)
                            [by theorem addIsCommutative]
                          @product-plus-one-is-one-plus-product |
                            add(productUpTo(primes, count), ONE) = add(ONE, productUpTo(primes, count))
                            [by forall_elim(productUpTo(primes, count), ONE) product-plus-one-commutes]
                          @difference-with-one-first |
                            sub(add(productUpTo(primes, count), ONE), productUpTo(primes, count)) = add(add(ONE, productUpTo(primes, count)), neg(productUpTo(primes, count)))
                            [by rewrite product-plus-one-is-one-plus-product difference-is-sum-with-negation]
                          @addition-associates |
                            forall x, y, z: Int; add(add(x, y), z) = add(x, add(y, z))
                            [by theorem addIsAssociative]
                          @regroup-difference |
                            add(add(ONE, productUpTo(primes, count)), neg(productUpTo(primes, count))) = add(ONE, add(productUpTo(primes, count), neg(productUpTo(primes, count))))
                            [by forall_elim(ONE, productUpTo(primes, count), neg(productUpTo(primes, count))) addition-associates]
                          @difference-regrouped |
                            sub(add(productUpTo(primes, count), ONE), productUpTo(primes, count)) = add(ONE, add(productUpTo(primes, count), neg(productUpTo(primes, count))))
                            [by rewrite regroup-difference difference-with-one-first]
                          @product-minus-itself-is-zero |
                            forall x: Int; add(x, neg(x)) = ZERO
                            [by theorem addNegRight]
                          @product-cancels |
                            add(productUpTo(primes, count), neg(productUpTo(primes, count))) = ZERO
                            [by forall_elim(productUpTo(primes, count)) product-minus-itself-is-zero]
                          @difference-is-one-plus-zero |
                            sub(add(productUpTo(primes, count), ONE), productUpTo(primes, count)) = add(ONE, ZERO)
                            [by rewrite product-cancels difference-regrouped]
                          @add-zero-on-the-right |
                            forall x: Int; add(x, ZERO) = x
                            [by theorem addZeroRight]
                          @one-plus-zero-is-one |
                            add(ONE, ZERO) = ONE
                            [by forall_elim(ONE) add-zero-on-the-right]
                          @difference-is-one |
                            sub(add(productUpTo(primes, count), ONE), productUpTo(primes, count)) = ONE
                            [by rewrite one-plus-zero-is-one difference-is-one-plus-zero]
                          @p-divides-one |
                            does_divide(p, ONE)
                            [by rewrite difference-is-one p-divides-difference]
                          // p | 1 with p > 1 is impossible: p ≤ 1 < p is below itself.
                          @zero-below-one |
                            below(ZERO, ONE)
                            [by theorem zeroBelowOne]
                          @less-than-transitive |
                            forall x, y, z: Int; below(x, y) -> below(y, z) -> below(x, z)
                            [by theorem lessThanTransitive]
                          @zero-below-p-if-chained |
                            below(ZERO, ONE) -> below(ONE, p) -> below(ZERO, p)
                            [by forall_elim(ZERO, ONE, p) less-than-transitive]
                          @p-positive |
                            below(ZERO, p)
                            [by tautology zero-below-p-if-chained zero-below-one p-above-one]
                          @divisor-at-most |
                            forall x, y: Int; below(ZERO, x) -> below(ZERO, y) -> does_divide(x, y) -> at_most(x, y)
                            [by theorem divisorAtMost]
                          @p-at-most-one-if |
                            below(ZERO, p) -> below(ZERO, ONE) -> does_divide(p, ONE) -> at_most(p, ONE)
                            [by forall_elim(p, ONE) divisor-at-most]
                          @p-at-most-one |
                            at_most(p, ONE)
                            [by tautology p-at-most-one-if p-positive zero-below-one p-divides-one]
                          @p-below-p-if |
                            at_most(p, ONE) -> below(ONE, p) -> below(p, p)
                            [by forall_elim(p, ONE, p) at-most-then-below]
                          @p-below-itself |
                            below(p, p)
                            [by tautology p-below-p-if p-at-most-one p-above-one]
                          @nothing-below-itself |
                            forall x: Int; not below(x, x)
                            [by theorem lessThanIrreflexive]
                          @p-not-below-itself |
                            not below(p, p)
                            [by forall_elim(p) nothing-below-itself]
                          @contradiction |
                            not (forall q: Int; (below(ONE, q) and (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBelow(q, primes, count))
                            [by absurd p-below-itself p-not-below-itself]
                        }
                      @contradiction |
                        not (forall q: Int; (below(ONE, q) and (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBelow(q, primes, count))
                        [by exists_elim with-prime-factor-p]
                    }
                  @list-complete-is-absurd |
                    (forall q: Int; (below(ONE, q) and (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBelow(q, primes, count)) -> (not (forall q: Int; (below(ONE, q) and (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBelow(q, primes, count)))
                    [by implies_intro given-list-is-complete]
                  @no-complete-finite-list |
                    not (forall q: Int; (below(ONE, q) and (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBelow(q, primes, count))
                    [by tautology list-complete-is-absurd]
                }
              @product-positive-implies-no-complete-list |
                below(ZERO, productUpTo(primes, count)) -> (not (forall q: Int; (below(ONE, q) and (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBelow(q, primes, count)))
                [by implies_intro given-product-positive]
            }
          @count-nonneg-implies-no-complete-list |
            is_nonneg(count) -> below(ZERO, productUpTo(primes, count)) -> (not (forall q: Int; (below(ONE, q) and (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBelow(q, primes, count)))
            [by implies_intro given-count-nonneg]
        }
      @discharge-count |
        forall count: Int; is_nonneg(count) -> below(ZERO, productUpTo(primes, count)) -> (not (forall q: Int; (below(ONE, q) and (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBelow(q, primes, count)))
        [by forall_intro generalize-count]
    }
  @conclusion |
    forall primes: Seq; forall count: Int; is_nonneg(count) -> below(ZERO, productUpTo(primes, count)) -> (not (forall q: Int; (below(ONE, q) and (forall d: Int; below(ZERO, d) -> does_divide(d, q) -> (d = ONE or d = q))) -> inSeqBelow(q, primes, count)))
    [by forall_intro generalize-primes]
qed
```

The proof reifies Judson's contradiction exactly: `primeFactorExists` supplies the
prime factor `p` of `P`; the completeness hypothesis places `p` in the list;
`memberDividesProduct` makes `p` divide the product; `dividesSubtracts` collapses
`p | P` and `p | product` to `p | 1`; and a positive divisor of `1` is `≤ 1`,
which `p > 1` refutes. The `count` is left unconstrained beyond nonnegativity —
the argument never inspects it — so the theorem denies a complete finite prime
list of *any* length.

## The Euclidean Algorithm

Judson closes §2.2 with the **Euclidean Algorithm**: to compute `gcd(a, b)`
concretely, repeatedly apply the Division Algorithm — `a = bq₁ + r₁`,
`b = r₁q₂ + r₂`, `r₁ = r₂q₃ + r₃`, … — until a zero remainder is reached; the
last nonzero remainder is `gcd(a, b)`, and back-substituting up the chain recovers
the Bézout coefficients `r`, `s` of the previous theorem. This is a *computation*
on particular integers rather than a universally-quantified statement, so we do
not transcribe a numeric instance here; `bezout` already establishes, for every
nonzero `a`, `b`, the existence and defining properties of the gcd that the
algorithm computes.

```bpa
// The Euclidean Algorithm (illustrative, not transcribed):
//   gcd(a, b): while b != 0 { (a, b) := (b, a mod b) }; return a
// It terminates because the nonnegative remainders strictly decrease
// (Well-Ordering), and back-substitution through the division steps yields the
// Bézout coefficients r, s with gcd(a, b) = a*r + b*s established by `bezout`.
```

## The Fundamental Theorem of Arithmetic

> **Theorem 2.15 (Fundamental Theorem of Arithmetic).** Let $n$ be an integer such
> that $n > 1$. Then $n = p_1 p_2 \cdots p_k$, where $p_1, \ldots, p_k$ are primes
> (not necessarily distinct). Furthermore, this factorization is unique; that is,
> if $n = q_1 q_2 \cdots q_l$ is a second factorization into primes, then $k = l$
> and the $q_i$ are, after reordering, the $p_i$.

The book's §2.2 capstone. We prove it in the form the ℤ sequence library makes
expressible: a **prime factorization of `n`** is a finite sequence `s` of length
`k` whose every entry is prime and whose finite product `productUpTo(s, k)` is
`n`. As throughout, "`x` is prime" is spelled inline as
`below(ONE, x) and (forall d: Int; below(ZERO, d) -> does_divide(d, x) -> (d = ONE or d = x))`.

### Existence

Judson: *every integer `n > 1` is a product of primes.* The argument is strong
induction on `n`. A prime factor `p` splits off (`primeFactorExists` gives a
prime `p` dividing `n`, hence a cofactor `c` with `n = p·c`); the cofactor is
positive, so either `c = 1` — and then `n = p` is itself prime, factored by the
singleton `[p]` — or `c > 1`, and then `c < n`, so the induction hypothesis
factors `c` as a sequence `t`, which we prepend `p` to. We first isolate three
order/arithmetic facts the induction leans on.

A nonnegative index strictly below `ONE` must be `ZERO`: `ONE = succ ZERO`, so
`m < ONE` splits (`lessThanSuccSplit`) into `m < ZERO` — impossible for a
nonnegative `m` — or `m = ZERO`.

```bpa
theorem nonnegBelowOneIsZero: forall m: Int; is_nonneg(m) -> below(m, ONE) -> m = ZERO
proof
  @generalize-m |
    fix m: Int {
      @given-m-nonneg |
        assume is_nonneg(m) {
          @given-m-below-one |
            assume below(m, ONE) {
              @m-nonneg |
                is_nonneg(m)
                [by hypothesis given-m-nonneg]
              @m-below-one |
                below(m, ONE)
                [by hypothesis given-m-below-one]
              @one-is-succ-zero |
                ONE = succ(ZERO)
                [by axiom oneIsSuccZero]
              @m-below-succ-zero |
                below(m, succ(ZERO))
                [by rewrite one-is-succ-zero m-below-one]
              @zero-is-nonneg |
                is_nonneg(ZERO)
                [by axiom nonnegZero]
              @succ-split |
                forall a, b: Int; is_nonneg(a) -> is_nonneg(b) -> below(a, succ(b)) -> (below(a, b) or a = b)
                [by theorem lessThanSuccSplit]
              @m-below-succ-zero-splits |
                is_nonneg(m) -> is_nonneg(ZERO) -> below(m, succ(ZERO)) -> (below(m, ZERO) or m = ZERO)
                [by forall_elim(m, ZERO) succ-split]
              @m-below-zero-or-equal-zero |
                below(m, ZERO) or m = ZERO
                [by tautology m-below-succ-zero-splits m-nonneg zero-is-nonneg m-below-succ-zero]
              @m-is-zero |
                m = ZERO
                case on m-below-zero-or-equal-zero {
                  @when-m-below-zero |
                    assume below(m, ZERO) {
                      @m-below-zero |
                        below(m, ZERO)
                        [by hypothesis when-m-below-zero]
                      @nothing-nonneg-below-zero |
                        forall a: Int; is_nonneg(a) -> (not below(a, ZERO))
                        [by theorem nonnegNotBelowZero]
                      @m-not-below-zero-if-nonneg |
                        is_nonneg(m) -> (not below(m, ZERO))
                        [by forall_elim(m) nothing-nonneg-below-zero]
                      @m-not-below-zero |
                        not below(m, ZERO)
                        [by modus_ponens m-not-below-zero-if-nonneg m-nonneg]
                      @conclusion-m-is-zero |
                        m = ZERO
                        [by absurd m-below-zero m-not-below-zero]
                    }
                  @when-m-equals-zero |
                    assume m = ZERO {
                      @conclusion-m-is-zero |
                        m = ZERO
                        [by hypothesis when-m-equals-zero]
                    }
                }
            }
          @below-one-implies-zero |
            below(m, ONE) -> m = ZERO
            [by implies_intro given-m-below-one]
        }
      @nonneg-implies-below-one-implies-zero |
        is_nonneg(m) -> below(m, ONE) -> m = ZERO
        [by implies_intro given-m-nonneg]
    }
  @conclusion |
    forall m: Int; is_nonneg(m) -> below(m, ONE) -> m = ZERO
    [by forall_intro generalize-m]
qed
```

`p > 1` and `c > 0` force `c < p·c`: write `p = 1 + q` with `q = p − 1 > 0`, so
`p·c = c·p = c·1 + c·q = c + c·q` with `c·q > 0`, whence `c < c + c·q = p·c`. This
is what makes the cofactor `c` strictly smaller than `n = p·c`, licensing the
induction hypothesis.

```bpa
theorem belowMulGreaterThanOne: forall p, c: Int;
  below(ONE, p) -> below(ZERO, c) -> below(c, mul(p, c))
proof
  @generalize-p |
    fix p: Int {
      @generalize-c |
        fix c: Int {
          @given-p-above-one |
            assume below(ONE, p) {
              @given-c-positive |
                assume below(ZERO, c) {
                  @p-above-one |
                    below(ONE, p)
                    [by hypothesis given-p-above-one]
                  @c-positive |
                    below(ZERO, c)
                    [by hypothesis given-c-positive]
                  // q := p − 1 is positive, and 1 + q = p.
                  @add-diff-reaches |
                    forall a, b: Int; add(a, sub(b, a)) = b
                    [by theorem addDiffReaches]
                  @one-plus-gap-is-p |
                    add(ONE, sub(p, ONE)) = p
                    [by forall_elim(ONE, p) add-diff-reaches]
                  @add-preserves-order |
                    forall a, b, x: Int; below(a, b) -> below(add(x, a), add(x, b))
                    [by theorem additionPreservesOrder]
                  @add-zero-right |
                    forall a: Int; add(a, ZERO) = a
                    [by theorem addZeroRight]
                  @one-plus-zero-is-one |
                    add(ONE, ZERO) = ONE
                    [by forall_elim(ONE) add-zero-right]
                  @one-is-one-plus-zero |
                    ONE = add(ONE, ZERO)
                    [by symmetry one-plus-zero-is-one]
                  @p-is-one-plus-gap |
                    p = add(ONE, sub(p, ONE))
                    [by symmetry one-plus-gap-is-p]
                  // recast 1 < p as (1+0) < (1+q), then cancel the leading 1.
                  @one-recast-below-p |
                    below(add(ONE, ZERO), p)
                    [by rewrite one-is-one-plus-zero p-above-one]
                  @one-plus-zero-below-one-plus-gap |
                    below(add(ONE, ZERO), add(ONE, sub(p, ONE)))
                    [by rewrite p-is-one-plus-gap one-recast-below-p]
                  @cancel-leading-one |
                    forall x, a, b: Int; below(add(x, a), add(x, b)) -> below(a, b)
                    [by theorem lessThanAddCancelLeft]
                  @cancel-one-for-gap |
                    below(add(ONE, ZERO), add(ONE, sub(p, ONE))) -> below(ZERO, sub(p, ONE))
                    [by forall_elim(ONE, ZERO, sub(p, ONE)) cancel-leading-one]
                  @gap-positive |
                    below(ZERO, sub(p, ONE))
                    [by modus_ponens cancel-one-for-gap one-plus-zero-below-one-plus-gap]
                  // c·q > 0 and c·1 + c·q = c·p.
                  @product-positive |
                    forall a, b: Int; below(ZERO, a) -> below(ZERO, b) -> below(ZERO, mul(a, b))
                    [by theorem productWithPositiveIsPositive]
                  @c-gap-positive-if-both |
                    below(ZERO, c) -> below(ZERO, sub(p, ONE)) -> below(ZERO, mul(c, sub(p, ONE)))
                    [by forall_elim(c, sub(p, ONE)) product-positive]
                  @c-times-gap-positive |
                    below(ZERO, mul(c, sub(p, ONE)))
                    [by tautology c-gap-positive-if-both c-positive gap-positive]
                  @distribute |
                    forall a, b, x: Int; mul(a, add(b, x)) = add(mul(a, b), mul(a, x))
                    [by theorem mulAddDistribLeft]
                  @c-times-p-distributes |
                    mul(c, add(ONE, sub(p, ONE))) = add(mul(c, ONE), mul(c, sub(p, ONE)))
                    [by forall_elim(c, ONE, sub(p, ONE)) distribute]
                  @mul-one-right |
                    forall a: Int; mul(a, ONE) = a
                    [by theorem mulOneRight]
                  @c-times-one-is-c |
                    mul(c, ONE) = c
                    [by forall_elim(c) mul-one-right]
                  @distribute-with-p |
                    mul(c, p) = add(mul(c, ONE), mul(c, sub(p, ONE)))
                    [by rewrite one-plus-gap-is-p c-times-p-distributes]
                  @c-times-p-is-c-plus-c-gap |
                    mul(c, p) = add(c, mul(c, sub(p, ONE)))
                    [by rewrite c-times-one-is-c distribute-with-p]
                  // c < c + c·q via additionPreservesOrder, then rewrite to c·p, p·c.
                  @c-below-c-plus-c-gap-raw |
                    below(ZERO, mul(c, sub(p, ONE))) -> below(add(c, ZERO), add(c, mul(c, sub(p, ONE))))
                    [by forall_elim(ZERO, mul(c, sub(p, ONE)), c) add-preserves-order]
                  @c-plus-zero-below-c-plus-gap |
                    below(add(c, ZERO), add(c, mul(c, sub(p, ONE))))
                    [by modus_ponens c-below-c-plus-c-gap-raw c-times-gap-positive]
                  @c-plus-zero-is-c |
                    add(c, ZERO) = c
                    [by forall_elim(c) add-zero-right]
                  @c-below-c-plus-c-gap |
                    below(c, add(c, mul(c, sub(p, ONE))))
                    [by rewrite c-plus-zero-is-c c-plus-zero-below-c-plus-gap]
                  @c-plus-c-gap-is-c-times-p |
                    add(c, mul(c, sub(p, ONE))) = mul(c, p)
                    [by symmetry c-times-p-is-c-plus-c-gap]
                  @c-below-c-times-p |
                    below(c, mul(c, p))
                    [by rewrite c-plus-c-gap-is-c-times-p c-below-c-plus-c-gap]
                  @mul-commutes |
                    forall a, b: Int; mul(a, b) = mul(b, a)
                    [by theorem mulIsCommutative]
                  @c-times-p-is-p-times-c |
                    mul(c, p) = mul(p, c)
                    [by forall_elim(c, p) mul-commutes]
                  @c-below-p-times-c |
                    below(c, mul(p, c))
                    [by rewrite c-times-p-is-p-times-c c-below-c-times-p]
                }
              @c-positive-implies-below |
                below(ZERO, c) -> below(c, mul(p, c))
                [by implies_intro given-c-positive]
            }
          @p-above-one-implies-below |
            below(ONE, p) -> below(ZERO, c) -> below(c, mul(p, c))
            [by implies_intro given-p-above-one]
        }
      @discharge-c |
        forall c: Int; below(ONE, p) -> below(ZERO, c) -> below(c, mul(p, c))
        [by forall_intro generalize-c]
    }
  @conclusion |
    forall p, c: Int; below(ONE, p) -> below(ZERO, c) -> below(c, mul(p, c))
    [by forall_intro generalize-p]
qed
```

The cofactor of a positive number by a positive factor is itself positive: from
`n = p·c` with `n, p > 0`, the sign of `c` (by `signTrichotomy`) cannot be zero
(that would make `n = 0`) nor negative (that would make `n = −(p·(−c)) ≤ 0`),
both against `n > 0`.

```bpa
theorem positiveCofactor: forall n, p, c: Int;
  below(ZERO, n) -> below(ZERO, p) -> n = mul(p, c) -> below(ZERO, c)
proof
  @generalize-n |
    fix n: Int {
      @generalize-p |
        fix p: Int {
          @generalize-c |
            fix c: Int {
              @given-n-positive |
                assume below(ZERO, n) {
                  @given-p-positive |
                    assume below(ZERO, p) {
                      @given-n-is-p-times-c |
                        assume n = mul(p, c) {
                          @n-positive |
                            below(ZERO, n)
                            [by hypothesis given-n-positive]
                          @p-positive |
                            below(ZERO, p)
                            [by hypothesis given-p-positive]
                          @n-is-p-times-c |
                            n = mul(p, c)
                            [by hypothesis given-n-is-p-times-c]
                          @positive-is-nonneg-lemma |
                            forall x: Int; below(ZERO, x) -> is_nonneg(x)
                            [by theorem positiveIsNonneg]
                          @p-nonneg-if-positive |
                            below(ZERO, p) -> is_nonneg(p)
                            [by forall_elim(p) positive-is-nonneg-lemma]
                          @p-nonneg |
                            is_nonneg(p)
                            [by modus_ponens p-nonneg-if-positive p-positive]
                          @sign-of-c |
                            forall x: Int; below(ZERO, x) or x = ZERO or below(x, ZERO)
                            [by theorem signTrichotomy]
                          @c-sign |
                            below(ZERO, c) or c = ZERO or below(c, ZERO)
                            [by forall_elim(c) sign-of-c]
                          @c-positive |
                            below(ZERO, c)
                            case on c-sign {
                              @when-c-positive |
                                assume below(ZERO, c) {
                                  @conclusion-c-positive |
                                    below(ZERO, c)
                                    [by hypothesis when-c-positive]
                                }
                              @when-c-zero |
                                assume c = ZERO {
                                  @c-is-zero |
                                    c = ZERO
                                    [by hypothesis when-c-zero]
                                  @product-with-zero-right |
                                    forall x: Int; mul(x, ZERO) = ZERO
                                    [by theorem mulZeroRight]
                                  @p-times-zero-is-zero |
                                    mul(p, ZERO) = ZERO
                                    [by forall_elim(p) product-with-zero-right]
                                  @n-is-p-times-zero |
                                    n = mul(p, ZERO)
                                    [by rewrite c-is-zero n-is-p-times-c]
                                  @n-is-zero |
                                    n = ZERO
                                    [by rewrite p-times-zero-is-zero n-is-p-times-zero]
                                  @zero-below-zero |
                                    below(ZERO, ZERO)
                                    [by rewrite n-is-zero n-positive]
                                  @nothing-below-itself |
                                    forall x: Int; not below(x, x)
                                    [by theorem lessThanIrreflexive]
                                  @zero-not-below-zero |
                                    not below(ZERO, ZERO)
                                    [by forall_elim(ZERO) nothing-below-itself]
                                  @conclusion-c-positive |
                                    below(ZERO, c)
                                    [by absurd zero-below-zero zero-not-below-zero]
                                }
                              @when-c-negative |
                                assume below(c, ZERO) {
                                  @c-negative |
                                    below(c, ZERO)
                                    [by hypothesis when-c-negative]
                                  @negative-neg-is-nonneg |
                                    forall x: Int; below(x, ZERO) -> is_nonneg(neg(x))
                                    [by theorem negBelowZero]
                                  @neg-c-nonneg-if-negative |
                                    below(c, ZERO) -> is_nonneg(neg(c))
                                    [by forall_elim(c) negative-neg-is-nonneg]
                                  @neg-c-nonneg |
                                    is_nonneg(neg(c))
                                    [by modus_ponens neg-c-nonneg-if-negative c-negative]
                                  @product-of-nonnegs-nonneg |
                                    forall a, b: Int; is_nonneg(a) -> is_nonneg(b) -> is_nonneg(mul(a, b))
                                    [by theorem nonnegMul]
                                  @p-times-neg-c-nonneg-if |
                                    is_nonneg(p) -> is_nonneg(neg(c)) -> is_nonneg(mul(p, neg(c)))
                                    [by forall_elim(p, neg(c)) product-of-nonnegs-nonneg]
                                  @p-times-neg-c-nonneg |
                                    is_nonneg(mul(p, neg(c)))
                                    [by tautology p-times-neg-c-nonneg-if p-nonneg neg-c-nonneg]
                                  @product-negates-right |
                                    forall a, b: Int; mul(a, neg(b)) = neg(mul(a, b))
                                    [by theorem mulNegRight]
                                  @p-times-neg-c-is-neg-product |
                                    mul(p, neg(c)) = neg(mul(p, c))
                                    [by forall_elim(p, c) product-negates-right]
                                  @neg-product-is-p-times-neg-c |
                                    neg(mul(p, c)) = mul(p, neg(c))
                                    [by symmetry p-times-neg-c-is-neg-product]
                                  @neg-n-refl |
                                    neg(n) = neg(n)
                                    [by reflexivity]
                                  @neg-n-is-neg-product |
                                    neg(n) = neg(mul(p, c))
                                    [by rewrite n-is-p-times-c neg-n-refl]
                                  @neg-n-is-p-times-neg-c |
                                    neg(n) = mul(p, neg(c))
                                    [by rewrite neg-product-is-p-times-neg-c neg-n-is-neg-product]
                                  @p-times-neg-c-is-neg-n |
                                    mul(p, neg(c)) = neg(n)
                                    [by symmetry neg-n-is-p-times-neg-c]
                                  @neg-n-nonneg |
                                    is_nonneg(neg(n))
                                    [by rewrite p-times-neg-c-is-neg-n p-times-neg-c-nonneg]
                                  @sum-with-negation-right |
                                    forall x: Int; add(x, neg(x)) = ZERO
                                    [by theorem addNegRight]
                                  @add-n-neg-n-is-zero |
                                    add(n, neg(n)) = ZERO
                                    [by forall_elim(n) sum-with-negation-right]
                                  @at-most-intro |
                                    forall a, g, b: Int; is_nonneg(g) -> add(a, g) = b -> at_most(a, b)
                                    [by theorem lessOrEqualIntro]
                                  @n-at-most-zero-if |
                                    is_nonneg(neg(n)) -> add(n, neg(n)) = ZERO -> at_most(n, ZERO)
                                    [by forall_elim(n, neg(n), ZERO) at-most-intro]
                                  @n-at-most-zero |
                                    at_most(n, ZERO)
                                    [by tautology n-at-most-zero-if neg-n-nonneg add-n-neg-n-is-zero]
                                  @below-gives-at-most |
                                    forall a, b: Int; below(a, b) -> at_most(a, b)
                                    [by theorem lessThanToLessOrEqual]
                                  @zero-at-most-n-if-below |
                                    below(ZERO, n) -> at_most(ZERO, n)
                                    [by forall_elim(ZERO, n) below-gives-at-most]
                                  @zero-at-most-n |
                                    at_most(ZERO, n)
                                    [by modus_ponens zero-at-most-n-if-below n-positive]
                                  @order-is-antisymmetric |
                                    forall a, b: Int; at_most(a, b) -> at_most(b, a) -> a = b
                                    [by theorem lessOrEqualAntisymmetric]
                                  @n-equals-zero-if-mutual |
                                    at_most(n, ZERO) -> at_most(ZERO, n) -> n = ZERO
                                    [by forall_elim(n, ZERO) order-is-antisymmetric]
                                  @n-is-zero |
                                    n = ZERO
                                    [by tautology n-equals-zero-if-mutual n-at-most-zero zero-at-most-n]
                                  @zero-below-zero |
                                    below(ZERO, ZERO)
                                    [by rewrite n-is-zero n-positive]
                                  @nothing-below-itself |
                                    forall x: Int; not below(x, x)
                                    [by theorem lessThanIrreflexive]
                                  @zero-not-below-zero |
                                    not below(ZERO, ZERO)
                                    [by forall_elim(ZERO) nothing-below-itself]
                                  @conclusion-c-positive |
                                    below(ZERO, c)
                                    [by absurd zero-below-zero zero-not-below-zero]
                                }
                            }
                        }
                      @is-p-times-c-implies-c-positive |
                        n = mul(p, c) -> below(ZERO, c)
                        [by implies_intro given-n-is-p-times-c]
                    }
                  @p-positive-implies |
                    below(ZERO, p) -> n = mul(p, c) -> below(ZERO, c)
                    [by implies_intro given-p-positive]
                }
              @n-positive-implies |
                below(ZERO, n) -> below(ZERO, p) -> n = mul(p, c) -> below(ZERO, c)
                [by implies_intro given-n-positive]
            }
          @discharge-c |
            forall c: Int; below(ZERO, n) -> below(ZERO, p) -> n = mul(p, c) -> below(ZERO, c)
            [by forall_intro generalize-c]
        }
      @discharge-p |
        forall p, c: Int; below(ZERO, n) -> below(ZERO, p) -> n = mul(p, c) -> below(ZERO, c)
        [by forall_intro generalize-p]
    }
  @conclusion |
    forall n, p, c: Int; below(ZERO, n) -> below(ZERO, p) -> n = mul(p, c) -> below(ZERO, c)
    [by forall_intro generalize-n]
qed
```

Now the existence half itself. Strong induction on `n` (`nonnegStrongInduction`):
take a prime factor `p | n` and the cofactor `c` with `n = p·c`, positive by
`positiveCofactor`. Split on `c`: when `c = 1`, `n = p` and the singleton `[p]`
(from `seqSingletonExists`) is the factorization; when `c > 1`, `c < n` so the
induction hypothesis gives a prime factorization `t` of length `j` for `c`, and
`seqConcatExists` prepends `[p]` — the resulting sequence has product
`p · productUpTo(t, j) = p·c = n`, and each of its entries is prime (index `0` is
`p`, the shifted indices are `t`'s prime entries). The `c < 1` branch is vacuous
(`c > 0` contradicts `c < 1`).

```bpa
theorem ftaExistence: forall n: Int; below(ONE, n) ->
  exists s: Seq; exists k: Int;
  is_nonneg(k)
  and (forall i: Int; is_nonneg(i) -> below(i, k) ->
  (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
  and n = productUpTo(s, k)
proof
  @zero-below-one |
    below(ZERO, ONE)
    [by theorem zeroBelowOne]
  @positive-is-nonneg |
    forall x: Int; below(ZERO, x) -> is_nonneg(x)
    [by theorem positiveIsNonneg]
  @one-nonneg-if-positive |
    below(ZERO, ONE) -> is_nonneg(ONE)
    [by forall_elim(ONE) positive-is-nonneg]
  @one-nonneg |
    is_nonneg(ONE)
    [by modus_ponens one-nonneg-if-positive zero-below-one]
  @less-than-transitive |
    forall a, b, c: Int; below(a, b) -> below(b, c) -> below(a, c)
    [by theorem lessThanTransitive]

  @strong-step |
    fix n: Int {
      @given-n-nonneg |
        assume is_nonneg(n) {
          @given-inductive-hypothesis |
            assume forall m: Int; is_nonneg(m) -> below(m, n) ->
              (below(ONE, m) -> exists s: Seq; exists k: Int;
              is_nonneg(k)
              and (forall i: Int; is_nonneg(i) -> below(i, k) ->
              (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
              and m = productUpTo(s, k)) {
              @inductive-hypothesis |
                forall m: Int; is_nonneg(m) -> below(m, n) ->
                  (below(ONE, m) -> exists s: Seq; exists k: Int;
                  is_nonneg(k)
                  and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                  (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                  and m = productUpTo(s, k))
                [by hypothesis given-inductive-hypothesis]
              @given-n-above-one |
                assume below(ONE, n) {
                  @n-above-one |
                    below(ONE, n)
                    [by hypothesis given-n-above-one]
                  @one-below-n-transitive |
                    below(ZERO, ONE) -> below(ONE, n) -> below(ZERO, n)
                    [by forall_elim(ZERO, ONE, n) less-than-transitive]
                  @n-positive |
                    below(ZERO, n)
                    [by tautology one-below-n-transitive zero-below-one n-above-one]

                  // ---- get a prime factor p and cofactor c with n = p·c -----
                  @prime-factor-exists-lemma |
                    forall x: Int; below(ONE, x)
                      -> exists p: Int; (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, x)
                    [by theorem primeFactorExists]
                  @prime-factor-exists |
                    below(ONE, n)
                      -> exists p: Int; (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, n)
                    [by forall_elim(n) prime-factor-exists-lemma]
                  @n-has-a-prime-factor |
                    exists p: Int; (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, n)
                    [by tautology prime-factor-exists n-above-one]
                  @with-prime-factor-p |
                    unpack p: Int from n-has-a-prime-factor {
                      @prime-and-divides |
                        (below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))) and does_divide(p, n)
                        [by hypothesis with-prime-factor-p]
                      @p-is-prime |
                        below(ONE, p) and (forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p))
                        [by and_elim_left prime-and-divides]
                      @p-above-one |
                        below(ONE, p)
                        [by and_elim_left p-is-prime]
                      @p-divides-n |
                        does_divide(p, n)
                        [by and_elim_right prime-and-divides]
                      @one-below-p-transitive |
                        below(ZERO, ONE) -> below(ONE, p) -> below(ZERO, p)
                        [by forall_elim(ZERO, ONE, p) less-than-transitive]
                      @p-positive |
                        below(ZERO, p)
                        [by tautology one-below-p-transitive zero-below-one p-above-one]
                      @divides-elim |
                        forall d, x: Int; does_divide(d, x) -> exists k: Int; x = mul(d, k)
                        [by theorem dividesElim]
                      @n-is-p-multiple-if |
                        does_divide(p, n) -> exists k: Int; n = mul(p, k)
                        [by forall_elim(p, n) divides-elim]
                      @n-is-p-multiple |
                        exists k: Int; n = mul(p, k)
                        [by modus_ponens n-is-p-multiple-if p-divides-n]
                      @with-cofactor-c |
                        unpack c: Int from n-is-p-multiple {
                          @n-is-p-times-c |
                            n = mul(p, c)
                            [by hypothesis with-cofactor-c]
                          @cofactor-positive-lemma |
                            forall x, y, z: Int; below(ZERO, x) -> below(ZERO, y) -> x = mul(y, z) -> below(ZERO, z)
                            [by theorem positiveCofactor]
                          @cofactor-positive-if |
                            below(ZERO, n) -> below(ZERO, p) -> n = mul(p, c) -> below(ZERO, c)
                            [by forall_elim(n, p, c) cofactor-positive-lemma]
                          @c-positive |
                            below(ZERO, c)
                            [by tautology cofactor-positive-if n-positive p-positive n-is-p-times-c]

                          // ---- split the cofactor: c = 1 or c > 1 ----------
                          @trichotomy-one-c |
                            forall a, b: Int; below(a, b) or a = b or below(b, a)
                            [by theorem trichotomy]
                          @one-versus-c |
                            below(ONE, c) or ONE = c or below(c, ONE)
                            [by forall_elim(ONE, c) trichotomy-one-c]
                          @factorization |
                            exists s: Seq; exists k: Int;
                              is_nonneg(k)
                              and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                              (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                              and n = productUpTo(s, k)
                            case on one-versus-c {
                              // c > 1: factor c by the IH, then prepend p via concat.
                              @when-c-above-one |
                                assume below(ONE, c) {
                                  @c-above-one |
                                    below(ONE, c)
                                    [by hypothesis when-c-above-one]
                                  @c-nonneg-if-positive |
                                    below(ZERO, c) -> is_nonneg(c)
                                    [by forall_elim(c) positive-is-nonneg]
                                  @c-nonneg |
                                    is_nonneg(c)
                                    [by modus_ponens c-nonneg-if-positive c-positive]
                                  // c < n = p·c, so the IH applies to c.
                                  @below-mul-lemma |
                                    forall x, y: Int; below(ONE, x) -> below(ZERO, y) -> below(y, mul(x, y))
                                    [by theorem belowMulGreaterThanOne]
                                  @c-below-p-times-c-if |
                                    below(ONE, p) -> below(ZERO, c) -> below(c, mul(p, c))
                                    [by forall_elim(p, c) below-mul-lemma]
                                  @c-below-p-times-c |
                                    below(c, mul(p, c))
                                    [by tautology c-below-p-times-c-if p-above-one c-positive]
                                  @p-times-c-is-n |
                                    mul(p, c) = n
                                    [by symmetry n-is-p-times-c]
                                  @c-below-n |
                                    below(c, n)
                                    [by rewrite p-times-c-is-n c-below-p-times-c]
                                  // apply the IH: c has a prime factorization (t, j).
                                  @ih-at-c |
                                    is_nonneg(c) -> below(c, n) ->
                                      (below(ONE, c) -> exists s: Seq; exists k: Int;
                                      is_nonneg(k)
                                      and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                      (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                      and c = productUpTo(s, k))
                                    [by forall_elim(c) inductive-hypothesis]
                                  @c-has-factorization |
                                    exists s: Seq; exists k: Int;
                                      is_nonneg(k)
                                      and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                      (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                      and c = productUpTo(s, k)
                                    [by tautology ih-at-c c-nonneg c-below-n c-above-one]
                                  @with-factor-sequence |
                                    unpack t: Seq from c-has-factorization {
                                      @factorization-of-c-with-length |
                                        exists k: Int;
                                          is_nonneg(k)
                                          and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                          (below(ONE, at(t, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(t, i)) -> (d = ONE or d = at(t, i)))))
                                          and c = productUpTo(t, k)
                                        [by hypothesis with-factor-sequence]
                                      @with-factor-length |
                                        unpack j: Int from factorization-of-c-with-length {
                                          @factorization-of-c |
                                            is_nonneg(j)
                                              and (forall i: Int; is_nonneg(i) -> below(i, j) ->
                                              (below(ONE, at(t, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(t, i)) -> (d = ONE or d = at(t, i)))))
                                              and c = productUpTo(t, j)
                                            [by hypothesis with-factor-length]
                                          @length-and-primes-of-c |
                                            is_nonneg(j)
                                              and (forall i: Int; is_nonneg(i) -> below(i, j) ->
                                              (below(ONE, at(t, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(t, i)) -> (d = ONE or d = at(t, i)))))
                                            [by and_elim_left factorization-of-c]
                                          @j-nonneg |
                                            is_nonneg(j)
                                            [by and_elim_left length-and-primes-of-c]
                                          @t-entries-prime |
                                            forall i: Int; is_nonneg(i) -> below(i, j) ->
                                              (below(ONE, at(t, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(t, i)) -> (d = ONE or d = at(t, i))))
                                            [by and_elim_right length-and-primes-of-c]
                                          @c-is-t-product |
                                            c = productUpTo(t, j)
                                            [by and_elim_right factorization-of-c]
                                          // singleton [p] to prepend.
                                          @singleton-exists-schema |
                                            forall x: Int; exists s: Seq; at(s, ZERO) = x and productUpTo(s, ONE) = x
                                            [by axiom seqSingletonExists]
                                          @singleton-p-exists |
                                            exists s: Seq; at(s, ZERO) = p and productUpTo(s, ONE) = p
                                            [by forall_elim(p) singleton-exists-schema]
                                          @with-singleton |
                                            unpack sp: Seq from singleton-p-exists {
                                              @singleton-entry-and-product |
                                                at(sp, ZERO) = p and productUpTo(sp, ONE) = p
                                                [by hypothesis with-singleton]
                                              @singleton-entry-is-p |
                                                at(sp, ZERO) = p
                                                [by and_elim_left singleton-entry-and-product]
                                              @singleton-product-is-p |
                                                productUpTo(sp, ONE) = p
                                                [by and_elim_right singleton-entry-and-product]
                                              // concatenate [p] (length 1) with t (length j).
                                              @concat-exists-schema |
                                                forall s: Seq; forall t2: Seq; forall a: Int; forall b: Int;
                                                  is_nonneg(a) -> is_nonneg(b) ->
                                                  exists u: Seq;
                                                  (forall i: Int; is_nonneg(i) -> below(i, a) -> at(u, i) = at(s, i))
                                                  and (forall i: Int; is_nonneg(i) -> below(i, b) -> at(u, add(a, i)) = at(t2, i))
                                                  and productUpTo(u, add(a, b)) = mul(productUpTo(s, a), productUpTo(t2, b))
                                                [by axiom seqConcatExists]
                                              @concat-sp-t-if |
                                                is_nonneg(ONE) -> is_nonneg(j) ->
                                                  exists u: Seq;
                                                  (forall i: Int; is_nonneg(i) -> below(i, ONE) -> at(u, i) = at(sp, i))
                                                  and (forall i: Int; is_nonneg(i) -> below(i, j) -> at(u, add(ONE, i)) = at(t, i))
                                                  and productUpTo(u, add(ONE, j)) = mul(productUpTo(sp, ONE), productUpTo(t, j))
                                                [by forall_elim(sp, t, ONE, j) concat-exists-schema]
                                              @concat-sp-t |
                                                exists u: Seq;
                                                  (forall i: Int; is_nonneg(i) -> below(i, ONE) -> at(u, i) = at(sp, i))
                                                  and (forall i: Int; is_nonneg(i) -> below(i, j) -> at(u, add(ONE, i)) = at(t, i))
                                                  and productUpTo(u, add(ONE, j)) = mul(productUpTo(sp, ONE), productUpTo(t, j))
                                                [by tautology concat-sp-t-if one-nonneg j-nonneg]
                                              @with-concat |
                                                unpack u: Seq from concat-sp-t {
                                                  @concat-clauses |
                                                    (forall i: Int; is_nonneg(i) -> below(i, ONE) -> at(u, i) = at(sp, i))
                                                      and (forall i: Int; is_nonneg(i) -> below(i, j) -> at(u, add(ONE, i)) = at(t, i))
                                                      and productUpTo(u, add(ONE, j)) = mul(productUpTo(sp, ONE), productUpTo(t, j))
                                                    [by hypothesis with-concat]
                                                  @concat-prefix-and-shift |
                                                    (forall i: Int; is_nonneg(i) -> below(i, ONE) -> at(u, i) = at(sp, i))
                                                      and (forall i: Int; is_nonneg(i) -> below(i, j) -> at(u, add(ONE, i)) = at(t, i))
                                                    [by and_elim_left concat-clauses]
                                                  @concat-prefix |
                                                    forall i: Int; is_nonneg(i) -> below(i, ONE) -> at(u, i) = at(sp, i)
                                                    [by and_elim_left concat-prefix-and-shift]
                                                  @concat-shift |
                                                    forall i: Int; is_nonneg(i) -> below(i, j) -> at(u, add(ONE, i)) = at(t, i)
                                                    [by and_elim_right concat-prefix-and-shift]
                                                  @concat-product |
                                                    productUpTo(u, add(ONE, j)) = mul(productUpTo(sp, ONE), productUpTo(t, j))
                                                    [by and_elim_right concat-clauses]
                                                  // length k = 1 + j is nonneg.
                                                  @nonneg-add |
                                                    forall a, b: Int; is_nonneg(a) -> is_nonneg(b) -> is_nonneg(add(a, b))
                                                    [by theorem nonnegAdd]
                                                  @length-nonneg-if |
                                                    is_nonneg(ONE) -> is_nonneg(j) -> is_nonneg(add(ONE, j))
                                                    [by forall_elim(ONE, j) nonneg-add]
                                                  @length-nonneg |
                                                    is_nonneg(add(ONE, j))
                                                    [by tautology length-nonneg-if one-nonneg j-nonneg]
                                                  // product: productUpTo(u, 1+j) = p·c = n.
                                                  @product-is-p-times-c-raw |
                                                    productUpTo(u, add(ONE, j)) = mul(p, productUpTo(t, j))
                                                    [by rewrite singleton-product-is-p concat-product]
                                                  @t-product-is-c |
                                                    productUpTo(t, j) = c
                                                    [by symmetry c-is-t-product]
                                                  @product-is-p-times-c |
                                                    productUpTo(u, add(ONE, j)) = mul(p, c)
                                                    [by rewrite t-product-is-c product-is-p-times-c-raw]
                                                  @product-is-n |
                                                    productUpTo(u, add(ONE, j)) = n
                                                    [by rewrite p-times-c-is-n product-is-p-times-c]
                                                  @n-is-concat-product |
                                                    n = productUpTo(u, add(ONE, j))
                                                    [by symmetry product-is-n]
                                                  // every entry of u below 1+j is prime.
                                                  @entries-of-concat-prime |
                                                    fix i: Int {
                                                      @given-i-nonneg |
                                                        assume is_nonneg(i) {
                                                          @given-i-below-length |
                                                            assume below(i, add(ONE, j)) {
                                                              @i-nonneg |
                                                                is_nonneg(i)
                                                                [by hypothesis given-i-nonneg]
                                                              @i-below-length |
                                                                below(i, add(ONE, j))
                                                                [by hypothesis given-i-below-length]
                                                              @one-refl |
                                                                forall x: Int; at_most(x, x)
                                                                [by theorem lessOrEqualRefl]
                                                              @below-to-at-most |
                                                                forall a, b: Int; below(a, b) -> at_most(a, b)
                                                                [by theorem lessThanToLessOrEqual]
                                                              @at-most-elim-schema |
                                                                forall a, b: Int; at_most(a, b) -> exists d: Int; is_nonneg(d) and add(a, d) = b
                                                                [by theorem lessOrEqualElim]
                                                              @at-most-elim-at-one-i |
                                                                at_most(ONE, i) -> exists d: Int; is_nonneg(d) and add(ONE, d) = i
                                                                [by forall_elim(ONE, i) at-most-elim-schema]
                                                              // split i < 1 (the p slot) vs 1 ≤ i (a t slot).
                                                              @trichotomy-i-one |
                                                                forall a, b: Int; below(a, b) or a = b or below(b, a)
                                                                [by theorem trichotomy]
                                                              @i-versus-one |
                                                                below(i, ONE) or i = ONE or below(ONE, i)
                                                                [by forall_elim(i, ONE) trichotomy-i-one]
                                                              @below-or-at-most |
                                                                below(i, ONE) or at_most(ONE, i)
                                                                case on i-versus-one {
                                                                  @when-i-below-one |
                                                                    assume below(i, ONE) {
                                                                      @i-below-one-here |
                                                                        below(i, ONE)
                                                                        [by hypothesis when-i-below-one]
                                                                      @conclusion-below-or-at-most |
                                                                        below(i, ONE) or at_most(ONE, i)
                                                                        [by or_intro_left i-below-one-here]
                                                                    }
                                                                  @when-i-equals-one |
                                                                    assume i = ONE {
                                                                      @i-equals-one-here |
                                                                        i = ONE
                                                                        [by hypothesis when-i-equals-one]
                                                                      @one-equals-i |
                                                                        ONE = i
                                                                        [by symmetry i-equals-one-here]
                                                                      @one-at-most-one |
                                                                        at_most(ONE, ONE)
                                                                        [by forall_elim(ONE) one-refl]
                                                                      @one-at-most-i |
                                                                        at_most(ONE, i)
                                                                        [by rewrite one-equals-i one-at-most-one]
                                                                      @conclusion-below-or-at-most |
                                                                        below(i, ONE) or at_most(ONE, i)
                                                                        [by or_intro_right one-at-most-i]
                                                                    }
                                                                  @when-one-below-i |
                                                                    assume below(ONE, i) {
                                                                      @one-below-i-here |
                                                                        below(ONE, i)
                                                                        [by hypothesis when-one-below-i]
                                                                      @one-below-i-at-most |
                                                                        below(ONE, i) -> at_most(ONE, i)
                                                                        [by forall_elim(ONE, i) below-to-at-most]
                                                                      @one-at-most-i |
                                                                        at_most(ONE, i)
                                                                        [by modus_ponens one-below-i-at-most one-below-i-here]
                                                                      @conclusion-below-or-at-most |
                                                                        below(i, ONE) or at_most(ONE, i)
                                                                        [by or_intro_right one-at-most-i]
                                                                    }
                                                                }
                                                              @entry-is-prime |
                                                                below(ONE, at(u, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))
                                                                case on below-or-at-most {
                                                                  // index 0: at(u,0) = at(sp,0) = p, prime.
                                                                  @when-i-in-prefix |
                                                                    assume below(i, ONE) {
                                                                      @i-below-one |
                                                                        below(i, ONE)
                                                                        [by hypothesis when-i-in-prefix]
                                                                      @nonneg-below-one-is-zero |
                                                                        forall x: Int; is_nonneg(x) -> below(x, ONE) -> x = ZERO
                                                                        [by theorem nonnegBelowOneIsZero]
                                                                      @i-is-zero-if |
                                                                        is_nonneg(i) -> below(i, ONE) -> i = ZERO
                                                                        [by forall_elim(i) nonneg-below-one-is-zero]
                                                                      @i-is-zero |
                                                                        i = ZERO
                                                                        [by tautology i-is-zero-if i-nonneg i-below-one]
                                                                      @concat-prefix-at-i |
                                                                        is_nonneg(i) -> below(i, ONE) -> at(u, i) = at(sp, i)
                                                                        [by forall_elim(i) concat-prefix]
                                                                      @u-at-i-is-sp-at-i |
                                                                        at(u, i) = at(sp, i)
                                                                        [by tautology concat-prefix-at-i i-nonneg i-below-one]
                                                                      @sp-at-i-refl |
                                                                        at(sp, i) = at(sp, i)
                                                                        [by reflexivity]
                                                                      @sp-at-i-is-sp-at-zero |
                                                                        at(sp, i) = at(sp, ZERO)
                                                                        [by rewrite i-is-zero sp-at-i-refl]
                                                                      @sp-at-i-is-p |
                                                                        at(sp, i) = p
                                                                        [by rewrite singleton-entry-is-p sp-at-i-is-sp-at-zero]
                                                                      @u-at-i-is-p |
                                                                        at(u, i) = p
                                                                        [by rewrite sp-at-i-is-p u-at-i-is-sp-at-i]
                                                                      @p-is-u-at-i |
                                                                        p = at(u, i)
                                                                        [by symmetry u-at-i-is-p]
                                                                      @entry-above-one |
                                                                        below(ONE, at(u, i))
                                                                        [by rewrite p-is-u-at-i p-above-one]
                                                                      @p-divisor-clause |
                                                                        forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)
                                                                        [by and_elim_right p-is-prime]
                                                                      @entry-divisor-clause |
                                                                        forall d: Int; below(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i))
                                                                        [by rewrite p-is-u-at-i p-divisor-clause]
                                                                      @conclusion-entry-is-prime |
                                                                        below(ONE, at(u, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))
                                                                        [by and_intro entry-above-one entry-divisor-clause]
                                                                    }
                                                                  // index ≥ 1: at(u,i) = at(t, i-1), prime by t's factorization.
                                                                  @when-i-in-shift |
                                                                    assume at_most(ONE, i) {
                                                                      @one-at-most-i |
                                                                        at_most(ONE, i)
                                                                        [by hypothesis when-i-in-shift]
                                                                      @gap-exists |
                                                                        exists d: Int; is_nonneg(d) and add(ONE, d) = i
                                                                        [by modus_ponens at-most-elim-at-one-i one-at-most-i]
                                                                      @with-gap |
                                                                        unpack g: Int from gap-exists {
                                                                          @gap-nonneg-and-reaches |
                                                                            is_nonneg(g) and add(ONE, g) = i
                                                                            [by hypothesis with-gap]
                                                                          @gap-nonneg |
                                                                            is_nonneg(g)
                                                                            [by and_elim_left gap-nonneg-and-reaches]
                                                                          @one-plus-gap-is-i |
                                                                            add(ONE, g) = i
                                                                            [by and_elim_right gap-nonneg-and-reaches]
                                                                          @i-is-one-plus-gap |
                                                                            i = add(ONE, g)
                                                                            [by symmetry one-plus-gap-is-i]
                                                                          // g < j from add(1,g) = i < add(1,j).
                                                                          @i-below-length-recast |
                                                                            below(add(ONE, g), add(ONE, j))
                                                                            [by rewrite i-is-one-plus-gap i-below-length]
                                                                          @cancel-leading-one |
                                                                            forall x, a, b: Int; below(add(x, a), add(x, b)) -> below(a, b)
                                                                            [by theorem lessThanAddCancelLeft]
                                                                          @gap-below-j-if |
                                                                            below(add(ONE, g), add(ONE, j)) -> below(g, j)
                                                                            [by forall_elim(ONE, g, j) cancel-leading-one]
                                                                          @gap-below-j |
                                                                            below(g, j)
                                                                            [by modus_ponens gap-below-j-if i-below-length-recast]
                                                                          // at(u, i) = at(u, 1+g) = at(t, g).
                                                                          @concat-shift-at-g |
                                                                            is_nonneg(g) -> below(g, j) -> at(u, add(ONE, g)) = at(t, g)
                                                                            [by forall_elim(g) concat-shift]
                                                                          @u-at-one-plus-gap-is-t-at-gap |
                                                                            at(u, add(ONE, g)) = at(t, g)
                                                                            [by tautology concat-shift-at-g gap-nonneg gap-below-j]
                                                                          @u-at-i-is-t-at-gap |
                                                                            at(u, i) = at(t, g)
                                                                            [by rewrite one-plus-gap-is-i u-at-one-plus-gap-is-t-at-gap]
                                                                          @t-at-gap-is-u-at-i |
                                                                            at(t, g) = at(u, i)
                                                                            [by symmetry u-at-i-is-t-at-gap]
                                                                          // t's g-th entry is prime.
                                                                          @t-entry-prime-at-g |
                                                                            is_nonneg(g) -> below(g, j) -> (below(ONE, at(t, g)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(t, g)) -> (d = ONE or d = at(t, g))))
                                                                            [by forall_elim(g) t-entries-prime]
                                                                          @t-gap-entry-prime |
                                                                            below(ONE, at(t, g)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(t, g)) -> (d = ONE or d = at(t, g)))
                                                                            [by tautology t-entry-prime-at-g gap-nonneg gap-below-j]
                                                                          @t-gap-above-one |
                                                                            below(ONE, at(t, g))
                                                                            [by and_elim_left t-gap-entry-prime]
                                                                          @t-gap-divisor-clause |
                                                                            forall d: Int; below(ZERO, d) -> does_divide(d, at(t, g)) -> (d = ONE or d = at(t, g))
                                                                            [by and_elim_right t-gap-entry-prime]
                                                                          @entry-above-one |
                                                                            below(ONE, at(u, i))
                                                                            [by rewrite t-at-gap-is-u-at-i t-gap-above-one]
                                                                          @entry-divisor-clause |
                                                                            forall d: Int; below(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i))
                                                                            [by rewrite t-at-gap-is-u-at-i t-gap-divisor-clause]
                                                                          @conclusion-entry-is-prime |
                                                                            below(ONE, at(u, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))
                                                                            [by and_intro entry-above-one entry-divisor-clause]
                                                                        }
                                                                      @conclusion-entry-is-prime |
                                                                        below(ONE, at(u, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))
                                                                        [by exists_elim with-gap]
                                                                    }
                                                                }
                                                            }
                                                          @i-below-length-implies-prime |
                                                            below(i, add(ONE, j)) -> (below(ONE, at(u, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i))))
                                                            [by implies_intro given-i-below-length]
                                                        }
                                                      @i-nonneg-implies-prime |
                                                        is_nonneg(i) -> below(i, add(ONE, j)) -> (below(ONE, at(u, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i))))
                                                        [by implies_intro given-i-nonneg]
                                                    }
                                                  @every-concat-entry-prime |
                                                    forall i: Int; is_nonneg(i) -> below(i, add(ONE, j)) -> (below(ONE, at(u, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i))))
                                                    [by forall_intro entries-of-concat-prime]
                                                  // assemble the existential.
                                                  @length-and-primes |
                                                    is_nonneg(add(ONE, j))
                                                      and (forall i: Int; is_nonneg(i) -> below(i, add(ONE, j)) -> (below(ONE, at(u, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))))
                                                    [by and_intro length-nonneg every-concat-entry-prime]
                                                  @factorization-body |
                                                    is_nonneg(add(ONE, j))
                                                      and (forall i: Int; is_nonneg(i) -> below(i, add(ONE, j)) -> (below(ONE, at(u, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))))
                                                      and n = productUpTo(u, add(ONE, j))
                                                    [by and_intro length-and-primes n-is-concat-product]
                                                  @factorization-with-length |
                                                    exists k: Int;
                                                      is_nonneg(k)
                                                      and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                                      (below(ONE, at(u, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(u, i)) -> (d = ONE or d = at(u, i)))))
                                                      and n = productUpTo(u, k)
                                                    [by exists_intro(add(ONE, j)) factorization-body]
                                                  @conclusion-factorization |
                                                    exists s: Seq; exists k: Int;
                                                      is_nonneg(k)
                                                      and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                                      (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                                      and n = productUpTo(s, k)
                                                    [by exists_intro(u) factorization-with-length]
                                                }
                                              @conclusion-concat-factorization |
                                                exists s: Seq; exists k: Int;
                                                  is_nonneg(k)
                                                  and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                                  (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                                  and n = productUpTo(s, k)
                                                [by exists_elim with-concat]
                                            }
                                          @conclusion-singleton-factorization |
                                            exists s: Seq; exists k: Int;
                                              is_nonneg(k)
                                              and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                              (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                              and n = productUpTo(s, k)
                                            [by exists_elim with-singleton]
                                        }
                                      @conclusion-factor-length |
                                        exists s: Seq; exists k: Int;
                                          is_nonneg(k)
                                          and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                          (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                          and n = productUpTo(s, k)
                                        [by exists_elim with-factor-length]
                                    }
                                  @conclusion-factorization |
                                    exists s: Seq; exists k: Int;
                                      is_nonneg(k)
                                      and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                      (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                      and n = productUpTo(s, k)
                                    [by exists_elim with-factor-sequence]
                                }
                              // c = 1: n = p·1 = p is itself prime; use [p].
                              @when-c-equals-one |
                                assume ONE = c {
                                  @one-is-c |
                                    ONE = c
                                    [by hypothesis when-c-equals-one]
                                  @c-is-one |
                                    c = ONE
                                    [by symmetry one-is-c]
                                  @n-is-p-times-one |
                                    n = mul(p, ONE)
                                    [by rewrite c-is-one n-is-p-times-c]
                                  @mul-one-right |
                                    forall x: Int; mul(x, ONE) = x
                                    [by theorem mulOneRight]
                                  @p-times-one-is-p |
                                    mul(p, ONE) = p
                                    [by forall_elim(p) mul-one-right]
                                  @n-is-p |
                                    n = p
                                    [by rewrite p-times-one-is-p n-is-p-times-one]
                                  // build the singleton [p].
                                  @singleton-exists-schema |
                                    forall x: Int; exists s: Seq; at(s, ZERO) = x and productUpTo(s, ONE) = x
                                    [by axiom seqSingletonExists]
                                  @singleton-p-exists |
                                    exists s: Seq; at(s, ZERO) = p and productUpTo(s, ONE) = p
                                    [by forall_elim(p) singleton-exists-schema]
                                  @with-singleton |
                                    unpack sp: Seq from singleton-p-exists {
                                      @singleton-entry-and-product |
                                        at(sp, ZERO) = p and productUpTo(sp, ONE) = p
                                        [by hypothesis with-singleton]
                                      @singleton-entry-is-p |
                                        at(sp, ZERO) = p
                                        [by and_elim_left singleton-entry-and-product]
                                      @singleton-product-is-p |
                                        productUpTo(sp, ONE) = p
                                        [by and_elim_right singleton-entry-and-product]
                                      // the sole entry (index 0) is p, which is prime.
                                      @entries-below-one-are-prime |
                                        fix i: Int {
                                          @given-i-nonneg |
                                            assume is_nonneg(i) {
                                              @given-i-below-one |
                                                assume below(i, ONE) {
                                                  @i-nonneg |
                                                    is_nonneg(i)
                                                    [by hypothesis given-i-nonneg]
                                                  @i-below-one |
                                                    below(i, ONE)
                                                    [by hypothesis given-i-below-one]
                                                  @nonneg-below-one-is-zero |
                                                    forall x: Int; is_nonneg(x) -> below(x, ONE) -> x = ZERO
                                                    [by theorem nonnegBelowOneIsZero]
                                                  @i-is-zero-if |
                                                    is_nonneg(i) -> below(i, ONE) -> i = ZERO
                                                    [by forall_elim(i) nonneg-below-one-is-zero]
                                                  @i-is-zero |
                                                    i = ZERO
                                                    [by tautology i-is-zero-if i-nonneg i-below-one]
                                                  @entry-refl |
                                                    at(sp, i) = at(sp, i)
                                                    [by reflexivity]
                                                  @entry-at-i-is-entry-at-zero |
                                                    at(sp, i) = at(sp, ZERO)
                                                    [by rewrite i-is-zero entry-refl]
                                                  @entry-at-i-is-p |
                                                    at(sp, i) = p
                                                    [by rewrite singleton-entry-is-p entry-at-i-is-entry-at-zero]
                                                  @p-is-entry-at-i |
                                                    p = at(sp, i)
                                                    [by symmetry entry-at-i-is-p]
                                                  @entry-above-one |
                                                    below(ONE, at(sp, i))
                                                    [by rewrite p-is-entry-at-i p-above-one]
                                                  @p-divisor-clause |
                                                    forall d: Int; below(ZERO, d) -> does_divide(d, p) -> (d = ONE or d = p)
                                                    [by and_elim_right p-is-prime]
                                                  @entry-divisor-clause |
                                                    forall d: Int; below(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i))
                                                    [by rewrite p-is-entry-at-i p-divisor-clause]
                                                  @conclusion-entry-is-prime |
                                                    below(ONE, at(sp, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i)))
                                                    [by and_intro entry-above-one entry-divisor-clause]
                                                }
                                              @i-below-one-implies-prime |
                                                below(i, ONE) -> (below(ONE, at(sp, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i))))
                                                [by implies_intro given-i-below-one]
                                            }
                                          @i-nonneg-implies-prime |
                                            is_nonneg(i) -> below(i, ONE) -> (below(ONE, at(sp, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i))))
                                            [by implies_intro given-i-nonneg]
                                        }
                                      @every-entry-is-prime |
                                        forall i: Int; is_nonneg(i) -> below(i, ONE) -> (below(ONE, at(sp, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i))))
                                        [by forall_intro entries-below-one-are-prime]
                                      // n = p = productUpTo(sp, 1).
                                      @p-is-singleton-product |
                                        p = productUpTo(sp, ONE)
                                        [by symmetry singleton-product-is-p]
                                      @n-is-singleton-product |
                                        n = productUpTo(sp, ONE)
                                        [by rewrite p-is-singleton-product n-is-p]
                                      @length-and-primes |
                                        is_nonneg(ONE)
                                          and (forall i: Int; is_nonneg(i) -> below(i, ONE) -> (below(ONE, at(sp, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i)))))
                                        [by and_intro one-nonneg every-entry-is-prime]
                                      @factorization-body |
                                        is_nonneg(ONE)
                                          and (forall i: Int; is_nonneg(i) -> below(i, ONE) -> (below(ONE, at(sp, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i)))))
                                          and n = productUpTo(sp, ONE)
                                        [by and_intro length-and-primes n-is-singleton-product]
                                      @factorization-with-length |
                                        exists k: Int;
                                          is_nonneg(k)
                                          and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                          (below(ONE, at(sp, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(sp, i)) -> (d = ONE or d = at(sp, i)))))
                                          and n = productUpTo(sp, k)
                                        [by exists_intro(ONE) factorization-body]
                                      @conclusion-factorization |
                                        exists s: Seq; exists k: Int;
                                          is_nonneg(k)
                                          and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                          (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                          and n = productUpTo(s, k)
                                        [by exists_intro(sp) factorization-with-length]
                                    }
                                  @conclusion-singleton-factorization |
                                    exists s: Seq; exists k: Int;
                                      is_nonneg(k)
                                      and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                      (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                      and n = productUpTo(s, k)
                                    [by exists_elim with-singleton]
                                }
                              @when-c-below-one |
                                assume below(c, ONE) {
                                  @c-nonneg-if-positive |
                                    below(ZERO, c) -> is_nonneg(c)
                                    [by forall_elim(c) positive-is-nonneg]
                                  @c-nonneg |
                                    is_nonneg(c)
                                    [by modus_ponens c-nonneg-if-positive c-positive]
                                  @c-below-one |
                                    below(c, ONE)
                                    [by hypothesis when-c-below-one]
                                  @nonneg-below-one-is-zero |
                                    forall x: Int; is_nonneg(x) -> below(x, ONE) -> x = ZERO
                                    [by theorem nonnegBelowOneIsZero]
                                  @c-is-zero-if |
                                    is_nonneg(c) -> below(c, ONE) -> c = ZERO
                                    [by forall_elim(c) nonneg-below-one-is-zero]
                                  @c-is-zero |
                                    c = ZERO
                                    [by tautology c-is-zero-if c-nonneg c-below-one]
                                  @zero-below-zero |
                                    below(ZERO, ZERO)
                                    [by rewrite c-is-zero c-positive]
                                  @nothing-below-itself |
                                    forall x: Int; not below(x, x)
                                    [by theorem lessThanIrreflexive]
                                  @zero-not-below-zero |
                                    not below(ZERO, ZERO)
                                    [by forall_elim(ZERO) nothing-below-itself]
                                  @conclusion-factorization |
                                    exists s: Seq; exists k: Int;
                                      is_nonneg(k)
                                      and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                                      (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                                      and n = productUpTo(s, k)
                                    [by absurd zero-below-zero zero-not-below-zero]
                                }
                            }
                        }
                      @conclusion-with-prime |
                        exists s: Seq; exists k: Int;
                          is_nonneg(k)
                          and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                          (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                          and n = productUpTo(s, k)
                        [by exists_elim with-cofactor-c]
                    }
                  @conclusion-factorization |
                    exists s: Seq; exists k: Int;
                      is_nonneg(k)
                      and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                      (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                      and n = productUpTo(s, k)
                    [by exists_elim with-prime-factor-p]
                }
              @above-one-implies-factorization |
                below(ONE, n) -> exists s: Seq; exists k: Int;
                  is_nonneg(k)
                  and (forall i: Int; is_nonneg(i) -> below(i, k) ->
                  (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
                  and n = productUpTo(s, k)
                [by implies_intro given-n-above-one]
            }
          @ih-implies-factorization |
            (forall m: Int; is_nonneg(m) -> below(m, n) ->
              (below(ONE, m) -> exists s: Seq; exists k: Int;
              is_nonneg(k)
              and (forall i: Int; is_nonneg(i) -> below(i, k) ->
              (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
              and m = productUpTo(s, k)))
              -> (below(ONE, n) -> exists s: Seq; exists k: Int;
              is_nonneg(k)
              and (forall i: Int; is_nonneg(i) -> below(i, k) ->
              (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
              and n = productUpTo(s, k))
            [by implies_intro given-inductive-hypothesis]
        }
      @strong-step-at-n |
        is_nonneg(n) ->
          (forall m: Int; is_nonneg(m) -> below(m, n) ->
          (below(ONE, m) -> exists s: Seq; exists k: Int;
          is_nonneg(k)
          and (forall i: Int; is_nonneg(i) -> below(i, k) ->
          (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
          and m = productUpTo(s, k)))
          -> (below(ONE, n) -> exists s: Seq; exists k: Int;
          is_nonneg(k)
          and (forall i: Int; is_nonneg(i) -> below(i, k) ->
          (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
          and n = productUpTo(s, k))
        [by implies_intro given-n-nonneg]
    }
  @strong-step-for-all-n |
    forall n: Int; is_nonneg(n) ->
      (forall m: Int; is_nonneg(m) -> below(m, n) ->
      (below(ONE, m) -> exists s: Seq; exists k: Int;
      is_nonneg(k)
      and (forall i: Int; is_nonneg(i) -> below(i, k) ->
      (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
      and m = productUpTo(s, k)))
      -> (below(ONE, n) -> exists s: Seq; exists k: Int;
      is_nonneg(k)
      and (forall i: Int; is_nonneg(i) -> below(i, k) ->
      (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
      and n = productUpTo(s, k))
    [by forall_intro strong-step]
  @factorization-for-nonneg |
    forall n: Int; is_nonneg(n) ->
      (below(ONE, n) -> exists s: Seq; exists k: Int;
      is_nonneg(k)
      and (forall i: Int; is_nonneg(i) -> below(i, k) ->
      (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
      and n = productUpTo(s, k))
    [by instantiate nonnegStrongInduction((fun n: Int => below(ONE, n) -> exists s: Seq; exists k: Int; is_nonneg(k) and (forall i: Int; is_nonneg(i) -> below(i, k) -> (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i))))) and n = productUpTo(s, k))) strong-step-for-all-n]
  @discharge-below-one |
    fix n: Int {
      @given-n-above-one |
        assume below(ONE, n) {
          @n-above-one |
            below(ONE, n)
            [by hypothesis given-n-above-one]
          @one-below-n-transitive |
            below(ZERO, ONE) -> below(ONE, n) -> below(ZERO, n)
            [by forall_elim(ZERO, ONE, n) less-than-transitive]
          @n-positive |
            below(ZERO, n)
            [by tautology one-below-n-transitive zero-below-one n-above-one]
          @n-nonneg-if-positive |
            below(ZERO, n) -> is_nonneg(n)
            [by forall_elim(n) positive-is-nonneg]
          @n-nonneg |
            is_nonneg(n)
            [by modus_ponens n-nonneg-if-positive n-positive]
          @factorization-at-n |
            is_nonneg(n) ->
              (below(ONE, n) -> exists s: Seq; exists k: Int;
              is_nonneg(k)
              and (forall i: Int; is_nonneg(i) -> below(i, k) ->
              (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
              and n = productUpTo(s, k))
            [by forall_elim(n) factorization-for-nonneg]
          @conclusion-factorization |
            exists s: Seq; exists k: Int;
              is_nonneg(k)
              and (forall i: Int; is_nonneg(i) -> below(i, k) ->
              (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
              and n = productUpTo(s, k)
            [by tautology factorization-at-n n-nonneg n-above-one]
        }
      @above-one-gives-factorization |
        below(ONE, n) ->
          exists s: Seq; exists k: Int;
          is_nonneg(k)
          and (forall i: Int; is_nonneg(i) -> below(i, k) ->
          (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
          and n = productUpTo(s, k)
        [by implies_intro given-n-above-one]
    }
  @conclusion |
    forall n: Int; below(ONE, n) ->
      exists s: Seq; exists k: Int;
      is_nonneg(k)
      and (forall i: Int; is_nonneg(i) -> below(i, k) ->
      (below(ONE, at(s, i)) and (forall d: Int; below(ZERO, d) -> does_divide(d, at(s, i)) -> (d = ONE or d = at(s, i)))))
      and n = productUpTo(s, k)
    [by forall_intro discharge-below-one]
qed
```
