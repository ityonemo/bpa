# Groups — Definitions and Basic Properties (Exercises)

Exercises for Judson **§3.2 "Definitions and Examples" / "Basic Properties of
Groups"** (source: `vendor/aata/src/groups.xml`; © 1997–2026 Judson, GFDL 1.3),
following `.claude/rules/aata-guide.md`. Unlike the main text, the exercises use
`std` **liberally** — the abstract-group results proved here alias
`std/group.bpa` and `std/group-power.bpa` freely, and the concrete-structure
exercises are **Deferred** with the standard-library construction they would
need named in place.

We reuse the main text's import header, aliasing `std/group.bpa`'s vocabulary to
the book's notation (`G`, `e`, `op`, `inv`) and importing the integer-exponent
power `g^n` from `std/group-power.bpa`.

```bpa
import group <<< "std/group.bpa"
import group_power <<< "std/group-power.bpa"

// The book's notation, aliased onto std/group.bpa's library names.
sort G = group.Grp
const e = group.E
func op = group.op
func inv = group.inverse
axiom associative = group.opAssoc
axiom identityLeft = group.opIdentityLeft
theorem identityRight = group.opIdentityRight
axiom inverseLeft = group.opInverseLeft
theorem inverseRight = group.opInverseRight
theorem cancelLeft = group.cancelLeft
theorem cancelRight = group.cancelRight
theorem invProduct = group.invProduct
theorem invInvolution = group.invInvolution

// Integer exponents and the power g^n (g^0 = e, g^{n+1} = g^n·g, g^{n-1} = g^n·g⁻¹).
sort Int = group_power.Int
const ZERO = group_power.ZERO
func succ = group_power.succ
func prev = group_power.prev
func power = group_power.power
axiom powerZero = group_power.powerZero
axiom powerSucc = group_power.powerSucc
axiom powerPrev = group_power.powerPrev
theorem induction = group_power.induction
```

## Exercises

**Exercise 1.** Find $x$ satisfying each of the given modular equations (e.g.
$3x \equiv 2 \pmod 7$).

**Deferred:** needs a concrete $\mathbb{Z}_n$ model (integers modulo $n$ as a
group/ring sort) — `bpa` has Peano naturals and `mod`, but not the quotient
structure $\mathbb{Z}/n\mathbb{Z}$ as a sort with its operations.

**Exercise 2.** Which of the given Cayley tables define groups?

**Deferred:** needs finite sets and an explicit enumeration of group elements (a
finite sort with a tabulated operation).

**Exercise 3.** Write out the Cayley table for the symmetries of a rectangle.

**Deferred:** needs finite sets + explicit element enumeration (a finite sort
with a tabulated operation).

**Exercise 4.** Describe the symmetries of a rhombus and prove that they form a
group.

**Deferred:** needs finite sets + explicit element enumeration (a finite sort
with a tabulated operation).

**Exercise 5.** Describe the symmetries of a square and prove that they form a
group.

**Deferred:** needs finite sets + explicit element enumeration (a finite sort
with a tabulated operation).

**Exercise 6.** Give a multiplication table for the group $U(12)$.

**Deferred:** needs a concrete $\mathbb{Z}_n$ / $U(n)$ model (integers modulo
$n$ as a group sort).

**Exercise 7.** Let $S = \mathbb{R} \setminus \{-1\}$ with $a * b = a + b + ab$;
prove $(S, *)$ is a group.

**Deferred:** needs the real numbers $\mathbb{R}$ as a sort — `bpa` does not
model $\mathbb{R}$.

**Exercise 8.** Give an example of two matrices $A, B$ with $AB \neq BA$.

**Deferred:** needs $2 \times 2$ real matrices (the matrix / $GL_2$
construction over $\mathbb{R}$).

**Exercise 9.** Prove that the product of two matrices in $SL_2(\mathbb{R})$ has
determinant one.

**Deferred:** needs $2 \times 2$ real matrices and the determinant (the $SL_2$
/ $GL_2$ construction over $\mathbb{R}$).

**Exercise 10.** Prove that the Heisenberg group is a group under
multiplication.

**Deferred:** needs $3 \times 3$ real matrices and matrix multiplication (the
matrix construction over $\mathbb{R}$).

**Exercise 11.** Prove that $\det(AB) = \det(A)\det(B)$ for $A, B \in
GL_2(\mathbb{R})$.

**Deferred:** needs $2 \times 2$ real matrices and the determinant (the $GL_2$
construction over $\mathbb{R}$).

**Exercise 12.** Prove that $\mathbb{Z}_n$ is a group under addition modulo $n$.

**Deferred:** needs a concrete $\mathbb{Z}_n$ model (integers modulo $n$ as a
group sort).

**Exercise 13.** Prove that $\mathbb{R}^\ast$, the nonzero reals, is a group
under multiplication.

**Deferred:** needs the real numbers $\mathbb{R}$ as a sort.

**Exercise 14.** Prove that $\mathbb{R}^\ast \times \mathbb{Z}$ is a group under
the componentwise operation.

**Deferred:** needs the real numbers $\mathbb{R}$ and the direct product of
groups.

**Exercise 15.** Prove or disprove that every group with six elements is
abelian.

**Deferred:** needs finite sets + an explicit order-6 nonabelian group ($S_3$,
a finite sort with a tabulated operation).

**Exercise 16.** Give a specific example of a group $G$ and elements $g, h \in
G$ with $(gh)^n \neq g^n h^n$.

**Deferred:** needs a concrete nonabelian group ($S_3$ / the square's symmetry
group — a finite sort with a tabulated operation) to exhibit the inequality;
the abstract *positive* law $(gh)^n = g^n h^n$ under commutativity is
`group_power.powerProductAbelian` (Exercise 29).

**Exercise 17.** Give an example of three different groups with eight elements.

**Deferred:** needs finite sets + explicit tabulated group structures (and the
notion of group isomorphism to distinguish them).

**Exercise 18.** Show that there are $n!$ permutations of a set of $n$ items.

**Deferred:** needs finite sets + cardinality/counting.

**Exercise 19.** Show that $0 + a \equiv a + 0 \equiv a \pmod n$ for all $a \in
\mathbb{Z}_n$.

**Deferred:** needs a concrete $\mathbb{Z}_n$ model (the $\mathbb{Z}_n$
identity).

**Exercise 20.** Prove that there is a multiplicative identity $a \cdot 1 \equiv
a \pmod n$ for the integers modulo $n$.

**Deferred:** needs a concrete $\mathbb{Z}_n$ model.

**Exercise 21.** For each $a \in \mathbb{Z}_n$ find $b$ with $a + b \equiv b + a
\equiv 0 \pmod n$.

**Deferred:** needs a concrete $\mathbb{Z}_n$ model (additive inverses in
$\mathbb{Z}_n$).

**Exercise 22.** Show that addition and multiplication mod $n$ are well defined.

**Deferred:** needs a concrete $\mathbb{Z}_n$ model (the quotient
$\mathbb{Z}/n\mathbb{Z}$ and representative-independence of its operations).

**Exercise 23.** Show that addition and multiplication mod $n$ are associative.

**Deferred:** needs a concrete $\mathbb{Z}_n$ model.

**Exercise 24.** Show that multiplication distributes over addition modulo $n$.

**Deferred:** needs a concrete $\mathbb{Z}_n$ model (distributivity in the ring
$\mathbb{Z}/n\mathbb{Z}$).

**Exercise 25.** Let $a$ and $b$ be elements in a group $G$. Prove that $a b^n
a^{-1} = (a b a^{-1})^n$ for $n \in \mathbb{Z}$.

Judson's hint expands $(aba^{-1})^n = (aba^{-1})(aba^{-1})\cdots(aba^{-1})$ and
telescopes the interior $a^{-1}a = e$ pairs, leaving $ab^na^{-1}$. Formally we
prove it by **bidirectional induction on the integer exponent $n$**, with the
conjugate $c = aba^{-1}$. The load-bearing step is the *conjugation lemma*
$c^{k+1} = (ab^ka^{-1})(aba^{-1}) = ab^{k+1}a^{-1}$ — where the adjacent
$a^{-1}a$ cancels — proved once and used in both the successor and predecessor
directions.

```bpa
// Conjugation lemma: (a·y·a⁻¹)·(a·z·a⁻¹) = a·(y·z)·a⁻¹ — the interior a⁻¹a
// telescopes. Used to fuse two conjugates in each induction step of Exercise 25.
theorem conjugateProduct: forall a, y, z: G;
  op(op(op(a, y), inv(a)), op(op(a, z), inv(a))) = op(op(a, op(y, z)), inv(a))
proof
  @generalize-a |
    fix a: G {
      @generalize-y |
        fix y: G {
          @generalize-z |
            fix z: G {
              // regroup the two conjugates so the interior a⁻¹·a is adjacent:
              // (a·y·a⁻¹)·(a·z·a⁻¹) = (a·y)·((a⁻¹·a)·(z·a⁻¹)).
              @expose-interior-inverse |
                op(op(op(a, y), inv(a)), op(op(a, z), inv(a))) = op(op(a, y), op(op(inv(a), a), op(z, inv(a))))
                [by assoc(associative)]
              // a⁻¹·a = e.
              @interior-inverse-cancels |
                op(inv(a), a) = e
                [by specialize inverseLeft(a)]
              // the interior cancellation leaves (a·y)·(e·(z·a⁻¹)).
              @after-interior-cancels |
                op(op(op(a, y), inv(a)), op(op(a, z), inv(a))) = op(op(a, y), op(e, op(z, inv(a))))
                [by rewrite interior-inverse-cancels expose-interior-inverse]
              // e·(z·a⁻¹) = z·a⁻¹, so we are at (a·y)·(z·a⁻¹).
              @identity-drops-out |
                op(e, op(z, inv(a))) = op(z, inv(a))
                [by specialize identityLeft(op(z, inv(a)))]
              @after-identity-drops |
                op(op(op(a, y), inv(a)), op(op(a, z), inv(a))) = op(op(a, y), op(z, inv(a)))
                [by rewrite identity-drops-out after-interior-cancels]
              // reassociate (a·y)·(z·a⁻¹) = (a·(y·z))·a⁻¹.
              @reassociate-remaining |
                op(op(a, y), op(z, inv(a))) = op(op(a, op(y, z)), inv(a))
                [by assoc(associative)]
              @conclusion-conjugate-product |
                op(op(op(a, y), inv(a)), op(op(a, z), inv(a))) = op(op(a, op(y, z)), inv(a))
                [by chain after-identity-drops reassociate-remaining]
            }
          @discharge-z |
            forall z: G; op(op(op(a, y), inv(a)), op(op(a, z), inv(a))) = op(op(a, op(y, z)), inv(a))
            [by forall_intro generalize-z]
        }
      @discharge-y |
        forall y, z: G; op(op(op(a, y), inv(a)), op(op(a, z), inv(a))) = op(op(a, op(y, z)), inv(a))
        [by forall_intro generalize-y]
    }
  @conclusion |
    forall a, y, z: G; op(op(op(a, y), inv(a)), op(op(a, z), inv(a))) = op(op(a, op(y, z)), inv(a))
    [by forall_intro generalize-a]
qed

// Exercise 25 proper: a·bⁿ·a⁻¹ = (a·b·a⁻¹)ⁿ, by bidirectional induction on n.
theorem conjugatePower: forall a, b: G; forall n: Int;
  op(op(a, power(b, n)), inv(a)) = power(op(op(a, b), inv(a)), n)
proof
  @generalize-a |
    fix a: G {
      @generalize-b |
        fix b: G {
          // base case n = 0: a·b⁰·a⁻¹ = a·e·a⁻¹ = a·a⁻¹ = e = (aba⁻¹)⁰.
          @power-of-b-at-zero |
            power(b, ZERO) = e
            [by specialize powerZero(b)]
          @a-times-identity |
            op(a, e) = a
            [by specialize identityRight(a)]
          @a-cancels-its-inverse |
            op(a, inv(a)) = e
            [by specialize inverseRight(a)]
          @conjugate-power-at-zero |
            power(op(op(a, b), inv(a)), ZERO) = e
            [by specialize powerZero(op(op(a, b), inv(a)))]
          @conjugate-power-at-zero-flipped |
            e = power(op(op(a, b), inv(a)), ZERO)
            [by symmetry conjugate-power-at-zero]
          // a·(b⁰)·a⁻¹ = a·e·a⁻¹ = a·a⁻¹ = e; but first re-fold a·e·a⁻¹ = a·a⁻¹.
          @conjugate-of-identity-collapses |
            op(op(a, power(b, ZERO)), inv(a)) = e
            [by chain power-of-b-at-zero a-times-identity a-cancels-its-inverse]
          @base-case |
            op(op(a, power(b, ZERO)), inv(a)) = power(op(op(a, b), inv(a)), ZERO)
            [by chain conjugate-of-identity-collapses conjugate-power-at-zero-flipped]

          // the conjugation fusion lemma, specialized to this a:
          @conjugates-fuse |
            forall y, z: G; op(op(op(a, y), inv(a)), op(op(a, z), inv(a))) = op(op(a, op(y, z)), inv(a))
            [by specialize conjugateProduct(a)]

          // -- successor step: n = k → n = succ(k) ---------------------------
          @successor-step |
            fix k: Int {
              @given-inductive-hypothesis |
                assume op(op(a, power(b, k)), inv(a)) = power(op(op(a, b), inv(a)), k) {
                  @inductive-hypothesis |
                    op(op(a, power(b, k)), inv(a)) = power(op(op(a, b), inv(a)), k)
                    [by hypothesis given-inductive-hypothesis]
                  // (aba⁻¹)^(succ k) = op((aba⁻¹)^k, aba⁻¹) = op(a·bᵏ·a⁻¹, a·b·a⁻¹) [IH].
                  @conjugate-power-unfolds |
                    power(op(op(a, b), inv(a)), succ(k)) = op(power(op(op(a, b), inv(a)), k), op(op(a, b), inv(a)))
                    [by specialize powerSucc(op(op(a, b), inv(a)), k)]
                  @inductive-hypothesis-flipped |
                    power(op(op(a, b), inv(a)), k) = op(op(a, power(b, k)), inv(a))
                    [by symmetry inductive-hypothesis]
                  @fold-inductive-hypothesis |
                    op(power(op(op(a, b), inv(a)), k), op(op(a, b), inv(a))) = op(op(op(a, power(b, k)), inv(a)), op(op(a, b), inv(a)))
                    [by rewrite inductive-hypothesis-flipped conjugate-power-refl-succ]
                  @conjugate-power-refl-succ |
                    op(power(op(op(a, b), inv(a)), k), op(op(a, b), inv(a))) = op(power(op(op(a, b), inv(a)), k), op(op(a, b), inv(a)))
                    [by reflexivity]
                  // fuse the two conjugates: op(a·bᵏ·a⁻¹, a·b·a⁻¹) = a·(bᵏ·b)·a⁻¹.
                  @conjugates-fuse-at-bk-b |
                    op(op(op(a, power(b, k)), inv(a)), op(op(a, b), inv(a))) = op(op(a, op(power(b, k), b)), inv(a))
                    [by forall_elim(power(b, k), b) conjugates-fuse]
                  // bᵏ·b = b^(succ k).
                  @b-power-refolds |
                    op(power(b, k), b) = power(b, succ(k))
                    [by symmetry b-power-succ]
                  @b-power-succ |
                    power(b, succ(k)) = op(power(b, k), b)
                    [by specialize powerSucc(b, k)]
                  @fused-conjugate-is-succ |
                    op(op(a, op(power(b, k), b)), inv(a)) = op(op(a, power(b, succ(k))), inv(a))
                    [by rewrite b-power-refolds fused-conjugate-refl]
                  @fused-conjugate-refl |
                    op(op(a, op(power(b, k), b)), inv(a)) = op(op(a, op(power(b, k), b)), inv(a))
                    [by reflexivity]
                  // chain: (aba⁻¹)^(succ k) = … = a·b^(succ k)·a⁻¹.
                  @conjugate-power-succ-flipped |
                    power(op(op(a, b), inv(a)), succ(k)) = op(op(a, power(b, succ(k))), inv(a))
                    [by chain conjugate-power-unfolds fold-inductive-hypothesis conjugates-fuse-at-bk-b fused-conjugate-is-succ]
                  @conclusion-successor |
                    op(op(a, power(b, succ(k))), inv(a)) = power(op(op(a, b), inv(a)), succ(k))
                    [by symmetry conjugate-power-succ-flipped]
                }
              @inductive-hypothesis-implies-successor |
                op(op(a, power(b, k)), inv(a)) = power(op(op(a, b), inv(a)), k) -> op(op(a, power(b, succ(k))), inv(a)) = power(op(op(a, b), inv(a)), succ(k))
                [by implies_intro given-inductive-hypothesis]
            }
          @successor-step-all-k |
            forall k: Int; op(op(a, power(b, k)), inv(a)) = power(op(op(a, b), inv(a)), k) -> op(op(a, power(b, succ(k))), inv(a)) = power(op(op(a, b), inv(a)), succ(k))
            [by forall_intro successor-step]

          // -- predecessor step: n = k → n = prev(k) -------------------------
          @predecessor-step |
            fix k: Int {
              @given-inductive-hypothesis |
                assume op(op(a, power(b, k)), inv(a)) = power(op(op(a, b), inv(a)), k) {
                  @inductive-hypothesis |
                    op(op(a, power(b, k)), inv(a)) = power(op(op(a, b), inv(a)), k)
                    [by hypothesis given-inductive-hypothesis]
                  // (aba⁻¹)^(prev k) = op((aba⁻¹)^k, (aba⁻¹)⁻¹). We instead go via
                  // the conjugate of b⁻¹: a·b⁻¹·a⁻¹ IS the inverse of a·b·a⁻¹.
                  // op((aba⁻¹)^k, a·b⁻¹·a⁻¹) = op(a·bᵏ·a⁻¹, a·b⁻¹·a⁻¹) [IH]
                  //   = a·(bᵏ·b⁻¹)·a⁻¹ = a·b^(prev k)·a⁻¹.
                  @inverse-of-conjugate-base |
                    inv(op(op(a, b), inv(a))) = op(op(a, inv(b)), inv(a))
                    [by chain inverse-of-conjugate double-inverse-of-a]
                  @inverse-of-conjugate |
                    inv(op(op(a, b), inv(a))) = op(inv(inv(a)), op(inv(b), inv(a)))
                    [by chain inv-outer inv-inner]
                  @inv-outer |
                    inv(op(op(a, b), inv(a))) = op(inv(inv(a)), inv(op(a, b)))
                    [by specialize invProduct(op(a, b), inv(a))]
                  @inv-inner |
                    inv(op(a, b)) = op(inv(b), inv(a))
                    [by specialize invProduct(a, b)]
                  @double-inverse-of-a |
                    op(inv(inv(a)), op(inv(b), inv(a))) = op(op(a, inv(b)), inv(a))
                    [by chain invinv-a reassoc-conjugate]
                  @invinv-a |
                    inv(inv(a)) = a
                    [by specialize invInvolution(a)]
                  @reassoc-conjugate |
                    op(a, op(inv(b), inv(a))) = op(op(a, inv(b)), inv(a))
                    [by symmetry conjugate-assoc]
                  @conjugate-assoc |
                    op(op(a, inv(b)), inv(a)) = op(a, op(inv(b), inv(a)))
                    [by specialize associative(a, inv(b), inv(a))]
                  // (aba⁻¹)^(prev k) = op((aba⁻¹)^k, (aba⁻¹)⁻¹).
                  @conjugate-power-unfolds |
                    power(op(op(a, b), inv(a)), prev(k)) = op(power(op(op(a, b), inv(a)), k), inv(op(op(a, b), inv(a))))
                    [by specialize powerPrev(op(op(a, b), inv(a)), k)]
                  @rewrite-inverse-base |
                    power(op(op(a, b), inv(a)), prev(k)) = op(power(op(op(a, b), inv(a)), k), op(op(a, inv(b)), inv(a)))
                    [by rewrite inverse-of-conjugate-base conjugate-power-unfolds]
                  @inductive-hypothesis-flipped |
                    power(op(op(a, b), inv(a)), k) = op(op(a, power(b, k)), inv(a))
                    [by symmetry inductive-hypothesis]
                  @fold-inductive-hypothesis |
                    op(power(op(op(a, b), inv(a)), k), op(op(a, inv(b)), inv(a))) = op(op(op(a, power(b, k)), inv(a)), op(op(a, inv(b)), inv(a)))
                    [by rewrite inductive-hypothesis-flipped fold-ih-prev-refl]
                  @fold-ih-prev-refl |
                    op(power(op(op(a, b), inv(a)), k), op(op(a, inv(b)), inv(a))) = op(power(op(op(a, b), inv(a)), k), op(op(a, inv(b)), inv(a)))
                    [by reflexivity]
                  // fuse: op(a·bᵏ·a⁻¹, a·b⁻¹·a⁻¹) = a·(bᵏ·b⁻¹)·a⁻¹.
                  @conjugates-fuse-at-bk-invb |
                    op(op(op(a, power(b, k)), inv(a)), op(op(a, inv(b)), inv(a))) = op(op(a, op(power(b, k), inv(b))), inv(a))
                    [by forall_elim(power(b, k), inv(b)) conjugates-fuse]
                  // bᵏ·b⁻¹ = b^(prev k).
                  @b-power-refolds |
                    op(power(b, k), inv(b)) = power(b, prev(k))
                    [by symmetry b-power-prev]
                  @b-power-prev |
                    power(b, prev(k)) = op(power(b, k), inv(b))
                    [by specialize powerPrev(b, k)]
                  @fused-conjugate-is-prev |
                    op(op(a, op(power(b, k), inv(b))), inv(a)) = op(op(a, power(b, prev(k))), inv(a))
                    [by rewrite b-power-refolds fused-conjugate-prev-refl]
                  @fused-conjugate-prev-refl |
                    op(op(a, op(power(b, k), inv(b))), inv(a)) = op(op(a, op(power(b, k), inv(b))), inv(a))
                    [by reflexivity]
                  @conjugate-power-prev-flipped |
                    power(op(op(a, b), inv(a)), prev(k)) = op(op(a, power(b, prev(k))), inv(a))
                    [by chain rewrite-inverse-base fold-inductive-hypothesis conjugates-fuse-at-bk-invb fused-conjugate-is-prev]
                  @conclusion-predecessor |
                    op(op(a, power(b, prev(k))), inv(a)) = power(op(op(a, b), inv(a)), prev(k))
                    [by symmetry conjugate-power-prev-flipped]
                }
              @inductive-hypothesis-implies-predecessor |
                op(op(a, power(b, k)), inv(a)) = power(op(op(a, b), inv(a)), k) -> op(op(a, power(b, prev(k))), inv(a)) = power(op(op(a, b), inv(a)), prev(k))
                [by implies_intro given-inductive-hypothesis]
            }
          @predecessor-step-all-k |
            forall k: Int; op(op(a, power(b, k)), inv(a)) = power(op(op(a, b), inv(a)), k) -> op(op(a, power(b, prev(k))), inv(a)) = power(op(op(a, b), inv(a)), prev(k))
            [by forall_intro predecessor-step]

          @conjugate-power-over-all-n |
            forall n: Int; op(op(a, power(b, n)), inv(a)) = power(op(op(a, b), inv(a)), n)
            [by instantiate induction((fun n: Int => op(op(a, power(b, n)), inv(a)) = power(op(op(a, b), inv(a)), n))) base-case successor-step-all-k predecessor-step-all-k]
        }
      @discharge-b |
        forall b: G; forall n: Int; op(op(a, power(b, n)), inv(a)) = power(op(op(a, b), inv(a)), n)
        [by forall_intro generalize-b]
    }
  @conclusion |
    forall a, b: G; forall n: Int; op(op(a, power(b, n)), inv(a)) = power(op(op(a, b), inv(a)), n)
    [by forall_intro generalize-a]
qed
```

**Exercise 26.** Let $U(n)$ be the group of units in $\mathbb{Z}_n$. If $n > 2$,
prove there is $k \in U(n)$ with $k^2 = 1$ and $k \neq 1$.

**Deferred:** needs a concrete $\mathbb{Z}_n$ / $U(n)$ model (integers modulo
$n$ and their units).

**Exercise 27.** Prove that the inverse of $g_1 g_2 \cdots g_n$ is $g_n^{-1}
g_{n-1}^{-1} \cdots g_1^{-1}$.

**Deferred:** the $n$-ary product form needs an **indexed group-product** (a
fold over a sequence of group elements), which `bpa` does not yet model. The
binary base case $(ab)^{-1} = b^{-1}a^{-1}$ is `invProduct` in `std/group.bpa`
(cited in the main text §3.2); the general statement is its iterate.

**Exercise 28.** Prove the remainder of the group-equations proposition: if $G$
is a group and $a, b \in G$, then $xa = b$ has a unique solution in $G$.

**Forwarded** — proved in the main text §3.2 (`solutionExists` /
`solutionUnique`, aliased from `std/group.bpa`). See §3.2 "Proposition — the
equations $ax = b$ and $xa = b$ have unique solutions."

**Exercise 29.** Prove the Laws of Exponents.

**Forwarded** — proved in the main text §3.2 (`exponentLawProduct` /
`exponentLawPower` / `exponentLawOfProduct` / `exponentLawAbelian`, from
`std/group-power.bpa`). See §3.2 "Theorem — Laws of Exponents."

**Exercise 30.** Prove the right and left cancellation laws for a group $G$.

**Forwarded** — proved in the main text §3.2 (`cancelRight` / `cancelLeft`,
aliased from `std/group.bpa`). See §3.2 "Proposition — the cancellation laws."

**Exercise 31.** Show that if $a^2 = e$ for all elements $a$ in a group $G$,
then $G$ must be abelian.

Judson's hint: since $abab = (ab)^2 = e = a^2 b^2 = aabb$, we know $ba = ab$.

**Solution.** If every element squares to $e$, then every element is its own
inverse (`inverseUnique`); combined with $(ab)^{-1} = b^{-1}a^{-1}$ this gives
$ab = (ab)^{-1} = b^{-1}a^{-1} = ba$. (This uses only the $n = 2$, i.e.
`op(a, a)`, instance of squaring — no general group power is needed.)

```bpa
theorem selfInverse = group.selfInverse

theorem squareIdImpliesAbelian = group.squareIdImpliesAbelian
```

**Exercise 32.** Show that if $G$ is a finite group of even order, then there is
an $a \in G$, $a \neq e$, with $a^2 = e$.

**Deferred:** needs finite-group order/cardinality and a pairing argument (pair
each non-self-inverse element with its distinct inverse; even order forces at
least one non-identity self-inverse element).

**Exercise 33.** Let $G$ be a group with $(ab)^2 = a^2 b^2$ for all $a, b \in
G$. Prove that $G$ is abelian.

**Solution.** From $(ab)(ab) = (aa)(bb)$, re-associate both sides to
$a\,(bab)$ and $a\,(abb)$; left-cancel $a$ to get $(ba)b = (ab)b$; right-cancel
$b$ to get $ba = ab$, i.e. $ab = ba$.

```bpa
theorem squareOfProductAbelian:
  (forall a, b: G; op(op(a, b), op(a, b)) = op(op(a, a), op(b, b))) -> forall a, b: G; op(a, b) = op(b, a)
proof
  @given-square-of-product-splits |
    assume forall a, b: G; op(op(a, b), op(a, b)) = op(op(a, a), op(b, b)) {
      @square-of-product-splits |
        forall a, b: G; op(op(a, b), op(a, b)) = op(op(a, a), op(b, b))
        [by hypothesis given-square-of-product-splits]
      @cancel-left-law |
        forall a, b, c: G; op(a, b) = op(a, c) -> b = c
        [by theorem cancelLeft]
      @cancel-right-law |
        forall b, a, c: G; op(b, a) = op(c, a) -> b = c
        [by theorem cancelRight]
      @generalize-a |
        fix a: G {
          @generalize-b |
            fix b: G {
              @square-hypothesis-at-a-b |
                op(op(a, b), op(a, b)) = op(op(a, a), op(b, b))
                [by forall_elim(a, b) square-of-product-splits]
              // regroup abab = a·((ba)·b) and aabb = a·((ab)·b).
              @regroup-left-square |
                op(op(a, b), op(a, b)) = op(a, op(op(b, a), b))
                [by assoc(associative)]
              @regroup-right-square |
                op(op(a, a), op(b, b)) = op(a, op(op(a, b), b))
                [by assoc(associative)]
              // so a·((ba)·b) = a·((ab)·b).
              @left-factored-equation |
                op(a, op(op(b, a), b)) = op(a, op(op(a, b), b))
                [by chain regroup-left-square square-hypothesis-at-a-b regroup-right-square]
              // left-cancel a: (ba)·b = (ab)·b.
              @left-cancellation-at-a |
                op(a, op(op(b, a), b)) = op(a, op(op(a, b), b)) -> op(op(b, a), b) = op(op(a, b), b)
                [by forall_elim(a, op(op(b, a), b), op(op(a, b), b)) cancel-left-law]
              @right-products-equal |
                op(op(b, a), b) = op(op(a, b), b)
                [by modus_ponens left-cancellation-at-a left-factored-equation]
              // right-cancel b: ba = ab.
              @right-cancellation-at-b |
                op(op(b, a), b) = op(op(a, b), b) -> op(b, a) = op(a, b)
                [by forall_elim(op(b, a), b, op(a, b)) cancel-right-law]
              @b-a-equals-a-b |
                op(b, a) = op(a, b)
                [by modus_ponens right-cancellation-at-b right-products-equal]
              @a-b-equals-b-a |
                op(a, b) = op(b, a)
                [by symmetry b-a-equals-a-b]
            }
          @discharge-b |
            forall b: G; op(a, b) = op(b, a)
            [by forall_intro generalize-b]
        }
      @commutes-for-all-a |
        forall a, b: G; op(a, b) = op(b, a)
        [by forall_intro generalize-a]
    }
  @conclusion |
    (forall a, b: G; op(op(a, b), op(a, b)) = op(op(a, a), op(b, b))) -> forall a, b: G; op(a, b) = op(b, a)
    [by implies_intro given-square-of-product-splits]
qed
```

Exercises 34–54 concern **subgroups** (§3.3) and are handled in a separate file.
