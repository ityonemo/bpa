# AATA Exercises — Deferred & Skipped (master index)

This is the running master list of every Judson exercise (Chapters 1–3) that the
`aata/*-exercises.md` transliterations could **not** encode, grouped by the piece
of **machinery** whose absence blocks it. When a piece of theory lands, this file
says which exercises it unlocks. The per-file `**Deferred:** …` notes remain in
context in each exercises file; this file is the index.

Format is plain markdown WITHOUT a `.md` extension so the literate checker never
tries to parse the fenced examples here as real proofs.

Legend: **Deferred** = beyond the current kernel/library (named machinery would
unlock it). **Skipped (brutal computation)** = a specific finite numeric
calculation with no abstract mathematical content (bpa has no computational
evaluator; the general theorem is already available). **RCA** = a bpa-architecture
obstacle; see `aata/EXERCISE-RCA`.

---

## By machinery

### ℚ / ℝ (rationals / reals + their order)  — the biggest single blocker
- §2.1 Ex 7 — AM–GM inequality (n-th roots of a product ≤ arithmetic mean).
- §2.1 Ex 10 — Σ 1/(k(k+1)) = n/(n+1) (rational summands).
- §2.1 Ex 11 — (1+x)ⁿ − 1 ≥ nx for real x ≥ 0 (Bernoulli; real order).
- §2.3 Ex 31 (final clause) — √2 ∉ ℚ. (The integer core "no p,q with p²=2q²" is
  handled separately in the §2.3 exercises; only the "therefore √2 is irrational"
  conclusion needs ℚ.)
- §2.3 Ex 17 (parts c,d) — Fibonacci closed form with √5 and the golden-ratio
  limit (reals + limits).

### Integer summation operator (Σ) + nonlinear polynomial induction
- §2.1 Ex 1 — 1²+2²+···+n² = n(n+1)(2n+1)/6.
- §2.1 Ex 2 — 1³+2³+···+n³ = n²(n+1)²/4.
- §2.1 Ex 4 — x+4x+···+(3n−2)x = n(3n−1)x/2.
  (Each needs a ℤ-native finite-sum Σ and division by a constant; the
  restate-as-`c·Σ = …` form still needs the Σ operator. `std/sequence.bpa` has an
  abstract fold but not an integer-native summation surface these can cite yet.)

### factorial + strict-inequality induction from a nonzero base
- §2.1 Ex 3 — n! > 2ⁿ for n ≥ 4 (needs `factorial` and `<`-induction from n₀=4).

### shifted-base / negative-exponent divisibility
- §2.1 Ex 6 — 99 | 4·10^{2n} + 9·10^{2n−1} + 5 (Judson intends n ≥ 1; the
  10^{2n−1} term is 10^{−1} at n=0, so it needs a base-shifted induction).

### finite cardinality / counting / power sets
- §2.1 Ex 12 — a set with n elements has a power set with 2ⁿ elements.

### recursively-defined integer sequences
- §2.3 Ex 17 (parts a,b,e) — Fibonacci inequalities / identity / coprimality.
  (Blocked on a recursively-defined ℤ sequence; may partially unlock before ℝ.)

### perfect-square predicate + FTA multiplicativity
- §2.3 Ex 19 — coprime x,y with xy a perfect square ⟹ x,y both perfect squares.

### heavy parity + coprimality case analysis
- §2.3 Ex 21 — pairwise-coprime a,b,r,s with a²+b²=r², a²−b²=s² ⟹ a,r,s odd,
  b even.

### geometric-series divisibility identity over ℤ
- §2.3 Ex 28 — 2^p − 1 prime ⟹ p prime (needs (2^a−1) | (2^{ab}−1)).

### Dirichlet-type prime constructions (residue-class specialization)
- §2.3 Ex 29 — infinitely many primes of the form 6n+5.
- §2.3 Ex 30 — infinitely many primes of the form 4n−1.

### gcd·lcm product identity
- §2.2 Ex 24 — d = gcd(a,b), m = lcm(a,b) ⟹ dm = |ab|.
- §2.2 Ex 25 — lcm(a,b) = ab ⟺ gcd(a,b) = 1 (depends on Ex 24).
- §2.2 Ex 26 — gcd(a,c)=gcd(b,c)=1 ⟺ gcd(ab,c)=1 (product-of-coprimes) — deferred
  if the Bézout-product argument does not close cleanly.

---

## Skipped (brutal computation)
- §2.1 Ex 15 / §2.2 Ex 15 — specific gcd(a,b) and Bézout-coefficient computations
  for numeric pairs (14 & 39, 234 & 165, …). The general `gcd`/`bezout` are in std;
  these instances are pure arithmetic evaluation, which bpa does not do.

---

_(Chapter 3 deferrals are appended as those exercise files are authored.)_
