# AATA — abstract algebra, verified in bpa

This directory holds **literate bpa transliterations of
*Abstract Algebra: Theory and Applications*** by Thomas W. Judson (the "AATA"
textbook). Each `.md` file is a **faithful transliteration of a textbook
section** — the book's own prose, definitions, and examples reproduced in order
— with a machine-checked ` ```bpa ` proof following each result the book states.
Every file is checked directly:

```
bpa check aata/groups.md
```

The document map mirrors the textbook's own structure
(`vendor/aata/src/aata.xml`), transliterated a section at a time. Where a
statement is beyond what bpa's first-order-logic kernel can express, the prose
is kept and marked in place with a note (or a `// this section cannot currently
be encoded [reason]` block) stating exactly what it would require — never
silently dropped.

## Attribution and license

The source textbook is vendored as a git submodule at **`vendor/aata`**
(`github.com/twjudson/aata`).

> *Abstract Algebra: Theory and Applications*
> Text © 1997–2025 **Thomas W. Judson**
> Sage © 2010–2025 **Robert A. Beezer**
>
> Permission is granted to copy, distribute and/or modify the textbook under the
> terms of the **GNU Free Documentation License, Version 1.3** or any later
> version, with no Invariant Sections and no Cover Texts. See
> `vendor/aata/COPYING`.

The files in this directory are **derived works** — transliterations of the
textbook's prose, statements, and proofs — and are distributed under the same
**GFDL 1.3** copyleft. The exposition is transcribed from the textbook; the bpa
proofs follow its arguments.

## What's here

| File | Chapter / section | Status |
|---|---|---|
| `sets.md` | 1 — Preliminaries, §1.2.1 Set Theory | ✅ full transliteration; set algebra + De Morgan (14 theorems), verified pure |
| `groups.md` | 3 — Groups, §3.2 Definitions and Examples | ✅ full transliteration; definition + 7 examples + basic-properties propositions (10 theorems) + Exercise 31, verified pure |

The set theory rests on `std/set.bpa` (sets over an abstract `Universe` of
elements, fixed membershipwise, with extensionality). Every identity is proved
by the same recipe — extensionality → fix an element → unfold `member(x, ·)` →
one `[by tautology …]` — so the whole chapter is oracle-free.

The group theory rests on `std/group.bpa` (the group axioms as a reusable bpa
theory) and uses the `assoc` tactic (associativity-only reordering) for the
non-commutative rearrangements.

## What's deferred (the forcing-function backlog)

Most of the textbook needs machinery bpa does not yet have. Rather than skip
those results silently, each is kept in the relevant `.md` with an in-place note
(`**Deferred:** …` for an exercise, or a `// this section cannot currently be
encoded [reason]` block for a stated theorem) saying exactly what it would
require — a running backlog of standard-library constructions that future
chapters will motivate:

- a concrete **ℤ_n / U(n)** model (modular arithmetic as a group carrier),
- **finite sets + cardinality** (counting arguments, Cayley tables, Lagrange),
- **matrix / GLₙ** constructions and the reals,
- a **group power** `gⁿ` (recursive over a Nat exponent) for the ℕ-indexed
  exercises,
- **sets-of-sets / quotient structure + a relation sort** — Chapter 1's §1.2.3
  (equivalence relations ⟺ partitions) needs equivalence classes `[a]` and the
  partition `X = ⋃[x]`, which `std/set.bpa`'s flat `member : Universe → Set`
  model cannot express; and the **indexed families** `⋃ᵢ Aᵢ` / `⋂ᵢ Aᵢ` need a
  sort of finite sequences of `Set`.

Each is a self-contained addition; the translation grows as they land.
