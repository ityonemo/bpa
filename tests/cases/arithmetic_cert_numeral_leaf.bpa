// The arithmetic equation certifier must cancel an additive-inverse pair when
// one side of the pair is a NUMERAL — `add(ONE, sub(b, ONE)) = b`. `sub(b, ONE)`
// unfolds to `add(b, neg(ONE))`, so the summand multiset is {ONE, b, neg(ONE)}
// with ONE = succ(ZERO). The ONE / neg(ONE) pair must cancel, leaving {b}.
//
// The wrinkle this exercises: `succ(ZERO)` is a `succ`-tower sitting as an INNER
// summand of the sum (not the top-level succ prefix parseTower folds into its
// `succs` count). The tower parser must recognize a numeral summand and fold it
// into the tower's constant offset, so ONE and neg(ONE) meet and cancel. A pure
// linear ℤ identity — [by arithmetic] should certify it strict.
import integer <<< "std/integer.bpa"
import order <<< "std/integer-order.bpa"
sort Int = integer.Int
const ZERO = integer.ZERO
const ONE = integer.ONE
func succ = integer.succ
func add = integer.add
func neg = integer.neg
func sub = order.sub
axiom definitionOfSubtraction = integer.definitionOfSubtraction
theorem addNegRight = integer.addNegRight
theorem addNegLeft = integer.addNegLeft
theorem negNeg = integer.negNeg
axiom addZeroLeft = integer.addZeroLeft
theorem addZeroRight = integer.addZeroRight
theorem addIsAssociative = integer.addIsAssociative
theorem addIsCommutative = integer.addIsCommutative
theorem addLeftSwap = integer.addLeftSwap
axiom oneIsSuccZero = integer.oneIsSuccZero
func prev = integer.prev
axiom addSuccLeft = integer.addSuccLeft
theorem addSuccRight = integer.addSuccRight
axiom addPrevLeft = integer.addPrevLeft
theorem addPrevRight = integer.addPrevRight
axiom negZero = integer.negZero
axiom negSucc = integer.negSucc
axiom negPrev = integer.negPrev
axiom prevSucc = integer.prevSucc
axiom succPrev = integer.succPrev

theorem oneReaches: forall b: Int; add(ONE, sub(b, ONE)) = b
proof
  @generalize-b |
    fix b: Int {
      @conclusion-one-reaches |
        add(ONE, sub(b, ONE)) = b
        [by arithmetic]
    }
  @conclusion |
    forall b: Int; add(ONE, sub(b, ONE)) = b
    [by forall_intro generalize-b]
qed
